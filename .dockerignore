# 🔴 年报 PDF 永不进镜像。
#
# 两条理由，各自都够：
#   1. 几百 MB，而服务根本不解析 PDF（抽取是离线那一步的事，见 `N-61`）。
#   2. 镜像会被推到仓库。`.gitignore` 挡住了 PDF 进版本控制，
#      **镜像是另一条同样会把文件带出去的路** —— 两处都得挡。
data/raw/
data/private/
data/cache/
data/parsed/
data/results/

# 凭据、环境文件：一个都不进
.env
*.env
.venv/
**/.credentials*

# 与服务无关的一切
.git/
.github/
.planning/
docs/
openspec/
references/
rules/
scripts/
tests/
eval/
!eval/frozen-01/fixtures/
**/__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
