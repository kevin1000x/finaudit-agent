# 非公开数据的物理隔离（D-010）——这些目录永远不进版本控制
data/raw/
data/private/
data/cache/
*.internal.*
*_internal.*

# 下载的年报与中间产物
*.pdf
data/parsed/
data/results/
reports/

# 凭据
.env
.env.*
!.env.example
*.key
*.pem

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
*.egg-info/

# 编辑器与系统
.DS_Store
Thumbs.db
.idea/
.vscode/

# Agent 工具的本机状态（权限白名单等，因机器而异，不进版本控制）
.claude/settings.local.json
.claude/settings.json.bak

# 本地评测输出（评测问题集本身要进版本控制，结果不进）
eval/runs/

# 子 agent 的独立工作树（`isolation: worktree`）。它们是 gitlink，
# 一旦被 `git add -A` 暂存就会以「子模块」的形状进库。
.claude/worktrees/

# 外部项目的阅读笔记与它的门禁。**刻意不上传**（`D-043`，2026-09-11）。
# 它们是作者自己的学习记录，不是这个项目的产物 —— 26,840 行，占全仓 markdown 四成，
# 而一个来看这个项目的人不需要读它们。README 的「借鉴」一节说明了读过什么。
# 本地保留，继续用。
references/
scripts/check_reading_ledger.py
tests/test_reading_ledger.py

# 工作记录。**刻意不上传**（`D-044`，2026-09-11）——
# 会话日志、未决事项台账、登记册、交接、失败模式清单、与外部资料库的关系。
# 它们是「活怎么干的」，不是「这个项目是什么」；对来看这个项目的人没有价值。
# 本地在 ../finaudit-workshop/ 保留，继续用。
claudedocs/
docs/agent/PROGRESS.md
docs/agent/OPEN-ITEMS.md
docs/agent/LANDING-BACKLOG.md
docs/agent/DELEGATED-DECISIONS.md
rules/failure-modes.md
rules/repo-context.md
