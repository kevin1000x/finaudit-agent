# 评测协议与问题集 — finaudit-agent

## 1. 目的

本协议测试三件互相独立的事，**任何一项的正面结果都不能替代另外两项**：

| 层 | 问的问题 | 对应假设 |
|---|---|---|
| **口径可判定性** | 指标口径能否被机械判定对错？ | H1 |
| **证据链可复核性** | 人能否仅凭证据链在 5 分钟内独立判断对错？ | H2 |
| **图谱必要性** | 是否存在纯向量 RAG 答不了而图能答的问题？ | H3 |

答案正确但证据链不可复核 = H1 通过、H2 失败。这两件事分开记分。

## 2. 冻结规则（D-012，2026-08-15 修订）

问题集与标准答案在**看到任何模型输出之前**冻结，冻结时记录 UTC 时间戳与文件 SHA-256。

- 冻结后**允许**：重跑评测、改进实现、追加新批次问题（新批次单独编号）。
- 冻结后**禁止**：修改已冻结问题的标准答案或判定规则来迁就模型表现。

### 2.1 冻结前置门禁

**未通过机器校验的内容不允许进入冻结集。** 冻结由脚本执行，脚本先跑校验，
校验不过就拒绝冻结（fail-closed），不由人手工打哈希。冻结前必须机械确认：

- 每份 case 是合法 YAML，必填字段齐全
- C1/C3/C4/C5 题的 `expected.metric_id` 在 `metrics/` 里**真实存在**
- C2 题引用的指标在 `metrics/` 里**真实不存在**（靶子必须是真缺口）
- case id 无重复；引用的夹具文件存在

来由：POC-01 的 `revenue_growth_yoy.yaml` 是冻结之后才被发现不是合法 YAML——
它冻结于 2026-08-10，能解析它的校验器 2026-08-15 才存在。

### 2.2 作废通道

发现某题有误时，按**能否不看系统输出就认定它不成立**分两类：

| 类型 | 判据 | 处理 |
|---|---|---|
| **构造错误** | 不看系统输出即可认定：YAML 非法、标准答案与定义语料矛盾、题面歧义到无唯一解、`expected` 引用了不存在的东西、题目重复 | 标注 `VOIDED`，**移出分母**，单独计数并在报告里公开列出 |
| **迁就模型** | 需要先看到系统的回答才能论证这题「不好」 | **禁止作废**，计为失败 |

分界线是**可判定性**：一旦需要引用系统输出来主张某题该废，它就落入第二类。

**作废率上限**：单批次构造错误作废率 > 10% ⇒ 该批次判为构造失败，**整批作废重建**，
且必须公开记录作废率。挑几题剔掉再报分数就是粉饰。

### 2.3 冻结范围

冻结集只含**题目、标准答案、判定规则、夹具**。运行器代码、指标定义、文档、报告
**不在冻结集内**。冻结集内一项一文件、哈希逐文件记录——作废一题只动一行，不触及其余。

## 3. 问题集结构

Phase 1 目标 ≥ 20 题，分五类。每类的存在理由不同，不要按"凑数量"分配。

| 类别 | 题量 | 考什么 | 陷阱设计 |
|---|---|---|---|
| **C1 口径歧义** | 6 | 同一个中文指标名对应多种口径，Agent 是否选对并声明版本 | 题面故意使用日常说法（"净利润"而非"归母扣非净利润"） |
| **C2 拒答边界** | 4 | 口径未定义时是否**拒绝作答**而不是猜 | 使用语义层里不存在的指标 |
| **C3 计算正确性** | 5 | 多期、多公司的聚合与比较 | 含缺失期、零分母、同名不同实体 |
| **C4 准则引用** | 3 | 回答能否指回具体准则条号 | 涉及新旧准则交替期 |
| **C5 图谱必要性** | 2 | 纯向量 RAG 是否真的答不了 | 关联方循环、跨年度传播路径 |

### 3.0 核查层题集（`V` 类，2026-09-11 新增，`D-041` §5）

`D-041` 在 `answer_question` 上面加了一层「结论核查」。**上面这半必须有自己的题集**，
理由不是完整性，是本仓已经为同一个形状栽过两次：

| | 那次栽的 |
|---|---|
| `N-64` | 生产接了模型，评测没接 |
| `N-78` | 评测臂不传 `ask_model` ⇒ frozen-01 的分数不是线上那套系统的分数 |

⇒ **`R-1` 与 `frozen-03` 是同一件事的两半，不许拆开先做一半**（`D-041` §5 逐字）。

`C1`…`C5` **一个字都不改**（`D-012`），它们覆盖的是下面那半。
`V` 类是**另一种题面**：输入是一段结论，期望是**五态判定的那张表**。

| 类别 | 题量 | 考什么 |
|---|---|---|
| **V1 读成什么** | 3 | 「有没有一个没被主体/期间消耗掉的数值」这条判据，含遮罩的负控制 |
| **V2 五态** | 8 | 一致 / 不一致 / 口径未声明 / 数据不覆盖 / 变动幅度 |
| **V3 切分与承接** | 3 | 三态混合、承接上下文、顿号不切 |

**判分三样都要对**：读成什么、切成几条、每条判什么。
🔴 **切分本身就是被判分的一部分** —— 只判最终五态而不判切分，
等于允许「把一段话切错、但每一片碰巧判对」拿满分，而那种报告在读者眼里是错的。

**冻结前置门禁**（`eval/freeze.py` 的 `V` 分支，负控制在 `tests/test_frozen03.py`）：
除 §2.1 的通用几条外，另加四条 ——
① `read_as` ∈ {QUESTION, STATEMENT}；② `read_as` 与 `claims` 不自相矛盾；
③ 每条 `verdict` 在五态封闭集里；④ 🔴 每条 `claims[i].text` **真的能从题面里切出来**
（切不出来的期望是永远不可能命中的期望）。
外加一条：**五态在门禁里硬编码，与实现比对，不一致就拒绝冻结** ——
一道只会跟着实现走的门禁挡不住「实现加了第六态」。

**运行**：`python -m eval.verify_run --suite frozen-03`（冻结校验与 `eval/run.py` 共用同一份实现）。

⚠️ **`frozen-03` 首跑 14/14，而它是回归不是检验** —— 出题人就是写实现的人，
限定逐条写在 `eval/frozen-03/FREEZE.md` §限定，台账 `N-80`。
**不得转述成「核查准确率 100%」。**

### 3.1 单题格式

```yaml
id: Q-C1-001
category: C1
frozen_at: "2026-xx-xxTxx:xx:xxZ"
question: |
  <自然语言问题，使用日常说法>
expected:
  metric_id: <语义层指标 id>
  metric_version: 1
  value: <标准答案>
  tolerance: {abs: 1e-6, rel: 1e-9}
required_evidence:                 # 证据链必须包含的字段
  - data_source
  - metric_definition_version
  - execution_hash
judging:
  correctness: exact | tolerance | set_equality
  refusal_expected: false
attribution_hint: null             # 仅用于事后分析，判分时不可见
```

### 3.2 拒答题的判定

C2 类题目的正确行为是**拒绝作答并给出机读理由**。

> 判定依赖 `PROJECT_SPEC.md` §5.1 的 `undefined_conditions` **可对给定数据求值**这一约束
> （行为契约见 `openspec/specs/semantic-layer/metric-definition/spec.md` R7）。
> 若某条未定义条件写成不可求值的自然语言，则该题无法机械判定——
> 这不算模型失败，算**定义不合规**，须先修定义再评测。

- 拒答且理由正确 → `PASS`
- 拒答但理由错误 → `FAIL`（归因：口径层）
- 强行给出答案 → `FAIL`（归因：口径层），**即使数字碰巧对也算失败**

这一条是 H1 的核心检验。一个会猜口径的系统在审计场景是负资产。

### 3.3 判分者约束（2026-08-22 新增，四条硬规则）

**来源**：hello-agents 第 12 章（智能体性能评估，2743 行全文读完，
`references/hello-agents-deep-read-part2.md`）暴露的四个缺陷，
以及 **POC-01 自己踩过的同一个坑**。四条都是本项目**已经或差点犯过**的错，不是预防性清单。

**J-1 判分者与被判者不得同源。**
LLM-as-judge 时，判分者必须与被判者**不同厂商或不同代**；做不到就**只能用机械判定**，
禁止用 LLM 判分并把结果当分数。
> 依据：hello-agents 的 judge 与被判模型是同一个（`part2` 已记）；
> **而 POC-01 的两位回答者也是同源模型、错误相关**
> （`docs/agent/poc-01/COMPARISON.md`，H1 的 PASS 因此只是「未被证伪」）。
> 我们在别人身上看见的缺陷，自己也有一份，且当时没写成规则。

**J-2 解析失败必须单独计类，不得并入任何实质结果。**
判分器解析不出模型输出时记 `UNPARSEABLE`，**不得计为平局、不得计为答错、不得计为拒答**，
且必须在报告里单独给出该类计数；`UNPARSEABLE` 占比 > 10% 时该轮评测结果不成立。
> 依据：hello-agents 把解析失败**静默归入 Tie**——失败被计成中性结果，分数看着正常。
> 本项目已有同型先例并已处理正确：DeepSeek 推理模型 `max_tokens` 不足会返回空正文，
> **空正文记 ERROR，不记「模型答错」**。J-2 把这条个案升级为协议规则。

**J-3 成对比较必须做顺序互换复跑。**
任何「A 与 B 哪个更好」式判分，必须把 A/B 顺序互换再跑一遍；
两次结论不一致的题记 `ORDER_UNSTABLE` 并**移出分子分母**，
不稳定率必须随报告一并给出。
> 依据：`position bias` / `自我偏好` / `self-preference` 三词在
> hello-agents **全书 16 章中英 32 份 md 里零命中**（2026-08-22 本项目实跑复核）。
> 它们不是没做好，是**从未被提出**。

**J-4 判分一致性必须有数字，没有就写「未测」。**
凡使用非机械判分，报告中必须给出一致性数字（同一判分者重跑的自洽率，
或两位判分者的一致率）；**给不出就明写「未测」，不得省略**。
> 依据：`校准` / `Kappa` / `标注一致` 三词在 hello-agents 全书同样零命中。
> 省略与零是两回事——这正是 `references/deepseek-harness-notes-part2.md` 记的
> 「**an omitted section cannot distinguish an audited absence from forgotten documentation**」。

**J-5 闸门类断言必须配一条负控制用例。**（2026-08-24 新增，登记册 `L-48`）
凡是断言「系统在某情况下会拒绝 / 会 fail-closed / 会留痕」的评测项，
必须同时存在一条**去掉该闸门后会失败**的用例。
判定分层里因此要能区分「闸门拒对了」与「闸门根本不可能触发」——
后者在当前的四层归因里会被记成前者。
> 依据：`references/deepseek-harness-docs-part2.md` 逐字引 harness `testing.md:34`
> **`A guard only guards if the regression actually fails it`**，
> 并在 `postmortem/0001:104` 有真实执行记录（178 个单测全绿 + 100% 行覆盖，产品完全不可用）。
> **本项目已有同型实证**：2026-08-24 建第六道门时首次实跑「造回归 → 看红 → 回退」，
> 造回归的脚本把受害文件切成语法错误，门禁崩在 traceback 上
> ——**它红了，但红的原因不是它声称在查的那件事**。
> 所以负控制不只要「会红」，还要**红的原因正确**。
> 实施侧的对应物是 `scripts/check_gates.py` 的 R2 与 `rules/commands.md` 的三步程序，
> **两者都不能替代本条**：那两者管的是仓库自己的门禁，本条管的是**被评测系统**的闸门。

**J-6 证据 id 的字面存在性必须被机械检查。**（2026-08-24 新增，登记册 `L-49`）
回答里出现的每一个证据标识（`field_id` / `page` / 表格锚点 / 准则条号）
必须**逐字出现在本次提供给系统的上下文或抽取记录里**；对不上的记 `FAIL`，
归因为**表达层**（2026-09-05 更正：原写「证据层」，而 §5 的分层表里没有这一层
—— 那是措辞失误，不是第五层。处置见 §5.5 ②），
**不得因为最终数字正确而放过**。
> 依据：hello-agents `ch9:1526` 的示例答案引用了一个
> **从未进入上下文的笔记 id**（`references/hello-agents-ch09-context.md` 的 `C9-B10` 条）。
> **这是唯一一条不需要 LLM 判分就能跑的证据链检查**，与 J-1 天然互补：
> J-1 限制什么时候能用 LLM 判分，本条给出一个**根本不需要 LLM** 的判据。
> 与 `AC-07`（准则引用可定位到条号）的区别：AC-07 查「能不能定位」，
> 本条查「这个标识是不是它自己编出来的」——**后者是前者的前提**。

**J-7 引用截断内容而未声明取数口径的，记失败。**（2026-08-24 新增，登记册 `L-2` 的评测侧）
答案引用的表格片段若来自被截断 / 被抽样 / 被重排的来源，
证据链里必须同时给出**取数口径三元组**（抽样方式 / 排序键 / 截断位置）；
缺任何一项记 `FAIL`，归因为**表达层**（同上，2026-09-05 更正）。
> 依据：`references/deepseek-harness-notes-part3.md` 的 `C-A1`
> ——它出自该文件 bug-fix 段的一条笔记，是该轮唯一一条
> 「工具成功、结果正确、模型结论错」的完整病例，
> 而年报抽取里「取前 N 行 / 按某列排序 / 页内截断」与它逐项同构。
> 架构侧已落于 `ARCHITECTURE.md` §8.5.2，**本条是它在评测协议里的对应判据**。

**与 §2 冻结规则的关系**：J-1…J-7 约束的是**判分过程与证据判据**，
§2 约束的是**题目与答案**。两者互不替代——
判分者有偏的评测，即使题目冻结得再干净，分数也不可用。

**两组的分工**：**J-1…J-4 防判分者不可信，J-5…J-7 防被判对象看起来对。**
两组都成立，评测结论才成立。

## 4. 复核实验（H2 判定门）

这是本项目最重要、也最容易被跳过的评测。

### 4.1 方法

1. 从已评测问题中随机抽 15 题，其中**必须包含至少 5 题模型答错的**。
2. 复核者（有审计或财务背景）**只看证据链**，看不到模型的自然语言解释、看不到标准答案。
3. 每题限时 5 分钟，判断"这个答案对不对"，输出 `对 / 错 / 无法判断`。
4. 与标准答案比对，计算一致率。

#### 4.1.1 可计分池不足时按普查执行（2026-09-05 增补，`D-037`）

第 1 步预设了「池子比 15 大」。frozen-01 上不成立：20 题里 5 题
`NOT_RUN`（C4 准则检索 / C5 图谱确实没有路径）⇒ **可计分池恰好 15，其中答错 4**。
于是「随机抽 15」抽的是整个池子（随机性为零），「至少 5 题答错」差 1 题不可满足。

**规则**：设可计分池为 `N`、其中答错为 `W`。

| 条件 | 做法 |
|---|---|
| `N > 15` | 按第 1 步随机抽 15，且必须含 ≥ 5 题答错 |
| `N ≤ 15` | **普查**：`N` 题全上，一题不挑 |

普查时报告**必须逐条写明**下面三项，缺一项即视为未按本节执行：

1. **这是普查，不是随机抽样。** 不许写成「按 §4.1 随机抽取」——
   两者的效力不同（普查更强，挑样本风险为零），但**说法必须与做法一致**。
2. **`≥5 题答错` 是否满足，以及 `W` 的实际值。** 不满足就写不满足。
3. 🔴 **错答案子集上的分辨率下限 = `1/W`。** `W = 4` 时最小刻度是 **25 个百分点** ——
   §4.2 那三档在这个分辨率下**分不开相邻两档**。这一条不许省略（见 §4.2 的附注）。

⚠️ **三条禁止**，都是为了防「照着手上的数据改协议」（`D-030` / `N-44` 在别处点名的同一形状）：

- **不得为了凑够 `W ≥ 5` 去挑题、换套件、或调判分口径。**
- **不得把 `≥5` 这个数下调**成手上恰好有的那个数。
- **不得因为普查「更强」就省掉上面三项标注。** 更强的是证据，不是措辞的豁免权。

### 4.2 判定门

| 一致率 | 结论 | 下一步 |
|---|---|---|
| ≥ 80% | H2 **通过** | 进入 Phase 3 |
| 60% – 80% | H2 **部分成立** | 分析哪些字段没起作用，删改证据链字段后重测一次 |
| < 60% | H2 **不成立** | **停止，回到 ARCHITECT 阶段**，不得进入 L2 |

⚠️ **附注（2026-09-05，`D-037`）：`W < 5` 时这张表分不开相邻两档。**
一致率整体按 `N` 题算，分辨率是 `1/N`；但这三档的**设计意图**是区分
「证据链够用 / 勉强 / 不够用」，而能区分这件事的信号主要来自**错答案**那一子集
（全判对也能在正确答案上拿高一致率）。`W = 4` 时该子集的最小刻度是 25 个百分点，
`80%` 与 `60%` 这两条线之间只放得下**不到一格**。
⇒ 落在 60%–80% 这一档时，**不许直接读成「部分成立」**，
必须同时给出错答案子集上的原始计数（`x / W`），由操作者判这一档是否可信。

### 4.3 反向检验

统计"复核者判为无法判断"的题目占比。若 > 20%，说明证据链缺关键字段，即使一致率达标也要补齐后重测。

## 5. 失败归因分层

每个失败必须落到至少一层。**允许多标签**，不允许"未知"。

```text
意图层  Intent       ——  题面里的三元组（指标 / 主体 / 期间）没解析出来，还没走到取数
检索层  Retrieval    ——  没找到该找的准则条文 / 年报段落
口径层  Definition   ——  用错指标定义、版本不匹配、该拒答却作答、拒答理由码给错
计算层  Computation  ——  口径对但算错：聚合粒度、缺失期、零分母、类型
表达层  Presentation ——  算对但**答案或其证据链**不符合契约：返回结构 / 单位 / 精度，
                         或证据标识对不上、取数口径三元组缺项（§3.3 的 J-6 / J-7）
```

**五层，2026-09-05 起**（原为四层，处置见 §5.5）。顺序即流水线顺序：
意图层在最前，它失败时后面四层一步都没走。

### 5.0 `UNPARSEABLE` 不进归因分层

按 J-2，解析失败**不是一种失败原因**，是**一次无效观测**。
它不落进上面四层中的任何一层，单独计数、单独报告。
把它塞进任一层都会让那一层的数字虚高——这正是 hello-agents 把解析失败并入 Tie 的错误形状。

### 5.1 归因不改变分数

归因是**诊断投影**，不是评分机制。

- 一道失败题无论归因到哪一层，它都仍然是失败。
- 分母永远是全部题目，不因归因而裁剪。
- 归因用于决定下一阶段修哪一层，不用于事后洗结果。

> **与 §2.2 作废通道的边界**（2026-08-15）：这里说的「分母不裁剪」管的是**归因**——
> 系统答错了，无论归到哪一层都仍是失败，不许用归因把失败洗掉。
> §2.2 的作废管的是**题目本身从一开始就不成立**，且必须**不看系统输出**即可认定。
> 两者不冲突：前者是「答错了不许赖」，后者是「这题根本没考成」。
> 判据一旦需要引用系统输出，就落回本节，不许走作废。

这条规则直接对应 `data secret` 项目里学到的教训：**没有这条约束，任何失败都能被解释成"不是我的问题"，实验就失去了可证伪性。**

### 5.2 多标签示例

> 问"某公司近三年扣非净利润复合增长率"，模型用了含非经常性损益的口径，且缺失年度按 0 处理。
> 归因：`口径层` + `计算层`。两个标签同时记录，不选"主要原因"。

### 5.3 归因分类**不得依赖上游可变措辞**（2026-08-27 新增，登记册 `L-50`）

出处 `references/deepseek-harness-notes-part3.md` C-N6
（源 `HN-B07:15,35`；`HN-` 是本行加的来源前缀，指 harness
`.agents/notes/implemented/bug-fix` 的第 7 篇——原编号裸写会被 `scripts/check_xrefs.py`
解析成本仓台账 B 区的条目）：

harness 把传输层错误分类**靠匹配上游的错误措辞**
（`terminated` / `stream ended before message_stop` → `TRANSPORT`）。
**他们自己把残余风险写死在笔记里**：

> a future pi-ai release that **rewords** these errors would **silently** fall back to `PI_AI_ERROR`

并留了 `XXX` 锚点与 Known-Limitations 一节。

**处置方式该学**（承认 + 留可搜的锚点 + 写进限制清单），**分类本身不能用在证据链上**。

⇒ **本协议的硬约束**：§5 四层归因与 §5.0 的 `UNPARSEABLE`，
**判据不得建立在被抽取文本的措辞或排版上**。
理由是本项目的上游比 harness 的更善变：`D-013` 已把数值权威源定为**年报 PDF 原文**，
而 PDF 的排版**每家公司、每一年都不一样**，且**改版不会通知任何人**。
一个「靠标题文字长什么样」判出来的归因，在排版一变时**静默退回「正常」**——
失败被记成成功，而 §5.1 说的「归因不改变分数」在这里救不了：
它管的是「答错了不许赖」，管不了「根本没被判成答错」。

⇒ **可用的判据形态**（按优先级）：
1. **结构位置** —— 页码 / 表格坐标 / 章节序号，来自 `D-019` 的 `page` + `anchor_page`；
2. **口径标识** —— `metric_id` / 口径版本号，来自语义层，与 PDF 文本无关；
3. **闸门返回值** —— `RefusalCode` 是封闭枚举（`ARCHITECTURE.md` §8.2），本身就是机读的。

⚠️ **确实只能靠文本判的那一类，必须显式登记**：写一条带固定前缀的注释锚点，
并列进本节下方的「已知限制」，**不许当成一条正常判据混在里面**。
当前已知限制：**0 条**（Phase 1.5 的抽取器尚未产出真实归因，
现在写下的任何一条都会是 F-2 那种「求不出值的规则」）。

### 5.4 一次外部对照：三分层与本项目四层的映射（2026-08-27 新增，登记册 `L-51`）

出处 `references/hello-agents-ch06-frameworks.md` S6-7（`ch6:1286`）——
该书给图结构 Agent 的调试三分层，产物判定它是「全章最有用的一句」：

| 书给的层 | 本项目的对应物 | 落在 §5 的哪一层 |
|---|---|---|
| ① 节点**内部逻辑**错 | **抽取错** —— 取到的数与年报原文对不上 | 检索层（找错段落）或计算层（读错单元格） |
| ② 节点**间传递的状态数据异变** | **口径传递错** —— 抽取时的口径版本与计算时用的不是同一个 | 口径层 |
| ③ **边跳转条件**失误 | **闸门判定错** —— 该拒答没拒、不该拒却拒了 | **口径层**（该拒未拒、拒答理由码给错，两者都是；2026-09-05 更正，见 §5.5 ①） |

**这是对照，不是替换。** §5 的四层按「错在哪一环」切，三分层按「错在节点里还是节点之间」切，
两者正交。放这里的用处是**做一次覆盖自检**：
四层能不能各自表达上面三种？上表说明能，**但第 ② 类落点最含糊**——
「口径传递错」在四层里只能记成口径层，而它与「一开始就选错口径」是两种不同的修法。

⇒ ~~**本轮不新增第五层**（那会动 `AC-08` 的 100% 归因覆盖率口径，属规格变更）。~~
🔴 **2026-09-05 更正：这句话的理由不成立。** `AC-08` 逐字是
「失败归因覆盖率 100%（每个失败都落到至少一层，不允许"未知"）」——
**它对层数不置一词**。加一层不动 `AC-08` 的口径；不加层反而让 2 / 4 条真实失败
被硬塞进一个不对的层，那才是让 `AC-08` 的 100% 变成假的。
第五层已于同日新增，见 §5.5 ③。

⇒ **本轮的产出是一条待验事实**：Phase 2 复核实验回来后，
若「口径层」的失败里**超过一半是传递错而不是选择错**，就说明这一层该拆。
判据写在这里，不写进 `PROJECT_SPEC.md`——它是**归因分层的演化触发条件**，不是验收标准。
⚠️ **2026-09-05 实测：传递错 : 选择错 = 0 : 2，但这个 0 是结构性零** ——
四条 FAIL 的 `gate` 全是 `not_reached`，而传递错至少要走到闸门之后才可能发生；
frozen-01 上 `CROSS_VERSION_COMPARISON` 是零样本。
**这是一条从不触发的判据，不许把它读成「已验证不需要拆」**（`N-56`）。

### 5.5 三处「规格自己对不上」的处置（2026-09-05，台账 `N-56`）

`02-02` 起草时的独立复核摆出三处不一致，**每一处都被复核过、不是转述**。
操作者 2026-09-05 授权由执行者裁。**判定与理由逐条如下**，落成 `D-035`。

**① 拒答理由码给错 → 口径层。** §3.2 与 §5.4 此前给出不同的层，§3.2 是对的。

理由：`RefusalCode` 是封闭枚举，**它就是这个系统对外可观测的判断本身**，
不是判断的一种排版。从外部**分不开**「判错了」与「判对了但贴错标签」——
后者要成立必须有「内部判断是对的」这个证据，而评测手上没有。
⇒ 归到**要求证据更少**的那一层，是 fail-closed 的方向。
§5.4 里那个「或表达层」的括号已划掉。

**② 「证据层」不是第五层，是措辞失误 → 归表达层，并把表达层的定义拓宽。**

§3.3 的 J-6 / J-7 写着「归因为证据层」，而 §5 的分层表里从来没有这一层。
两条讲的都是**答案的证据链站不站得住**（编出来的证据标识 / 缺取数口径三元组），
而证据链是在**组装答案**这一步产出的 ⇒ 表达层。
⇒ 表达层的定义相应拓宽：原来只写「返回结构/单位/精度」，
那是在 `D-003` 把证据链定为第一类产物**之前**的写法，覆盖不到它自己的失败。

⚠️ **这不是把一个空层填满。** 归表达层之后它**仍然一次都没被吐出来过** ——
而且不是「这一批恰好没有」，是**结构上不可达**：当前实现里没有任何一条路产得出它
（合成夹具没有 PDF、没有页码，`J-6` / `J-7` 要到 Phase 3 的真实抽取路径才可能触发）。
⇒ 报告把这两件事**分开报**（`eval/attribution.py` 的 `reachable_layers()`
从映射表机械算出，不是手写的一句话）：

**③ 「意图解析失败」新增第五层：意图层。**

这是三条里**唯一有真实数据**的一条：4 条真实 FAIL 里有 2 条是这一类
（`Q-C3-003` 题面无主体、`Q-C3-004` 题面出现两个期间），
两条的 `Answer.gate` 都是 `not_reached`。它不是检索层（没去找任何东西）、
不是口径层（口径没被选错，压根没走到选口径）、不是计算层、不是表达层。

**边界写死，不靠感觉**：

| 机读状态 | 层 | 为什么 |
|---|---|---|
| `INTENT_INCOMPLETE` | **意图层** | 题面缺主体 / 期间，或出现多个期间 —— 与口径无关 |
| `METRIC_NOT_DEFINED` | **口径层** | 别名表认不出这个说法，**别名表是口径产物** |

⇒ 归因覆盖率仍是 4/4，但其中 2 条从一个借来的层换回了它真正的层。
**覆盖率不变、真实性变了** —— 这正是 `AC-08` 那句「不允许未知」要的东西。

⚠️ **加层的成本要如实记**（2026-09-05 frozen-01 全量实测）：

| 层 | 本批吃到的失败题 | 结构上产得出来吗 |
|---|---|---|
| 意图层 | 2 | ✅ |
| 检索层 | 0 | ✅（`UNAVAILABLE`） |
| 口径层 | 2 | ✅ |
| 计算层 | 0 | ✅（数值超容差等四种） |
| 表达层 | 0 | 🔴 **不可达** |

**「本批零产出」与「结构上不可达」是两件事。** 前者是数据，
后者是 `L-25` 那种死抽象 —— 混成一句「零产出」，读的人会以为再多跑几批就有了。
⇒ **盯着表达层**：到 Phase 3 真实抽取路径接上之后仍不可达，就该重新问它该不该在这。

## 6. 评测指标

| 指标 | 定义 |
|---|---|
| 口径正确率 | 选对指标定义且版本匹配的题目占比 |
| 拒答准确率 | C2 类题中正确拒答且理由正确的占比 |
| 答案正确率 | 数值/集合在容差内匹配标准答案的占比 |
| 证据链完整率 | 证据链必需字段齐全的题目占比（目标 100%） |
| **复核一致率** | H2 实验中复核者判断与标准答案一致的占比 |
| 引用可定位率 | 准则引用能定位到具体条号的占比 |
| 归因覆盖率 | 失败题中已归因的占比（目标 100%） |
| **核查判定正确率**（`V` 类） | 读成什么 / 切分 / 每条五态**三样全对**的题目占比。⚠️ 三样里**少一样都算不通过** —— 切错但碰巧判对不给分 |

## 7. 阶段门

### Phase 1 门（语义层）

1. 语义层覆盖 ≥ 20 个指标，字段齐全（AC-01）
2. C2 类拒答准确率 = 100%（**一个都不能猜**）。分母为**未作废**的 C2 题；
   且 C2 存活题数 < 3 时本条不成立——否则作废到只剩 1 题再报 100% 就是钻空子
3. 问题集 ≥ 20 题且已冻结（含时间戳与哈希），冻结**通过 §2.1 前置门禁**
4. 仓库零非公开数据（自动扫描通过）
5. 构造错误作废率 ≤ 10%（§2.2），且作废清单已公开列出

### Phase 2 门（证据链）

1. 证据链完整率 = 100%
2. **复核一致率 ≥ 80%**（H2 判定门）
3. 「无法判断」占比 ≤ 20%
4. 归因覆盖率 = 100%

### Phase 3 门（知识层）

1. 引用可定位率 ≥ 90%（抽检 20 条）
2. C5 类问题已给出「图能答、纯 RAG 答不了」的**实证对比**，而不是论证
3. 若 C5 无法证明图的必要性 → **不建图，记录并跳过**（D-007）

## 8. 决策分支

| 情况 | 动作 |
|---|---|
| 全部阶段门通过 | 进入 Phase 4，产出对外材料 |
| H1 不成立（>5 个指标无法给出无歧义口径） | 停止，重新定义问题 |
| H2 不成立（一致率 < 60%） | 回到 ARCHITECT，重设计证据链，不进 L2 |
| H3 不成立 | 跳过图谱，**这不是失败** |
| 口径正确但答案错误率高 | 问题在计算层，修 L3 而不是 L1 |
| 答案正确但复核一致率低 | 问题在证据链设计，修 L3 展示而不是模型 |

## 9. 本协议不做的事

- 不与其他 Agent 产品做横向 benchmark。样本量和场景都不支持这种比较。
- 不报告"准确率 XX%"作为对外宣传数字。分层结果才有信息量。
- 不在同一次评测里同时改多个变量。一次只动一层。
- 不把 `data secret` 的任何评测数据、Trial 结果或 Iteration 结论用作本项目的证据（D-004）。
