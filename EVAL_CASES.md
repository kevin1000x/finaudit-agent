# 评测协议与问题集 — finaudit-agent

## 1. 目的

本协议测试三件互相独立的事，**任何一项的正面结果都不能替代另外两项**：

| 层 | 问的问题 | 对应假设 |
|---|---|---|
| **口径可判定性** | 指标口径能否被机械判定对错？ | H1 |
| **证据链可复核性** | 人能否仅凭证据链在 5 分钟内独立判断对错？ | H2 |
| **图谱必要性** | 是否存在纯向量 RAG 答不了而图能答的问题？ | H3 |

答案正确但证据链不可复核 = H1 通过、H2 失败。这两件事分开记分。

## 2. 冻结规则（D-012，2026-08-15 修订）

问题集与标准答案在**看到任何模型输出之前**冻结，冻结时记录 UTC 时间戳与文件 SHA-256。

- 冻结后**允许**：重跑评测、改进实现、追加新批次问题（新批次单独编号）。
- 冻结后**禁止**：修改已冻结问题的标准答案或判定规则来迁就模型表现。

### 2.1 冻结前置门禁

**未通过机器校验的内容不允许进入冻结集。** 冻结由脚本执行，脚本先跑校验，
校验不过就拒绝冻结（fail-closed），不由人手工打哈希。冻结前必须机械确认：

- 每份 case 是合法 YAML，必填字段齐全
- C1/C3/C4/C5 题的 `expected.metric_id` 在 `metrics/` 里**真实存在**
- C2 题引用的指标在 `metrics/` 里**真实不存在**（靶子必须是真缺口）
- case id 无重复；引用的夹具文件存在

来由：POC-01 的 `revenue_growth_yoy.yaml` 是冻结之后才被发现不是合法 YAML——
它冻结于 2026-08-10，能解析它的校验器 2026-08-15 才存在。

### 2.2 作废通道

发现某题有误时，按**能否不看系统输出就认定它不成立**分两类：

| 类型 | 判据 | 处理 |
|---|---|---|
| **构造错误** | 不看系统输出即可认定：YAML 非法、标准答案与定义语料矛盾、题面歧义到无唯一解、`expected` 引用了不存在的东西、题目重复 | 标注 `VOIDED`，**移出分母**，单独计数并在报告里公开列出 |
| **迁就模型** | 需要先看到系统的回答才能论证这题「不好」 | **禁止作废**，计为失败 |

分界线是**可判定性**：一旦需要引用系统输出来主张某题该废，它就落入第二类。

**作废率上限**：单批次构造错误作废率 > 10% ⇒ 该批次判为构造失败，**整批作废重建**，
且必须公开记录作废率。挑几题剔掉再报分数就是粉饰。

### 2.3 冻结范围

冻结集只含**题目、标准答案、判定规则、夹具**。运行器代码、指标定义、文档、报告
**不在冻结集内**。冻结集内一项一文件、哈希逐文件记录——作废一题只动一行，不触及其余。

## 3. 问题集结构

Phase 1 目标 ≥ 20 题，分五类。每类的存在理由不同，不要按"凑数量"分配。

| 类别 | 题量 | 考什么 | 陷阱设计 |
|---|---|---|---|
| **C1 口径歧义** | 6 | 同一个中文指标名对应多种口径，Agent 是否选对并声明版本 | 题面故意使用日常说法（"净利润"而非"归母扣非净利润"） |
| **C2 拒答边界** | 4 | 口径未定义时是否**拒绝作答**而不是猜 | 使用语义层里不存在的指标 |
| **C3 计算正确性** | 5 | 多期、多公司的聚合与比较 | 含缺失期、零分母、同名不同实体 |
| **C4 准则引用** | 3 | 回答能否指回具体准则条号 | 涉及新旧准则交替期 |
| **C5 图谱必要性** | 2 | 纯向量 RAG 是否真的答不了 | 关联方循环、跨年度传播路径 |

### 3.1 单题格式

```yaml
id: Q-C1-001
category: C1
frozen_at: "2026-xx-xxTxx:xx:xxZ"
question: |
  <自然语言问题，使用日常说法>
expected:
  metric_id: <语义层指标 id>
  metric_version: 1
  value: <标准答案>
  tolerance: {abs: 1e-6, rel: 1e-9}
required_evidence:                 # 证据链必须包含的字段
  - data_source
  - metric_definition_version
  - execution_hash
judging:
  correctness: exact | tolerance | set_equality
  refusal_expected: false
attribution_hint: null             # 仅用于事后分析，判分时不可见
```

### 3.2 拒答题的判定

C2 类题目的正确行为是**拒绝作答并给出机读理由**。

> 判定依赖 `PROJECT_SPEC.md` §5.1 的 `undefined_conditions` **可对给定数据求值**这一约束
> （行为契约见 `openspec/specs/semantic-layer/metric-definition/spec.md` R7）。
> 若某条未定义条件写成不可求值的自然语言，则该题无法机械判定——
> 这不算模型失败，算**定义不合规**，须先修定义再评测。

- 拒答且理由正确 → `PASS`
- 拒答但理由错误 → `FAIL`（归因：口径层）
- 强行给出答案 → `FAIL`（归因：口径层），**即使数字碰巧对也算失败**

这一条是 H1 的核心检验。一个会猜口径的系统在审计场景是负资产。

## 4. 复核实验（H2 判定门）

这是本项目最重要、也最容易被跳过的评测。

### 4.1 方法

1. 从已评测问题中随机抽 15 题，其中**必须包含至少 5 题模型答错的**。
2. 复核者（有审计或财务背景）**只看证据链**，看不到模型的自然语言解释、看不到标准答案。
3. 每题限时 5 分钟，判断"这个答案对不对"，输出 `对 / 错 / 无法判断`。
4. 与标准答案比对，计算一致率。

### 4.2 判定门

| 一致率 | 结论 | 下一步 |
|---|---|---|
| ≥ 80% | H2 **通过** | 进入 Phase 3 |
| 60% – 80% | H2 **部分成立** | 分析哪些字段没起作用，删改证据链字段后重测一次 |
| < 60% | H2 **不成立** | **停止，回到 ARCHITECT 阶段**，不得进入 L2 |

### 4.3 反向检验

统计"复核者判为无法判断"的题目占比。若 > 20%，说明证据链缺关键字段，即使一致率达标也要补齐后重测。

## 5. 失败归因分层

每个失败必须落到至少一层。**允许多标签**，不允许"未知"。

```text
检索层  Retrieval    ——  没找到该找的准则条文 / 年报段落
口径层  Definition   ——  用错指标定义、版本不匹配、该拒答却作答
计算层  Computation  ——  口径对但算错：聚合粒度、缺失期、零分母、类型
表达层  Presentation ——  算对但返回结构/单位/精度不符合契约
```

### 5.1 归因不改变分数

归因是**诊断投影**，不是评分机制。

- 一道失败题无论归因到哪一层，它都仍然是失败。
- 分母永远是全部题目，不因归因而裁剪。
- 归因用于决定下一阶段修哪一层，不用于事后洗结果。

> **与 §2.2 作废通道的边界**（2026-08-15）：这里说的「分母不裁剪」管的是**归因**——
> 系统答错了，无论归到哪一层都仍是失败，不许用归因把失败洗掉。
> §2.2 的作废管的是**题目本身从一开始就不成立**，且必须**不看系统输出**即可认定。
> 两者不冲突：前者是「答错了不许赖」，后者是「这题根本没考成」。
> 判据一旦需要引用系统输出，就落回本节，不许走作废。

这条规则直接对应 `data secret` 项目里学到的教训：**没有这条约束，任何失败都能被解释成"不是我的问题"，实验就失去了可证伪性。**

### 5.2 多标签示例

> 问"某公司近三年扣非净利润复合增长率"，模型用了含非经常性损益的口径，且缺失年度按 0 处理。
> 归因：`口径层` + `计算层`。两个标签同时记录，不选"主要原因"。

## 6. 评测指标

| 指标 | 定义 |
|---|---|
| 口径正确率 | 选对指标定义且版本匹配的题目占比 |
| 拒答准确率 | C2 类题中正确拒答且理由正确的占比 |
| 答案正确率 | 数值/集合在容差内匹配标准答案的占比 |
| 证据链完整率 | 证据链必需字段齐全的题目占比（目标 100%） |
| **复核一致率** | H2 实验中复核者判断与标准答案一致的占比 |
| 引用可定位率 | 准则引用能定位到具体条号的占比 |
| 归因覆盖率 | 失败题中已归因的占比（目标 100%） |

## 7. 阶段门

### Phase 1 门（语义层）

1. 语义层覆盖 ≥ 20 个指标，字段齐全（AC-01）
2. C2 类拒答准确率 = 100%（**一个都不能猜**）。分母为**未作废**的 C2 题；
   且 C2 存活题数 < 3 时本条不成立——否则作废到只剩 1 题再报 100% 就是钻空子
3. 问题集 ≥ 20 题且已冻结（含时间戳与哈希），冻结**通过 §2.1 前置门禁**
4. 仓库零非公开数据（自动扫描通过）
5. 构造错误作废率 ≤ 10%（§2.2），且作废清单已公开列出

### Phase 2 门（证据链）

1. 证据链完整率 = 100%
2. **复核一致率 ≥ 80%**（H2 判定门）
3. 「无法判断」占比 ≤ 20%
4. 归因覆盖率 = 100%

### Phase 3 门（知识层）

1. 引用可定位率 ≥ 90%（抽检 20 条）
2. C5 类问题已给出「图能答、纯 RAG 答不了」的**实证对比**，而不是论证
3. 若 C5 无法证明图的必要性 → **不建图，记录并跳过**（D-007）

## 8. 决策分支

| 情况 | 动作 |
|---|---|
| 全部阶段门通过 | 进入 Phase 4，产出对外材料 |
| H1 不成立（>5 个指标无法给出无歧义口径） | 停止，重新定义问题 |
| H2 不成立（一致率 < 60%） | 回到 ARCHITECT，重设计证据链，不进 L2 |
| H3 不成立 | 跳过图谱，**这不是失败** |
| 口径正确但答案错误率高 | 问题在计算层，修 L3 而不是 L1 |
| 答案正确但复核一致率低 | 问题在证据链设计，修 L3 展示而不是模型 |

## 9. 本协议不做的事

- 不与其他 Agent 产品做横向 benchmark。样本量和场景都不支持这种比较。
- 不报告"准确率 XX%"作为对外宣传数字。分层结果才有信息量。
- 不在同一次评测里同时改多个变量。一次只动一层。
- 不把 `data secret` 的任何评测数据、Trial 结果或 Iteration 结论用作本项目的证据（D-004）。
