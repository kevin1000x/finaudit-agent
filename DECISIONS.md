# 决策记录 — finaudit-agent

## 1. 状态词汇

- **Accepted** — 有约束力，除非出现新证据触发重新评估。
- **Provisional** — 用于当前阶段，但未确立为长期架构。
- **Deferred** — 有意排除，直到出现可测量的触发条件。
- **Rejected** — 已考虑并排除。

## 2. 决策索引

| ID | 决策 | 状态 | 适用范围 |
|---|---|---|---|
| D-001 | 垂直优先于通用：不做通用 Data Agent | Accepted | 整个项目 |
| D-002 | 语义层先于 Agent | Accepted | Phase 1 |
| D-003 | 证据链是第一类产物，不是日志 | Accepted | 整个项目 |
| D-004 | 与 `data secret` 机制复用、资产隔离 | Accepted | 整个项目 |
| D-005 | 主仓暂不开源；姊妹仓只放语义层与评测集 | Accepted | 至 Phase 4 |
| D-006 | 界面是交付形式不是卖点（2026-08-15 交付形态改为 Web 为主） | Accepted | Phase 2 |
| D-007 | 图谱必须由「非图不可的问题」驱动 | Accepted | Phase 3 |
| D-008 | 投入以 changelog 记录，**不设时间预算上限** | Accepted（2026-08-10 修订） | 整个项目 |
| D-009 | GSD 拥阶段状态，Superpowers 拥门禁，OpenSpec 拥变更契约 | Provisional | 规划与执行 |
| D-010 | 仅公开数据，禁止任何雇主/客户数据 | Accepted | 整个项目，不可协商 |
| D-011 | 不训练、不微调 | Accepted | Phase 0–4 |
| D-012 | 评测集在看到模型输出前冻结 | Accepted（2026-08-15 修订：加冻结前置门禁与作废通道） | 整个项目 |
| D-013 | 财务数值以年报 PDF 原文为准；二手源只作对照，不作权威源 | Accepted（2026-08-10） | 整个项目 |
| D-014 | PDF 抽取器不得链接 AGPL 组件；主库 pdfplumber | Accepted（2026-08-16） | Phase 1.5 起 |
| D-015 | 抽取器与语义层平级；依赖方向单向且由测试强制 | Accepted（2026-08-21） | Phase 1.5 起 |
| D-016 | 映射表按命名空间拆分；标签是有序片段序列，逐字匹配 | Accepted（2026-08-21） | Phase 1.5 起 |
| D-017 | `formula` 的数值执行层自建封闭算术解析器，不引三方求值库 | Accepted（2026-08-21） | Phase 1.5 起 |
| D-018 | 勾稽校验是批次级闸门；fail-closed 放在计算层的读入边界 | Accepted（2026-08-21） | Phase 1.5 起 |
| D-019 | 抽取记录的页码拆成 `page` 与 `anchor_page` 两个整数 | Accepted（2026-08-21） | Phase 1.5 起 |
| D-020 | `scope_change` 改挂新字段 `notes.consolidation_scope_change` | Accepted（2026-08-21） | Phase 1.5 起 |
| D-021 | NFR-02 加静态托管豁免；NFR-04 改写为「巨潮年报 PDF 为唯一权威源」 | Accepted（2026-08-21） | 整个项目 |

---

## D-001 — 垂直优先于通用

- 状态：Accepted
- 背景：通用企业 Data Agent 赛道已有成熟开源项目。`datagallery-lab/datafoundry` 在 2026-06-18 创建后 7 周内达到 648 star（Apache-2.0，TypeScript），已实现 28 数据源、统一语义层、Trace 可审计；`fengyede79/text-to-sql-agent-system`（77★，中文）已覆盖语义预检、多路元数据召回、LangGraph 编排、SQL 安全校验、有界纠错、Recovery Agent 与评测闭环。
- 决策：不做通用 Data Agent。只做财务/审计垂直领域，**指标口径硬编码进语义层**——这是通用 agent 结构上做不到的。
- 被拒方案：把 `cninfo` 扩展成多数据源通用问数平台。
- 主要风险：垂直领域用户基数小，项目的"产品感"弱于通用工具。
- 反转触发条件：出现证据表明财务垂直的口径问题可以被通用语义层完全覆盖。

## D-002 — 语义层先于 Agent

- 状态：Accepted
- 背景：Agent 编排是当前最热但也最容易废弃的一层；指标口径是最不容易过时、也最难被别人复制的一层。
- 决策：先做 L1 语义层，**在没有任何 Agent 的情况下就要能独立成立并可展示**。Agent 编排排在其后。
- 理由：即使后续所有阶段都没做完，L1 本身就是可以写进简历、可以在面试里讲 10 分钟的东西。这是抗风险设计。
- 主要风险：语义层做完但从未被 Agent 消费，变成一堆没人用的 YAML。
- 缓解：Phase 1 的验收标准要求语义层能被评测脚本机械消费（AC-02），而不是只给人看。

## D-003 — 证据链是第一类产物

- 状态：Accepted
- 背景：大多数 agent 项目把 trace 当日志：出问题时翻，平时没人看。本项目的产品价值恰恰在 trace 本身。
- 决策：
  - 每次回答**必须**产出结构化证据链，字段固定；
  - 给不出证据链的回答判为**失败**，不判为"降级成功"；
  - 证据链的可复核性单独设验收标准（AC-06），与答案正确性分开评。
- 主要风险：证据链字段设计过早固化，后期难改。
- 反转触发条件：Phase 2 复核实验表明某些字段从未被复核者使用 → 删掉它们（Harness 做减法）。

## D-004 — 与 `data secret` 机制复用、资产隔离

- 状态：Accepted（2026-08-09 确立；2026-08-10 补充「无时序依赖」条款）
- 背景：两个项目验证的是完全不同的假设。`data secret` 问的是「同一份程序能否在脱敏视图与隐藏数据间不加修改地转移」；本项目问的是「财务口径能否被无歧义定义、结论能否被人独立复核」。共同点只有方法论，没有实质重叠。
- 决策：
  - **可复用**：SHA-256 双校验思想、fail-closed 思想、评测器与标准答案隔离、append-only 证据、失败归因不改分数。
  - **不可复用**：代码、评测语料、Trial 数据、任何 Iteration 结论、任何 D-0xx 决策的权威性。
  - 两个仓库、两套语料、两套结论，互不引用为证据。
- **无时序依赖（2026-08-10 补充）**：
  - 本项目的任何阶段**都不以 `data secret` 的进度、结论或解除阻塞为前置条件**。
  - `data secret` 当前是 Iteration 1 INCONCLUSIVE、Iteration 2 pre-freeze 阻塞——**这是它的状态，不是本项目的约束**。它永远停在那里也不影响本项目推进到 Phase 4。
  - 反向同样成立：本项目的任何结果都不能用于解除 `data secret` 的阻塞或影响其判定。
  - 因此不存在"等 data secret 完成再做 X"这种排期。若未来任何计划、handoff 或会话出现这类表述，视为措辞错误，直接删除。
- 理由：`data secret` 是冻结协议下的可证伪实验，任何交叉污染会同时毁掉两边的可信度。但"不能污染"跟"要等它"是两回事——前者是资产隔离，后者是时序依赖，本决策只要求前者。
- **曾以为唯一真实的耦合是时间**——2026-08-10 D-008 修订后取消了一切时间预算上限，该耦合随之消失（U-04 因前提不存在而关闭）。两个项目现在**在任何维度上都不互为约束**：不共用语料、不互引结论、不互为前置、也不再切分同一份预算。
- 主要风险：机制思想被复用时连带把不适用的严格性也搬过来（例如本项目并不需要单次评分不可重跑）。
- 澄清：本项目的评测**允许重跑**，因为它不是一次性冻结实验。冻结的只有问题集（D-012）。

## D-005 — 主仓暂不开源

- 状态：Accepted（操作者决定，2026-08-09）
- 决策：主仓库保持私有直到 Phase 4。可对外的替代展示物为三样：
  1. 一页架构图（不含实现细节）
  2. **可访问的 Web 演示链接**（2026-08-15 随 D-006 修订，原为「60 秒 TUI 录屏」；
     演示环境不连任何私有数据，只跑公开年报）；无法长期托管时退回 60 秒录屏
  3. 一页脱敏评测报告
- 同时：**语义层定义文件 + 评测问题集**可在 Phase 1 结束时单独开源为姊妹仓库，两者均不含实现代码与未公开结论。
- 理由：不开源在审计/金融方向是加分而非减分，但"完全无法展示"是硬伤，必须用替代物补上。
- 反转触发条件：Phase 4 完成且已有 offer → 重新评估开源。

## D-006 — 界面是交付形式不是卖点

- 状态：Accepted（2026-08-09 确立为「TUI 是交付形式不是卖点」；**2026-08-15 修订交付形态**）
- 背景：界面本身不构成技术亮点；没有面试官会因为界面形态而更认可一个项目。
- 决策：界面排在 Phase 2，且只做到「能展示证据链」的最小程度。不做主题、不做插件、不做快捷键体系。
- 主要风险：界面好玩，容易吸走本该给语义层的时间。
- 缓解：Phase 2 的验收标准里不含任何界面美观项。

### 2026-08-15 修订：交付形态由 TUI 改为 Web 为主 + 本地 GUI 减量

**原决策的理由没变，恰恰是它推出了新结论。** 既然「界面形态不构成亮点」，
那就该选**招聘方点开链接就能看**的那个，而不是需要对方 clone 仓库、装依赖、跑起来才能看的那个。
坚持 TUI 反而是让形式凌驾于目的。

- **Web 为主**：在既有的 `../cninfo-analyzer-web`（React 19 + Vite + Tailwind v4）
  上**增量加页面**，复用其 SSE、刷新恢复与 Cloudflare 部署链路。不新建前端仓库。
- **本地 GUI 减量**：以操作者自用适配为主，**不占 Phase 2 验收**，不进成功标准。
- **前置约束**：必须先把**内核与壳切干净**（语义层 + 执行层 + 证据链为内核，
  Web 与 GUI 各是薄壳）。不切干净，两个界面就是做两遍——那才是本决策原本要防的事。

**未变的部分**：不做主题美化、不做插件体系、不做快捷键；验收标准里不含界面美观项。
**连带影响**：U-03（CLI / TUI 单入口之争）的前提已变，须重新表述。
`.planning/ROADMAP.md` 的 Phase 2 与 Phase 4 已同步。

## D-007 — 图谱必须由问题驱动

- 状态：Accepted
- 背景：知识图谱是最容易被"为了简历而做"的组件。把文档切块连成图不产生任何信息增益。
- 决策：建图前必须先写出**至少 3 个纯向量 RAG 答不了、图能答**的具体问题，并证明其答不了。候选方向：
  - 关联方循环交易识别（供应商同时是客户）
  - 跨年度准则适用变更的传播路径
  - 一笔交易涉及哪些准则条文 + 历史类似争议
- 若找不到这样的问题 → **不建图谱，Phase 3 直接跳过图谱部分**。这不是失败，是省下的工作量。
- 主要风险：为了满足这条规则而人为编造"非图不可"的问题。
- 缓解：问题必须来自真实年报语料中能找到实例的场景。

## D-008 — 投入以 changelog 记录，不设预算上限

- 状态：Accepted（操作者约束，2026-08-09 确立；**2026-08-10 修订：取消一切时间上限**）
- 背景：原决策设了「每周 ≤ 8 小时、只在周末、述职期暂停」。操作者 2026-08-10 判断这类预算约束在本项目上没有产生价值——它既不影响技术决策，也无法机械执行，反而在 12 个文件里制造了需要同步维护的数字。
- 决策：
  - **不设每周小时上限，不限制工作时段，不设暂停期。**
  - 改为：**在 `docs/agent/PROGRESS.md` 的 changelog 里记录每次会话实际做了什么。**
    记录的是**已发生的事实**，不是预算与计划。
  - 范围决策**依据产出与假设验证进度**，不再依据剩余小时数。
- 为什么这不是放松纪律：
  被取消的是**预算**（对未来的承诺），保留并强化的是**记账**（对过去的记录）。
  本项目的纪律一直落在证据侧——冻结、哈希、真实输出、UNVERIFIED 不记 PASS——那些一条没动。
  一个自己给自己定的、无人核查的小时数，从来就不是纪律，是仪式（参见 D-009 对 Ceremony Engineering 的警惕）。
- 保留的信号：**连续两周无 changelog 条目** → 说明范围估计有问题，触发范围重估（`PROJECT_SPEC.md` §10）。
  这条保留是因为它读的是**实际产出记录**，不是预算。
- 影响的历史文件：`docs/agent/IDEA.md`、`docs/agent/ARCHITECTURE.md`、`claudedocs/handoffs/` 里仍写着 8h/周。
  **这些是历史 Artifact，故意不改**——ARCHITECTURE.md 的四方案对比当时确实是按 8h/周 评估可行性的，
  改掉就是篡改决策依据的记录。按 `AGENTS.md` 文档优先级，本文件上位覆盖 `docs/agent/`。
- 反转触发条件：若出现"因为没有上限所以挤占了睡眠或实习表现"的实际情况 → 重新设定约束。
  触发依据必须是**已发生的事实**，不是担心。

## D-009 — 三套方法论的职责划分

- 状态：Provisional
- 决策：
  - **GSD** 拥有 `.planning/` 状态、路线图阶段、阶段计划、进度与验证路由；
  - **Superpowers** 提供行为门禁：设计澄清（brainstorming）、决策完备的计划（writing-plans）、TDD、完成前验证（verification-before-completion）、独立评审（requesting-code-review）；
  - **OpenSpec** 提供单个变更的规格契约：`propose → tasks → apply → archive`，产物落在 `openspec/changes/`。
- 约束：三者**不得**为同一件事创建重复的权威文档。规格的唯一权威是 `PROJECT_SPEC.md`；决策的唯一权威是本文件；评测协议的唯一权威是 `EVAL_CASES.md`。
- 主要风险：三套流程叠加产生 Ceremony Engineering——流程成本超过项目本身。
- 反转触发条件：如果任一阶段的流程开销超过实际产出时间的 30%，删掉贡献最小的那一套。**Harness 要定期做减法。**

## D-010 — 仅公开数据（不可协商）

- 状态：Accepted
- 决策：本仓库只允许出现以下来源的数据：
  - 巨潮资讯网公开披露的年报与公告
  - AKShare 公开财务指标
  - 公开发布的会计准则与审计准则文本
  - 公开发表的学术词典（如姜富伟等 2020 中文金融情感词典，按其 NOTICE 引用）
- **禁止**：任何雇主内部数据、实习期间接触的数据、客户数据、来自内部系统的字段名或表结构——**包括脱敏后的、聚合后的、改写后的**。
- **禁止**：把本项目与任何雇主内部系统对接。
- 执行方式：这不能只靠自觉。Phase 1 起加入自动扫描（AC-10），扫描规则本身也进版本控制。
- 反转触发条件：无。这条不可协商。

## D-011 — 不训练、不微调

- 状态：Accepted
- 决策：Phase 0–4 不训练模型、不微调、不做蒸馏。全部使用现成 API。
- 理由：本项目的假设与模型能力无关，全在口径与证据链。训练会引入与假设无关的变量，且成本不可控。
- 反转触发条件：证据表明现成模型在准则理解上存在系统性缺陷，且该缺陷无法用检索与提示解决。

## D-012 — 评测集冻结

- 状态：Accepted（2026-08-09 确立；**2026-08-15 修订**，见下方「修订」小节）
- 决策：评测问题集与标准答案必须在**看到任何模型输出之前**冻结，冻结时记录时间戳与哈希。
- 冻结后允许：重跑评测、改进实现、增加新问题（新问题单独编号，不混入已冻结批次）。
- 冻结后禁止：修改已冻结问题的标准答案或判定规则来迁就模型表现。
- 理由：做评测最大的失效模式是先看结果再定标准。这条是本项目最重要的自律。
- 反转触发条件：无。若发现某题的标准答案确实错了，作废该题并公开记录，不修改后重用。

### 修订（2026-08-15）—— 冻结前置门禁与可执行的作废通道

**触发**：POC-01 的 `revenue_growth_yoy.yaml` 冻结后才被发现不是合法 YAML。
它是 2026-08-10 冻结的，而能解析它的校验器 2026-08-15 才存在——
**冻结了从未被机器校验过的东西，等于保证缺陷在不可逆的门之后才被发现。**

那一次的代价其实很小（一个 R0 规则码加一条回归断言）。但同样的机制在 wave 4 会放大：
`EVAL_CASES.md` §7 要求 C2 类 4 题拒答准确率 **100%**，而原作废规则是
「作废题仍留在分母里」。两条叠加的后果是——**只要 4 道 C2 里有 1 道构造错误，
上限就变成 3/4 = 75%，Phase 1 的门永久达不到，且与系统质量无关。**
这才是「推进到一半发现冻结物有问题然后大返工」的真实落点。

本次修订**不放松**「不得修改标准答案迁就模型表现」这条核心——那是评测可信度的全部来源，
为省事松掉它等于把产品本身交换掉。修订的是它周围的工程，让缺陷在门之前被拦住、
让残余缺陷的处理成本从「返工」降到「一行」。

**修订一：冻结前置门禁（新增，硬约束）**

任何东西进入冻结集之前，必须先通过机器校验，且校验结果与冻结记录一并留存。
未通过机器校验的内容**不允许冻结**。冻结动作本身要由脚本执行，不由人手工打哈希——
脚本先跑校验，校验不过就拒绝冻结（fail-closed）。

具体到评测集，冻结前必须机械确认：每份 case 是合法 YAML、必填字段齐全、
`expected.metric_id` 在 `metrics/` 里真实存在（C2 类则必须真实不存在）、
case id 无重复、引用的夹具存在。

**修订二：作废区分构造错误与迁就模型（改写原作废规则）**

原规则「作废题仍留在分母里」不再一刀切，改为按作废原因分两类：

| 类型 | 判据 | 处理 |
|---|---|---|
| **构造错误** | **不看系统输出就能认定**该题不成立：YAML 非法、标准答案与定义语料矛盾、题面歧义到无唯一解、`expected` 引用了不存在的东西、题目重复 | 移出分母，单独计数并公开列出 |
| **迁就模型** | 需要先看到系统的回答才能论证这题「不好」 | **禁止作废**，计为失败，原规则不变 |

判据的关键是**可判定性**：构造错误必须能由机器校验或仅凭定义语料认定。
一旦需要引用系统输出来主张某题该废，它就落入第二类。这道分界线是刻意画的——
没有它，「构造错误」会变成「迁就模型」的通道。

**修订三：作废率上限（新增）**

单个冻结批次的构造错误作废率 **> 10%** ⇒ 该批次判为**构造失败**，整批作废重建，
且必须公开记录作废率。理由：个别构造错误是可容忍的残余，成批出现说明
前置门禁没起作用或题目设计本身不成熟，此时挑几题剔掉再报分数就是粉饰。

**修订四：只冻真正需要冻的东西（澄清，非放松）**

冻结集只含**题目、标准答案、判定规则、夹具**。评测运行器代码、指标定义、
文档、报告**不在冻结集内**——它们本来就允许改（原规则的「允许改进实现」）。
冻结集内一项一文件、哈希逐文件记录，作废一题只动一行，不触及其余。

**这四条的净效果**：缺陷发现点从「冻结之后」前移到「冻结之前」；
残余缺陷的处理从「重构」降为「记一行并移出分母」；
而「先看结果再定标准」这个真正要防的失效模式，防线反而更紧了
（作废需要不看输出即可认定，比原来只靠自律更难钻空子）。

## D-013 — 财务数值以年报 PDF 原文为准；二手源只作对照

- 状态：Accepted（操作者决定，2026-08-10）
- 背景：POC-01 的 3 份口径定义在 `cninfo-financial-analyzer` 上一个都取不到数——
  它的财务数据层只有 `["stock_code", "year", "roa", "ocf"]`，取自 AKShare 的加工指标。
- 决策：
  - **财务数值的第一来源是年报 PDF 原文**（巨潮 `category_ndbg_szsh` 全文），
    由 `pdf_parser` 抽取三大表与附注，而不是消费第三方算好的指标。
  - **AKShare 降级为对照，不作为数据源。** PDF 抽取值与 AKShare 值不一致时，
    该「不一致」本身作为证据链的一项记录，不静默取其一。
  - **二手源（AKShare、雪球、同花顺等）一律不作权威源**，只可作对照。
    判据是「能不能当权威源」，不是「是不是二手源」——见下方 2026-08-10 修正。
- 理由（这条比"方便"强，是立论层面的）：
  本项目的卖点是**答案必须能追到口径**。二手源给的是别人算好的数、口径不公开，
  用它等于把一个不可见的口径写进证据链——**恰好是本项目声称要消灭的东西**。
  年报 PDF 是原始披露：口径由准则规定，附注与审计报告在同一份文件里，证据链能闭合。
  换句话说，AKShare 从一开始就是错的源，不是"字段不够"，是"层次不对"。
- **关于雪球等行情站点（2026-08-10 修正）**：
  操作者指出「口径不明显」是**不能当权威源**的理由，不是**不能当对照**的理由——这个判断成立，
  初稿一刀切拒绝是把论据用过头了。降级为对照后，口径矛盾本身已经消解。
  修正后的立场：
  - **作为对照源，原则上不排斥。** 判据统一为「是否当权威源」，而不是「是不是二手源」。
  - **但目前不加，理由是边际信息为零**：AKShare 本身就聚合了同类行情端点，
    再直连雪球拿到的是同一批加工值，**多一个源不多一份信息，只多一份 ToS 暴露面**。
  - 若将来出现 AKShare 覆盖不到、且确有对照价值的字段 → 那时再加，
    但**必须先修订 D-010** 的允许来源清单，不得顺手接入。
- 已知代价（如实记录，不粉饰）：
  中文年报 PDF 的三大表跨页、合并与母公司双版本、单位（元/万元/千元）不统一、
  各家事务所排版各异。现有 `extract_financial_statements()` **撑不住**——
  它是子串匹配 + `statements['balance_sheet'] = table` 后写覆盖，
  合并表与母公司表含相同关键词，结果是最后一个匹配的表静默胜出。
  这是 `ROA_COLUMN_CANDIDATES` 同一失效模式的第二例。
  → 表格定位与列语义识别是**真实工作量**，不是"改一下"。
- 但这份工作量不是额外的：它正是 L0→L1 的接缝，产出的恰好是 POC-01 指出缺失的
  `grain` / 缺失表示 / 符号约定在数据侧的对应物。提前暴露，不是新增。
- 反转触发条件：若 PDF 表格抽取的准确率经实测无法达到可用水平，
  且已尝试过 camelot/tabula 等替代引擎仍不达标 → 重新评估，届时须**实测数据**支撑，不凭预期。

## D-014 — PDF 抽取器不得链接 AGPL 组件；主库 pdfplumber

- 状态：Accepted（**操作者决定**，2026-08-16。这是台账 A-3 的裁决，归属方是操作者不是 agent，
  因为它是许可证/开源策略问题，不是技术选型）
- 背景：2026-08-15 的 PDF 探测全程用 `fitz`（PyMuPDF）。深读 cninfo 时发现
  PyMuPDF 是 AGPL v3 —— 证据是 `cninfo-financial-analyzer/LICENSE` 第三方段第 3 条
  与 `requirements.txt` 的 `PyMuPDF>=1.23.0`，cninfo 自己的 LICENSE 就写了
  「AGPL requires source disclosure for network services」。
- 冲突：D-006（2026-08-15 修订）把交付形态定为 **Web 为主**，Web 演示是网络服务；
  AGPL v3 §13 的网络条款要求向通过网络交互的用户提供对应源码。
  而 D-005 规定主仓私有至 Phase 4。**二者不能同时成立。**
- 决策：
  - **Phase 1.5 起，PDF 抽取路径只使用可闭源分发的组件**：
    主库 **pdfplumber（MIT）**，备选 **pypdfium2（BSD-3 / Apache-2.0）**。
  - **PyMuPDF / `fitz` 禁止进入本仓库的任何依赖**，包括间接依赖。
  - 2026-08-15 scratchpad 里的 fitz 探测代码**不进仓库**。
    其**方法**保留（`words` + 按 y 坐标聚簇重组行），**实现**改用
    `pdfplumber` 的 `page.extract_words()`——它同样给出 `x0/x1/top/bottom`。
  - 连带作废 A-4 的双库方案（「PyMuPDF 做全文定位 + pdfplumber 做数字」），**收敛为单库**。
- 为什么选 (a) 而不是 (b) 或 (c)：
  - **(b) 演示服务开源** —— 等于让一个许可证约束反过来触发项目范围变更（提前改 D-005）。
    用最大的代价解最小的问题。
  - **(c) 演示只用预抽好的静态数据** —— 恰好抽掉本项目最需要当众展示的那一环：
    从真实 PDF 抽出数字并挂上证据链。演示价值大幅缩水。
  - **(a) 换库** —— 代价局限在一次坐标重组重写，且 `extract_words()` 提供的坐标信息
    与 fitz 同级。这是三者中唯一不改变项目范围也不削弱交付物的选项。
- **这是许可证决策，不是技术优劣判断。** 必须写清楚：cninfo 仓库里
  **没有任何 pdfplumber vs PyMuPDF 的对比逻辑或测试**（见 `references/cninfo-deep-read.md`），
  因此**不存在**「PyMuPDF 抽得更准」这种实测证据可供权衡。谁若日后想换回去，
  要先补的是对比数据，不是重开这场许可证讨论。
- 判据：
  1. Phase 1.5 的 `pyproject.toml` / `requirements*.txt` 中不出现 PyMuPDF；**待 Phase 1.5**
  2. ~~`scripts/verify_deps.py` 增加**许可证禁列检查**~~ **已完成 2026-08-16**：
     直接依赖查 PyPI 的 `license` / `license_expression` / `License ::` classifier 三处，
     另加已装分发全量扫描覆盖传递依赖；命中 AGPL 系即 exit 非 0。
     负向用例以 PyMuPDF 的真实元数据形状为输入（`tests/test_verify_deps_license.py`）。
     **禁的是 AGPL 不是 copyleft**：GPL / LGPL 明确放行——GPL 不因网络服务触发披露义务，
     一起禁掉会重犯 D-013 初稿对雪球一刀切的同型错误。
     豁免名单保持为空，且有一条测试锁住这一点：往里加东西必须先修订本决策。
- 反转触发条件：pdfplumber 实测无法完成章节定位或坐标重组（达不到 Phase 1.5 成功标准）
  → 先试 pypdfium2；两者皆不达标才重开本条，**届时须带实测准确率数据**，不凭预期。

## D-015 — 抽取器与语义层平级；依赖方向单向且由测试强制

- 状态：Accepted（**操作者裁决**，2026-08-21。CONTEXT §4.1 的待裁决问题）
- 背景：Phase 1.5 要新增 PDF 抽取器。它住在哪里牵涉 U-03「内核与壳的边界」，
  而 U-03 明令**不得在本阶段提前拍板**。
- 决策：
  - 抽取器落在 `src/extractor/`，与 `src/semantic_layer/` **平级**，同一仓库内的普通包。
  - **依赖方向单向**：`extractor` 可以 import `semantic_layer`；
    `semantic_layer` 的任何模块**不得** import `extractor`。
  - 该约束由一条测试强制，不靠自觉。
- **本决策不构成 U-03 的边界决策。** 它选的是「本阶段能自洽的最小结构」，
  真正的内核/壳边界仍等 Phase 2 的 `ARCHITECTURE.md`。
- 为什么是这个形状：这一条的实质不是「放哪个目录」，而是**让「边界待定」不靠自觉**。
  单向依赖守住之后，将来无论拆成独立包还是并进内核，都还是纯搬运，不是重写。
- 为什么不选另外两条路：
  - **独立包（自带 `pyproject`）** —— 现在就得定下公开接口与版本策略，
    而接口形状正是 U-03 要留到 Phase 2 才定的东西。**等于用包结构提前拍了板。**
  - **放进 `semantic_layer/` 做子模块** —— 三个选项里唯一会实质堵死 U-03 的：
    抽取一旦进了内核，两者之后很难再分开。
- 判据：
  1. 存在 `src/extractor/` 包；**待 Phase 1.5**
  2. 存在一条测试，断言 `src/semantic_layer/**` 的源码中不出现对 `extractor` 的 import
     （反方向不断言）；**待 Phase 1.5**
     ⚠️ **该测试必须走 AST，不得用行首正则**（2026-08-22 补充）。
     实证：hello-agents 框架的依赖环**全部藏在函数内 import 与 `TYPE_CHECKING` 块里**
     （`core/agent.py:49-50,65,70`），行首 `^from|^import` 扫不到
     （`references/hello-agents-framework-core.md` §1）。
     用 grep 实现这条判据，等于本决策自带一个洞。
     检查对象是 `ast.Import` / `ast.ImportFrom` 的**全部出现位置**，含函数体内与条件块内。
- 反转触发条件：Phase 2 的 `ARCHITECTURE.md` 定下内核/壳边界后，
  若该边界要求抽取器独立分发，则按那份文档重新安排位置——**届时本条自动让位，不需要重开讨论**。

## D-016 — 映射表按命名空间拆分；标签是有序片段序列，逐字匹配

- 状态：Accepted（**操作者裁决**，2026-08-21。CONTEXT §4.2 的待裁决问题）
- 背景：SC-2 要求「每个映射有版本化的映射表条目，**不允许模糊匹配**」，但形式未定。
- 决策：
  - **物理粒度按命名空间拆分**：`data/mappings/pdf/{bs,is,cfs,notes,kpi}.yaml`，
    与 `dsl.py` 的 `ROOT_NAMESPACES` 及 `scan_rules.yaml` 的 `allowed_field_prefixes`
    对齐（这两处已有防漂移断言，映射表复用同一套心智模型，不新发明分类）。
  - **AKShare 列名映射另立文件**，不与 PDF 标签混在一份里。
    两者虽然都是「外部标签 → 字段 id」，但失效面完全不同（Q2 与 Q3 的关注点）。
  - **标签是有序文本片段序列，不是单一字符串**：schema 把 `label_fragments: [片段…]`
    当作一等构造，要求按顺序逐字匹配，片段之间**只允许**出现纯数值行。
  - 每份映射文件带整数 `mapping_version`，加载器 fail-closed（缺必需键即拒绝加载，
    不补默认值），沿用 `vocabulary.py` 的既有模式。
- 为什么标签必须是片段序列（这一条不是选项，是被两条已有约束夹出来的唯一解）：
  A-6 已实测「行项目标签折行，且数值行夹在两个标签片段之间」。
  若 schema 只能表达「一个字段 id → 一个字符串」，作者只有两条路：
  用子串/正则容错折行（**退化成 SC-2 明令禁止的模糊匹配**），
  或每遇折行就手工拼接原文（报表排版一变就对不上）。两条都不可接受。
- 为什么按命名空间拆而不是单一全局文件：**`notes` 条目与报表行是两种结构。**
  2026-08-21 实测（茅台 2023 p120–121）：附注侧的「合并范围的变更」是
  证监会披露模板的六个固定子项，每项自带 `√适用 / □不适用` 标记——
  它的形状是「章节锚点 + 适用性标记」，与报表行的「片段序列 + 数值格子」不同。
  塞进一份文件就得引入判别式联合；拆开后各自 schema 干净。
  单一全局文件（类比 `_flags.yaml`）的好处在本阶段是真的（30 个字段约 150 行、
  单人开发无合并冲突），但它解不了上面这个结构差异。
- 判据：
  1. 五份映射文件存在且各带 `mapping_version`；**待 Phase 1.5**
  2. 一条断言：五份文件的 key 并集 **恰好等于** `metrics/` 全部 `source_fields[].id`
     的集合（不多不少）。这是防映射表与定义漂移的唯一机制，目前无先例代码；**待 Phase 1.5**
  3. 加载器对缺必需键 fail-closed，有负向用例；**待 Phase 1.5**
- 反转触发条件：覆盖到多家公司后，若发现按报表拆分并不能把排版差异的失效面切开
  （即同一字段的标签变体跨报表大量重复），重新评估是否改为「按公司/排版模板」维度组织。

### 2026-08-21 补充 —— 列必须由表头文字显式绑定（台账 A-9 的裁决）

- **背景**：本决策初稿的 schema 只约束了「行怎么定位」（`label_fragments`），
  **没有任何东西约束「取哪一列」**。而 A-6 已验证的方法（标题锚点 + 按 y 聚簇重组行 +
  两位小数正则）**全是行的逻辑**；30 个字段里 15 个 `bs.*` 全要「期末余额」列，
  `is.operating_revenue_prior_as_presented` 要「上期比较金额」列。
- **决定性推论（逻辑必然，不需实测）**：勾稽恒等式
  「资产总计 = 负债合计 + 所有者权益合计」**在期末列与期初列上都成立**。
  若抽取器把三个数全取自期初列，恒等式照样通过，**D-018 的批次级闸门放行**。
  ⇒ **勾稽能证明「同一列内部自洽」，证明不了「取的是对的那一列」。**
- **这是第四个「自证机制说通过了，但它证明的不是它声称证明的事」的实例**，
  前三个（cninfo 的零支撑物 / harness postmortem 0002 期望值被 refresh 成错误值 /
  hello-agents 正文数字与产物不符）**都是外部项目，这一个在我们自己的设计里**。
- **补充决策**：映射表 schema 除 `label_fragments` 外**必须**再绑
  `column_header`（列表头文字，如「期末余额」/「上期金额」）。
  **列由表头文字显式绑定，不得靠列序号。** 理由与「标签必须是片段序列」同源：
  不显式绑定，取列就只能靠位置约定，而位置约定在换一家公司的排版时**静默失效**。
- **补充判据**：存在一条回归用例，输入是「期末列与期初列都能各自凑出平衡表」的真实报表，
  断言抽取器取到的是期末列的值；且**单独跑勾稽闸门不足以让该用例通过**
  ——即证明这条用例查的是列绑定，不是恒等式。


## D-017 — `formula` 的数值执行层自建封闭算术解析器，不引三方求值库

- 状态：Accepted（**操作者裁决**，2026-08-21。CONTEXT §4.3 的待裁决问题）
- 背景：SC-5 要求至少 5 个指标算出真实数值，而受限布尔 DSL **刻意不含算术**
  （`openspec/specs/semantic-layer/metric-definition/spec.md:140` 明文「两者共用一套语法，
  MUST NOT 各自扩展」）。`formula` 目前只是字符串，从未被任何模块解析为表达式。
- 决策：
  - 新建独立的算术表达式解析器（`src/extractor/formula.py` 或等价位置），
    语法边界 = 字段引用（点号路径）、`+ - * /`、括号、数值字面量；
    **不含**比较运算符、逻辑运算符、`is_missing()`。
  - **只读地**从 `dsl.py` 导入 `ROOT_NAMESPACES`，**不改 `dsl.py` 一个字节**。
  - 新增一条一致性测试，断言两个词法器接受**同一套字段引用**，防止那约 30–40 行
    重复的词法逻辑各自漂移。
  - **`eval()` 在任何情况下不得使用**（这一条不是选项讨论，是硬性排除）。
- 契约安全性（已逐条核实，不是估计）：25 条规则码里只有 R2 的两条与 `formula` 有关，
  且只在 `validate.py:59-61` 做一次**子串包含检查**（`field_id in formula`），不检验语法。
  只要 `formula` 仍是包含字段 id 文本的字符串、且不改 `dsl.py` 服务
  `flags` / `undefined_conditions` 的语法，扩展在契约层面安全。
- 为什么不引 `simpleeval`（MIT，过得了许可证门禁）：
  它建在 Python `ast` 上，而 `dsl.py` 的 docstring（1-12 行）逐字记着它当初**不用 `ast`**
  的两条理由——① `is.` 前缀与 Python 关键字 `is` 冲突；② L3 的 `pre_execute` 要对语法树
  做静态检查，检查对象必须是本模块自己定义的**封闭节点集**。
  **这两条对 `formula` 同样成立**：`formula` 里全是 `is.net_profit` 这种引用，
  用 `ast` 会撞上同一个关键字冲突，得在库外再包一层名字改写——省下的代码又吐回去了；
  同时放弃封闭节点集，未来静态检查要反过来适配第三方库的内部结构。
- 为什么不给定义新增结构化计算字段：该路与 CONTEXT §5 非目标
  「不动 `metrics/` 的 20 份定义」直接冲突，**而它换来的东西本决策也能给**
  ——自建解析器不改任何 YAML 就能求值。为一个可替代的收益去松一条已写下的非目标，是净亏。
- 判据：
  1. 至少 5 个指标由该解析器算出真实数值；**待 Phase 1.5**
  2. `dsl.py` 在本阶段的 diff 为空；**待 Phase 1.5**
  3. 一致性测试存在且通过；**待 Phase 1.5**
- 反转触发条件：若 `formula` 的真实语法在后续定义中出现本解析器覆盖不了的构造
  （例如条件表达式、聚合函数），先扩本解析器；只有在扩不动时才重开三方库讨论，
  **届时须带具体的语法需求清单**，不凭「三方库更省事」这类预期。

## D-018 — 勾稽校验是批次级闸门；fail-closed 放在计算层的读入边界

- 状态：Accepted（**操作者裁决**，2026-08-21。本条不在 CONTEXT §4 原四问之列，
  由 `01.5-RESEARCH.md` Open Questions 新提）
- 背景：SC-7 要求勾稽校验是**阻断式**的（不通过即拒答，不是记日志），但粒度未定。
- 决策：
  - 勾稽校验对**一次抽取批次整体跑一次**（资产总计 = 负债合计 + 所有者权益合计），
    结果写进批次元数据，**不嵌进每个指标的计算路径**。
  - **计算层在读入边界检查该标记**：批次未通过校验即拒答，
    并返回**封闭理由码**（沿用 `resolve.py` 的 `RefusalCode` 模式），不是自由文本。
- 为什么是批次级：勾稽是**报表的性质**，不是某个指标的性质。
  嵌进每个指标会对同一批数据重复跑 N 次得到 N 个相同结果，
  且失败时归因混乱——看上去像该指标错了，实际是整张表不平。
- 为什么 fail-closed 放在读入边界：这与 harness 的既有结论一致
  （`references/deepseek-harness-deep-read-part?.md`：fail-closed 在 persistence 的 load
  边界，不在 projection）。校验放在写入侧，绕过写入侧的任何路径都会静默逃逸；
  放在读入侧，**所有消费者都必须过这道门**。
- 判据：
  1. 有一条真实年报驱动的通过用例与一条构造的失败用例；**待 Phase 1.5**
  2. 失败时计算层拒答且理由码来自封闭枚举，有测试锁住「不是自由文本」；**待 Phase 1.5**
- 反转触发条件：若出现「同一批次内部分报表可用、部分不可用」的真实场景
  （例如只抽了利润表而没抽资产负债表），届时重新定义「批次」的边界，而不是改闸门位置。

## D-019 — 抽取记录的页码拆成 `page` 与 `anchor_page` 两个整数

- 状态：Accepted（**操作者裁决**，2026-08-21。由 `01.5-RESEARCH.md` Q4 提出）
- 背景：SC-4 要求抽取产物带页码，但没说页码是什么类型。
  A-6 已实测两条与页码直接冲突的事实：**表标题印在上一页的页脚位置**；
  **合并表与母公司表的边界在页内**。
- 决策：每条抽取记录带两个整数字段——
  - `page`：**数值行所在页**。永远是单一整数。
  - `anchor_page`：**表标题锚点所在页**。可以不等于 `page`。
- 为什么页码区间是不需要的：一个数值的数字**不可能横跨两页**，
  所以「数值所在页」天然是单值。真正跨页的是表标题锚点——
  那是另一个字段的事，不是把一个字段撑成区间。
- 为什么单一整数不够：复核者翻到 `page` 那一页可能**看不到表标题**（标题在上一页），
  也无法区分合并表与母公司表——这两件事都是实测撞到过的，不是假想。
- 为什么不再加第三个字段（`label_pages`）：SC-4 只点名了「页码」，
  `anchor_page` 已经是最小必要的拆分；再加一个就是在提前描 U-01 的完整证据链字段集。
- 判据：抽取产物的每条记录含 `page` 与 `anchor_page` 两个整数；**待 Phase 1.5**
- 反转触发条件：若 U-01 在 Phase 2 定下的证据链字段集要求更细的页级定位
  （例如标签片段的页归属），按那份字段集扩展——本条不构成阻碍。

## D-020 — `scope_change` 改挂新字段 `notes.consolidation_scope_change`

- 状态：Accepted（**操作者裁决**，2026-08-21。台账 A-8 的处置，选项 (a)）
- 背景：2026-08-21 实测查实一处**真实年报上的假阴性**。三处原文可独立复核：
  `metrics/_flags.yaml:38` 说 `scope_change` 是「合并范围发生变动」；
  `metrics/minority_interest_share.yaml:59` 的 trigger 是
  `notes.business_combination_type != "无"`；
  茅台 2023 p120–121 企业合并三项**全部「不适用」**，
  而「5、其他原因的合并范围变动」**是「适用」**（定制营销公司清算注销）
  ⇒ 字段取「无」→ trigger 为假 → **flag 不置位，而合并范围确实变了**。
- 决策：
  - **新增字段 `notes.consolidation_scope_change`**（布尔）：本期合并范围是否发生变动
    = 披露模板「九、合并范围的变更」六个子项中**任一为「适用」**。
  - **`scope_change` 的 trigger 改挂它。**
  - **`notes.business_combination_type` 保留原义**（只表企业合并类型），不被本条改写。
  - **`metrics/_flags.yaml:38` 的 description 一并收窄**：现文案把「合并范围发生变动」
    与「同一控制下合并追溯调整比较期」混在一句里，而**处置子公司不产生追溯调整**。
    两件事必须分开表述。
- **这条放宽了一条已写下的非目标**：CONTEXT §5「不动 `metrics/` 的 20 份定义」。
  操作者已明示放宽，**记在这里而不是让它悄悄发生**。放宽范围**仅限本条**——
  其他定义改动仍受该非目标约束。
- 已核实**不碰冻结产物**（D-012 安全）：`minority_interest_share` 与 `scope_change`
  在 `eval/frozen-01/` 全目录**零命中**；且夹具是**超集**
  （`notes.listing_first_disclosure_flag` 在夹具里却不属于任何定义的 `source_fields`），
  仓库中**没有**「夹具字段集 == `source_fields` 集」的相等断言。
- 为什么不选另外两条路：
  - **(b) 扩 `business_combination_type` 的取值域为可多值** —— 让一个标量字段承担集合语义，
    正是 POC-01 的 C7（`docs/agent/poc-01/COMPARISON.md:140`，两位回答者独立提出）
    那个老毛病的延续。
  - **(c) 维持现状、把 flag 描述收窄为「发生企业合并」** —— 不修问题，只是把名字改成
    与缺陷一致，`revenue_growth_yoy` 真正需要的那条覆盖仍然没有。
- **执行放 Phase 1.5，不在本轮改 YAML**：新字段的取值判定依赖映射表的 `notes` 侧
  （六个 `√适用 / □不适用` 复选项怎么读），而映射表尚不存在。
  先定枚举再定映射，等于凭印象造取值域——**那正是 F-2 的形状**。
- 判据：
  1. `notes.consolidation_scope_change` 进入 `minority_interest_share` 的 `source_fields`，
     trigger 改挂它；**待 Phase 1.5**
  2. `_flags.yaml` 的 `scope_change` description 不再把两件事混写；**待 Phase 1.5**
  3. 一条以茅台 2023 为输入的回归用例，锁住「无企业合并但合并范围变动」时
     `scope_change` **会**置位；**待 Phase 1.5**
- 反转触发条件：若映射表落地后发现六个子项无法可靠判读（例如 `√` / `□` 在部分公司的
  PDF 里抽不出来），则本条退回，改为在证据链里显式记录「该 flag 不可判定」，
  **不得**用一个求不出值的 trigger 冒充规则。

## D-021 — NFR-02 加静态托管豁免；NFR-04 改写为「巨潮年报 PDF 为唯一权威源」

- 状态：Accepted（**操作者裁决**，2026-08-21。台账 A-7 的处置）
- 背景：2026-08-16 第一次读 `PROJECT_SPEC.md` 原文时发现两条 NFR 已与后续决策不一致。
  这是范围层的判断，归属方是操作者。
- **NFR-02 的处置 —— 加豁免（选项 a）**：
  - 冲突：原文禁「部署基础设施」，而 D-006（2026-08-15 修订）把交付形态定为 Web 为主，
    ROADMAP Phase 2 SC-7 要求「可分享链接」——可分享链接必然要部署。
  - 决策：**静态托管**（纯静态资源、无服务端运行时、无运维负担）**不视为部署基础设施**。
  - 为什么不选 (b) 收窄 Phase 2 SC-7 为本地可跑：那是**用削减交付物来迁就一条措辞**，
    而 NFR-02 的用意是防止项目滑向运维工程，静态托管并不产生运维负担。
  - **豁免边界写死**：一旦演示需要服务端运行时、数据库或计划任务，
    即回到 NFR-02 约束之内，**须先修订本条**。这条边界是本决策的实质，不是补充说明。
- **NFR-04 的处置 —— 改写（措辞过时，非真冲突）**：
  - 冲突：原文「Phase 0–4 只支持已有的 cninfo 数据」，而 D-013 已把财务数值的第一来源
    定为年报 PDF 原文，Phase 1.5 要自建抽取器。**严格按字面读，Phase 1.5 违反 NFR-04。**
  - 决策：改写为「不做多数据源适配；财务数值只来自巨潮公开年报 PDF 原文，
    AKShare 公开指标仅作对照」。
  - **判断依据**：数据**源**没有变（都是巨潮），变的是**抽取方式**。
    原措辞把「数据源」与「某个上游仓库的数据层」混为一谈——
    而那个数据层只有 `roa` / `ocf` 两个字段，撑不起任何一个指标。
    **这是措辞过时，不是范围扩张**；改写后 Non-goals 里「多数据源接入」一条完全不受影响。
- 判据：两条 NFR 在 `PROJECT_SPEC.md` 里各有明确处置且注明日期与本决策编号。
  **已完成 2026-08-21**（NFR-02 加豁免段，NFR-04 改写并附原文与理由）。
- 反转触发条件：若 Phase 2 的 Web 演示最终确实需要服务端运行时，
  NFR-02 的豁免不自动扩大，**须重开本条**并连带评估 D-005（仓库可见性）与 D-014（许可证）。

### 2026-08-22 修订 —— 开发期 CI 不视为部署基础设施（第二条豁免）

- 状态：Accepted（**操作者裁决**，2026-08-22）
- 起因：读 hello-agents 框架源码时对照出一个活样本——它**无 `.github`**、
  `pytest` **从未声明为依赖**、19 个测试文件 4892 行 655 条 `assert`，
  却**只有 3 处 `pytest.raises`**，而同一份产物数出的吞异常点是 **35 处**
  （`references/hello-agents-framework-core.md` §7，本会话复核属实）。
  **它有测试，但没有任何机制保证测试被跑过、被跑对。**
- 冲突：`PROJECT_SPEC.md` §11 要求「完成必须有证据：实际命令 → 真实输出」，
  而在本决策之前，那份证据是**由被验证方（agent）手动跑一遍再粘进 commit message**。
  **证据由被验证方自己产生**——这正是本项目反复记录的那类自证缺陷
  （台账已记四个实例，其中 A-9 就在我们自己的设计里）。
- 决策：**开发期 CI 不视为 NFR-02 的「部署基础设施」。** 落地为
  `.github/workflows/gates.yml`，在 push / PR 时跑既有门禁，不新增任何检查项。
- **豁免边界写死**（与静态托管那条同一写法，边界即实质）：
  CI **只允许跑验证**，**不得**用于构建产物、发布、部署或任何写操作；
  工作流声明 `permissions: contents: read`。越出此范围须先修订本条。
- 为什么不选另外两条路：
  - **(b) 认定 CI 算部署基础设施、不配** —— 维持了措辞，代价是 §11 的证据链
    永远留着「自证」这个结构性弱点，而这与项目主线（可审计）直接矛盾。
  - **(c) 改做本地 `pre-push` hook** —— 不引入任何仓库外服务，与 NFR-02 零冲突；
    但 hook 在本地、换机器即失效、且可被 `--no-verify` 绕过，
    **仍然是「自己监督自己」，只是难忘一点**。它解决的是「忘记跑」，
    不解决「证据由被验证方产生」。
- **附带收益（不是选它的理由，但值得记）**：冻结校验放在 Linux runner 上跑，
  等于机器化验证了 D-012 的物理保障——`.gitattributes` 强制 LF 入库、
  SHA-256 必须跨平台稳定。**这一条在本地 Windows 上跑不出来**：
  CRLF 一旦混进冻结产物，只有 CI 会红。
- 判据：
  1. `.github/workflows/gates.yml` 存在，跑四道门 + 两处冻结校验 + 供应链门禁；
     **已完成 2026-08-22**（本地先验证全部命令为绿再提交，不提交第一天就红的门禁）
  2. 工作流权限为只读，无任何写/发布步骤；**已完成**
  3. 首次 push 后 CI 实跑通过 —— **待验证**（本地为绿不等于 runner 上为绿：
     Linux 与 Windows 在换行符、路径大小写、locale 上都可能分叉，
     **这正是要它的理由，不是它的风险**）
- 反转触发条件：若 CI 开始承担验证之外的职责（构建、发布、部署），
  或其维护成本超过它挡下的问题，重开本条。

## 3. 未解决决策

- **U-01**：证据链的字段集尚未确定。等待 Phase 2 的复核实验数据。不得在 Phase 1 提前拍板。
- **U-02**：准则语料的获取方式（人工整理 vs 公开数据集）尚未确定。Phase 3 前需专项调研。
- **U-03**（2026-08-15 **重新表述**，原问题前提已随 D-006 修订消失）：
  原问题是「CLI 与 TUI 两套入口，还是 TUI 单入口」。TUI 已不是交付形态，该问法作废。
  **新问题**：内核与壳的边界画在哪？Web 与本地 GUI 各自需要什么，二者能共用多少？
  判据：`ARCHITECTURE.md` 里有一张内核/壳边界图，且第二个壳的增量成本可估。
  等 Phase 2 内核成形后再定，**不得在 Phase 1.5 提前拍板**。
- ~~**U-04**：`data secret` 与本项目并行推进时，可支配小时数如何分配？~~
  **已关闭（2026-08-10，当日新增当日关闭）。** 关闭原因不是"想清楚了"，而是**前提消失**：
  同日 D-008 修订取消了一切时间预算上限，"如何切分预算"这个问题不再存在。
  留档而不删除，是因为「问题因前提消失而消解」和「问题被回答」是两回事，混同会让决策记录失真。
