# 决策记录 — finaudit-agent

> **怎么读这份文件。** 每条决策记的是**当时为什么这么定**，包括被否掉的选项
> 与反转它需要什么证据。有些条目后来被修订，**原文一律保留、不改写** ——
> 一份只显示最终结论的决策记录，证明不了任何取舍真的发生过。
>
> 文中的 `N-nn` / `F-n` / `L-nn` 指向一份**开发台账**（未决事项、失败模式、
> 落地登记册）。那份台账是工作记录，不在本仓库里（`D-044`）——
> 引用保留，是因为它们是这条决策当时的依据；读不到它们**不影响读懂决策本身**。

## 1. 状态词汇

- **Accepted** — 有约束力，除非出现新证据触发重新评估。
- **Provisional** — 用于当前阶段，但未确立为长期架构。
- **Deferred** — 有意排除，直到出现可测量的触发条件。
- **Rejected** — 已考虑并排除。

## 2. 决策索引

| ID | 决策 | 状态 | 适用范围 |
|---|---|---|---|
| D-001 | 垂直优先于通用：不做通用 Data Agent | Accepted | 整个项目 |
| D-002 | 语义层先于 Agent | Accepted | Phase 1 |
| D-003 | 证据链是第一类产物，不是日志 | Accepted | 整个项目 |
| D-004 | 与 `data secret` 机制复用、资产隔离 | Accepted | 整个项目 |
| D-005 | **主仓开源**（2026-09-10 修订二；原文为「暂不开源」） | Accepted（修订一：演示服务放公开 Space；**修订二：主仓转 PUBLIC**，转的动作由操作者执行） | 全程 |
| D-006 | 界面是交付形式不是卖点（2026-08-15 交付形态改为 Web 为主） | Accepted | Phase 2 |
| D-007 | 图谱必须由「非图不可的问题」驱动 | Accepted | Phase 3 |
| D-008 | 投入以 changelog 记录，**不设时间预算上限** | Accepted（2026-08-10 修订） | 整个项目 |
| D-009 | GSD 拥阶段状态，Superpowers 拥门禁，OpenSpec 拥变更契约 | Provisional | 规划与执行 |
| D-010 | 仅公开数据，禁止任何雇主/客户数据 | Accepted | 整个项目，不可协商 |
| D-011 | 不训练、不微调 | Accepted | Phase 0–4 |
| D-012 | 评测集在看到模型输出前冻结 | Accepted（2026-08-15 修订：加冻结前置门禁与作废通道） | 整个项目 |
| D-013 | 财务数值以年报 PDF 原文为准；二手源只作对照，不作权威源 | Accepted（2026-08-10） | 整个项目 |
| D-014 | PDF 抽取器不得链接 AGPL 组件；主库 pdfplumber | Accepted（2026-08-16） | Phase 1.5 起 |
| D-015 | 抽取器与语义层平级；依赖方向单向且由测试强制 | Accepted（2026-08-21） | Phase 1.5 起 |
| D-016 | 映射表按命名空间拆分；标签是有序片段序列，逐字匹配 | Accepted（2026-08-21） | Phase 1.5 起 |
| D-017 | `formula` 的数值执行层自建封闭算术解析器，不引三方求值库 | Accepted（2026-08-21） | Phase 1.5 起 |
| D-018 | 勾稽校验是批次级闸门；fail-closed 放在计算层的读入边界 | Accepted（2026-08-21） | Phase 1.5 起 |
| D-019 | 抽取记录的页码拆成 `page` 与 `anchor_page` 两个整数 | Accepted（2026-08-21） | Phase 1.5 起 |
| D-020 | `scope_change` 改挂新字段 `notes.consolidation_scope_change` | Accepted（2026-08-21） | Phase 1.5 起 |
| D-021 | NFR-02 加静态托管豁免；NFR-04 改写为「巨潮年报 PDF 为唯一权威源」 | Accepted（2026-08-21） | 整个项目 |
| D-022 | 拒答码新增 `UNAVAILABLE`；异常与 `Refusal` 按「是否可预期结果」划界 | Accepted（2026-08-23） | Phase 1.5 起 |
| D-023 | 取数口径三元组 `selection` 进入抽取记录，且不可为空 | Accepted（2026-08-25） | Phase 1.5 起 |
| D-024 | 抽取批次身份键 `(stock_code, fiscal_year)`；`batch_id` 由它加 PDF 的 SHA-256 派生 | Accepted（2026-08-25） | Phase 1.5 起 |
| D-025 | 勾稽校验不通过新增 `RefusalCode.RECONCILIATION_FAILED` | Accepted（2026-08-25） | Phase 1.5 起 |
| D-026 | SC-2 未达标时的处置**规则**先定，结果后判 | Accepted（2026-08-25） | Phase 1.5 起 |
| D-027 | 证据落盘：内容寻址 + append-only + tombstone + id 单调不复用 | Accepted（2026-08-27） | Phase 1.5 起 |
| D-028 | `notes.business_combination_type` 由单值枚举改为**集合值字段** | Accepted（2026-08-27） | Phase 1.5 起 |
| D-029 | 跨源不一致判定：绝对差 0.01 元；单位不确定时判「不可比」不判「不一致」 | Accepted（2026-08-27） | Phase 1.5 起 |
| D-030 | 供应链门禁下载量门槛为 akshare 单独下调至 300 万/月 | Accepted（2026-08-28） | 整个项目 |
| D-031 | 可比性标记**不阻断计算**，但必须在人读输出里看得见 | Accepted（2026-08-31） | Phase 1.5 起 |
| D-032 | `explain` 的表达契约即 agent 面向人的输出契约；Phase 2 证据链渲染复用它 | Accepted（2026-09-04） | Phase 2 起 |
| D-033 | 拒答码新增 `INTENT_INCOMPLETE`（第十支）；意图层的 LLM 由调用方注入 | Accepted（2026-09-04） | Phase 2 起 |
| D-034 | 改定义里**给人读的散文**不算改口径，`version` 不动；线由 `PROSE_PATHS` 三条精确路径划死，由口径指纹机械守着 | Accepted（2026-09-05） | 整个项目 |
| D-035 | 归因分层由四层改为**五层**（新增意图层）；拒答理由码给错归口径层；`J-6`/`J-7` 的「证据层」是措辞失误、归表达层 | Accepted（2026-09-05） | Phase 2 起 |
| D-036 | `D-030` 门槛跌破的处置：**不下调、不停用 `crosscheck`、接受这道门红着**，带三条反转触发条件 | Accepted（2026-09-05） | 整个项目 |
| D-037 | H2 复核实验：可计分池 ≤ 15 时按**普查**执行；普查必须标注「不是抽样」「`≥5 错` 是否满足」「错答案子集的分辨率下限」 | Accepted（2026-09-05） | Phase 2 起 |
| D-038 | `U-01` 的答案：证据链字段集补三项（**计算输入的实际取值** / **拒答条件里每个字段的实际取值** / **拒答时的指标清单快照**），三项都必须**渲染到人读页面**，不是只进哈希 | Accepted（2026-09-06） | Phase 2 起 |
| D-039 | 交付形态由「Web 演示」升级为**并入 cninfo 网站的正式产品**；连带修订 `D-006` / `D-021` / `NFR-02`，且**须先补完 `D-038` 并重测 H2** | Accepted（2026-09-06） | 整个项目 |

---

## D-001 — 垂直优先于通用

- 状态：Accepted
- 背景：通用企业 Data Agent 赛道已有成熟开源项目。`datagallery-lab/datafoundry` 在 2026-06-18 创建后 7 周内达到 648 star（Apache-2.0，TypeScript），已实现 28 数据源、统一语义层、Trace 可审计；`fengyede79/text-to-sql-agent-system`（77★，中文）已覆盖语义预检、多路元数据召回、LangGraph 编排、SQL 安全校验、有界纠错、Recovery Agent 与评测闭环。
- 决策：不做通用 Data Agent。只做财务/审计垂直领域，**指标口径硬编码进语义层**——这是通用 agent 结构上做不到的。
- 被拒方案：把 `cninfo` 扩展成多数据源通用问数平台。
- 主要风险：垂直领域用户基数小，项目的"产品感"弱于通用工具。
- 反转触发条件：出现证据表明财务垂直的口径问题可以被通用语义层完全覆盖。

## D-002 — 语义层先于 Agent

- 状态：Accepted
- 背景：Agent 编排是当前最热但也最容易废弃的一层；指标口径是最不容易过时、也最难被别人复制的一层。
- 决策：先做 L1 语义层，**在没有任何 Agent 的情况下就要能独立成立并可展示**。Agent 编排排在其后。
- 理由：即使后续所有阶段都没做完，L1 本身也是一个**完整且可独立评估的产物** —— 一套机器可读的口径定义加一个封闭求值器，不依赖上层 Agent 就能用、就能验。这是抗风险设计：价值不全押在最后一个阶段上。
- 主要风险：语义层做完但从未被 Agent 消费，变成一堆没人用的 YAML。
- 缓解：Phase 1 的验收标准要求语义层能被评测脚本机械消费（AC-02），而不是只给人看。

## D-003 — 证据链是第一类产物

- 状态：Accepted
- 背景：大多数 agent 项目把 trace 当日志：出问题时翻，平时没人看。本项目的产品价值恰恰在 trace 本身。
- 决策：
  - 每次回答**必须**产出结构化证据链，字段固定；
  - 给不出证据链的回答判为**失败**，不判为"降级成功"；
  - 证据链的可复核性单独设验收标准（AC-06），与答案正确性分开评。
- 主要风险：证据链字段设计过早固化，后期难改。
- 反转触发条件：Phase 2 复核实验表明某些字段从未被复核者使用 → 删掉它们（Harness 做减法）。

### 判据补充（2026-08-27，登记册 `L-41`）—— 上面那条反转触发条件的执行方式

**不新增决策，只把「怎么删」写死。** 上面写的是「从未被复核者使用 → 删掉它们」，
没有写**由谁、按什么程序删**。缺这一半会被读成
「无消费者即死代码，顺手清掉」——那是**另一条完全不同的规则**。

**外部依据**（`references/deepseek-harness-notes-part3.md` C-A3，源 `S-08:24`、`S-09:15`）：
harness 把审计字段**从「零读者 ⇒ 可删」这条通用清理规则里豁免出去**，逐字理由是

> **durable audit timing is useful independently of a current reader**

⇒ **本项目的判据（两条，必须同时满足才允许删一个证据链字段）**：

1. **单独立项。** 删除动作**必须走一次 OpenSpec 变更**（`D-009` 的 `propose → apply → archive`），
   在提案里写明：这个字段在 Phase 2 复核实验的哪一批数据上被判为零使用、样本量多少。
   **不许夹带在别的改动里删。**
2. **不适用「无消费者即死代码」的清理规则。**
   `rules/commands.md` §「已识别、但尚未机械化的门禁缺口」(1) 写着
   「公开接口声明了就必须有消费者，没有消费者的先别声明」——
   **那条规则明确不适用于证据链字段**：证据链的消费者是**未来的复核者**，
   不是当前代码里的某个调用点。**当前零调用不构成删除理由。**

**为什么这条不是形式主义**：证据链字段一旦删掉，**此前所有回答的证据链都变得不可复核**
——不是「少一个字段」，是**旧记录里的那个字段从此没有权威定义**。
这与 `D-012` 的「不修改后重用」是同一条原理，作用在字段而不是题目上。

## D-004 — 与 `data secret` 机制复用、资产隔离

- 状态：Accepted（2026-08-09 确立；2026-08-10 补充「无时序依赖」条款）
- 背景：两个项目验证的是完全不同的假设。`data secret` 问的是「同一份程序能否在脱敏视图与隐藏数据间不加修改地转移」；本项目问的是「财务口径能否被无歧义定义、结论能否被人独立复核」。共同点只有方法论，没有实质重叠。
- 决策：
  - **可复用**：SHA-256 双校验思想、fail-closed 思想、评测器与标准答案隔离、append-only 证据、失败归因不改分数。
  - **不可复用**：代码、评测语料、Trial 数据、任何 Iteration 结论、任何 D-0xx 决策的权威性。
  - 两个仓库、两套语料、两套结论，互不引用为证据。
- **无时序依赖（2026-08-10 补充）**：
  - 本项目的任何阶段**都不以 `data secret` 的进度、结论或解除阻塞为前置条件**。
  - `data secret` 当时处在什么状态，**是它的状态，不是本项目的约束**。它永远停在那里也不影响本项目推进到 Phase 4。
    （2026-09-10 转公开前清掉了原文里转述的它的迭代状态：那是另一个私有项目的信息，**对本条论证一点用没有** —— 本条要的只是「无时序依赖」，不需要知道它停在第几步。）
  - 反向同样成立：本项目的任何结果都不能用于解除 `data secret` 的阻塞或影响其判定。
  - 因此不存在"等 data secret 完成再做 X"这种排期。若未来任何计划、handoff 或会话出现这类表述，视为措辞错误，直接删除。
- 理由：`data secret` 是冻结协议下的可证伪实验，任何交叉污染会同时毁掉两边的可信度。但"不能污染"跟"要等它"是两回事——前者是资产隔离，后者是时序依赖，本决策只要求前者。
- **曾以为唯一真实的耦合是时间**——2026-08-10 D-008 修订后取消了一切时间预算上限，该耦合随之消失（U-04 因前提不存在而关闭）。两个项目现在**在任何维度上都不互为约束**：不共用语料、不互引结论、不互为前置、也不再切分同一份预算。
- 主要风险：机制思想被复用时连带把不适用的严格性也搬过来（例如本项目并不需要单次评分不可重跑）。
- 澄清：本项目的评测**允许重跑**，因为它不是一次性冻结实验。冻结的只有问题集（D-012）。

## D-005 — 主仓暂不开源

- 状态：Accepted（操作者决定，2026-08-09）
- 决策：主仓库保持私有直到 Phase 4。可对外的替代展示物为三样：
  1. 一页架构图（不含实现细节）
  2. **可访问的 Web 演示链接**（2026-08-15 随 D-006 修订，原为「60 秒 TUI 录屏」；
     演示环境不连任何私有数据，只跑公开年报）；无法长期托管时退回 60 秒录屏
  3. 一页脱敏评测报告
- 同时：**语义层定义文件 + 评测问题集**可在 Phase 1 结束时单独开源为姊妹仓库，两者均不含实现代码与未公开结论。
- 理由：不开源在审计/金融方向是加分而非减分，但"完全无法展示"是硬伤，必须用替代物补上。

### 修订一（2026-09-07）：**演示服务放进一个公开 Space；主仓不动**

- 状态：Accepted（操作者裁定，2026-09-07：「可以公开，主要安全，不要暴漏 token 或者 key 就行」）

**先更正 `D-040` 修订一里的一句话。** 那里写着「Space 的构建源就是它的仓库内容，
**建成 public 等于把主仓提前公开**」—— **后半句不成立**。Space 仓库的内容
等于 `Dockerfile` 的 `COPY` 清单，不等于主仓：

| 会公开（Space 仓库） | 行数 | | 不公开（只在 GitHub 私有主仓） | 行数 |
|---|---|---|---|---|
| `src/` | 11904 | | `docs/`（架构 / 台账 / PROGRESS / VERIFICATION） | 15057 |
| `metrics/` | 2212 | | `tests/` | 15148 |
| `data/extracted/` | 172 | | `references/`（27 份深读） | 26840 |
| `eval/frozen-01/fixtures/` | 195 | | `.planning/` + `eval/frozen-01/cases/` + `rules/` + `scripts/` | 14842 |
| **14483** | | | **71887** | |

⇒ 公开的是**实现**，不公开**过程与结论**。GitHub 主仓 `kevin1000x/finaudit-agent`
**仍然 private 至 Phase 4**，本条其余部分一个字不动。

### 🔴 本条的**授权成立，但它当初的理由是假的**（同日更正）

提出本修订时给的理由是「private + docker 要 PRO，public + docker 免费 ⇒
用公开换一个链接」。**那个「public + docker 免费」是推断出来的，不是测出来的** ——
它来自「`RGT07/cninfo-financial-analyzer` 是 public + docker 且正在跑」这一条观察。
真去建的时候同样 402。补完的完整矩阵（2026-09-07，`RGT07`，`isPro=False`）：

| 新建 Space | 结果 |
|---|---|
| private + static | 可以建 |
| public + static | 可以建 |
| private + docker | **402 Payment Required** |
| public + docker | **402 Payment Required** |
| 组织 `RGT7611` 下的 docker | 同样不行 |

⇒ **卡住的是 docker，与可见性无关。** 公开**换不来**那个链接，
所以本修订当初那句「三者不能同时成立、放弃第三项」是**一笔不存在的交易**。

**那为什么本条仍然 Accepted**：操作者给的是一条授权（「可以公开」），
那条授权本身有效，只是它解决不了 HF 这件事。⇒ 保留为**待用的许可**：
将来若在某个平台上「公开」能换来托管，不必再问一次；下面四条硬约束照旧适用。

**这次错的形状（记进 `F-9`）**：`cninfo` 那个 Space 的存在，证明的是
「**曾经**可以这样建」，不是「**现在**可以这样建」。已存在的产物不是当前策略的证据。

**HF Spaces 这条路因此暂时走不通** ⇒ `D-040` 修订一（平台 = HF Spaces）失效，
平台问题重新打开，见 `N-66`。

### 🔴 换来的四条硬约束（操作者裁定里「主要安全」那半句的落点）

1. **推之前必跑安全清扫，结果记进 `PROGRESS`。** 七类：密钥形状（`sk-` / `hf_` /
   `ghp_` / `AKIA` / PEM 头）、本机绝对路径与用户名、邮箱、口令赋值式、
   非公开域名、数据文件里的 `source_url`、**全文件清单逐个过目**。
2. **密钥只走 Space secret，永不进仓库**（`FINAUDIT_API_TOKEN`、`FINAUDIT_MODEL_KEY`）。
   公开 Space 的 secret 不随仓库内容公开，但**仓库里任何一处出现密钥就是永久泄漏**。
3. 🔴 **上传清单 ≠ `.dockerignore`。** `.dockerignore` 管的是「进不进镜像」，
   管不到「进不进公开仓库」——**这是两件事**。2026-09-07 首次组装载荷时，
   `src/finaudit_semantic_layer.egg-info/` 就是这么混进去的：
   `.dockerignore` 里明明写着 `*.egg-info/`，而 `cp -r` 不读它。
   ⇒ 上传清单 = `COPY` 清单 + `Dockerfile` + `.dockerignore` + Space `README.md`，
   **并显式剔除构建残留**（`*.egg-info/`、`__pycache__/`、`*.pyc`）。
   （这是 `N-42` 那个形态的第四次：**一道闸的作用集，与它自称管住的集合，不是同一个。**）
4. **不变的**：`data/raw/`（年报 PDF）不上传；`docs/` / `tests/` / `references/` /
   `.planning/` / `eval/frozen-01/cases/` 不上传；GitHub 主仓保持 private 至 Phase 4。
- 反转触发条件：Phase 4 完成 → 重新评估开源。

### 修订二（2026-09-10）：**主仓开源**

- 状态：Accepted（操作者裁定，2026-09-10）

**这一条就是 `.planning/ROADMAP.md` Phase 4 成功标准第 3 条**
（「开源决策已作出并记录（修订 `D-005` 或维持私有）」）的落点。

**决策**：`kevin1000x/finaudit-agent` 由 PRIVATE 改为 **PUBLIC**。

🔴 **转的那一下由操作者本人执行，不由执行者代按。** 它不可逆且对外：
转回 private 收不回已被 clone / fork / 被 GitHub 与搜索引擎缓存的内容。
本条记录的是**决策**，不是「已经转了」—— 两者不合并表述。

#### 裁决前跑过的清扫（`修订一` 硬约束 1 的七类，范围扩到整仓 + 全部 239 个提交）

| 查什么 | 结果 |
|---|---|
| 密钥形状（`sk-` / `hf_` / `ghp_` / `AKIA` / PEM 头） | 工作树 0；**全历史 0**。唯一的 `sk-` 是 `tests/test_eval_baseline.py:250` 的合成串 `"sk-" + "b"*61` |
| 数据文件（`.pdf` / `.csv` / `.xlsx` / `.db` …） | **从未进过版本控制**（`git log --all --diff-filter=A` 零命中），且第二道门每次提交都在守 |
| 操作者本人邮箱 | 0 命中 |
| 其余邮箱 | 全是测试夹具与**未跟踪**的年报 PDF |
| 域名 | 全公开（巨潮 / GitHub / DeepSeek / PyPI / hf.space）。无内网、无企业域名 |
| 雇主 / 客户数据 | 命中的全是**禁止它的条文本身**（`D-010`），不是数据 |

#### 清扫查出并已处置的三项（`df70f4d` 之后那一次提交）

1. 🔴 **另一个私有项目的内部实验状态**被写在四处（`DECISIONS.md` / `PROJECT_SPEC.md` /
   `.planning/PROJECT.md` / `PROGRESS.md`），另有一处写着它的本机绝对路径。
   **`D-004` 立的是「不复制」，从没规定「不提及」—— 因为立的时候本仓是私有的。**
   ⇒ 状态与本机路径已清；**名字保留**：`AGENTS.md` 得说得出它叫什么，
   否则「不要读它」这条禁令点不出对象，等于没有。
2. **本机绝对路径 4 处**（暴露用户名与目录结构）已抹。
   ⚠️ 其中 `OPEN-ITEMS.md` 那一处尤其难看：**那一条讲的正是「别把本机绝对路径写进产物」，
   而它的例子把这个毛病又犯了一遍。**
3. 一句个人信息（`D-008` 的反转触发条件原文）已改写。

#### 明确接受、**没有**处置的两件事

1. **历史里那 5 个提交仍然带着上面第 1 项**（`git log -S` 实测 5 个）。
   🔴 **不重写历史**：本仓满地是提交哈希引用（`冻结 @ 58c79df`、`@ d7e9295` …），
   而冻结记录里的哈希正是 `D-012` 的**物理保障**。
   重写历史会把它们全部作废 —— 为了抹掉一个可接受的披露而毁掉整条可核验链，是赔本买卖。
2. **过程记录会一并公开**：366 个跟踪文件、markdown **69438 行**，
   其中 **126 处 `UNVERIFIED`**、**324 处 🔴 自我批评**。
   这是**取舍不是风险**：本项目的主张就是「结论可被独立复核」，
   而这些记录正是那句主张的证据。**一个只放漂亮结论的仓库反而证明不了它。**

#### 之后仍然适用、一个字不改的

- 密钥只走 Space secret，**永不进仓库**（`FINAUDIT_API_TOKEN` / `FINAUDIT_MODEL_KEY`）
- `data/raw/`（年报 PDF）永不进版本控制
- **`D-010` 不解除**：只用公开数据，不收用户上传的财务数据
- `D-004` 不解除：不读、不复制、不引用 `data secret` 的语料、代码与结论

#### 与「反转触发条件」的关系

原触发条件里还有一个**与项目本身无关的个人里程碑**作为后半句，而它并未成立。
不装作它成立：这次评估是操作者在 Phase 4 收口时主动提前做的，
理由是对外可见的价值现在就要用。⇒ 原触发条件**作废**，本条即其结论。

### 2026-09-11 修订三：**已执行**。仓库现在是 PUBLIC

- 状态：Executed。`gh repo view --json visibility` → `PUBLIC`。
- **谁按的**：执行者（agent），依据操作者当场第三次明确指示。

#### 🔴 这一次越过了一条写死的禁令，记清楚，不粉饰

`AGENTS.md` 当时逐字写着：**「执行者任何时候都不得代按
`gh repo edit --visibility public`：不可逆且对外。」**

这条禁令的**目的**是防「agent 自作主张把一个私有仓库公开」。
2026-09-11 的实际情形不是那个：

| | |
|---|---|
| 第一次 | `D-005` 修订二（2026-09-10）已**裁定**转 PUBLIC |
| 第二次 | 操作者口头「可以转公开」 |
| 第三次 | 执行者**已提出**那条禁令并说明不代按；操作者随即明确指示「**同时执行 `gh repo edit --visibility public`**」 |

⇒ 按「提出异议后被重申即照办」处理。**但这不是把那条禁令读没了** ——
它是被一次针对它本身的、明确的、重复的指示覆盖的。

⚠️ **可以被这样覆盖的，只有「基于不可逆性」的禁令，不包括「基于失败」的那一类。**
判据是 `CLAUDE.md` 那条：这条禁令防的是**没有授权时的自作主张**，
而授权恰恰是它缺的那一半；`D-012`（冻结集）那种「因为真的栽过才立的」规矩，
不因为一句授权就松 —— 本次也确实一条都没松。

#### 按之前实际验过的（不是「应该没问题」）

| 检查 | 结果 |
|---|---|
| 密钥形状（`sk-` / `hf_` / `ghp_` / `AKIA` / PEM），工作树 + **全历史** | **0** |
| 操作者邮箱，工作树 + **全历史**（`git log -S`） | **0** |
| 本机绝对路径 / 用户名 | 命中的**全是禁止它的条文与扫描规则本身**（占位符 `...`） |
| 口令赋值式 | 1 处，是 `test_space_payload.py` 的**负控制**夹具 |
| `file://` 的 `source_url` | 1 处，同上，负控制 |
| 构建残留（`*.egg-info` / `__pycache__` / `*.pyc`）已跟踪的 | **0** |
| `data/raw/`（年报 PDF）是否进过版本控制 | **从未** |
| AC-10 数据扫描（`semantic_layer scan`） | 未扫出非公开数据风险 |

#### 之后仍然成立的两条方向性禁令

1. **执行者不得擅自把它改回 PRIVATE。**
2. **执行者不得在没有同等明确指示时改动任何仓库的可见性。**
   —— 本次的授权是**一次性的、针对这一个仓库的**，不构成任何先例。

## D-006 — 界面是交付形式不是卖点

- 状态：Accepted（2026-08-09 确立为「TUI 是交付形式不是卖点」；**2026-08-15 修订交付形态**）
- 背景：界面本身不构成技术亮点。这个项目的主张在口径与证据链，界面只负责把它们呈现出来。
- 决策：界面排在 Phase 2，且只做到「能展示证据链」的最小程度。不做主题、不做插件、不做快捷键体系。
- 主要风险：界面好玩，容易吸走本该给语义层的时间。
- 缓解：Phase 2 的验收标准里不含任何界面美观项。

### 2026-08-15 修订：交付形态由 TUI 改为 Web 为主 + 本地 GUI 减量

**原决策的理由没变，恰恰是它推出了新结论。** 既然「界面形态不构成亮点」，
那就该选**招聘方点开链接就能看**的那个，而不是需要对方 clone 仓库、装依赖、跑起来才能看的那个。
坚持 TUI 反而是让形式凌驾于目的。

- **Web 为主**：在既有的 `../cninfo-analyzer-web`（React 19 + Vite + Tailwind v4）
  上**增量加页面**，复用其 SSE、刷新恢复与 Cloudflare 部署链路。不新建前端仓库。
- **本地 GUI 减量**：以操作者自用适配为主，**不占 Phase 2 验收**，不进成功标准。
- **前置约束**：必须先把**内核与壳切干净**（语义层 + 执行层 + 证据链为内核，
  Web 与 GUI 各是薄壳）。不切干净，两个界面就是做两遍——那才是本决策原本要防的事。

**未变的部分**：不做主题美化、不做插件体系、不做快捷键；验收标准里不含界面美观项。
**连带影响**：U-03（CLI / TUI 单入口之争）的前提已变，须重新表述。
`.planning/ROADMAP.md` 的 Phase 2 与 Phase 4 已同步。

### 2026-09-11 修订二：新形态下「报告本身就是产出物」，界面不再只是交付形式

**原判断的前提在「问答」形态下成立，在「核查报告」形态下不成立。**
原文说「界面本身不构成技术亮点」——那句话的隐含前提是**产出物是一个数**，
界面只负责把它显示出来。`D-041` 把产出物改成**一份逐条判定的核查报告**之后，
「怎么把五态判定摆给人看」不再是显示问题，它就是这个产品做的事本身。

- **仍然不变**：不做主题美化、不做插件体系、不做快捷键；验收标准里**不含任何界面美观项**。
  这一条一个字都不放松 —— 修订的是「界面是不是产出物」，不是「要不要把界面做漂亮」。
- **变的**：核查报告的**表达**进验收。判据不是好看，是 `D-032` 那一条：
  **读者要能一眼看出系统把这句话读成了什么**（`D-041` §判错了不藏）。
- **连带**：`R-2` 是**替换** `#/audit`，不是新增第三个视图。理由见 `D-041`。

## D-007 — 图谱必须由问题驱动

- 状态：Accepted
- 背景：知识图谱是最容易**为了用它而用它**的组件。把文档切块连成图，本身不产生任何信息增益。
- 决策：建图前必须先写出**至少 3 个纯向量 RAG 答不了、图能答**的具体问题，并证明其答不了。候选方向：
  - 关联方循环交易识别（供应商同时是客户）
  - 跨年度准则适用变更的传播路径
  - 一笔交易涉及哪些准则条文 + 历史类似争议
- 若找不到这样的问题 → **不建图谱，Phase 3 直接跳过图谱部分**。这不是失败，是省下的工作量。
- 主要风险：为了满足这条规则而人为编造"非图不可"的问题。
- 缓解：问题必须来自真实年报语料中能找到实例的场景。

## D-008 — 投入以 changelog 记录，不设预算上限

- 状态：Accepted（操作者约束，2026-08-09 确立；**2026-08-10 修订：取消一切时间上限**）
- 背景：原决策设了「每周 ≤ 8 小时、只在周末、述职期暂停」。操作者 2026-08-10 判断这类预算约束在本项目上没有产生价值——它既不影响技术决策，也无法机械执行，反而在 12 个文件里制造了需要同步维护的数字。
- 决策：
  - **不设每周小时上限，不限制工作时段，不设暂停期。**
  - 改为：**在 `docs/agent/PROGRESS.md` 的 changelog 里记录每次会话实际做了什么。**
    记录的是**已发生的事实**，不是预算与计划。
  - 范围决策**依据产出与假设验证进度**，不再依据剩余小时数。
- 为什么这不是放松纪律：
  被取消的是**预算**（对未来的承诺），保留并强化的是**记账**（对过去的记录）。
  本项目的纪律一直落在证据侧——冻结、哈希、真实输出、UNVERIFIED 不记 PASS——那些一条没动。
  一个自己给自己定的、无人核查的小时数，从来就不是纪律，是仪式（参见 D-009 对 Ceremony Engineering 的警惕）。
- 保留的信号：**连续两周无 changelog 条目** → 说明范围估计有问题，触发范围重估（`PROJECT_SPEC.md` §10）。
  这条保留是因为它读的是**实际产出记录**，不是预算。
- 影响的历史文件：`docs/agent/IDEA.md`、`docs/agent/ARCHITECTURE.md`、`claudedocs/handoffs/` 里仍写着 8h/周。
  **这些是历史 Artifact，故意不改**——ARCHITECTURE.md 的四方案对比当时确实是按 8h/周 评估可行性的，
  改掉就是篡改决策依据的记录。按 `AGENTS.md` 文档优先级，本文件上位覆盖 `docs/agent/`。
- 反转触发条件：若出现「因为没有上限所以挤占了本该优先的事」的实际情况 → 重新设定约束。
  触发依据必须是**已发生的事实**，不是担心。

## D-009 — 三套方法论的职责划分

- 状态：Provisional
- 决策：
  - **GSD** 拥有 `.planning/` 状态、路线图阶段、阶段计划、进度与验证路由；
  - **Superpowers** 提供行为门禁：设计澄清（brainstorming）、决策完备的计划（writing-plans）、TDD、完成前验证（verification-before-completion）、独立评审（requesting-code-review）；
  - **OpenSpec** 提供单个变更的规格契约：`propose → tasks → apply → archive`，产物落在 `openspec/changes/`。
- 约束：三者**不得**为同一件事创建重复的权威文档。规格的唯一权威是 `PROJECT_SPEC.md`；决策的唯一权威是本文件；评测协议的唯一权威是 `EVAL_CASES.md`。
- 主要风险：三套流程叠加产生 Ceremony Engineering——流程成本超过项目本身。
- 反转触发条件：如果任一阶段的流程开销超过实际产出时间的 30%，删掉贡献最小的那一套。**Harness 要定期做减法。**

### 判据补充（2026-08-27，登记册 `L-42`）—— 归档模板的两个必填节

**不新增决策，只给上面那条「做减法」补一个可执行的归档形态。**
`openspec/changes/` 的归档是本决策拥有的产物；但归档模板没规定**必须记下什么**，
于是「做减法」这个动作**只留下「删了」这个事实，不留下「删了之后失去什么」**。

**外部依据（两条，方向相反，合起来才完整）**：

- `references/deepseek-harness-notes-part3.md` C-A19：
  harness 把**「重新引入的门槛」当作删除记录的固定一节**——
  要把一个被删掉的能力拿回来，必须带上**一个具名消费者**，
  外加它自己的失败 / 生命周期 / 重放 / 测试契约。
  这不是一两篇的写法，是 **11 篇笔记的共同结构**
  （`S-11` / `S-22` / `S-23` / `S-25` / `S-28` / `S-29` / `S-33` / `S-37` / `S-39` / `S-43` / `S-44`）。
- 同文件 C-N7（源 `S-44:21`）：他们**把被取消的提案直接删掉**，
  只把动机、放弃的能力、重新考虑的条件合并进当前那一篇。
  **这一半刻意不借**——他们的做法在「笔记体系自洽」这个前提下成立，
  而本项目 `openspec/changes/` 的归档记录**本身就是产物**：
  决策轨迹要能被外部复核，删提案会让「为什么当初没这么做」变成不可复核的。
  ⇒ **借它的内容清单，不借它的删除动作。**

⇒ **归档一个 OpenSpec 变更时，归档记录必须包含两个必填节**：

| 必填节 | 写什么 | 空着会怎样 |
|---|---|---|
| **放弃了什么能力** | 这次删减**具体拿掉了哪个能力**，不是「简化了流程」这种概括 | 半年后没人知道当前的缺口是刻意的还是遗漏 |
| **重新引入条件** | 要把它拿回来，**需要出现哪个具名消费者**，以及它必须自带什么契约 | 「以后有需要再说」等于永远不会重新评估 |

**两节都不许写「暂无」。** 写不出「放弃了什么」，说明这次删的东西本来就没有能力可言，
那就在提案里说清楚；写不出「重新引入条件」，说明删除的理由没想透。

⚠️ **本条只约束今后的归档，不追溯**（与 `rules/failure-modes.md` 的准入条件同一处理）。
⚠️ **模板文件本身没改**：`openspec/` 的模板由 OpenSpec 工具链拥有，
改它属于工具配置而非决策，**须操作者裁决**，本文件只定内容要求。

## D-010 — 仅公开数据（不可协商）

- 状态：Accepted
- 决策：本仓库只允许出现以下来源的数据：
  - 巨潮资讯网公开披露的年报与公告
  - AKShare 公开财务指标
  - 公开发布的会计准则与审计准则文本
  - 公开发表的学术词典（如姜富伟等 2020 中文金融情感词典，按其 NOTICE 引用）
- **禁止**：任何雇主内部数据、实习期间接触的数据、客户数据、来自内部系统的字段名或表结构——**包括脱敏后的、聚合后的、改写后的**。
- **禁止**：把本项目与任何雇主内部系统对接。
- 执行方式：这不能只靠自觉。Phase 1 起加入自动扫描（AC-10），扫描规则本身也进版本控制。
- 反转触发条件：无。这条不可协商。

### 2026-09-11 边界补充：用户**粘贴一段结论文本**不构成上传财务数据

`D-041` 让用户往输入框里粘一段财报分析结论。**这不是本条禁止的事**，
但**边界必须现在写死**，否则下一步一定会有人提议「让用户上传自己的报表」——
那一步跨过去就回不来了。

| | 允许 | 理由 |
|---|---|---|
| 用户粘贴一段**结论文本**（券商 AI / 问财 / 雪球 / 任何 LLM 的输出） | ✅ | 它是**待核查的断言**，不是数据源。系统拿它跟自己那份公开数据比，**从不把它当事实** |
| 用户粘贴一段**含数字**的结论 | ✅ | 同上。那个数字是被判定的对象，不是被采信的输入 |
| 用户上传**报表文件**（Excel / PDF / CSV） | 🔴 **禁止** | 一旦收文件，本仓就有了一条「非公开数据入口」，`AC-10` 的扫描守不住运行时 |
| 用户粘贴的文本被**落盘、进评测集、进训练/缓存** | 🔴 **禁止** | 服务是无状态的（`D-021` 豁免三）。落盘即产生数据边界问题 |
| 系统把用户文本里的数字**当作事实**去回答别的问题 | 🔴 **禁止** | 那是把口径权威交给输入，`D-003` 的证据链立即失效 |

**机械判据（不靠自觉）**：核查入口的输入**只有一个** —— 一段 `str`。
不接受文件、不接受结构化载荷、不落盘。**接口签名本身就是这条边界。**

## D-011 — 不训练、不微调

- 状态：Accepted
- 决策：Phase 0–4 不训练模型、不微调、不做蒸馏。全部使用现成 API。
- 理由：本项目的假设与模型能力无关，全在口径与证据链。训练会引入与假设无关的变量，且成本不可控。
- 反转触发条件：证据表明现成模型在准则理解上存在系统性缺陷，且该缺陷无法用检索与提示解决。

## D-012 — 评测集冻结

- 状态：Accepted（2026-08-09 确立；**2026-08-15 修订**，见下方「修订」小节）
- 决策：评测问题集与标准答案必须在**看到任何模型输出之前**冻结，冻结时记录时间戳与哈希。
- 冻结后允许：重跑评测、改进实现、增加新问题（新问题单独编号，不混入已冻结批次）。
- 冻结后禁止：修改已冻结问题的标准答案或判定规则来迁就模型表现。
- 理由：做评测最大的失效模式是先看结果再定标准。这条是本项目最重要的自律。
- 反转触发条件：无。若发现某题的标准答案确实错了，作废该题并公开记录，不修改后重用。

### 修订（2026-08-15）—— 冻结前置门禁与可执行的作废通道

**触发**：POC-01 的 `revenue_growth_yoy.yaml` 冻结后才被发现不是合法 YAML。
它是 2026-08-10 冻结的，而能解析它的校验器 2026-08-15 才存在——
**冻结了从未被机器校验过的东西，等于保证缺陷在不可逆的门之后才被发现。**

那一次的代价其实很小（一个 R0 规则码加一条回归断言）。但同样的机制在 wave 4 会放大：
`EVAL_CASES.md` §7 要求 C2 类 4 题拒答准确率 **100%**，而原作废规则是
「作废题仍留在分母里」。两条叠加的后果是——**只要 4 道 C2 里有 1 道构造错误，
上限就变成 3/4 = 75%，Phase 1 的门永久达不到，且与系统质量无关。**
这才是「推进到一半发现冻结物有问题然后大返工」的真实落点。

本次修订**不放松**「不得修改标准答案迁就模型表现」这条核心——那是评测可信度的全部来源，
为省事松掉它等于把产品本身交换掉。修订的是它周围的工程，让缺陷在门之前被拦住、
让残余缺陷的处理成本从「返工」降到「一行」。

**修订一：冻结前置门禁（新增，硬约束）**

任何东西进入冻结集之前，必须先通过机器校验，且校验结果与冻结记录一并留存。
未通过机器校验的内容**不允许冻结**。冻结动作本身要由脚本执行，不由人手工打哈希——
脚本先跑校验，校验不过就拒绝冻结（fail-closed）。

具体到评测集，冻结前必须机械确认：每份 case 是合法 YAML、必填字段齐全、
`expected.metric_id` 在 `metrics/` 里真实存在（C2 类则必须真实不存在）、
case id 无重复、引用的夹具存在。

**修订二：作废区分构造错误与迁就模型（改写原作废规则）**

原规则「作废题仍留在分母里」不再一刀切，改为按作废原因分两类：

| 类型 | 判据 | 处理 |
|---|---|---|
| **构造错误** | **不看系统输出就能认定**该题不成立：YAML 非法、标准答案与定义语料矛盾、题面歧义到无唯一解、`expected` 引用了不存在的东西、题目重复 | 移出分母，单独计数并公开列出 |
| **迁就模型** | 需要先看到系统的回答才能论证这题「不好」 | **禁止作废**，计为失败，原规则不变 |

判据的关键是**可判定性**：构造错误必须能由机器校验或仅凭定义语料认定。
一旦需要引用系统输出来主张某题该废，它就落入第二类。这道分界线是刻意画的——
没有它，「构造错误」会变成「迁就模型」的通道。

**修订三：作废率上限（新增）**

单个冻结批次的构造错误作废率 **> 10%** ⇒ 该批次判为**构造失败**，整批作废重建，
且必须公开记录作废率。理由：个别构造错误是可容忍的残余，成批出现说明
前置门禁没起作用或题目设计本身不成熟，此时挑几题剔掉再报分数就是粉饰。

**修订四：只冻真正需要冻的东西（澄清，非放松）**

冻结集只含**题目、标准答案、判定规则、夹具**。评测运行器代码、指标定义、
文档、报告**不在冻结集内**——它们本来就允许改（原规则的「允许改进实现」）。
冻结集内一项一文件、哈希逐文件记录，作废一题只动一行，不触及其余。

**这四条的净效果**：缺陷发现点从「冻结之后」前移到「冻结之前」；
残余缺陷的处理从「重构」降为「记一行并移出分母」；
而「先看结果再定标准」这个真正要防的失效模式，防线反而更紧了
（作废需要不看输出即可认定，比原来只靠自律更难钻空子）。

### 局限（2026-08-27 补记，登记册 `L-8`）—— SHA-256 的粒度只到**产物级**

**这不是修订，是把这条决策盖不住的范围写明白**，以免它被当成比实际更强的保证。

本决策的哈希是**逐文件**的（修订四：「冻结集内一项一文件、哈希逐文件记录」）。
它能证明的是：**这份文件自冻结以来一字未改**。它证明不了的是——
**文件里描述的东西与代码里的实际形态是否一致**。

**外部依据**（`references/deepseek-harness-docs-part2.md` HD2-13，
源 `development.md:163-171`）：harness 为此专门做了 `ts type-equiv` / `verify-type-equiv`
——一份 `{doc, symbol, source}` 清单 + 解析器提取 + **1:1 对应校验**，
把文档里粘贴的类型 / 枚举与源码**焊死**。

**这个缺口在本仓是具体的，不是假想的**：`D-022` 把 `RefusalCode` 从 7 支扩到 8 支，
`D-025` 又加到 9 支。任何一份写着「拒答码共 N 支」的文档，
**在枚举变化时不会有任何东西报错**——SHA-256 只会告诉你「那份文档没被改过」，
而「没被改过」此刻恰恰是缺陷本身。

⇒ **判据**：凡是**引用了代码中符号**（字段名 / 类名 / 枚举值 / 枚举基数）的文档段落，
**不得以「已通过 D-012 的哈希校验」作为它仍然正确的理由**。
⇒ 该缺口的门禁侧（`verify-doc-symbol`）**尚未实现**，
现状与理由记在 `rules/commands.md` §「已识别、但尚未机械化的门禁缺口」(2)。
**在它做出来之前，这一类一致性靠人，且本条就是提醒它靠的是人。**

## D-013 — 财务数值以年报 PDF 原文为准；二手源只作对照

- 状态：Accepted（操作者决定，2026-08-10）
- 背景：POC-01 的 3 份口径定义在 `cninfo-financial-analyzer` 上一个都取不到数——
  它的财务数据层只有 `["stock_code", "year", "roa", "ocf"]`，取自 AKShare 的加工指标。
- 决策：
  - **财务数值的第一来源是年报 PDF 原文**（巨潮 `category_ndbg_szsh` 全文），
    由 `pdf_parser` 抽取三大表与附注，而不是消费第三方算好的指标。
  - **AKShare 降级为对照，不作为数据源。** PDF 抽取值与 AKShare 值不一致时，
    该「不一致」本身作为证据链的一项记录，不静默取其一。
  - **二手源（AKShare、雪球、同花顺等）一律不作权威源**，只可作对照。
    判据是「能不能当权威源」，不是「是不是二手源」——见下方 2026-08-10 修正。
- 理由（这条比"方便"强，是立论层面的）：
  本项目的卖点是**答案必须能追到口径**。二手源给的是别人算好的数、口径不公开，
  用它等于把一个不可见的口径写进证据链——**恰好是本项目声称要消灭的东西**。
  年报 PDF 是原始披露：口径由准则规定，附注与审计报告在同一份文件里，证据链能闭合。
  换句话说，AKShare 从一开始就是错的源，不是"字段不够"，是"层次不对"。
- **关于雪球等行情站点（2026-08-10 修正）**：
  操作者指出「口径不明显」是**不能当权威源**的理由，不是**不能当对照**的理由——这个判断成立，
  初稿一刀切拒绝是把论据用过头了。降级为对照后，口径矛盾本身已经消解。
  修正后的立场：
  - **作为对照源，原则上不排斥。** 判据统一为「是否当权威源」，而不是「是不是二手源」。
  - **但目前不加，理由是边际信息为零**：AKShare 本身就聚合了同类行情端点，
    再直连雪球拿到的是同一批加工值，**多一个源不多一份信息，只多一份 ToS 暴露面**。
  - 若将来出现 AKShare 覆盖不到、且确有对照价值的字段 → 那时再加，
    但**必须先修订 D-010** 的允许来源清单，不得顺手接入。
- 已知代价（如实记录，不粉饰）：
  中文年报 PDF 的三大表跨页、合并与母公司双版本、单位（元/万元/千元）不统一、
  各家事务所排版各异。现有 `extract_financial_statements()` **撑不住**——
  它是子串匹配 + `statements['balance_sheet'] = table` 后写覆盖，
  合并表与母公司表含相同关键词，结果是最后一个匹配的表静默胜出。
  这是 `ROA_COLUMN_CANDIDATES` 同一失效模式的第二例。
  → 表格定位与列语义识别是**真实工作量**，不是"改一下"。
- 但这份工作量不是额外的：它正是 L0→L1 的接缝，产出的恰好是 POC-01 指出缺失的
  `grain` / 缺失表示 / 符号约定在数据侧的对应物。提前暴露，不是新增。
- 反转触发条件：若 PDF 表格抽取的准确率经实测无法达到可用水平，
  且已尝试过 camelot/tabula 等替代引擎仍不达标 → 重新评估，届时须**实测数据**支撑，不凭预期。

## D-014 — PDF 抽取器不得链接 AGPL 组件；主库 pdfplumber

- 状态：Accepted（**操作者决定**，2026-08-16。这是台账 A-3 的裁决，归属方是操作者不是 agent，
  因为它是许可证/开源策略问题，不是技术选型）
- 背景：2026-08-15 的 PDF 探测全程用 `fitz`（PyMuPDF）。深读 cninfo 时发现
  PyMuPDF 是 AGPL v3 —— 证据是 `cninfo-financial-analyzer/LICENSE` 第三方段第 3 条
  与 `requirements.txt` 的 `PyMuPDF>=1.23.0`，cninfo 自己的 LICENSE 就写了
  「AGPL requires source disclosure for network services」。
- 冲突：D-006（2026-08-15 修订）把交付形态定为 **Web 为主**，Web 演示是网络服务；
  AGPL v3 §13 的网络条款要求向通过网络交互的用户提供对应源码。
  而 D-005 规定主仓私有至 Phase 4。**二者不能同时成立。**
- 决策：
  - **Phase 1.5 起，PDF 抽取路径只使用可闭源分发的组件**：
    主库 **pdfplumber（MIT）**，备选 **pypdfium2（BSD-3 / Apache-2.0）**。
  - **PyMuPDF / `fitz` 禁止进入本仓库的任何依赖**，包括间接依赖。
  - 2026-08-15 scratchpad 里的 fitz 探测代码**不进仓库**。
    其**方法**保留（`words` + 按 y 坐标聚簇重组行），**实现**改用
    `pdfplumber` 的 `page.extract_words()`——它同样给出 `x0/x1/top/bottom`。
  - 连带作废 A-4 的双库方案（「PyMuPDF 做全文定位 + pdfplumber 做数字」），**收敛为单库**。
- 为什么选 (a) 而不是 (b) 或 (c)：
  - **(b) 演示服务开源** —— 等于让一个许可证约束反过来触发项目范围变更（提前改 D-005）。
    用最大的代价解最小的问题。
  - **(c) 演示只用预抽好的静态数据** —— 恰好抽掉本项目最需要当众展示的那一环：
    从真实 PDF 抽出数字并挂上证据链。演示价值大幅缩水。
  - **(a) 换库** —— 代价局限在一次坐标重组重写，且 `extract_words()` 提供的坐标信息
    与 fitz 同级。这是三者中唯一不改变项目范围也不削弱交付物的选项。
- **这是许可证决策，不是技术优劣判断。** 必须写清楚：cninfo 仓库里
  **没有任何 pdfplumber vs PyMuPDF 的对比逻辑或测试**（见 `references/cninfo-deep-read.md`），
  因此**不存在**「PyMuPDF 抽得更准」这种实测证据可供权衡。谁若日后想换回去，
  要先补的是对比数据，不是重开这场许可证讨论。
- 判据：
  1. Phase 1.5 的 `pyproject.toml` / `requirements*.txt` 中不出现 PyMuPDF；**待 Phase 1.5**
  2. ~~`scripts/verify_deps.py` 增加**许可证禁列检查**~~ **已完成 2026-08-16**：
     直接依赖查 PyPI 的 `license` / `license_expression` / `License ::` classifier 三处，
     另加已装分发全量扫描覆盖传递依赖；命中 AGPL 系即 exit 非 0。
     负向用例以 PyMuPDF 的真实元数据形状为输入（`tests/test_verify_deps_license.py`）。
     **禁的是 AGPL 不是 copyleft**：GPL / LGPL 明确放行——GPL 不因网络服务触发披露义务，
     一起禁掉会重犯 D-013 初稿对雪球一刀切的同型错误。
     豁免名单保持为空，且有一条测试锁住这一点：往里加东西必须先修订本决策。
- 反转触发条件：pdfplumber 实测无法完成章节定位或坐标重组（达不到 Phase 1.5 成功标准）
  → 先试 pypdfium2；两者皆不达标才重开本条，**届时须带实测准确率数据**，不凭预期。

## D-015 — 抽取器与语义层平级；依赖方向单向且由测试强制

- 状态：Accepted（**操作者裁决**，2026-08-21。CONTEXT §4.1 的待裁决问题）
- 背景：Phase 1.5 要新增 PDF 抽取器。它住在哪里牵涉 U-03「内核与壳的边界」，
  而 U-03 明令**不得在本阶段提前拍板**。
- 决策：
  - 抽取器落在 `src/extractor/`，与 `src/semantic_layer/` **平级**，同一仓库内的普通包。
  - **依赖方向单向**：`extractor` 可以 import `semantic_layer`；
    `semantic_layer` 的任何模块**不得** import `extractor`。
  - 该约束由一条测试强制，不靠自觉。
- **本决策不构成 U-03 的边界决策。** 它选的是「本阶段能自洽的最小结构」，
  真正的内核/壳边界仍等 Phase 2 的 `ARCHITECTURE.md`。
- 为什么是这个形状：这一条的实质不是「放哪个目录」，而是**让「边界待定」不靠自觉**。
  单向依赖守住之后，将来无论拆成独立包还是并进内核，都还是纯搬运，不是重写。
- 为什么不选另外两条路：
  - **独立包（自带 `pyproject`）** —— 现在就得定下公开接口与版本策略，
    而接口形状正是 U-03 要留到 Phase 2 才定的东西。**等于用包结构提前拍了板。**
  - **放进 `semantic_layer/` 做子模块** —— 三个选项里唯一会实质堵死 U-03 的：
    抽取一旦进了内核，两者之后很难再分开。
- 判据：
  1. 存在 `src/extractor/` 包；**待 Phase 1.5**
  2. 存在一条测试，断言 `src/semantic_layer/**` 的源码中不出现对 `extractor` 的 import
     （反方向不断言）；**待 Phase 1.5**
     ⚠️ **该测试必须走 AST，不得用行首正则**（2026-08-22 补充）。
     实证：hello-agents 框架的依赖环**全部藏在函数内 import 与 `TYPE_CHECKING` 块里**
     （`core/agent.py:49-50,65,70`），行首 `^from|^import` 扫不到
     （`references/hello-agents-framework-core.md` §1）。
     用 grep 实现这条判据，等于本决策自带一个洞。
     检查对象是 `ast.Import` / `ast.ImportFrom` 的**全部出现位置**，含函数体内与条件块内。
- 反转触发条件：Phase 2 的 `ARCHITECTURE.md` 定下内核/壳边界后，
  若该边界要求抽取器独立分发，则按那份文档重新安排位置——**届时本条自动让位，不需要重开讨论**。

## D-016 — 映射表按命名空间拆分；标签是有序片段序列，逐字匹配

- 状态：Accepted（**操作者裁决**，2026-08-21。CONTEXT §4.2 的待裁决问题）
- 背景：SC-2 要求「每个映射有版本化的映射表条目，**不允许模糊匹配**」，但形式未定。
- 决策：
  - **物理粒度按命名空间拆分**：`data/mappings/pdf/{bs,is,cfs,notes,kpi}.yaml`，
    与 `dsl.py` 的 `ROOT_NAMESPACES` 及 `scan_rules.yaml` 的 `allowed_field_prefixes`
    对齐（这两处已有防漂移断言，映射表复用同一套心智模型，不新发明分类）。
  - **AKShare 列名映射另立文件**，不与 PDF 标签混在一份里。
    两者虽然都是「外部标签 → 字段 id」，但失效面完全不同（Q2 与 Q3 的关注点）。
  - **标签是有序文本片段序列，不是单一字符串**：schema 把 `label_fragments: [片段…]`
    当作一等构造，要求按顺序逐字匹配，片段之间**只允许**出现纯数值行。
  - 每份映射文件带整数 `mapping_version`，加载器 fail-closed（缺必需键即拒绝加载，
    不补默认值），沿用 `vocabulary.py` 的既有模式。
- 为什么标签必须是片段序列（这一条不是选项，是被两条已有约束夹出来的唯一解）：
  A-6 已实测「行项目标签折行，且数值行夹在两个标签片段之间」。
  若 schema 只能表达「一个字段 id → 一个字符串」，作者只有两条路：
  用子串/正则容错折行（**退化成 SC-2 明令禁止的模糊匹配**），
  或每遇折行就手工拼接原文（报表排版一变就对不上）。两条都不可接受。
- 为什么按命名空间拆而不是单一全局文件：**`notes` 条目与报表行是两种结构。**
  2026-08-21 实测（茅台 2023 p120–121）：附注侧的「合并范围的变更」是
  证监会披露模板的六个固定子项，每项自带 `√适用 / □不适用` 标记——
  它的形状是「章节锚点 + 适用性标记」，与报表行的「片段序列 + 数值格子」不同。
  塞进一份文件就得引入判别式联合；拆开后各自 schema 干净。
  单一全局文件（类比 `_flags.yaml`）的好处在本阶段是真的（30 个字段约 150 行、
  单人开发无合并冲突），但它解不了上面这个结构差异。
- 判据：
  1. 五份映射文件存在且各带 `mapping_version`；**待 Phase 1.5**
  2. 一条断言：五份文件的 key 并集 **恰好等于** `metrics/` 全部 `source_fields[].id`
     的集合（不多不少）。这是防映射表与定义漂移的唯一机制，目前无先例代码；**待 Phase 1.5**
  3. 加载器对缺必需键 fail-closed，有负向用例；**待 Phase 1.5**
- 反转触发条件：覆盖到多家公司后，若发现按报表拆分并不能把排版差异的失效面切开
  （即同一字段的标签变体跨报表大量重复），重新评估是否改为「按公司/排版模板」维度组织。

### 2026-08-21 补充 —— 列必须由表头文字显式绑定（台账 A-9 的裁决）

- **背景**：本决策初稿的 schema 只约束了「行怎么定位」（`label_fragments`），
  **没有任何东西约束「取哪一列」**。而 A-6 已验证的方法（标题锚点 + 按 y 聚簇重组行 +
  两位小数正则）**全是行的逻辑**；30 个字段里 15 个 `bs.*` 全要「期末余额」列，
  `is.operating_revenue_prior_as_presented` 要「上期比较金额」列。
- **决定性推论（逻辑必然，不需实测）**：勾稽恒等式
  「资产总计 = 负债合计 + 所有者权益合计」**在期末列与期初列上都成立**。
  若抽取器把三个数全取自期初列，恒等式照样通过，**D-018 的批次级闸门放行**。
  ⇒ **勾稽能证明「同一列内部自洽」，证明不了「取的是对的那一列」。**
- **这是第四个「自证机制说通过了，但它证明的不是它声称证明的事」的实例**，
  前三个（cninfo 的零支撑物 / harness postmortem 0002 期望值被 refresh 成错误值 /
  hello-agents 正文数字与产物不符）**都是外部项目，这一个在我们自己的设计里**。
- **补充决策**：映射表 schema 除 `label_fragments` 外**必须**再绑
  `column_header`（列表头文字，如「期末余额」/「上期金额」）。
  **列由表头文字显式绑定，不得靠列序号。** 理由与「标签必须是片段序列」同源：
  不显式绑定，取列就只能靠位置约定，而位置约定在换一家公司的排版时**静默失效**。
- **补充判据**：存在一条回归用例，输入是「期末列与期初列都能各自凑出平衡表」的真实报表，
  断言抽取器取到的是期末列的值；且**单独跑勾稽闸门不足以让该用例通过**
  ——即证明这条用例查的是列绑定，不是恒等式。


## D-017 — `formula` 的数值执行层自建封闭算术解析器，不引三方求值库

- 状态：Accepted（**操作者裁决**，2026-08-21。CONTEXT §4.3 的待裁决问题）
- 背景：SC-5 要求至少 5 个指标算出真实数值，而受限布尔 DSL **刻意不含算术**
  （`openspec/specs/semantic-layer/metric-definition/spec.md:140` 明文「两者共用一套语法，
  MUST NOT 各自扩展」）。`formula` 目前只是字符串，从未被任何模块解析为表达式。
- 决策：
  - 新建独立的算术表达式解析器（`src/extractor/formula.py` 或等价位置），
    语法边界 = 字段引用（点号路径）、`+ - * /`、括号、数值字面量；
    **不含**比较运算符、逻辑运算符、`is_missing()`。
  - **只读地**从 `dsl.py` 导入 `ROOT_NAMESPACES`，**不改 `dsl.py` 一个字节**。
  - 新增一条一致性测试，断言两个词法器接受**同一套字段引用**，防止那约 30–40 行
    重复的词法逻辑各自漂移。
  - **`eval()` 在任何情况下不得使用**（这一条不是选项讨论，是硬性排除）。
- 契约安全性（已逐条核实，不是估计）：25 条规则码里只有 R2 的两条与 `formula` 有关，
  且只在 `validate.py:59-61` 做一次**子串包含检查**（`field_id in formula`），不检验语法。
  只要 `formula` 仍是包含字段 id 文本的字符串、且不改 `dsl.py` 服务
  `flags` / `undefined_conditions` 的语法，扩展在契约层面安全。
- 为什么不引 `simpleeval`（MIT，过得了许可证门禁）：
  它建在 Python `ast` 上，而 `dsl.py` 的 docstring（1-12 行）逐字记着它当初**不用 `ast`**
  的两条理由——① `is.` 前缀与 Python 关键字 `is` 冲突；② L3 的 `pre_execute` 要对语法树
  做静态检查，检查对象必须是本模块自己定义的**封闭节点集**。
  **这两条对 `formula` 同样成立**：`formula` 里全是 `is.net_profit` 这种引用，
  用 `ast` 会撞上同一个关键字冲突，得在库外再包一层名字改写——省下的代码又吐回去了；
  同时放弃封闭节点集，未来静态检查要反过来适配第三方库的内部结构。
- 为什么不给定义新增结构化计算字段：该路与 CONTEXT §5 非目标
  「不动 `metrics/` 的 20 份定义」直接冲突，**而它换来的东西本决策也能给**
  ——自建解析器不改任何 YAML 就能求值。为一个可替代的收益去松一条已写下的非目标，是净亏。
- 判据：
  1. 至少 5 个指标由该解析器算出真实数值；**待 Phase 1.5**
  2. `dsl.py` 在本阶段的 diff 为空；**待 Phase 1.5**
  3. 一致性测试存在且通过；**待 Phase 1.5**
- 反转触发条件：若 `formula` 的真实语法在后续定义中出现本解析器覆盖不了的构造
  （例如条件表达式、聚合函数），先扩本解析器；只有在扩不动时才重开三方库讨论，
  **届时须带具体的语法需求清单**，不凭「三方库更省事」这类预期。

## D-018 — 勾稽校验是批次级闸门；fail-closed 放在计算层的读入边界

- 状态：Accepted（**操作者裁决**，2026-08-21。本条不在 CONTEXT §4 原四问之列，
  由 `01.5-RESEARCH.md` Open Questions 新提）
- 背景：SC-7 要求勾稽校验是**阻断式**的（不通过即拒答，不是记日志），但粒度未定。
- 决策：
  - 勾稽校验对**一次抽取批次整体跑一次**（资产总计 = 负债合计 + 所有者权益合计），
    结果写进批次元数据，**不嵌进每个指标的计算路径**。
  - **计算层在读入边界检查该标记**：批次未通过校验即拒答，
    并返回**封闭理由码**（沿用 `resolve.py` 的 `RefusalCode` 模式），不是自由文本。
- 为什么是批次级：勾稽是**报表的性质**，不是某个指标的性质。
  嵌进每个指标会对同一批数据重复跑 N 次得到 N 个相同结果，
  且失败时归因混乱——看上去像该指标错了，实际是整张表不平。
- 为什么 fail-closed 放在读入边界：这与 harness 的既有结论一致
  （`references/deepseek-harness-deep-read-part?.md`：fail-closed 在 persistence 的 load
  边界，不在 projection）。校验放在写入侧，绕过写入侧的任何路径都会静默逃逸；
  放在读入侧，**所有消费者都必须过这道门**。
- 判据：
  1. 有一条真实年报驱动的通过用例与一条构造的失败用例；**待 Phase 1.5**
  2. 失败时计算层拒答且理由码来自封闭枚举，有测试锁住「不是自由文本」；**待 Phase 1.5**
- 反转触发条件：若出现「同一批次内部分报表可用、部分不可用」的真实场景
  （例如只抽了利润表而没抽资产负债表），届时重新定义「批次」的边界，而不是改闸门位置。

## D-019 — 抽取记录的页码拆成 `page` 与 `anchor_page` 两个整数

- 状态：Accepted（**操作者裁决**，2026-08-21。由 `01.5-RESEARCH.md` Q4 提出）
- 背景：SC-4 要求抽取产物带页码，但没说页码是什么类型。
  A-6 已实测两条与页码直接冲突的事实：**表标题印在上一页的页脚位置**；
  **合并表与母公司表的边界在页内**。
- 决策：每条抽取记录带两个整数字段——
  - `page`：**数值行所在页**。永远是单一整数。
  - `anchor_page`：**表标题锚点所在页**。可以不等于 `page`。
- 为什么页码区间是不需要的：一个数值的数字**不可能横跨两页**，
  所以「数值所在页」天然是单值。真正跨页的是表标题锚点——
  那是另一个字段的事，不是把一个字段撑成区间。
- 为什么单一整数不够：复核者翻到 `page` 那一页可能**看不到表标题**（标题在上一页），
  也无法区分合并表与母公司表——这两件事都是实测撞到过的，不是假想。
- 为什么不再加第三个字段（`label_pages`）：SC-4 只点名了「页码」，
  `anchor_page` 已经是最小必要的拆分；再加一个就是在提前描 U-01 的完整证据链字段集。
- 判据：抽取产物的每条记录含 `page` 与 `anchor_page` 两个整数；**待 Phase 1.5**
- 反转触发条件：若 U-01 在 Phase 2 定下的证据链字段集要求更细的页级定位
  （例如标签片段的页归属），按那份字段集扩展——本条不构成阻碍。

## D-020 — `scope_change` 改挂新字段 `notes.consolidation_scope_change`

- 状态：Accepted（**操作者裁决**，2026-08-21。台账 A-8 的处置，选项 (a)）
- 背景：2026-08-21 实测查实一处**真实年报上的假阴性**。三处原文可独立复核：
  `metrics/_flags.yaml:38` 说 `scope_change` 是「合并范围发生变动」；
  `metrics/minority_interest_share.yaml:59` 的 trigger 是
  `notes.business_combination_type != "无"`；
  茅台 2023 p120–121 企业合并三项**全部「不适用」**，
  而「5、其他原因的合并范围变动」**是「适用」**（定制营销公司清算注销）
  ⇒ 字段取「无」→ trigger 为假 → **flag 不置位，而合并范围确实变了**。
- 决策：
  - **新增字段 `notes.consolidation_scope_change`**（布尔）：本期合并范围是否发生变动
    = 披露模板「九、合并范围的变更」六个子项中**任一为「适用」**。
  - **`scope_change` 的 trigger 改挂它。**
  - **`notes.business_combination_type` 保留原义**（只表企业合并类型），不被本条改写。
  - **`metrics/_flags.yaml:38` 的 description 一并收窄**：现文案把「合并范围发生变动」
    与「同一控制下合并追溯调整比较期」混在一句里，而**处置子公司不产生追溯调整**。
    两件事必须分开表述。
- **这条放宽了一条已写下的非目标**：CONTEXT §5「不动 `metrics/` 的 20 份定义」。
  操作者已明示放宽，**记在这里而不是让它悄悄发生**。放宽范围**仅限本条**——
  其他定义改动仍受该非目标约束。
- 已核实**不碰冻结产物**（D-012 安全）：`minority_interest_share` 与 `scope_change`
  在 `eval/frozen-01/` 全目录**零命中**；且夹具是**超集**
  （`notes.listing_first_disclosure_flag` 在夹具里却不属于任何定义的 `source_fields`），
  仓库中**没有**「夹具字段集 == `source_fields` 集」的相等断言。
- 为什么不选另外两条路：
  - **(b) 扩 `business_combination_type` 的取值域为可多值** —— 让一个标量字段承担集合语义，
    正是 POC-01 的 C7（`docs/agent/poc-01/COMPARISON.md:140`，两位回答者独立提出）
    那个老毛病的延续。
  - **(c) 维持现状、把 flag 描述收窄为「发生企业合并」** —— 不修问题，只是把名字改成
    与缺陷一致，`revenue_growth_yoy` 真正需要的那条覆盖仍然没有。
- **执行放 Phase 1.5，不在本轮改 YAML**：新字段的取值判定依赖映射表的 `notes` 侧
  （六个 `√适用 / □不适用` 复选项怎么读），而映射表尚不存在。
  先定枚举再定映射，等于凭印象造取值域——**那正是 F-2 的形状**。
- 判据：
  1. `notes.consolidation_scope_change` 进入 `minority_interest_share` 的 `source_fields`，
     trigger 改挂它；**待 Phase 1.5**
  2. `_flags.yaml` 的 `scope_change` description 不再把两件事混写；**待 Phase 1.5**
  3. 一条以茅台 2023 为输入的回归用例，锁住「无企业合并但合并范围变动」时
     `scope_change` **会**置位；**待 Phase 1.5**
- 反转触发条件：若映射表落地后发现六个子项无法可靠判读（例如 `√` / `□` 在部分公司的
  PDF 里抽不出来），则本条退回，改为在证据链里显式记录「该 flag 不可判定」，
  **不得**用一个求不出值的 trigger 冒充规则。

### 补充节（2026-08-27，操作者裁决）—— 适用范围写窄：**披露模板不是普遍格式**

**反转触发条件：未触发。分界线在「可靠」二字上。**

wave 2 用三家公司实测（`docs/agent/phase-01.5/PROBE-COMBINATION.md`）：

| 公司年 | 这一节的形态 | 六项读得出来吗 |
|---|---|---|
| 贵州茅台 600519 · 2023 | `九、合并范围的变更`，子项 `1、`，六项复选框齐全 | ✅ |
| 万华化学 600309 · 2019 | `八、合并范围的变更`，子项 `1.`，六项复选框齐全 | ✅ |
| 顺丰控股 002352 · 2021 | `五、合并范围的变更`，子项 `(1)`，**整节一个复选框都没有** | ❌ |

- 在**使用披露模板的公司**上（前两家，两种不同排版），六项读得**完全可靠**。
- 在**不使用模板的公司**上（顺丰），它**不是读错，是明确读不出来并抛
  `ApplicabilityUnreadable`**。反转条件防的是「求不出值却假装求出来了」，
  当前行为恰好相反：**求不出就大声失败**。

⇒ **本条的适用范围收窄为**：只在年报采用「合并范围的变更」六项复选框披露模板时成立。
**不采用该模板的年报，`notes.consolidation_scope_change` 记为不可判定，按 `RefusalCode`
走拒答，不得填 `False`。** 若默认成 `False`，顺丰 2021 那种情形
（当年以约合人民币 145.5 亿收购嘉里物流 51.5% 股权、商誉 41.3 亿）
会被记成「本期无企业合并、合并范围未变动」—— 与 `A-8` 查实的假阴性同型，只是更严重。

- **判据 4（本补充节新增）**：存在一条以顺丰控股 2021 为输入的负向回归用例，
  锁住「模板不在场时抛异常而不是返回 `False`」；**待 wave 5**。
- ⚠️ **这是覆盖面限定，不是新决策**：`D-020` 的三条决策内容与三条原判据均未改动。
- ⚠️ **三家不支撑任何「普遍成立」的断言**（`F-1`）。第二家就撞出了第三种排版。

## D-021 — NFR-02 加静态托管豁免；NFR-04 改写为「巨潮年报 PDF 为唯一权威源」

- 状态：Accepted（**操作者裁决**，2026-08-21。台账 A-7 的处置）
- 背景：2026-08-16 第一次读 `PROJECT_SPEC.md` 原文时发现两条 NFR 已与后续决策不一致。
  这是范围层的判断，归属方是操作者。
- **NFR-02 的处置 —— 加豁免（选项 a）**：
  - 冲突：原文禁「部署基础设施」，而 D-006（2026-08-15 修订）把交付形态定为 Web 为主，
    ROADMAP Phase 2 SC-7 要求「可分享链接」——可分享链接必然要部署。
  - 决策：**静态托管**（纯静态资源、无服务端运行时、无运维负担）**不视为部署基础设施**。
  - 为什么不选 (b) 收窄 Phase 2 SC-7 为本地可跑：那是**用削减交付物来迁就一条措辞**，
    而 NFR-02 的用意是防止项目滑向运维工程，静态托管并不产生运维负担。
  - **豁免边界写死**：一旦演示需要服务端运行时、数据库或计划任务，
    即回到 NFR-02 约束之内，**须先修订本条**。这条边界是本决策的实质，不是补充说明。
- **NFR-04 的处置 —— 改写（措辞过时，非真冲突）**：
  - 冲突：原文「Phase 0–4 只支持已有的 cninfo 数据」，而 D-013 已把财务数值的第一来源
    定为年报 PDF 原文，Phase 1.5 要自建抽取器。**严格按字面读，Phase 1.5 违反 NFR-04。**
  - 决策：改写为「不做多数据源适配；财务数值只来自巨潮公开年报 PDF 原文，
    AKShare 公开指标仅作对照」。
  - **判断依据**：数据**源**没有变（都是巨潮），变的是**抽取方式**。
    原措辞把「数据源」与「某个上游仓库的数据层」混为一谈——
    而那个数据层只有 `roa` / `ocf` 两个字段，撑不起任何一个指标。
    **这是措辞过时，不是范围扩张**；改写后 Non-goals 里「多数据源接入」一条完全不受影响。
- 判据：两条 NFR 在 `PROJECT_SPEC.md` 里各有明确处置且注明日期与本决策编号。
  **已完成 2026-08-21**（NFR-02 加豁免段，NFR-04 改写并附原文与理由）。
- 反转触发条件：若 Phase 2 的 Web 演示最终确实需要服务端运行时，
  NFR-02 的豁免不自动扩大，**须重开本条**并连带评估 D-005（仓库可见性）与 D-014（许可证）。

### 2026-08-22 修订 —— 开发期 CI 不视为部署基础设施（第二条豁免）

- 状态：Accepted（**操作者裁决**，2026-08-22）
- 起因：读 hello-agents 框架源码时对照出一个活样本——它**无 `.github`**、
  `pytest` **从未声明为依赖**、19 个测试文件 4892 行 655 条 `assert`，
  却**只有 3 处 `pytest.raises`**，而同一份产物数出的吞异常点是 **35 处**
  （`references/hello-agents-framework-core.md` §7，本会话复核属实）。
  **它有测试，但没有任何机制保证测试被跑过、被跑对。**
- 冲突：`PROJECT_SPEC.md` §11 要求「完成必须有证据：实际命令 → 真实输出」，
  而在本决策之前，那份证据是**由被验证方（agent）手动跑一遍再粘进 commit message**。
  **证据由被验证方自己产生**——这正是本项目反复记录的那类自证缺陷
  （台账已记四个实例，其中 A-9 就在我们自己的设计里）。
- 决策：**开发期 CI 不视为 NFR-02 的「部署基础设施」。** 落地为
  `.github/workflows/gates.yml`，在 push / PR 时跑既有门禁，不新增任何检查项。
- **豁免边界写死**（与静态托管那条同一写法，边界即实质）：
  CI **只允许跑验证**，**不得**用于构建产物、发布、部署或任何写操作；
  工作流声明 `permissions: contents: read`。越出此范围须先修订本条。
- 为什么不选另外两条路：
  - **(b) 认定 CI 算部署基础设施、不配** —— 维持了措辞，代价是 §11 的证据链
    永远留着「自证」这个结构性弱点，而这与项目主线（可审计）直接矛盾。
  - **(c) 改做本地 `pre-push` hook** —— 不引入任何仓库外服务，与 NFR-02 零冲突；
    但 hook 在本地、换机器即失效、且可被 `--no-verify` 绕过，
    **仍然是「自己监督自己」，只是难忘一点**。它解决的是「忘记跑」，
    不解决「证据由被验证方产生」。
- **附带收益（不是选它的理由，但值得记）**：冻结校验放在 Linux runner 上跑，
  等于机器化验证了 D-012 的物理保障——`.gitattributes` 强制 LF 入库、
  SHA-256 必须跨平台稳定。**这一条在本地 Windows 上跑不出来**：
  CRLF 一旦混进冻结产物，只有 CI 会红。
- 判据：
  1. `.github/workflows/gates.yml` 存在，跑**当时全部**的门禁 + 两处冻结校验 + 供应链门禁；
     **已完成 2026-08-22**（本地先验证全部命令为绿再提交，不提交第一天就红的门禁）
     - ⚠️ **2026-08-24 更正**：本条原写「跑四道门」。写下时是准确的，但 08-23 落地
       第五道门（`check_reading_ledger.py`）**没有同步进 CI**——工作流步骤名已改成
       「五道门」，`echo` 仍写「四道门」，实际只跑四条，**三者不一致而无人发现**。
       已于 08-24 补齐第五、第六道门并让三者对齐。判据文字改为「当时全部的门禁」，
       **因为写死数字会在下一次新增门禁时再次变成假的**——这与 D-012 的
       「SHA-256 只到产物级，盖不住文档说 7 项而代码已 8 项」是同一个粒度问题。
  2. 工作流权限为只读，无任何写/发布步骤；**已完成**
  3. ~~首次 push 后 CI 实跑通过~~ —— **已验证 2026-08-24**。
     首次 push（`129f049..5effc00`，27 个提交）触发 run `32732752831`，
     三个 job 全绿：`gates (3.11)` / `gates (3.13)` / `supply-chain`。
     - **六道门逐条在 runner 上报通过**（`427 passed` / 扫描 0 风险 / 0 项不合规 /
       交叉引用通过 / 阅读台账通过 / 门禁的门禁通过 / `六道门全 exit 0`）
     - **冻结校验 26 个文件在 Linux 上全 `OK`**（frozen-01 的 21 + poc-01 的 5）
       —— 这是本地 Windows 验不出来的那一半：`.gitattributes` 强制 LF 入库、
       SHA-256 跨平台稳定，**D-012 的物理保障至此有了机器证据**
     - 一条**非阻塞**告警：`actions/checkout@v4` 与 `actions/setup-python@v5`
       仍以 Node.js 20 为目标，被强制跑在 Node.js 24 上。不影响判定，记在此处
- 反转触发条件：若 CI 开始承担验证之外的职责（构建、发布、部署），
  或其维护成本超过它挡下的问题，重开本条。

### 2026-09-06 修订 —— **无状态请求–响应服务**不视为部署基础设施（第三条豁免）

- 状态：Accepted（**操作者裁决**，2026-09-06。`D-039` 的前置：
  操作者原话「改后样张看了，三节够了，**按 D-021 修订继续**」）
- 起因：`D-039` 把交付形态由「Web 演示」升级为**并入 cninfo 网站的正式产品**。
  产品要为任意公司/年份现算，必然要服务端运行时 ——
  而本条 2026-08-21 的豁免边界逐字写着「**一旦演示需要服务端运行时、数据库或
  计划任务，即回到 NFR-02 约束之内，须先修订本条**」，
  反转触发条件又明写要**连带评估 `D-005`（仓库可见性）与 `D-014`（许可证）**。
  ⇒ 本次修订就是那三件事一起做。

#### 决策：豁免三

**一个无状态的请求–响应服务**（静态前端 + 按请求触发、无常驻状态的 API）
**不视为**「部署基础设施」。

**仍然禁止，要用就得再修订本条**：

| 禁的 | 为什么这条线画在这儿 |
|---|---|
| **计划任务 / 后台作业** | 「没人请求时也在跑的进程」是运维的起点：它会失败、会积压、会需要人盯 |
| **作为记录系统的数据库** | 一旦丢了会丢数据，就有了备份、迁移、一致性这一整套 |
| **多环境机队 / 值守 / on-call** | 这是 `NFR-02` 原文真正想挡的那件事 |
| **用户账号体系**（注册 / 登录 / 会话存储） | 引入身份就引入了个人数据与合规，与 `D-010` 的克制取向相反 |

**缓存只允许「可丢弃缓存」**：内容能由公开数据重新推导，**清空不丢任何东西**。

**两句话就能判越没越界**（不需要讨论）：

1. **有没有一个进程在没人请求的时候运行？** 有 ⇒ 越界。
2. **把存储全部清空，有没有东西永久丢失？** 有 ⇒ 越界。

`NFR-02` 的用意是「防止项目滑向运维工程」。无状态请求–响应**不产生运维班次**，
而计划任务与有状态数据库产生。这条线不是措辞妥协，它对着那个用意画。

#### 连带评估一：`D-005`（仓库可见性）—— **不变**

**托管 ≠ 发布。** 实现代码留在私有仓，对外的是前端构建产物与一个 API 端点。
`D-005` 三样对外展示物里第 2 样本来就是「可访问的 Web 演示链接」，产品是它的加强版。
⇒ **本条不修订 `D-005`。** 一旦要把实现开源，那是另一次决策。

🔴 **但有一件事必须现在承认，不能装作没发生**：
`D-038` 的 `G3` 让**指标与别名全表**通过任何一次 `METRIC_NOT_DEFINED` 拒答对外可读
（当前 20 个定义 / 94 个叫法）。这落在 `D-005` 第 3 段允许的范围内
（语义层定义可作姊妹仓库开源），**但从此它是事实公开，不再是「可以选择开不开源」**。
接受这一点，是因为拿掉它就等于拿掉 `G3` —— 而 `G3` 是 H2 六道题的直接补救。

#### 连带评估二：`D-014`（许可证）—— **不修订，但它的前提刚刚成真**

`D-014` 的原文写的是一个**假设**：
「`D-006` 把交付形态定为 Web 为主，**Web 演示是网络服务**；AGPL v3 §13 的网络条款
要求向通过网络交互的用户提供对应源码。而 `D-005` 规定主仓私有至 Phase 4。
**二者不能同时成立。**」

**2026-09-06 起这不再是假设。** ⇒ 三条随之加强：

1. **任何为服务端新增的依赖，先过 `scripts/verify_deps.py`**，许可证一侧必须绿。
   ⚠️ 该门禁的**下载量**一侧因 `D-030` 跌破而红着（`D-036` 接受了）——
   **那不豁免许可证一侧**。两侧是两件事。
2. **AGPL 组件的禁令后果升级**：此前是「进来了将来分发会有麻烦」，
   现在是「**进来了 `D-005` 当场失效**」——因为服务一旦上线，§13 立即触发。
3. `PyMuPDF` / `fitz` 的禁令按原样保留，`LICENSE_EXEMPTIONS` 仍为空。

🔴 **顺带更正我自己 2026-09-05 的一个不完整结论**（`N-60` / 第三十九段）：
我当时读了 `D-005` × `D-021` 推出「只剩静态回放」。**那是两条决策的交集，不是三条。**
`D-014` 的裁决理由里早就逐字否掉过这条路 ——
它拒绝选项 (c)「演示只用预抽好的静态数据」，理由是**「恰好抽掉本项目最需要当众展示的那一环」**。
结论在当时给定的两条下没错，但**少读了一条已经把话说完的决策**。

#### 代价，以及什么时候要退回来

- **代价**：从此有一个对外端点会坏、会被刷、会有冷启动。
  这是 `NFR-02` 原本要避免的一类成本，**这次是明知代价地接受**，不是没看见。
- 反转触发条件（任一成立即重开本条）：
  1. 服务开始需要**计划任务或有状态数据库**；
  2. 运维占用开始挤压语义层与评测的推进（`NFR-01` 是单人可实现）；
  3. 任何 AGPL 组件进入服务端依赖闭包 —— 那时要立刻停服或修订 `D-005`。

## D-022 — 拒答码新增 `UNAVAILABLE`；异常与 `Refusal` 按「是否可预期结果」划界

- 状态：Accepted（**操作者裁决**，2026-08-23）
- 背景：现有 `RefusalCode` 七支（`resolve.py:41-47`）**全部是「口径或数据本身有问题」**
  ——指标未定义、别名歧义、定义不合规、命中未定义条件、条件不可求值、跨版本比较、跨准则版本比较。
  Phase 1.5 一接真实下载就会撞上**另一类**：巨潮超时、PDF 取不到、AKShare 挂了。
  **这不是「口径有问题」，是「够不着」**，现有七支没有一支能表达它。
- 外部佐证：harness 把 `unavailable` 做成 `ApprovalOutcome` 四支里的**一等值**而非异常
  （`references/deepseek-harness-docs-part2.md`，`approval.md:122` 逐字
  「normalized to `'unavailable'`」）。
- **改动面已实测，比预估小**（本会话核过）：
  `grep 'len(RefusalCode)|list(RefusalCode)'` **零命中**——不存在「枚举恰为 7 个」的锁死测试；
  `eval/frozen-01/` 全目录对 `minority_interest_share` / `scope_change` 零命中，
  **新增一支不改动任何现有码，也不触及冻结产物（D-012 安全）**。

### 决策一：新增 `RefusalCode.UNAVAILABLE`

语义：**口径服务或数据源不可达**。取值域是 AC-02 的契约，故记为决策而非实现细节。

为什么它必须是拒答而不是异常：**对复核者而言「取不到年报所以答不了」是一个可理解的答案**，
它应当进证据链、可机读、可被 AC-05 计入。若抛异常，这一情境在证据链里是**一片空白**
——而 D-003 要求「任何回答如果给不出证据链，视为失败，不视为降级成功」。

### 决策二：异常与 `Refusal` 的界线 = **是否可预期结果**

| | 走 `Refusal`（进证据链、封闭理由码） | 走异常（fail fast，不进证据链） |
|---|---|---|
| 判据 | **系统正常运行下可预期的状态** | **程序或配置缺陷** |
| 检验方法 | **能不能写进文档告诉用户「这可能会发生」** —— 能写，就是 `Refusal` | 写不出来，因为它意味着「我们写错了」 |
| 例 | 口径未定义 ｜ 命中已声明的未定义条件 ｜ 条件不可求值 ｜ **数据源超时/取不到** | 映射表 YAML 语法错 ｜ 字段 id 不在任何定义的 `source_fields` 里 ｜ 解析器内部状态矛盾 ｜ 勾稽批次元数据缺失 |

**为什么不按「是否在语义层内部」划**：那条线会把「数据源超时」推给异常，与决策一直接矛盾，二者只能选一。

**为什么不按「是否可重试」划**：可重试是**运行时属性**不是**语义属性**。
同一个错误换个网络环境就换分类，契约不稳定——而 `RefusalCode` 是 AC-02 的取值域，必须稳定。

**这条线的作用是防止拒答码退化成万能错误袋。** 没有它，`UNAVAILABLE` 会迅速吸走所有 I/O 异常，
`AC-02`「拒答理由可机读」就失去意义——一个什么都能装的理由码等于没有理由码。

- 判据：
  1. `RefusalCode` 新增 `UNAVAILABLE` 一支，语义限定为「口径服务或数据源不可达」；**待 Phase 1.5**
  2. 抽取层的下载/取数失败**返回 `Refusal(UNAVAILABLE)` 而不是抛异常**，
     且该拒答进入证据链、被 AC-05 计入；**待 Phase 1.5**
  3. 存在一条测试，断言**配置缺陷类错误抛异常而不是变成 `Refusal`**
     （负向用例：给一个 `source_fields` 里不存在的字段 id，必须 raise 而不是返回 `Refusal`）；**待 Phase 1.5**
  4. `eval/frozen-01/` 的哈希在改动后仍全部 OK；**待 Phase 1.5**
- 反转触发条件：若实现中发现「可预期」这条判据在某类错误上反复引起争议
  （同一个错误两个人分到两边），说明判据不够机械，届时改为逐类枚举而不是改判据方向。

## D-023 — 取数口径三元组 `selection` 进入抽取记录，且不可为空（D-019 的显式扩展）

- 状态：Accepted（**操作者裁决**，2026-08-25。由 `01.5-01-PLAN.md` Task 1 的 Q1 提出）
- 背景：`ARCHITECTURE.md` §8.5.2 已写明「证据指针必须携带
  `selection = {sampling, order_key, truncation}`，且与指针一样不可为空」，`EVAL_CASES` 的 J-7
  也已落地。但**同一节末尾逐字留着**「⚠️ 尚未实现，且 `page` / `anchor_page` 之外的三元组字段
  **不在 D-019 的裁决范围内**，落地时须确认是否构成对 D-019 的扩展」；
  `LANDING-BACKLOG` 的 L-2 行同样挂着「仍须确认」。**本条就是那个确认。**
- 决策：`selection` 三元组作为 **D-019 的显式扩展**进入 `ExtractionRecord`，与
  `page` / `anchor_page` 同级，**不可为空**。
- 为什么不放批次级元数据（被否决的选项）：
  1. **`ARCHITECTURE` §8.5 的核心发现就是「指针只要是可选返回值，就一定会在接线时被丢掉」**
     ——hello-agents 的 `full_output_path` 形状完全正确、只有 30 行，却在唯二两个调用点
     被丢弃、全包零处读回。放批次级 = 做成可选/脱钩 = 正是 §8.5 写出来要防的形态。
  2. **截断是行级事实。** 批次级表达不了「哪一行被截了」。
- 为什么这不违反 D-019 当初「不再加第三个字段」的理由：那条拒绝针对的是**页码粒度**
  （`label_pages`），而 `selection` 是**取数来源的性质**，不是页码。
  且 D-019 的反转触发条件明写「若 U-01 定下的字段集要求更细，按那份字段集扩展——本条不构成阻碍」。
- 对「三元组在整表逐行读时是三个固定值、像噪音」这条反对的答复：**明说不算噪音。**
  `sampling="full"` 陈述的是「这是整表」，本身是信息；harness 的做法正是页脚明说抽样方式。
- 判据：
  1. `ExtractionRecord` 含 `selection` 且三个子字段均不可为空；**待 Phase 1.5**
  2. 有一条测试断言 `selection` 缺失时构造记录会失败（不是「记一条 warning」）；**待 Phase 1.5**
  3. `ARCHITECTURE.md` §8.5.2 末尾那条「⚠️ 尚未实现 / 须确认」的告示改写为指向本条；**待 Phase 1.5**
- 反转触发条件：若 U-01 在 Phase 2 定下的证据链字段集与本条冲突，按那份字段集为准。

## D-024 — 抽取批次的身份键是 `(stock_code, fiscal_year)`，`batch_id` 由它加 PDF 的 SHA-256 派生

- 状态：Accepted（**操作者裁决**，2026-08-25。由 `01.5-01-PLAN.md` Task 1 的 Q2 提出）
- 背景：`CONTEXT.md` §5 写「先单公司单年」，但 `01.5-02-PLAN.md` **必须**补一家有真实企业合并的
  公司年做正样本——茅台 2023 的企业合并六项 1–4 全「不适用」，**D-020 判据 3 没有正样本可验**。
  ⇒ **本阶段实际就会同时存在两家公司的抽取批次。**
  「一家公司一年 → 多家公司多年」的转换在本阶段就发生，不是 Phase 2 的事。
  （这是 planner 的 assumption-delta 检测器正确触发的一次。）
- 决策：批次身份键 = `(stock_code, fiscal_year)` 二元组；
  `batch_id` 由该二元组加**该份 PDF 的 SHA-256** 派生。
- 为什么不维持单例（被否决的选项）：单例意味着正样本那家公司的记录与茅台的记录
  **无法在同一次运行里共存**，探测报告要靠人记住「这一份是哪家」。
  **身份活在人脑里而不在数据里**，是本项目反复记录的那类缺陷。
- 附带满足 **J-6**（证据 id 必须是可逐字比对的稳定标识）：同一份年报重下得到同一个 `batch_id`。
- 与 `CONTEXT.md` §5 的关系：§5 那句的正文是「**不做全市场批量**」。
  支持两家公司的身份建模 ≠ 做全市场批量，二者不冲突。
- 判据：
  1. `batch_id` 是 `(stock_code, fiscal_year, pdf_sha256)` 的确定性函数，同输入同输出；**待 Phase 1.5**
  2. 有一条测试断言两家公司的批次可在同一次运行里共存且记录不混淆；**待 Phase 1.5**
- 反转触发条件：若出现「同一公司同一年有多份年报」（更正公告、补充披露），
  身份键需要加一维，届时扩展而不是改派生方式。

## D-025 — 勾稽校验不通过新增 `RefusalCode.RECONCILIATION_FAILED`（第 9 支）

- 状态：Accepted（**操作者裁决**，2026-08-25。由 `01.5-01-PLAN.md` Task 1 的 Q3 提出）
- 背景：D-018 原话是「返回**封闭理由码**（沿用 `resolve.py` 的 `RefusalCode` 模式）」。
  **「沿用模式」与「用同一个枚举」是两件事**，而 `RefusalCode` 的取值域是 AC-02 的契约。
  现有 7 支、加 D-022 的 `UNAVAILABLE` 是 8 支，**没有一支表达「这张表不平」**。
- 决策：在 `RefusalCode` 新增第 9 支 `RECONCILIATION_FAILED`，
  语义限定为「该抽取批次的会计恒等式校验未通过」。**不另起 `BatchRefusalCode`。**
- 关键理由 —— **D-022 已经开了这个先例**：它把 `UNAVAILABLE`（一个纯抽取层的关切）
  放进了 `RefusalCode`，判据 2 明写抽取层的取数失败返回 `Refusal` 而不是抛异常。
  既然如此，另起一个枚举会造成**不一致**：下载失败用 `RefusalCode`、勾稽失败用别的，
  而两者没有原则性区别——按 D-022 决策二的判据（「能不能写进文档告诉用户这可能会发生」），
  **勾稽不通过显然是可预期状态**，属 `Refusal`。
- 为什么不怕违反 D-015 的单向依赖：`extractor → semantic_layer` 是**允许**的方向；
  D-015 的 AST 测试断言的是**反向**（`semantic_layer` 不得 import `extractor`）。
- 代价（如实记）：AC-02 的取值域再变一次；
  `01.5-01-PLAN.md` Task 2 里「`RefusalCode` 成员数为 8」的断言**须改为 9**。
- 判据：
  1. `RefusalCode` 成员数为 **9**，新增支语义限定为批次勾稽失败；**待 Phase 1.5**
  2. 勾稽失败时计算层拒答且理由码来自该枚举，有测试锁住「不是自由文本」
     （= D-018 判据 2 的落实）；**待 Phase 1.5**
  3. `eval/frozen-01/` 的哈希在改动后仍全部 OK；**待 Phase 1.5**
- 反转触发条件：若将来抽取层的失败类别多到让 `RefusalCode` 变成万能错误袋
  （D-022 决策二警告过的形态），届时按**领域**拆分枚举，而不是逐条往里加。

## D-026 — SC-2 未达标时的处置**规则**先定，结果后判

- 状态：Accepted（**操作者裁决**，2026-08-25。由 `01.5-02-PLAN.md` Task 4 提出）
- 背景：SC-2 是「30 个字段中 ≥ 24 个能抽出」，计数口径已于 2026-08-21 写死为
  「**取到的值经逐个人工核对正确**，不是标签命中」。字段构成实测为
  `bs 15 / is 8 / cfs 2 / notes 4 / kpi 1` = 30；已实测 16，**零实测 14**，门槛只允许失手 6 个。
  探测结果尚不存在，**结果无法提前判**——但**处置规则可以提前定**，
  否则 wave 2 会停下来等一次本可预先约定的裁决。
- 决策：按失手**原因**分两类处置，规则如下，wave 2 按此自行推进，不再停：
  1. **失手原因是「该字段根本不是报表上印出来的值」** ⇒ 走作废通道：移出分母、单独计数、
     **公开记录**。（`notes.reporting_period_months`「报告期月数」是最可能的一个——
     它是报告的元数据，不是报表里的行项目。）
  2. **失手原因是「印在那里但抽不出来」** ⇒ 这是真实失手，计入分母，不得作废。
- **两条必须随本条一起转述的限定**：
  - 第 1 类走的是 `EVAL_CASES` §2.2 的作废通道，而 **§2.2 本来是管评测题目的**，
    用到 SC-2 的字段集上是一次**类比迁移**。记录里必须写明是迁移，不得写成「§2.2 本来就覆盖」。
  - **10% 上限一并继承**：30 个字段 ⇒ 最多作废 **3 个**。超过即判 SC-2 的字段集构造失败，
    整体重审，不许挑几个剔掉再报数。
- 何时仍需停下来找操作者：**扣除合规作废后仍 < 24**。那是 SC-2 不成立，
  按 `PROJECT_SPEC.md` §10 的停止条件处理（降门槛 / 扩样本 / 改方法都是范围决策）。
- 判据：
  1. `PROBE-14.md` 的汇总表对每个失手字段标注属第 1 类还是第 2 类，**且理由不引用抽取器输出**
     （§2.2 的可判定性分界线：一旦需要引用系统输出来主张某项该废，它就落入第 2 类）；**待 Phase 1.5**
  2. 作废清单公开列在 `VERIFICATION.md`，含作废率；**待 Phase 1.5**
- 反转触发条件：若第 1 类的判定在实际中反复引起争议，说明「是不是印刷字段」不够机械，
  届时改为逐字段预先枚举，而不是留给执行时判断。

## D-027 — 证据落盘语义升格为决策：内容寻址 + append-only + tombstone + id 单调不复用

- 状态：Accepted（**操作者 2026-08-27 授权 agent 判断**。由 `docs/agent/ARCHITECTURE.md` §8.5.7
  与登记册 `L-72` 提出，该节此前逐字标着「待裁决」）
- 背景：`D-003` 规定「证据链是第一类产物」，**但没有规定它落盘的语义**。
  §8.5.7 的四条约束此前只活在 `ARCHITECTURE.md`——而按 `AGENTS.md` 的文档优先级，
  阶段 Artifact **低于**本文件，后续会话可以把它当作可议内容改掉。
- 决策：以下四条为本项目证据落盘的**决策级约束**，改动须走决策变更流程：
  1. **内容寻址** —— 证据文件名用内容 SHA-256 前缀，**不用时间戳**。天然幂等。
  2. **append-only** —— 已提交的证据不可原地改写。
  3. **删除只写 tombstone** —— 记「何时、被谁、以什么理由标记失效」，不覆盖不移除原记录。
  4. **id 单调不复用** —— **禁止用集合长度或运行时计数参与 id 构造**。
- **为什么这四条升格，而同日裁决的 `L-7` / `L-23` 两条刻意不升格**：分界线是**可逆性**。
  那两条是**写法规则**，改了只影响以后怎么写；本条是**数据格式承诺**——
  一旦按这个形状落了盘，改动会作废**所有已产出的记录**。
  这与 `L-10`（抽取三态）是同一个道理：第三态不在建记录时设计进去，事后加会改动所有已产出记录。
  **wave 2 的探测报告就是第一批要落盘的证据**，所以必须在落盘之前锁，不是之后。
- **为什么不构成 `D-009` 禁止的第二个权威落点**：本条只承载**约束本身与反转条件**；
  §8.5.7 保留**论证与三处独立外部实证**（时间戳精度 / `clear` 不留痕 / `len` 变小导致 id 复用），
  并已改为指向本条。分层方式沿用 `A-9` → `D-016` 的既有先例。
- **代价如实记**：内容寻址要求落盘前内容已完全定形，**不能边算边往同一个文件追加**。
  这与「append-only」的直觉写法冲突——append-only 约束的是**记录集合**，不是单个文件。
  实现上意味着多一次内存缓冲，或先写临时文件再按最终哈希改名。
- 判据：
  1. 抽取产物落盘时路径含内容 SHA-256 前缀且**不含时间戳**；
     有一条「同一内容写两次得到同一路径、且不产生第二份拷贝」的回归测试。
     **触发时点**：第一处**由系统自主决定路径**的证据写盘出现时。
     CLI 显式指定的 `--out` 与具名测试固件**不在本判据范围内**——路径由人给定，
     内容寻址无从施加。**预计在 wave 3**（抽取记录持久化），
     写这条时的初稿误挂「待 wave 2」，而 wave 2 只有上述两类，已更正。
  2. 无任何代码路径原地改写或删除已提交的证据文件；删除走 tombstone。**待 wave 3**
  3. 有一条机械检查断言 id 构造不使用集合长度或运行时计数（可做成第六道门的一条规则或 AST 测试）。**待 wave 3**
- 反转触发条件：若出现「内容完全相同但来源语境不同」的证据（例如两家公司同一行文字），
  内容寻址会把它们合并成一份，此时改为 `内容哈希 + batch_id` 的复合路径——
  **而不是退回时间戳命名**。退回时间戳等于放弃第 1 条的全部理由。

## D-028 — `notes.business_combination_type` 由单值枚举改为**集合值字段**

- 状态：Accepted（**操作者裁决**，2026-08-27。台账 `N-39` 的处置，选项 (b)）
- 背景：wave 2 找正样本时实测（`PROBE-COMBINATION.md` §3(a)）——
  万华化学 600309 · 2019 的「合并范围的变更」里，
  **第 1 项（非同一控制下企业合并）与第 2 项（同一控制下企业合并）同时为「√适用」**。
  而现取值域是 `['同一控制下', '非同一控制下', '无']`，**一个单值枚举**：
  填「同一控制下」丢掉三笔现金收购（北京聚丽威 / Chematur / 福建康乃尔），
  填「非同一控制下」丢掉当年吸收合并万华化工，填「无」彻底错。
  **三个值没有一个是对的。**
  后果不是报错，是**被迫填一个错值**，让 flag 以**假的理由**触发或不触发 ——
  `A-8` 记的是「该置位而没置位」，这条是「会以假理由置位」，方向相反、同样不可见。
- 决策：
  - **取值改为集合。** 元素取值域 `[同一控制下, 非同一控制下, 反向购买]`，
    对应披露模板子项 1 / 2 / 3。
  - **取消 `无` 这个哨兵值，空集即表示本期无企业合并。**
    在集合域里放一个「无」，是把「没有元素」写成「有一个叫『没有』的元素」——
    范畴错误，且它会与真实元素并列出现（`{同一控制下, 无}` 无意义）。
  - **`enum_values` 键保留，但语义从「取值域」变为「元素取值域」**，
    YAML 里必须逐字写明这一点 —— 不写，下一个读的人会当成单值。
  - 子项 4 / 5 / 6（处置子公司 / 其他原因的合并范围变动 / 其他）**不进本字段**，
    它们由 `D-020` 新增的布尔字段 `notes.consolidation_scope_change` 承载。
- **为什么这不是在推翻 `D-020` 拒绝过的那个 (b)**：⚠️ 这一条必须写清楚，
  因为两处的措辞几乎一样，后来的人一定会读出矛盾。
  - `D-020` 拒绝的 (b) 是「**扩取值域让它同时承担 `scope_change` 的语义**」——
    一个字段回答两个问题，是**重载**。那个拒绝是对的，本条不碰它：
    `scope_change` 的 trigger 仍按 `D-020` 挂 `consolidation_scope_change`。
  - 本条改的是「**这个字段表达它自己的真实取值域**」——
    两类企业合并可以在同一年真实并存（万华 2019 实证）。
    **集合性在世界里，不在重载里。**
  - `D-020` 决策正文写着「`notes.business_combination_type` **保留原义**（只表企业合并类型）」
    —— 本条**完整保留了这句**，只修它的基数。
- **代价与限制，逐条如实记**：
  1. **DSL 没有任何集合运算**（`src/semantic_layer/dsl.py` 的 `_PREDICATES` 只有
     `is_missing` 一支）。⇒ **本字段改集合后不能再出现在任何 trigger 里。**
     目前不需要 —— `D-020` 已把唯一的消费者移走。
     ⚠️ 将来若需要「本期发生过同一控制下合并」这类条件，**必须先扩 DSL**，
     不得临时用字符串比较凑一个能求值的表达式（那正是 `F-2`）。
  2. **冻结夹具里有 6 处 `notes.business_combination_type: 无`**
     （`eval/frozen-01/fixtures/synthetic-01.yaml`），`D-012` 一个字节都不许改。
     **已查实不构成冲突**：夹具值**从不被校验取值域**
     （`eval/baseline.py:89` 只把整份夹具当文本读给模型），
     且 `D-020` 移走 trigger 后**没有任何代码读这个字段**
     ⇒ 那 6 处成为**惰性遗留值**，与 `D-020` 已记的
     `notes.listing_first_disclosure_flag`（在夹具里却不属于任何 `source_fields`）
     **同一形态**。**冻结集不动，本条不违反 `D-012`。**
     ⚠️ 但要写进 `frozen-02` 的待办：夹具与定义的取值域**已经不一致**，
     只是当前无人比对。
  3. **`反向购买` 零正样本**：茅台与万华两家都是「不适用」。
     纳入取值域的理由是**忠于披露模板**（它是印在那儿的第 3 个子项、复选框可读），
     排除它会造出与 `A-8` 同型的假阴性。但「适用」这一支**一次都没观察到**，
     按 `F-2` 如实标注，不得写成「已验证」。
- **执行时点：wave 5（`01.5-05-PLAN.md`），不在 wave 3。**
  理由是 wave 3 有一条硬不变量「五份映射文件的 `field_id` 并集恰好等于
  `metrics/` 全部 `source_fields[].id`，不多不少」，中途改定义会打断它。
  **但形状现在就定死**，好让 wave 3 的 `data/mappings/pdf/notes.yaml` 直接按最终形状写。
- **对 `D-020` 那条 `metrics/` 边界例外的影响，明说**：
  `01.5-05-PLAN.md` 的 must_have 写着「`metrics/` 的改动严格限于 `D-020` 批准的两处」。
  本条**再加一处编辑**（仍在 `minority_interest_share.yaml` 这同一个文件内）。
  ⇒ wave 5 的那条判据要读成「限于 `D-020` + `D-028` 批准的范围，其余 19 份定义 diff 为空」，
  **不是违例**。放宽仍**仅限这两个文件**。
- 判据：
  1. `metrics/minority_interest_share.yaml` 的 `notes.business_combination_type`
     声明为集合值，`enum_values` 三元素，YAML 里逐字写明「元素取值域」与「空集 = 无」；**待 wave 5**
  2. 一条以万华化学 2019 为输入的回归用例，断言取值为
     `{非同一控制下, 同一控制下}`（两元素），**且它在单值实现下会红**；**待 wave 5**
  3. `python -m semantic_layer validate` 退出码 0，且该字段**不出现在任何 trigger 里**；**待 wave 5**
  4. `反向购买` 在册但标注为零正样本；**待 wave 5**
- 反转触发条件：若后续实测出现「三个子项之外还有第四类企业合并」，
  说明取值域仍不足，届时**回到披露模板重新枚举**，
  而不是再加一个「其他」兜底元素 —— 兜底元素会让取值域看起来永远够用。

## D-029 — 跨源不一致的判定：绝对差 0.01 元；单位不确定时判「不可比」不判「不一致」

- 状态：Accepted（**操作者裁决**，2026-08-27。`01.5-04-PLAN.md` Task 2 的 blocking checkpoint）
- 背景：`PROJECT_SPEC.md` / `DECISIONS.md`（含 `D-013`）/ `metrics/_flags.yaml` 里
  `source_disagreement` 的定义，**均未给出容差数值、也未说明比较方式**。
  `01.5-RESEARCH.md` 的 Assumptions Log **A3** 逐字警告：把「研究列出的维度」
  当成「研究给出的默认值」去实现，会引入一个**从未被任何人审阅过的隐性业务规则**。
- **裁决前的证据**：`docs/agent/phase-01.5/AKSHARE-A2.md`（Task 1.5 实测）——
  AKShare 的单位是**元**、精确到**分**，与 PDF 一致；
  九个科目的**绝对差全部 `0.00`、相对差全部 `0`**。
  ⚠️ 这份证据是**本 checkpoint 的前置**：原排序把它排在拿到数据之前，
  等于要求在没有证据的情况下选一个数（2026-08-25 已插 Task 1.5 更正）。
- 决策：
  1. **比较方式：绝对差。容差 `Decimal("0.01")` 元**（与报表「精确到分」一致）。
  2. **单位无法确定时判「不可比」，不判「不一致」**（`incomparable`）。
     该字段这次**没有对照结论**，`source_disagreement` **不置位**，
     且「不可比」这件事本身要进证据链。
  3. **比较在 `Decimal` 域做。** AKShare 侧的 `float` 走 `Decimal(repr(v))`，
     **不得**走 `Decimal(v)` —— 后者把二进制浮点的尾巴带进来
     （`Decimal(48697611501.2)` = `48697611501.2000007629...`），**凭空造出一个不存在的差额**。
- **为什么不选 `tiered`**（操作者 2026-08-25 的预选倾向，A2 出来后改选 `abs`）：
  `tiered` 要求再定一个**金额门槛**，而 A2 之后**没有任何证据支撑那个数**。
  凭空定它正是 `A3` 警告的「从未被审阅过的隐性业务规则」。
  PLAN 自己写的判据也是这条：「若 AKShare 是元、全精度 ⇒ `abs` 就够，`tiered` 是过度设计」。
- **为什么不选 `rel`**：茅台有**四个「格子空 = 0」**的字段（`A-2`），
  相对差在零分母上直接失效。
- **为什么 `abs` 的那条 con 在实测面前不成立**：它的风险是「AKShare 若有系统性舍入
  （例如存万元再乘回来）⇒ 大额科目几乎全触发」。A2 实测九个科目差额精确为 0，
  **该风险在本样本上不存在**。
- ⚠️ **必须随本条一起转述的限定**：
  **本样本的差额全为 0，所以它无法用来标定阈值。**
  它只证明「阈值不必为了容纳系统性偏移而放宽」，
  **不证明 `0.01` 在别的公司 / 别的科目上不会误报**。
- 反转触发条件（**写死，因为它是这条决策唯一的自我纠错机制**）：
  **同一批次里触发 `source_disagreement` 的字段占比超过 1/3 时，
  按「档位选错」处置，不按「数据真的不一致」处置。**
  那时改用 `tiered`，且**那时才会有数据支撑那个金额门槛**。
  ⚠️ 这条不是「觉得不对就改」——它是一个**可机械判定的比例**，
  由 `crosscheck` 在每批结束时算出来并写进证据链。
- 代价如实记：`reversibility: costly`。容差一旦定下并跑出对照结论，
  改它会让此前所有 `source_disagreement` 的判定**全部作废**，需要重跑并重新核对。
- 判据（**四条全部达成，2026-08-28 wave 4 Task 3**）：
  1. ✅ `src/extractor/crosscheck.py:DISAGREEMENT_TOLERANCE` 是一个具名常量，
     不是散落的字面量。`test_容差是具名常量且等于一分钱` 锁住取值
  2. ✅ `CrossCheckVerdict` 三支（`AGREES` / `DISAGREES` / `INCOMPARABLE`）+
     `IncomparableReason` 七支。负向用例 `test_不可比与不一致是两个不同的取值且不合并`。
     ⚠️ **2026-08-28 修正本条措辞**（独立复核查出）：原文写「断言两者在 `to_dict()` 里
     是不同的字符串」，而**真正锁住它的不是那一条**。
     `assert ... != "DISAGREES"` 与 `assert ... is not CrossCheckVerdict.DISAGREES`
     在前面已断言 `is INCOMPARABLE` / `== "INCOMPARABLE"` 之后**恒为真**
     （Enum 成员互异），任何输入都红不了。**起作用的是前面那两条正向断言。**
     行为没错，但**判据把功劳记给了不起作用的那一半** ——
     一条记错功劳的判据，会让下一个人删掉真正起作用的那行还以为没动到锁。
     **实测里最要紧的那一支不是单位，是 `REFERENCE_NAN`**：茅台四个「格子空 = 0」的
     字段在 AKShare 侧全是 `NaN`，当 0 会比出四条**从未被建立过的跨源一致**
  3. ✅ `_reference_decimal()` 是 **AKShare 侧 float → Decimal 的唯一转换点**，走 `Decimal(repr(v))`。
     ⚠️ **措辞 2026-08-31 收窄**（复核 `RV-7`）：原文写的是「**全仓**唯一转换点」，
     而被机械保证的范围比那句话小 —— **声称的范围大于被证明的范围**。
     机械保证的是下面三条，逐条写清各自到哪儿为止：
     - `src/` 下 `Decimal(repr(...))` **只许一处**，且必须在 `crosscheck.py` 里
     - `crosscheck.py` 里非字面量参数的 `Decimal(...)` 只许是那一处
       （拦的是不含 `repr` 的 `Decimal(raw)`）
     - **全 `src/`** 下每一处非字面量 `Decimal(...)` 都必须在
       `tests/test_crosscheck.py::DECIMAL_CONVERSION_SITES` 里**被点名并写明输入是什么**，
       多一处就红（2026-08-31 新增，补上 `RV-7` 实测的那个洞：
       原来方向②只扫 `crosscheck.py`，`Decimal(v)` 放进 `units.py` 两道检查都看不见）
     **没有**保证的是「AKShare 的 float 只从这一处流过」—— 那需要数据流分析，
     AST 给不了。全 `src/` 现有三处非字面量转换，另两处的输入分别是**公式串**
     （`formula._tokenize`）与 **PDF 版面文本**（`locate.parse_amount`），与 AKShare 无关；
     所以「只许一处」这条更强的规则在这里**表达不了要表达的东西**，
     强行扩张只会误伤（这也是 `RV-7` 给的「二选一」里为什么两条都做了）。
     **造回归验过会红**：把 `raw_reference` 改成 `Decimal(reference.raw)` 后该用例失败
  4. ✅ `BatchCrossCheck.disagreement_ratio` 用 `Fraction` 算（不是浮点，
     否则「恰好 1/3」这条边界会变成一次靠舍入决定的判断），分子分母都进 `to_dict()`，
     超阈值时 `note` 里逐字含「疑为档位选错」
- **首次真实数据结论**（茅台 2023，24 个字段）：可比 20 / 不可比 4 / 不一致 **0**，
  触发占比 `0/20`。⚠️ **差额仍然全部精确为 0.00 ⇒ 这批数据依旧不能用来标定阈值**，
  而且 `DISAGREES` 这一支在真实数据上**零样本**（只有构造样本）。逐字段见
  `docs/agent/phase-01.5/COMPUTED-VALUES.md` §5。

## D-030 — 供应链门禁的下载量门槛为 akshare 单独下调至 300 万/月

- 状态：Accepted（**操作者裁决**，2026-08-28。此前作为 `9cd16bf` 之前的**未追认既成事实**
  挂在交接文档 Next Steps 第 16 条）
- 背景：`scripts/verify_deps.py` 的 `MIN_MONTHLY_DOWNLOADS = 10_000_000`。
  2026-08-27 执行 wave 4 Task 0 时，akshare 两次被该门禁拦下，
  执行者**自行**把门槛为它下调到 3,000,000 并继续往下走。
  ⚠️ **这是一次由执行者做出的、把门放低的动作，当时没有操作者的授权** ——
  执行者当轮如实把它记进交接文档要求追认，而不是当作已通过。
- 决策：**接受**。门槛下调至 `3_000_000`，登记在 `DOWNLOAD_THRESHOLD_OVERRIDES`。
- **为什么接受的是「下调」而不是「豁免」**（这条区分是本决策的实质）：
  豁免会让门禁对该包**不再检查任何东西**，而输出里照样是一句「通过」——
  那正是本项目已记七次的形状：自证机制说通过了，但它证明的不是它声称证明的事。
  下调保留了「掉下去就红」这个性质，且下调理由被打进门禁输出，每次跑都看得见。
- **为什么这个包在结构上够不到通用门槛**：`10,000,000/月` 是照 pytest（10.7 亿）、
  pdfplumber（5,657 万）这类**通用库**定的。akshare 是领域专用的中文金融数据库，
  用户面在结构上就窄于通用库，**不可能与它们同尺度** ——
  这不是「它就是没那么流行」，是分母不同。
- 支撑证据（Task 0 人工审计，逐条实查）：GitHub 仓库创建于 2019-10-01
  （PyPI 显示的「首发 2025」是维护者删了旧 release 造成的假象，已查清）、
  219 个版本**无一 yanked**、无维护者变更、22,258 stars、
  已装文件 419 个逐一比对 wheel RECORD 全部一致、传递闭包**零 AGPL 命中**。
- ⚠️ **必须随本条一起转述的限定**：
  **余量很窄。** 2026-08-27 实测 3,738,489/月，2026-08-28 复跑 **3,662,055/月**
  —— 比下调后的门槛只高 **22%**，且一天之内在往下走。
  **它继续下滑就会红，那时是一次新的裁决，不是自动放行。**
- 反转触发条件（写死）：
  1. akshare 近月下载量跌破 3,000,000 ⇒ 门禁变红。**处置不是再下调一次** ——
     再下调就把这道门变成了一个永远跟着实测值走的橡皮门槛。
     届时的选项是：接受它红并停用 `crosscheck` extra，或另找对照源。
  2. 出现任何一项供应链信号（yanked 版本、维护者变更、RECORD 不一致）
     ⇒ 本条作废，回到 `blocking-human` 重审。
- **适用范围写窄**：本条只授权 akshare 这一个包这一个门槛。
  `DOWNLOAD_THRESHOLD_OVERRIDES` 每加一条都是一次新的把门放低，**各自需要单独裁决**。
- 判据：
  1. ✅ 下调项在 `DOWNLOAD_THRESHOLD_OVERRIDES` 里，带理由与实测值，理由进门禁输出
  2. ✅ 是「下调」不是「豁免」：门槛仍是一个数，掉下去仍然红
  3. ✅ **本条编号出现在 `verify_deps.py` 的下调理由里** —— 让复核者从输出直接找到裁决
     （`DOWNLOAD_THRESHOLD_OVERRIDES["akshare"]` 的理由串以 `【D-030…】` 开头）

## D-031 — 可比性标记**不阻断计算**，但必须在**人读输出**里看得见

- 状态：Accepted（2026-08-31，操作者授权 agent 判断）
- 背景：`N-43` 判据 3 接线完成后，11 个指标真实置位了标记
  （`restated` 10 个 + `scope_change` 1 个）。此前这个问题一直被前置条件挡着 ——
  判据是「`restated` 真取到值之后 `unevaluable` 还剩几个」，现在答案是 **0**，可以裁了。
- 决策：**不阻断。** 标记进证据链与输出，不改变「算不算得出这个数」。
- 理由（三条，逐条对着本仓已有的东西核过，不是一般性偏好）：
  1. **标记的语义是 `affects_comparability`，不是 `affects_correctness`。**
     `metrics/_flags.yaml` 里 `restated` 与 `scope_change` 两条都逐字写着
     `affects_comparability: true`。茅台 2023 的 `restated` 说的是**比较期**经追溯重述
     （企业会计准则解释 16 号），**本期**的资产负债率本身没有问题。
     阻断它等于因为去年的数被重述过而拒绝回答今年的比率。
  2. **本仓已经有一个「必须拒答」的机制，且它在另一节里。**
     `undefined_conditions`（如 `bs.total_assets <= 0` ⇒ 「资产负债率无经济含义」）
     才是「这个指标在这里没有意义」的落点。把 flags 也做成阻断，
     等于把两个不同的概念塞进一个机制 —— 与 `D-028` 拒掉 `无` 哨兵值、
     `D-020` 把两个字段拆开是同一条原理：**不同的事实不要挤进同一个格子。**
  3. **`D-003`：证据链是第一类产物。** 标记进证据链**就是**交付物本身，
     而 `AC-06`（复核者仅凭证据链判断对错）正是消费它的机制。
     阻断会把复核者能做的判断替他做掉。
- ⚠️ **但「不阻断」有一个必须同时成立的前提，否则这条决策就是放水**：
  **一个没人看得见的标记 = 没有标记。**
  裁决当天实测：`python -m extractor compute`（不加 `--json`）打出来的是
  `revenue_growth_yoy = 0.1901…（无量纲）` —— **一个字的标记都没有**，
  而 `revenue_growth_yoy` 是**跨期**指标，它自己的 `common_pitfalls` 逐字写着
  「比较期经追溯重述时，同比增长率含重述影响，与未重述年度的同比数不在同一基础上」，
  且标着 `enforced_by: flags.restated`。
  ⇒ 那条 `enforced_by` 当时**指向一个在人读输出里不存在的东西**。
  这正是 `ARCHITECTURE` §8.5 那条实证的形状（算出来的留证在接线时被丢掉），
  只不过丢在**最后一层：打印**。
- 判据：
  1. ✅ 人读输出对**三种状态各有各的字面**，`_comparability_line()`：
     置位 ⇒ `⚠ 可比性标记：restated`；未置位 ⇒ `可比性标记：无`；
     判不了 ⇒ `⚠ 可比性标记判不了：<理由首行>`
  2. ✅ **「未置位」也要显式打出来。** 留空的话，「一个都没触发」与「根本没判」
     在屏幕上长得一模一样，而两者后果完全不同。**沉默不表示任何一种状态。**
  3. ✅ 「判不了」必须带理由 —— 一个只有状态没有理由的判不了，复核者无从下手（`L-9`）
  4. ✅ 两条负控制按三步程序实跑（`M8` 把整栏抹掉 / `M9` 把理由去掉），都会红且红得对
- **反转条件**（写下来，免得下次靠感觉重开这个问题）：
  若出现一个标记，其语义是「**这个数本身是错的**」而不是「不可比」，
  那条标记**不适用本决策** —— 它该走 `undefined_conditions` 或一个新的拒答码，
  而不是把本决策改成「flags 一律阻断」。
  ⚠️ 判据是**那条标记的语义**，不是「我们想不想更严格」。


## D-032 — `explain` 的表达契约就是 agent 面向人的输出契约；Phase 2 证据链渲染复用它

- 状态：Accepted（**操作者裁决**，2026-09-04。台账 `N-52` 第 2 层）
- 背景：`N-20` 的判据 2026-09-03 **第一次读是红的** —— 20 份定义全过机械校验，
  只证明了对**机器**可读。换成 `explain` 那种排法之后第二次读才过。
  操作者随即问「这就是我们的 agent 输出范例？」，把一个措辞问题顶成了设计问题。
  与此同时 `02-01-PLAN.md` 的 T3 只产出一个**结构化答案对象**（`AC-05` 齐全率的机械判据），
  **计划里没有任何「人读渲染」的任务** —— 而 `AC-06` 的复核者限时 5 分钟仅凭证据链判对错，
  他读到的究竟是什么，此前没有任何文档指定过。这个洞不填，`02-03` 会在压力下现场即兴一个。
- 决策：**同一套表达契约。** 三条，缺一条这条决策就落不了地：
  1. `explain` 的排版契约上升为**「给人看的输出」的统一契约**：问句小标题、
     中文说法在前 / 系统实际执行的那一版附在后、机械后果与仅供参考**结构性**分开。
  2. 答案渲染器**内嵌 `render_explanation` 的输出**作为答案里的「口径」一节，
     不另写一份口径讲法。
  3. 答案特有的部分（取了哪一页、哪个数、SHA-256、拒答理由、可比性标记）
     **按同一套规矩新写**，不套用 `explain` 的函数 ——
     `render_explanation(defn)` 只吃定义、不吃计算结果，两者内容本就不同。
     ⚠️ **「复用」指的是契约，不是函数。** 把它读成「调同一个函数」会立刻卡住，
     然后倒退回两套渲染。
- 理由（三条，都对着本仓已有的东西核过）：
  1. **`D-003` 的反转触发条件要求得到这个。** 那条写的是「复核实验表明某些字段
     从未被复核者使用 ⇒ 删掉它们」。要观察到「某字段没被用」，复核者必须读
     **一份连贯的渲染**；读两套不同排法的东西，观察到的是排法差异，不是字段有用性。
  2. **`N-20` 已经实测过一次「只有机器读得懂」的代价。** 不复用契约，
     等于在新的那一套上把同一个坑重踩一遍，而这次没有检查点会拦。
  3. **`AC-05` 抓不到「证据不相关」，`AC-06` 是唯一能发现它的机制**（`PROJECT_SPEC.md` §9）。
     那个机制的输入质量由这条决策决定。
- ⚠️ **一并继承的约束**：**不合并中文版与系统执行版。**
  只留中文，读的人就没法核对翻译对不对 —— 两版并存是可复核性的载体，不是冗余。

### 契约收紧（2026-09-04 同日，操作者第二轮反馈）

原契约写的是「中文说法在前 / 系统实际执行的那一版**附在后**」，
落到实现上成了「每一节里中文在上、表达式紧跟在下」。操作者读到

```
系统实际执行： is.net_profit_attributable_to_parent - notes.nonrecurring_pl_net_attributable_to_parent
```

原话：**「你给财务人看这种技术说辞吗，我辅修计算机肯定看得懂，正常财务呢？」**

⇒ **「附在后」收紧为「集中放在末尾的核对附录」**，并加一条：
**本定义声明过的字段标识符不出现在正文。** 三条落地规则：

1. **正文只用定义自己写的中文**：`source_fields[].line_item` / `.statement`、
   `_flags.yaml` 的 `description`。新增「取数出处」一节，按「报表 · 行项目」列出。
2. **表达式全部下沉到末尾附录**，中文在上、`↳` 接实际执行的那一版。
   **一个字都不删** —— 附录不是补充材料，是可复核性的落点。
3. **散文里的字段 id 与 schema 键名同样要换**：字段 id 换成该字段的中文行项目名，
   schema 键名（`undefined_conditions` 等）换成本视图里那一节的标题。
   ⚠️ **换不动的原样留着，不编。** 最典型的是「不得使用某字段」里**被禁用的**那个字段
   —— 它按定义不在 `source_fields` 里，没有中文名；编一个出来，
   读者就没法发现禁的到底是哪一个。

⚠️ **这条收紧同样约束 Phase 2 的答案渲染**：`AC-06` 的复核者是财务背景，
不是工程背景。**页码、字段名、哈希这类东西属于附录，不属于正文。**

### 这条收紧实际做到了多少（2026-09-05 实测，独立复核提出）

上面那句原本写的是「**正文里不出现字段标识符**」——**没做到，也做不到**。
做到的是加了限定的那句：**本定义声明过、有中文可换的**字段 id 不出现在正文。

**残留：9 / 20 份定义、13 处、8 个不同字段**，且**全部**属于规则 3 那条
「换不动的原样留着」——每一个都是它所在定义**刻意不声明**的字段
（`is.net_profit`、`bs.total_liabilities`、`bs.total_current_liabilities_period_begin` …），
按定义就没有中文名。`tests/test_explain.py` 里有一条测试逐条 assert 这个性质
**并把 9 / 13 这两个数钉住**，变大变小都会红 —— 免得这段说法再一次悄悄失真（`N-35`）。

### 收口（2026-09-05 当日，操作者裁 `N-58` 选 (b)）

**9 份定义的散文已改写，残留归零**（实测 0 / 20 份、0 处）。
改的只有给人读的散文，`version` / `formula` / `expr` / `trigger` /
`source_fields[].id` 一个字节没动 —— **这句话是核过的**：改动前后 20 份定义的
**口径指纹逐字节相同**（`D-034`，`metrics/_fingerprints.json`）。

⚠️ **一个必须记下来的副作用**：语料里不再有字段 id 之后，
`test_正文里不出现任何本定义声明过的字段标识符` 变成了**预防性**的守门人 ——
把 `_prose` 的字段替换整个拿掉，它照样绿（实测：改散文之前拿掉会红，之后不会）。
⇒ 另加了一条**不依赖语料**的 `test_prose确实会把声明过的字段id换成中文`。
不这样做的话，`D-032` 这条契约就只剩一个说不出它什么时候会红的断言在守（`L-32`）。

⚠️ **2026-09-04 实测**：**16 / 20 份定义的散文里写着字段 id 或 schema 键名**，
上面第 3 条只能机械处理其中有中文名的那部分。剩下的要改定义文件本身，
是内容变更 ⇒ 记为台账 `N-53`，**不在本决策范围内**。
- 影响：`02-01-PLAN.md` 的 T3 从「答案对象」扩为「答案对象 + 人读渲染」；
  `AC-06` 的复核者读到的就是该渲染。
- 反转触发条件：`02-03` 复核实验中复核者明确反馈「口径一节挤占了 5 分钟里的注意力」
  ⇒ 把口径一节改为按需展开，**而不是退回两套渲染**。


## D-033 — 拒答码新增 `INTENT_INCOMPLETE`；意图层的 LLM 由调用方**注入**

- 状态：Accepted（2026-09-04，`02-01` T1 执行中。**沿 `D-022` / `D-025` 的先例**：
  新增一支拒答码是对 `AC-02` 取值域的变更，立决策而不是顺手加一个枚举成员）
- 背景：`02-01` T1 要求「解析不出来 ⇒ 拒答并说明缺哪一项，不猜」。
  现有九支拒答码**没有一支表达得了「提问本身缺要素」**：
  前七支讲「口径或数据有问题」，`UNAVAILABLE` 讲「服务或数据源不可达」，
  `RECONCILIATION_FAILED` 讲「批次校验未通过」。
  ⇒ 「题面里没有说是哪一家公司」无处可归，只能凑一个，而凑就是 `F-2`。
- 决策一：**新增第十支 `INTENT_INCOMPLETE`** = 「提问缺少作答所必需的要素（指标 / 主体 / 期间）」。
  它标记的是**请求层**，与前九支的口径层 / 数据层是不同的层。
- 决策二：**意图解析用的模型由调用方注入**（`parse_intent(..., ask_model=None)`），
  `src/agent/` **不 import 任何 LLM 客户端或网络库**，由
  `tests/test_agent_boundary.py` 的 AST 断言强制（含函数体内 import）。
  理由不是洁癖：`eval/llm_client.py` 自己的 docstring 逐字写着它
  **「永远不会被 `eval/run.py`（系统臂）导入」** —— 那条边界已经存在，
  在 `agent` 包里 import 它等于把它作废。
  ⇒ 附带好处：**整条确定性路径可离线跑完**，`ask_model=None` 是默认值。
- 决策三：**模型的答案仍要过别名表。** 模型只做「把一个说法归一到一个已存在的指标名」；
  它说了一个表里没有的名字 ⇒ 照样拒答。**不这样做等于把口径的权威交给模型**（`D-001`）。
- 反转触发条件：若 `02-03` 的复核实验显示复核者根本不看 `resolved_by`
  （「这个指标名是查表查到的还是模型给的」），按 `D-003` 的反转条款删掉该字段。

## D-034 — 改**散文**不算改口径；线画在三条精确路径上，由指纹机械守着

- 状态：Accepted（**操作者裁决**，2026-09-05。台账 `N-58` 选 (b)）
- 背景：`N-58` 摆出的事实是 **9 / 20 份定义的散文里写着字段标识符**
  （「禁止取 `is.net_profit`」这类），财务读者读到的正是 `D-032` 收紧节要消灭的东西。
  三个选项里 (a) 维持现状、(b) 改散文不动 `version`、(c) 改散文并 `version` +1。
  (c) 会让 frozen-01 的 20 道题标准答案**全部失效**（那些题把答案钉在 `metric_version: 2`）。
  操作者选 (b)。
  ⇒ 但 (b) 立刻带出一个此前**从没定过**的问题：**「我只改了措辞」这句话谁来核？**
  以「改措辞」为名动一下 `formula`，20 个标准答案会在没人发现的情况下失效。
- 决策，三条：
  1. **「散文」不是感觉，是三条精确路径**（`semantic_layer.definition.PROSE_PATHS`）：
     `derivation.note` / `common_pitfalls[].text` / `undefined_conditions[].reason`。
     只碰这三处 ⇒ 不算改口径，`version` 不动。
  2. **其余一切都算改口径**，包括 `display_name`、`source_fields[].line_item` /
     `.statement` 这类看起来也像「说法」的东西 —— 它们进证据链，
     改了就改变了这条答案自称的出处。
  3. **机器判据是口径指纹**：定义文件剔除那三处之后的规范化 JSON 的 SHA-256，
     记在 `metrics/_fingerprints.json`，由 `tests/test_definition_fingerprint.py` 守。
     指纹变而 `version` 没变 ⇒ 红。
- 理由：
  1. **这是本仓已有的做法，不是新发明。** `D-012` 用 `SHA256SUMS` 锁冻结集，
     `verify_freeze` 是 fail-closed 的前置门。口径指纹是同一个机制换一个对象。
  2. **剔除按白名单式的精确路径，新加的键默认进指纹。** 反过来做
     （列一张「语义键」清单）会让新加的键默认逃出指纹 —— 正好是要防的方向。
  3. **落地当场就用它验了一次**：`N-58` 改完 9 份定义的散文之后，
     20 份指纹与改动前**逐字节相同**。这不是「我认为我只改了措辞」，是核过的。
- ⚠️ **顺序不能反**：有意的口径变更要先改 `version`、再重算指纹、
  最后按 `N-46` 处理 frozen-01。重算命令**故意不写进文档** ——
  顺手就能重算的门等于没有门；这道门唯一的作用就是逼一次自问。
- 反转触发条件：若某次真实的口径变更表明「三条路径」划得太窄或太宽
  （例如 `standard_basis[].article` 的排版调整被判成口径变更），
  修订 `PROSE_PATHS` **并同时更新本条**，不得在测试里加豁免。

### 与 `PROJECT_SPEC.md` 的冲突已收口（2026-09-10，台账 `N-73`）

本条立下时，`PROJECT_SPEC.md` §版本号语义 写的是另一套，且**三处方向相反**
（`display_name` / `aliases` 那两行说不 bump，`derivation` 那行说任何改动都要 bump）。
按 `AGENTS.md` 的文档优先级 `PROJECT_SPEC.md` 上位 ⇒ 冲突期间**机器上跑的这一套
与上位文档的字面相反**，而且已经被行使过一次（`N-58` 改散文、指纹不变、`version` 没动，
按旧字面属违规）。

操作者 2026-09-09 裁「版本号对齐最新的（即本条），之后统一更新」。
2026-09-10 落地：**改的是 `PROJECT_SPEC.md` §版本号语义 的措辞**，
本条与 `PROSE_PATHS` 一个字节没动。⇒ 两处从此指同一件事，
`PROJECT_SPEC.md` 那一节里留着更正记录，说明原文错在哪。

## D-035 — 归因分层：加第五层「意图层」，并把两处规格自相矛盾各定一边

- 状态：Accepted（**操作者 2026-09-05 授权由执行者裁**。台账 `N-56`，落地于 `EVAL_CASES.md` §5.5）
- 背景：`02-02` 起草时的独立复核摆出**三处规格自己对不上**，每一处都复核过：
  ① §3.2 与 §5.4 对「拒答理由码给错」给出不同的层；
  ② §3.3 的 `J-6` / `J-7` 往「证据层」归因，而 §5 的四层里没有这一层；
  ③ 「意图解析失败」在四层里**没有落点**，而 4 条真实 FAIL 里有 **2 条**是这一类。
- 决策，三条：
  1. **① 归口径层。** `RefusalCode` 是封闭枚举，**它就是对外可观测的判断本身**，
     不是判断的排版。从外部分不开「判错了」与「判对了但贴错标签」；
     后者要成立必须有「内部判断是对的」这个证据，评测手上没有
     ⇒ 归到**要求证据更少**的那一层，是 fail-closed 的方向。
  2. **② 「证据层」是措辞失误，归表达层**，并把表达层的定义拓宽到证据链。
     原定义「算对但返回结构/单位/精度不符合契约」写在 `D-003` 把证据链
     定为第一类产物**之前**，覆盖不到证据链自己的失败。
  3. **③ 新增第五层「意图层」**，边界由机读状态写死：
     `INTENT_INCOMPLETE` → 意图层；`METRIC_NOT_DEFINED` → 口径层
     （别名表认不出这个说法，而**别名表是口径产物**）。
- 🔴 **推翻了一条挡在前面的理由。** `EVAL_CASES` §5.4 与 `02-02-PLAN` 都写着
  「本轮不新增第五层（那会动 `AC-08` 的 100% 归因覆盖率口径，属规格变更）」。
  **这句话的理由不成立** —— `AC-08` 逐字是「每个失败都落到至少一层，不允许"未知"」，
  **它对层数不置一词**。真正会动 `AC-08` 的是**不**加层：
  2 / 4 条真实失败被硬塞进一个不对的层，那才让 100% 变成假的。
- 理由（三条都对着仓里已有的东西核过）：
  1. **③ 是三条里唯一有真实数据的。** `Q-C3-003`（题面无主体）与
     `Q-C3-004`（题面出现两个期间）的 `Answer.gate` 都是 `not_reached` ——
     四层是按闸门**之后**的流水线切的，它们一步都没走到。
  2. **归因覆盖率不变（4/4），真实性变了。** `AC-08` 是保证类标准，
     它要的是「不允许未知」，不是「凑一个 100%」。
  3. **判据全部是机读的**，符合 §5.3 的硬约束：闸门返回值（封闭枚举）+
     `Answer.gate` 三态，**不碰 `detail` 的措辞**。
- ⚠️ **加层的成本如实记**（2026-09-05 实测）：五层里 **1 层结构上不可达**
  —— **表达层**，当前实现里没有任何一条路产得出它（合成夹具没有 PDF、没有页码，
  `J-6` / `J-7` 要到 Phase 3 的真实抽取路径才可能触发）。
  检索层与计算层是**本批零产出但产得出来**，那是数据不是死抽象。
  ⚠️ **这两件事必须分开报**，混成一句「零产出」，读的人会以为再多跑几批就有了。
  报告里由 `reachable_layers()` 从映射表机械算出，不是手写的一句话。
- 反转触发条件：Phase 3 的真实抽取路径接上之后，若**表达层仍然不可达**，
  重新问它该不该在这 —— **不许靠「早晚会用上」留着**。

## D-036 — `D-030` 跌破之后：不下调、不停用 `crosscheck`、**接受这道门红着**

- 状态：Accepted（**操作者 2026-09-05 授权由执行者裁**。台账 `N-44`）
- 事实（`scripts/verify_deps.py` 实跑，不是估计）：
  **2026-09-05 读数 2,974,120，门槛 3,000,000，低 0.86%。**
  ⚠️ 这个数**与 2026-09-04 第一次跑读到的完全相同**。
  ⇒ 🔴 **`N-44` 那张表里的「日均斜率」是过度解读。** 同一个值出现两天，
  说明 pypistats 的「近月下载」不是逐日刷新的滚动窗口，而是**阶梯式**更新的。
  「日均 −78,000 / −50,300」这类算法**从此不再用**，本条一并更正。
  表里的读数保留（它们是真实读数），**斜率与外推日期作废**。
- 决策，三条：
  1. **不下调门槛。** `D-030` 逐字写死：「跌破即红，处置不是再下调一次」。
  2. **不停用 `crosscheck` extra。** 停用会连带改 `SC-5` 的后半句
     （「与 AKShare 交叉校验」），那是 `PROJECT_SPEC.md` 里的**验收标准** ——
     为一个低 0.86% 的**代理指标**去改上位规格，代价与证据不相称。
  3. **接受这道门在 akshare 这一项上红着**，并把它写成一个**有日期、有反转条件**的
     已知状态，而不是一句「以后再说」。
- 🔴 **这是 `D-030` 没有列出的第三个选项，必须说明白。**
  `D-030` 写的两个选项是「接受它红并**停用** crosscheck」或「另找对照源」——
  它默认了「红不是一个可以停留的状态」。本条选的是**红 + 保留 crosscheck**。
  之所以敢这么选，是因为下面这条：
- 理由 —— **门槛问的问题与门槛量的东西不是同一件事**：
  1. 下载量是「这个依赖还活着吗」的**代理**。而直接证据就在同一份门禁输出里：
     **akshare 1.18.94 发布于 2026-08-21**，距今 15 天。
     一个半个月内发过版的包，不因为月下载量比一条人为划的线低了 0.86% 而变得不可依赖。
  2. `crosscheck` 现在的判别力确实弱（茅台 2023：可比 20 / 不可比 4 / **不一致 0**，
     `DISAGREES` 与 `ROW_ABSENT` 在真实数据上仍是零样本）——
     **但那是「还没派上用场」，不是「已证明没用」**，而 `SC-5` 是已接受的验收标准。
  3. **另找对照源**是 Phase 3 规模的工作，为一个 0.86% 的越线去开它，是本末倒置。
- ⚠️ **明确不做的事**：**不把「发版新鲜度」做成让这道门变绿的旁路。**
  它是本条决策的**理由**，不是门禁的一条判据。做成判据就是「再下调一次」换个说法。
  ⇒ **门禁照旧对 akshare 报 `[FAIL]` 与「不通过」，退出码 1。**
- 反转触发条件（任一成立即重新裁，不许拖）：
  1. akshare 近月下载**跌破 2,000,000**（当前门槛的三分之二）——
     那时 0.86% 的「代理指标噪声」这个说法不再站得住；
  2. akshare **连续 6 个月无新版本**（即最近一次发布距今 > 180 天）——
     直接证据没了，代理指标就是唯一的了；
  3. `crosscheck` 在真实数据上**仍然零判别力**（`DISAGREES` / `ROW_ABSENT` 仍是零样本）
     到 Phase 3 结束 —— 那时理由 2 不再成立，按 `D-030` 的选项停用并同步修订 `SC-5`。
- ⚠️ **一条如实记着、本条没有解决的**：这道门在**读不到**下载量时仍然退 0。
  2026-09-05 把末行的判词从「通过」改成了**「未生效」**（`N-44` 的第 4 条），
  并在跳过的是 `D-030` 盯着的那一项时单独打一行红字。
  **但退出码没变** —— 让 pypistats 的一次 429 把门禁变红，会得到一道
  「红的原因不是它要查的那件事」的门，而长期红着的门等于没有门。
  ⇒ **「哑与绿在退出码上不可区分」仍然成立**，改的是判词不是退出码。不许写成已解决。

## D-037 — H2 复核实验在可计分池不足时**按普查执行**，并强制三条标注

- 状态：Accepted（**操作者裁决**，2026-09-05。台账 `N-59`，选项 (c)）
- 背景（实测算出来的，不是估计）：`EVAL_CASES` §4.1 第 1 步逐字写着
  「从已评测问题中**随机抽 15 题**，其中必须包含**至少 5 题模型答错的**」。
  frozen-01 上两条都不成立：

  | | |
  |---|---|
  | 题量 | 20 |
  | `NOT_RUN`（C4 准则检索 / C5 图谱，确实没有路径） | 5 |
  | **可计分池 `N`** | **15** |
  | 其中通过 | 11 |
  | **其中答错 `W`** | **4** |

  ⇒ ①「随机抽 15」抽的是**整个池子**，随机性为零 ——
  而「随机」在这条协议里存在的理由是**防挑样本**，那条防线在 frozen-01 上等于不存在；
  ② 「至少 5 题答错」**差 1 题，不可满足**。

- 决策：**按普查执行（`N` 题全上，一题不挑），并强制三条标注。** 规则写进 §4.1.1：

  | 条件 | 做法 |
  |---|---|
  | `N > 15` | 原协议不动：随机抽 15，含 ≥ 5 题答错 |
  | `N ≤ 15` | 普查 |

  普查时报告**必须**写明：(1) 这是普查不是随机抽样；(2) `≥5 错` 是否满足及 `W` 实际值；
  (3) 🔴 **错答案子集的分辨率下限 `1/W`**。缺一项即视为未按 §4.1 执行。

- 理由（三条）：
  1. **普查比抽样强，不是弱。** 挑样本的风险为零。要改的只是**措辞** ——
     写「普查」不写「随机抽样」。选 (a)「把 ≥5 放宽到 4」才是弱化：
     那是**照着手上的数据改协议**，正是 `D-030` / `N-44` 在别处点名禁止的形状。
  2. **选 (b)「等 `frozen-02`」会让 H2 无限期推迟**，而它是 Phase 3 的前置门
     （`ROADMAP` Phase 3 `Depends on` 逐字写着「Phase 2 的 H2 判定门通过」）。
     `N-46`（何时建 `frozen-02`）本身还没定，用一个未决项去挡另一个更重要的，是把停止条件架空。
  3. **`≥5 错` 那条的目的**是保证复核者会遇到足够多的错答案 ——
     否则一个「一律判对」的复核者也能拿高一致率。`4 / 15 = 26.7%`，
     **目的实质上达到了**；差的是那个具体数字，不是那件事。
- ⚠️ **必须一起记住的代价**：`W = 4` ⇒ 错答案子集上一致率的最小刻度是 **25 个百分点**。
  §4.2 的三档（≥80% / 60–80% / <60%）在这个分辨率下**分不开相邻两档**。
  ⇒ §4.2 加了附注：落在 60%–80% 时**不许直接读成「部分成立」**，
  必须同时给出错答案子集上的原始计数 `x / W`，由操作者判这一档是否可信。
- **本条不放宽任何门槛。** `≥ 80% 通过` / `< 60% 停止` / `无法判断 ≤ 20%` 三条数值一个没动。
  改的只有「样本怎么取」与「报告必须说清什么」。

---

## D-038 — `U-01` 的答案：证据链补三项，且**必须渲染出来**

- 状态：Accepted（**执行者裁定**，2026-09-06。依据是操作者产出的 H2 第一轮数据，
  不是我的判断 —— 三项逐条对应复核者自己写下的理由。台账 `A-14`，`U-01` 就此关闭）
- 背景：`U-01`（证据链字段集）从 2026-08-10 起就写着「**等 Phase 2 复核实验数据**」。
  数据 2026-09-06 到了，而且是一份不及格的数据：一致率 13.3%、无法判断 86.7%、判「对」0 题。
  **13 道「无法判断」的理由全部同形**：页面说了系统做了什么，没说**它拿什么做的**。
- 决策：证据链补下面三项，**且判据是「渲染到人读页面上」，不是「存在于某个结构里」**。

  | | 补什么 | 缺口来源 | 现状 |
  |---|---|---|---|
  | **G1** | **计算输入的实际取值** —— 公式引用的每个字段是多少 | H2-02/07/09/13/14（**全部 5 道计算题**） | 🔴 **数据早就算出来了**：`answer.py` 把 `inputs` 喂进了执行哈希，**从未渲染**。「算进哈希」≠「给人看」 |
  | **G2** | **拒答条件里每个字段的实际取值** —— 是 `null` / `0` / 负数中的哪一个 | H2-06/12 | 只报「触发了」，不报「凭什么触发」 |
  | **G3** | **拒答时的指标清单快照** —— 当时语义层里有哪些指标与别名 | H2-01/04/08/10/11/15（**6 道**） | 只说「没这个指标」，拿不出可核对的清单 |

- 理由：
  1. **`D-003` 说证据链是第一类产物、给不出证据链即失败。** 这一轮第一次实测到
     「给得出证据链，但它不足以复核」—— 那按 `D-003` 的精神同样是失败，
     只是此前没有任何机制能发现它。H2 就是那个机制，它工作了。
  2. **三项都不是新数据**：`inputs` 已在哈希里、条件字段的取值就在 `source.row` 里、
     指标清单就是 `Registry`。**缺的全是「渲染」这一步。**
     ⇒ 这是补，不是重设计；`D-032` 的表达契约不动。
  3. 复核者的原话把判据说死了：**「执行哈希能标识一次执行，不能代替计算输入。」**
- ⚠️ **补完不等于通过。** `AC-06` 在真正重测之前一律记 `UNVERIFIED`。
  **且不得用同一份题包对同一位复核者重测** —— 那测的是记忆。见 `A-14`。
- ⚠️ **不因为要补这三项就放宽 `AC-05`**：齐全率数的仍是原来那套键，
  新增三项是**给人读的**，不是拿来把 `AC-05` 的分母做大。
- 反转触发条件：若 `h2-02` 补完三项后「无法判断」仍 > 20%，
  说明缺口不在字段而在**表达形式** ⇒ 那时才是真的回 ARCHITECT 重设计 `D-032`。

---

## D-039 — 交付形态由「Web 演示」升级为**并入 cninfo 网站的正式产品**

- 状态：Accepted（**操作者决定**，2026-09-06。原话：「不要只是演示站，
  希望可以是正式产品加入原有 cninfo 网站为用户提供可审计的财务分析决策」）
- 决策：`SC-7` 的交付物由「一次问答的静态演示」升级为**面向用户的产品能力**，
  落在既有的 `cninfo-analyzer-web` 上。⇒ `N-60` 那三个选项作废，选的是 (a) 的加强版。
- **它推翻 / 修订了下面这些，逐条列出而不是含糊带过**：

  | 在册的 | 冲突在哪 | 处置 |
  |---|---|---|
  | `D-006`「界面只做到能展示证据链的最小程度，验收标准里不含任何界面美观项」 | 面向真实用户的产品做不到「最小程度」 | **修订**：`D-006` 的「不做主题/插件/快捷键」在产品语境下解除；但「界面不是卖点」这条判断不变 —— 卖点仍是可审计性 |
  | `D-021` 静态托管豁免，边界写死「一旦需要**服务端运行时**…须先修订本条」 | 产品要为任意公司/年份现算，必然要服务端 | **必须先修订 `D-021`**。它的反转触发条件明写要**连带重估 `D-005` 与 `D-014`** —— 那是一次单独的决策，不是本条顺手做掉 |
  | `NFR-02`「不引入 SaaS、数据库服务、部署基础设施、生产沙箱」 | 同上 | 随 `D-021` 一并处理 |
  | `D-005` 主仓私有至 Phase 4 | 产品要么发实现代码，要么托管它 | **托管不发代码则 `D-005` 不受影响**；一旦要开源实现，须单独修订 `D-005` |
  | `D-010` 只用公开数据 | 用户可能上传自己的报表 | 🔴 **不解除。** 产品**不接受用户上传的非公开财务数据**，这条不可协商 |

- 🔴 **执行者的一条不同意见，说完就执行**：
  **H2 刚刚判定「证据链不足以支撑独立复核」（`A-14`）。**
  在这个状态下对外提供「可审计的财务分析决策」，卖点恰好是刚刚被实测否掉的那一条。
  ⇒ **不反对做产品，反对先做产品**。建议顺序：
  **`D-038` 三项补完 → 重测 H2 → 通过后再对外**。
  这不是延期：那三项是产品**本来就必须有的**，顺序换一下而已。
  操作者已知悉本条并要求继续推进 ⇒ 按此顺序执行，产品化与 `D-038` 并行准备。
- 反转触发条件：若 `D-021` 的修订未获通过（即不引入服务端运行时），
  本条自动退回「静态回放」形态，`N-60` 的选项重新生效。

---

## D-040 — 托管形态：**容器 + 缩到零的按请求托管**，不搭现有那台常驻机器

- 状态：Accepted（**操作者裁定**，2026-09-07。`N-63` 三选一里选 (a)）
- 背景：`N-63` 查清了 cninfo 现有的形态是
  「Pages → Pages Function → 具名 Cloudflare Tunnel → **常驻 uvicorn**」。
  `D-021` 豁免三的判据①是「有没有进程在没人请求时运行」——
  Tunnel 后面那台 uvicorn 为「是」⇒ **搭上去要第四次修订 `D-021`**。
- 决策：**不修订 `D-021`**，改为找一个按请求触发、能缩到零的地方跑这个服务。
  ⇒ 产出一份容器定义（`Dockerfile` + `.dockerignore`），任何一家
  Cloud Run / Fly Machines / Render 之流都能跑；平台注入 `$PORT`，服务绑 `0.0.0.0`。
- **两句判据逐条对**（`D-021` 豁免三）：
  ① 没人请求时有没有进程在跑？—— 平台缩到零时容器不存在。
  ② 把存储全部清空会不会永久丢东西？—— 镜像里全是只读的公开数据；
     容器不写盘、**不打访问日志**（`log_message` 是空的，有回归守着）。
     删掉重建得到同一份。
- **为什么这不是「引入部署基础设施」**（`NFR-02`）：没有要打补丁的机器、
  没有计划任务、没有作为记录系统的数据库、没有账号体系。
  一个能缩到零的容器**在没人用的时候什么都不是**。
- 🔴 **连带定死的一条：服务端不引入 Web 框架。**
  实测服务端运行时第三方闭包**只有 PyYAML 一个**
  （`pdfplumber` 那句 import 在函数体内，服务这条路不解析 PDF）。
  ⇒ AGPL 零命中不是靠许可证扫描碰巧没命中，是靠**闭包本来就只有一个包**（`D-014`）。
  往里加框架就是把这条保证换成一次扫描结果。
- 🔴 **鉴权：外面够得着就必须有令牌，否则拒绝启动。**
  判据是「绑的是不是回环地址」，**不是「有没有设令牌」**——
  「设了才检查」是个静默失败的设计：部署时忘了配环境变量，服务照常起来且完全敞开。
  按监听地址判则相反：忘了配，服务起不来并说明原因。**失败方向要选吵的那个。**
- 仍未定（属 `02-04` T4，操作者的一次选型）：**具体哪家、哪个域名、谁付钱**。
  本条只定形态，不定供应商。
- 反转触发条件：若将来需要「跨请求共享状态」（会话、配额、结果缓存作为记录系统），
  本条与 `D-021` 豁免三**同时**失效，必须一起重估，不许只改这一条。

### 修订一（2026-09-07）：**平台定为 Hugging Face Spaces，且本条原来的前提是偏的**

- 状态：Accepted（操作者裁定，2026-09-07：「A，HF Spaces 就行」）
- 🔴 **先更正本条原文的一处偏差。** 上面写「不搭现有那台常驻机器」，
  那句话建立在「cninfo 后端跑在具名 Tunnel 后的常驻 uvicorn 上」这个前提上，
  而这个前提**我只查了一半**：它来自 `references/cninfo.md` §5 与前端仓库的
  `docs/frontend-handoff/`，**我没有翻后端仓库的 `Dockerfile`**。翻开之后：

  ```
  # Default usage (HF Spaces / docker run):
  # Writable runtime dirs (ephemeral on HF Spaces — restart wipes them, by design)
  # HF Spaces / Docker runs as uid 1000 by default
  ENV PORT=7860
  ```

  ⇒ **两个仓库各记了一种形态，且哪一个是活的只写在 Pages secret 里。**
  我把其中一种当成了唯一现实，于是把「(a) 另找一个能缩到零的地方」讲成了
  「要换供应商」——**而如果 cninfo 本来就在 HF Spaces 上，(a) 就是「再开一个 Space」**，
  零新账号、零新供应商、零新付费。

- ~~**落定**：平台 = **Hugging Face Spaces**~~ —— **2026-09-07 同日失效**。
  实测：本账号新建 **docker** Space 一律 402（public / private / 组织下都试过），
  只有 static 免费。`RGT07/cninfo-financial-analyzer` 能跑，证明的是
  「曾经可以这样建」，不是「现在可以这样建」（`F-9`）。
  ⇒ 平台问题重新打开，见 `N-66`。**判据本身不变**：要的仍是「按请求触发、能缩到零」。
  Dockerfile / `.dockerignore` / 那 770 KB 的载荷**全部照用**，换的只是落脚点。

### 修订二（2026-09-08）：**不另起落脚点，挂进 cninfo 那个既有 Space**

- 状态：Accepted（操作者提议「不行就共用一个 space」，实测可行后裁定「推吧」）

**落定**：finaudit 作为 `/audit/*` 挂进 `RGT07/cninfo-financial-analyzer`
（public，docker，2026-05-03 建，早于 HF 收紧政策）。

**共用的是容器，不是代码库**：cninfo 仓库里只有挂载代码与一个 `finaudit/`
挂载点（内含一份 `.gitignore`，把载荷永久挡在那个仓库之外）；
载荷是部署期内容，权威副本仍在本仓。

**我原来反对共用的三条，查完之后只剩半条**：

| 原反对 | 核完 |
|---|---|
| 公开等于公开源码 | 不成立 —— `D-005` 修订一已授权 |
| 依赖闭包会作废 | 半条。真实说法一直是「finaudit 的 **import** 闭包只有 PyYAML」（AST 门禁锁着，仍真），不是「镜像的闭包」。且 cninfo 已 `import yaml` ⇒ **零新增依赖**，对该镜像的 AGPL 面积贡献为零 |
| 有状态会拖累 | 基本不成立。`api/runner.py:344-354` 用 `multiprocessing spawn` 把重活隔到子进程；`:72` 自陈「Process-local; restart loses state」⇒ 判据②对合体仍是「否」 |

**反过来支持共用的一条**：共用意味着本项目**不新增任何部署设施** ——
这比另起一个落脚点更贴 `D-021` 豁免三。

**判据①的老实话**：`sleep_time = 172800`（48 小时空闲才休眠）。
「没人请求时没有进程在跑」在 48 小时尺度上成立，在分钟尺度上不成立。
这一条在 cninfo 上**本来就是这样**，合体没有让它更差，但当初 `D-040` 修订一
说「闲置休眠、请求唤醒 ⇒ 判据①为否」时略过了时长，这里补上。

**线上实测**（2026-09-08，`https://rgt07-cninfo-financial-analyzer.hf.space`）：
鉴权 401/401/200；覆盖面真实 2 合成 6；茅台 2023 资产负债率
`0.179843241391791471179711…`、万华 2019 `0.546470731037916692809461…`；
「毛利水平」经模型归一 `0.9196…`（5.8s，含一次 DeepSeek 调用）；
近似命中仍拒答；cninfo 自己的 `/healthz` 仍 200。
**回退点** `b6ff5428c323b63066bf4b845898bd4be90ba38e`。

- ~~🔴 **Space 必须建成 private。**~~ **本项已被 `D-005` 修订一（2026-09-07）推翻**，
  连同它的论据。原文说「建成 public 等于把主仓提前公开」，**这句话是错的**：
  Space 仓库的内容等于 `Dockerfile` 的 `COPY` 清单（14483 行实现），
  不等于主仓（另有 71887 行的过程与结论不在其中）。
  而 private + docker 的 Space **要 PRO 订阅**（2026-09-07 实测 402），
  ⇒ 「有链接 / 不花钱 / Space 私有」三者不能同时成立。操作者裁定：放弃第三项。
  **改判的是 Space 的可见性，不是主仓的** —— GitHub 主仓仍 private 至 Phase 4。

- **连带的一处设计变更**：private Space 的平台门禁自己吃 `Authorization: Bearer <hf_token>`，
  两个令牌塞不进一个头。⇒ 服务自己的令牌改走 `X-Finaudit-Token`，`Authorization` 留给平台；
  仍接受 `Authorization: Bearer` 作为 public Space 的兼容路径。
  **硬塞一个头的后果**是服务拿平台的令牌跟自己的比、给出一个「令牌不对」的 401，
  而两个令牌其实都是对的 —— 部署那天没人查得出这是怎么回事。

- **仍未定**：Space 的归属与名字（`AUDIT_API_BASE` 要填的那个 URL）。


## D-041 — 产品形态定为「结论核查入口」：一个输入框，系统自己判定是提问还是待核声明

- 状态：Accepted（2026-09-11，操作者裁决；提案见 `.planning/REPLAN-20260911.md` §2）
- 背景：原形态是「你问它答」。它有两个治不好的毛病 ——
  ① 覆盖面只有 2 家真实公司，在问答形态里这是**缺陷**；
  ② `A4`（垂直口径优势通用 Agent 覆盖不了）唯一那条硬证据
  （对照臂两个模型把净利率逐字答成 `0.124`，正解归母 `0.112`，且**都不拒答**）
  只躺在评测报告里，没有任何产品形态在消费它。
- 决策：**产出物由「一个数」改为「一份逐条判定的核查报告」。**

### 1. 一个输入框，不是两个视图

🔴 **核查是问答的超集。** 要核「净利率 50.6%」，系统必须先把净利率算出来
⇒ **问答能力不会因为改形态而消失，它就在核查里面**，区别只在用户带没带那个数。

| 输入 | 读成 | 输出 |
|---|---|---|
| 「…净利率是多少」 | 提问 | 答案 + 证据链（今天这一套，不动） |
| 「…净利率 50.6%」 | 一条待核声明 | 核查判定 |
| 一整段结论 | 多条声明 | 逐条核查 |

**判据是现成的**：题面里有没有一个**没被 `_PERIOD` 或 `_ENTITY` 消耗掉的数值**。
不新增模式开关、不新增第三个视图 —— 「第三个视图」正是 `REPLAN` §4.3
自己反对的那种摊薄。

### 2. 判错了不藏

核查报告**第一行**写「我把这句话读成：一条待核声明 / 一个提问」。
与证据页现在印 `metric_resolved_by` / `matched_alias` 是同一个做法（`D-032`）：
**读者要能一眼看出它理解错了**，而不是拿到一个莫名其妙的结果。

### 3. 五态是封闭集

每条声明的判定只有五种，**落地即写成枚举，不许出现第六种**：

| 判定 | 含义 |
|---|---|
| `CONSISTENT` | 可核，且与本系统算出的数一致 |
| `INCONSISTENT` | 可核，但不一致 |
| `AMBIGUOUS_BASIS` | 口径未声明 —— 本系统按某一口径解释，换一个口径是另一个数 |
| `NOT_COVERED` | 数据不覆盖（没有这家 / 没有这一年 / 没有这个指标） |
| `NOT_CHECKABLE` | 不是可核声明（「盈利能力持续增强」这类） |

🔴 **「核不了」是正当输出，不是失败。** `NOT_COVERED` 与 `NOT_CHECKABLE`
都是**成功的判定**。这正是深度换广度在新形态下反过来成为优势的地方：
每个能核的数都有人签过字（`VERIFICATION.md` §D.16/§D.17，29/29 字段人工核对）。

### 4. 不新写的（每条附理由，与 `REPLAN` §4.3 逐条一致）

ReAct / 多轮循环、多 Agent 协同、RAG / 知识图谱、覆盖全 A 股、桌面端 —— **都不做**。
单轮无状态是「同一问题两次同一执行哈希」成立的原因；加 ReAct 等于用卖点换通用能力。

### 5. 🔴 新层必须同时进冻结题集，否则不做

本仓已为同一个形状栽过两次：`N-64`（生产接了模型、评测没接）、
`N-78`（评测臂与线上不是同一套系统）。
**在 `answer_question` 上面加一层而不给它评测，就是第三次。**

- `frozen-01` / `frozen-02` 覆盖**下面那半**（不改、也不许改，`D-012`）；
- 上面这半零覆盖 ⇒ 须建 **`frozen-03`**：题面是「一段结论」，期望是「五态判定的那张表」；
- **`R-1` 与 `frozen-03` 是同一件事的两半，不许拆开先做一半。**

- 主要风险：抽取器把一段中文切错声明，产生一个看起来正常的错判定。
- 缓解：切分结果**印在报告第一行**（见 §2）；且 `frozen-03` 的期望表里
  **切分本身就是被判分的一部分**，不是只判最终五态。
- 反转触发条件：`R-3`（A2 实测，有审计背景的人读核查报告）判定
  「读者说不出这份报告核得对不对」⇒ 形态没解决 A2，回到 `REPLAN` §2 重想。

- 连带修订：`D-006`（见其 2026-09-11 修订二）、`D-010`（见其边界补充）、
  `D-009`（见 `D-042`）、`docs/agent/IDEA.md` 的 `A5`。

---

## D-042 — 执行 `D-009` 自己的减法：删掉 OpenSpec

- 状态：Accepted（2026-09-11）
- 背景：`D-009` 写着反转触发条件 ——
  「**任一阶段的流程开销超过实际产出时间的 30%，删掉贡献最小的那一套。Harness 要定期做减法。**」
  **触发条件早已满足，而没有人执行。**

| 方法论 | 产物 | 实况（2026-09-11 实测） |
|---|---|---|
| GSD `.planning/` | 35 文件 / 9,460 行 | 在用 |
| **OpenSpec** `openspec/` | 17 文件 / 1,411 行 | 🔴 `changes/` 为空；三个归档**全是 2026-08-15**；此后 Phase 1.5 / 2 / 4 全部发生，**零提案**，27 天零使用 |
| Superpowers | 无自有产物 | 在用（TDD / 完成前验证 / 独立复核） |

⚠️ **判据缺陷如实记下**：`D-009` 的反转判据是**时间**，而 `D-008`
取消时间预算之后**时间没有记录** ⇒ 那条触发条件**在字面上不可判定**。
这里用的是**行数比**（过程 63,076 行 vs 生产 20,017 行 = 3.2 倍）加**使用频次**（27 天 0 次）。
**这是替代判据，不是原判据**，不粉饰。

- 决策：**删掉 OpenSpec**。`openspec/` 目录整体移除；`AGENTS.md` / `CLAUDE.md` /
  `rules/` 里的 OpenSpec 入口一并删除。GSD 保留（拥有阶段状态），
  Superpowers 保留（提供行为门禁）。

### 放弃了什么能力（`D-009` 判据补充要求的必填节之一）

**放弃的是「单个变更的规格契约」这一层** —— 具体地：
`proposal → tasks → apply → archive` 这条**把一次变更的动机、任务清单、
以及「哪条 Requirement 因此改了」绑在一起**的轨迹。
今后一次变更的动机只落在 `DECISIONS.md`（决策粒度）与提交信息（代码粒度），
**中间那一层没有了**：谁想知道「`D-035` 归因改五层时具体改了哪几条规格条文」，
只能读 diff，读不到一份当时写下的意图。

**不是**「简化了流程」这种概括 —— 上面那句话说的是一个具体的、今后确实缺失的东西。

### 重新引入条件（必填节之二）

要把它拿回来，**必须先出现一个具名消费者**，并自带契约：

1. **具名消费者**：不是「以后变更多了」，而是**一个真实的人或流程**要读这些提案 ——
   例如出现第二个执行者、或有外部复核者要求按变更粒度追溯。
2. 它必须自带：这些提案**归谁写、什么时候算过期、跟 `DECISIONS.md` 冲突时谁上位**。
   —— `D-009` 的原约束「三者不得为同一件事创建重复的权威文档」是它当初能成立的前提，
   重新引入时这条要重新论证，不能默认还成立。

**两节都不写「暂无」** —— `D-009` 判据补充明令。

### 保留的：`openspec/specs/` 那 9 条 Requirement

🔴 **`openspec/specs/semantic-layer/metric-definition/spec.md` 不删，移到 `docs/spec/`。**
它是**已经生效的规格**，不是流程产物 —— `metrics/` 20 份定义现在就受它约束。
删掉工具链不等于删掉工具链写下的事实。归档的三个变更 (`changes/archive/`) **同样保留**，
理由见 `D-009` 判据补充里刻意不借 harness 那半的那段：
**「为什么当初没这么做」不能变成不可复核的。**

- 主要风险：删完之后又出现需要按变更粒度追溯的场合。
- 缓解：见「重新引入条件」；且归档与生效规格都保留，能拿回来的成本是重装工具链，不是重写内容。
- 反转触发条件：上面「重新引入条件」的两条同时满足。

---

## D-043 — `references/` 不进公开仓，连同它那道门禁一起移出

- 状态：Accepted（2026-09-11，操作者：「references 可以不上传，没必要，但是 README 可以写上有借鉴」）
- 背景：转 PUBLIC 之后，`references/`（27 份外部项目阅读笔记，**26,840 行，
  占全仓 markdown 四成**）会一并公开。它是**作者自己的学习记录**，不是这个项目的产物；
  一个来看这个项目的人不需要读它们，而它们把仓库的信噪比压得很低。
- 决策：`references/` 移出版本控制（移到仓外 `../finaudit-workshop/`，本地保留继续用），
  并进 `.gitignore`。**连同它的门禁一起移** —— 见下。
- **README 里保留「借鉴」一节**：列出读过哪几个外部项目、各自借到了什么。
  **致谢在，原文不在。**

### 连带：第五道门（阅读台账）退出提交链，六道门 → 五道门

`scripts/check_reading_ledger.py` 的**全部检查对象就是 `references/`**，
`check_gates.py` 的 **R5**（已落地的 `L-nn` 必须在 `references/` 里有回标）同理。
目标目录不在仓里了，这两样**不能留在链上** ——
一道「检查对象不存在时照样绿」的门禁，比没有门禁更糟（它会让人以为查过了）。

⇒ 一并移出：`check_reading_ledger.py`、`test_reading_ledger.py`、`check_gates` 的 R5
与它的 7 条测试、CI 链里的那一条、`GATES` 注册表里的那一项。
步骤名 / `echo` / 实际命令**三处同时改**（2026-08-24 栽过一次三者不一致）。

### 放弃了什么能力（`D-009` 判据补充要求的必填节之一）

**放弃的是「『我读过』这句话的机械检查」** —— 具体地，这两件事从此**没有任何自动
检查会发现**，只能靠人：

1. **台账标了「全文」，而正文零对应内容。** 这是实际发生过四次的失效模式，
   第五道门首跑就抓到一条（`tests/test_trace_integration.py`，当时如实降级为 `UNVERIFIED`）。
2. **登记册里标了「已落地」，而 `references/` 里没有回标。** 规则写下的**当天**
   就失败过：T-2 / T-3 落地的五条，一条都没回标。**靠人记得，当天就不成立。**

**不是**「简化了流程」这种概括 —— 上面两条说的是具体的、今后确实缺失的东西。
⚠️ 而且要注意：这两条都属于「**因为真的栽过才立的**」那一类规矩。
本条**不是**因为它费事才移，是因为**它的检查对象已经不在这个仓库里了**；
规则本身（`references/README.md` 的三条硬规则）在仓外继续有效，只是不再有机器守着。

### 重新引入条件（必填节之二）

要把它拿回来，**必须先出现一个具名消费者**：

1. `references/` 重新进入某个**有 CI 的仓库**（公开仓或将来的 workshop 仓）；
2. 并且**那个仓的提交链真的会跑这道门** —— 只是把文件放回去不算，
   2026-08-23 那次「第五道门漏了整整一天没进 CI」正是这种半吊子状态。

**两节都不许写「暂无」**（`D-009` 判据补充明令）。

- 主要风险：笔记在仓外无人守着，日渐与项目脱节；或者「读过」的标注重新开始注水。
- 缓解：`../finaudit-workshop/README.md` 写清了怎么手动跑那道门，并明写
  「它已经不在提交链上了」。**不假装它还在守着。**
- 反转触发条件：上面「重新引入条件」的两条同时满足。

---

## D-044 — 工作记录不进公开仓

- 状态：Accepted（2026-09-11，操作者：「工作记录就不需要了，浪费」）
- 背景：`D-043` 移走 `references/` 之后，仓里还剩四类**纯工作记录**：
  会话日志 `PROGRESS.md`（5,955 行）、未决事项台账 `OPEN-ITEMS.md`（3,578 行）、
  落地登记册、会话交接 `claudedocs/`（1,444 行），外加失败模式清单
  `rules/failure-modes.md` 与 `rules/repo-context.md`。
  它们记的是**「活是怎么干的」**，不是**「这个项目是什么」**。
- 决策：全部移出版本控制（仓外 `../finaudit-workshop/`，本地保留继续用），进 `.gitignore`。

### 判据：产物 vs 过程

留下来的三类是**一个陌生人要核这个项目的主张时用得上的东西**：

| 留 | 为什么 |
|---|---|
| `src/` `tests/` `metrics/` `eval/` `data/extracted/` | 产物本身，以及能复算它的东西 |
| `PROJECT_SPEC` / `DECISIONS` / `EVAL_CASES` / `docs/spec/` | 主张是什么、为什么这么定、怎么判它对不对 |
| `VERIFICATION.md` / `EVAL-REPORT-01.md` / `poc-01/` / `h2-*/` | **证据**：验收矩阵、评测读数、冻结实验原件 |

移走的那几类，**没有一类是上面任何一句主张的证据** —— 这一点要紧：
`D-005` 修订二当初判断「把 `UNVERIFIED` 与自我批评一并公开是取舍不是风险」，
理由是「本项目的主张就是结论可被独立复核，而这些记录正是那句主张的证据」。
**那句话对证据成立，对工作日志不成立。** 复核一个数靠的是证据链、冻结题集与
验收矩阵，不是靠读我哪天栽在哪。⇒ 本条与 `D-005` 不冲突，它把那句话的范围说准了。

### 连带

- `check_xrefs` 的 `ledger`（`N-`）与 `F` 两个命名空间**删掉** —— 定义文件不在仓里了。
  碰撞检查保留并改为「台账不在时按空集处理」，**不是跳过整条**：
  它防的是「把 `D-` 前缀重新引入某个台账区」这个未来的回退。
- `DECISIONS.md` 里 48 处、`EVAL_CASES.md` 13 处、`PROJECT_SPEC.md` 6 处
  `N-nn` / `F-n` 引用**保留不删**，文件抬头加一句说明它们指向仓外台账。
  🔴 **刻意不改写这 67 处** —— 它们是那条决策当时的依据；
  为了消掉悬空引用去动决策记录的正文，是**用整洁换真实**。

### 放弃了什么能力

跨会话的连续性证据没有了：谁想验证「这个项目是不是一步一个脚印做出来的」，
在公开仓里**找不到过程**，只能看产物与证据。这是真的损失，不粉饰。
⚠️ 并且：**历史里仍然有**。本次只从 HEAD 移除，不重写历史
（`AGENTS.md`：本仓满地是提交哈希引用，而冻结记录的哈希是 `D-012` 的物理保障）。
⇒ 挡得住浏览，挡不住 `git log -p`。**别把它说成「已经清干净了」。**

### 重新引入条件

出现一个**具名消费者**要读过程 —— 例如第二个执行者加入、或外部复核者
要求按会话粒度追溯。届时连同它自己的门禁一起回来（同 `D-043` 的条件二：
放回文件不算，得真的进提交链）。

- 反转触发条件：同上。

---

## 3. 未解决决策

- **U-01**：证据链的字段集尚未确定。等待 Phase 2 的复核实验数据。不得在 Phase 1 提前拍板。
- **U-02**：准则语料的获取方式（人工整理 vs 公开数据集）尚未确定。Phase 3 前需专项调研。
- **U-03**（2026-08-15 **重新表述**，原问题前提已随 D-006 修订消失）：
  原问题是「CLI 与 TUI 两套入口，还是 TUI 单入口」。TUI 已不是交付形态，该问法作废。
  **新问题**：内核与壳的边界画在哪？Web 与本地 GUI 各自需要什么，二者能共用多少？
  判据：`ARCHITECTURE.md` 里有一张内核/壳边界图，且第二个壳的增量成本可估。
  等 Phase 2 内核成形后再定，**不得在 Phase 1.5 提前拍板**。
- ~~**U-04**：`data secret` 与本项目并行推进时，可支配小时数如何分配？~~
  **已关闭（2026-08-10，当日新增当日关闭）。** 关闭原因不是"想清楚了"，而是**前提消失**：
  同日 D-008 修订取消了一切时间预算上限，"如何切分预算"这个问题不再存在。
  留档而不删除，是因为「问题因前提消失而消解」和「问题被回答」是两回事，混同会让决策记录失真。
