---
phase: 00-cninfo-empirical
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ../cninfo-financial-analyzer/scripts/readability_vs_performance.py
  - ../cninfo-financial-analyzer/docs/FINDING-01.md
  - ../cninfo-financial-analyzer/README.md
autonomous: false
requirements: [AC-03, AC-10]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 4
  confidence: medium

must_haves:
  truths:
    - 结论里每个数字都能用仓库里的一条命令重跑出来（Phase 0 SC-2）
    - 样本范围、方法、显著性、局限四样齐全（AC-03）
    - 全程只用公开数据（D-010）；PDF 不进版本控制
    - Fog 与 ROA 两侧都各有至少 3 个样本被人工核对过取值正确
    - 未绕过验证码；触发即停并交回操作者
  artifacts:
    - docs/FINDING-01.md（结论 + 复现命令）
    - scripts/readability_vs_performance.py
    - README.md 首屏重排
---

<objective>
给 `cninfo-financial-analyzer` 补一条实证结论：**年报可读性与当期经营业绩的关系**。

**方向由操作者 2026-09-02 裁定改过**（原方向「Fog vs 非标审计意见」见 `A-11`）：
非标意见约占 1%，随机抽样要几千个公司年、7.4–21 GB，且要先写审计意见抽取器。
**换成业绩做因变量：亏损公司每年 10%+，200 家就够，而 ROA / OCF 的取数通路已经存在**
（`src/financial_data_sources.py:16` 的 `METRIC_COLUMNS`，走 AKShare + Supabase 缓存）。

⇒ **新方向要写的新数据能力是零。** Fog 有（`text_analyzer.py:265`），ROA 有，
缺的只是把两侧拼起来跑一遍的分析脚本。

Output: `docs/FINDING-01.md` + 一个可重跑的脚本 + README 首屏重排。
</objective>

<context>
@.planning/phases/00-cninfo-empirical/CONTEXT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
**T1 —— 建样本：~200 个公司年的 Fog 指数 + ROA。**

沪深主板随机抽 200 家（抽样种子写死在脚本里，**可重跑**），取同一会计年度的年报。

- Fog：走既有 pipeline（`extract_mda_section` → `calculate_fog_index`）
- ROA / OCF：走既有 `financial_data_sources`

🔴 **撞到验证码就停**，不试第二种绕法，交回操作者决定是否本人登录导出 cookies。
⚠️ PDF 落 gitignore 覆盖的目录，文档里只记 SHA-256。
⚠️ 抽不满 200 是可以接受的结果 —— **如实记实际 n 与失败原因分布**，不要偷偷补抽到凑数。
</task>

<task type="auto">
**T2 —— 跑关系，并把「没有关系」也当成一个正当结论。**

主检验：`Fog ~ ROA`（或分组比较：亏损组 vs 盈利组的 Fog 均值）。
报告：样本量、系数/组间差、p 值、效应量。

⚠️ **不做的**：不换模型直到 p 变小、不删离群点直到显著、不事后挑年份。
**「在本样本上没有观察到显著关系」是一条合格的实证结论**，写清楚即可。
</task>

<task type="auto">
**T3 —— 人工核对：Fog 与 ROA 各抽 3 个样本核到底。**

Fog 侧：翻到那份年报的 MD&A，确认抽出的文本确实是 MD&A（不是目录，README 记着这是已知故障模式）。
ROA 侧：与年报上的净利润 / 总资产对一下量级。

⚠️ **`N-48` 的形态在这里是**：ROA 与年报数值「差不多」不等于对 ——
两边口径可能不同（AKShare 的 ROA 分母是期末还是平均，未核）。**核不出就写核不出。**
</task>

<task type="checkpoint">
**T4 —— 写 `docs/FINDING-01.md`，重排 README 首屏，交操作者过目。**

`FINDING-01.md` 四样齐全（`AC-03`）：数据范围 / 方法 / 显著性 / **局限**。
局限至少要写：单年度、样本 n、Fog 指数是中文适配版不是原版 Gunning-Fog、MD&A 抽取有失败率。

README 按「问题 → 方法一张图 → 结论 → 怎么跑」重排，结论在首屏。

🔴 **前置：`A-13` 必须先有处置**（仓库 PUBLIC + MIT 声明 + 无条件 import AGPL 的 PyMuPDF）。
本任务就是把那个仓库变成对外的脸，**不解决 `A-13` 不做这一步**。
</task>

</tasks>

<verification>
- `FINDING-01.md` 里每个数字都有一条能原样重跑的命令
- 抽样种子写死，重跑得到同一批公司
- 本仓七道门链照跑（改的是 markdown）
- ⚠️ 结论若不显著，README 照写不显著 —— **不换方向重跑到显著为止**
</verification>
