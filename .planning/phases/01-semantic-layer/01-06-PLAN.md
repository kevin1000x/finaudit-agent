---
phase: 01-semantic-layer
plan: 06
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - metrics/operating_cash_flow_ratio.yaml
  - metrics/ocf_to_net_profit_attributable.yaml
  - metrics/free_cash_flow.yaml
  - metrics/inventory_turnover_days.yaml
  - metrics/accounts_receivable_turnover_days.yaml
  - metrics/accounts_payable_turnover_days.yaml
  - metrics/total_asset_turnover.yaml
  - tests/test_metrics_cashflow_turnover.py
  - docs/agent/phase-01/open-questions.md
autonomous: true
requirements: [SC-1, AC-01]
user_setup: []

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "7 个现金流与周转指标各有一份 version: 2 定义，且全部被校验器判定合规"
    - "周转类指标整体补位——POC-01 C9 指出的缺位被填上"
    - "天数基数（360 还是 365）这类会静默改变结果的约定被显式声明"
    - "自由现金流这种无准则定义的指标，其自定义性质被显式承认而不是伪装成有权威依据"
    - "现金循环周期因需要「指标引用指标」的能力而被显式记录为 Open Question，不被静默丢弃"
  artifacts:
    - metrics/operating_cash_flow_ratio.yaml
    - metrics/ocf_to_net_profit_attributable.yaml
    - metrics/free_cash_flow.yaml
    - metrics/inventory_turnover_days.yaml
    - metrics/accounts_receivable_turnover_days.yaml
    - metrics/accounts_payable_turnover_days.yaml
    - metrics/total_asset_turnover.yaml
    - tests/test_metrics_cashflow_turnover.py
    - docs/agent/phase-01/open-questions.md
  key_links:
    - "docs/agent/phase-01/open-questions.md → 01-08 的收尾汇总（现金循环周期同时是 C2 类拒答题的靶子）"
    - "7 份定义的 flags[].name → metrics/_flags.yaml（只引用，不新增）"
---

<objective>
产出 20 个指标中的最后 7 个：**现金流与周转**批次。

POC-01 的 C9 是双方收敛缺陷之一，原话是「周转类指标整体缺位，且字段粒度不统一」。
本批次直接回应它：4 个周转指标一次补齐，并把「期初期末平均 vs 期末」「天数基数 360 vs 365」
这两个会静默改变结果的约定写成显式声明。

同时本批次要处理一件**不做**的事：现金循环周期（存货周转天数 + 应收周转天数 − 应付周转天数）
需要「指标引用指标」的复合能力，而现行 schema 只支持字段引用。
按 `design.md` §Risks 的规定——写不出来的记为 Open Question，**不许就地扩语法**——
它被显式记录并排除，不是悄悄漏掉。它随后成为 01-07 的 C2 类拒答题靶子：
一个真实存在于财务实务、但语义层确实没有定义的指标，比编造的靶子更有说服力。

Purpose: 覆盖 AC-01 的最后 7/20，填上 POC-01 C9 的缺位。
Output: 7 份 `version: 2` 定义 + 批次测试文件 + Open Questions 记录。

**模式：IMPLEMENT。** 只写定义、测试与 Open Questions 记录。
不改 `src/`、不改 `metrics/_flags.yaml`、不改权威文档、**不扩 DSL 语法**。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@PROJECT_SPEC.md
@openspec/specs/semantic-layer/metric-definition/spec.md
@docs/agent/poc-01/field_inventory.md
@docs/agent/poc-01/COMPARISON.md
@openspec/changes/archive/2026-08-15-extend-metric-definition-schema/design.md
@metrics/_flags.yaml
@metrics/net_profit_attributable_excl_nonrecurring.yaml
@.planning/phases/01-semantic-layer/01-02-SUMMARY.md
</context>

<batch_rules>

以下规则对本批次全部 7 份定义生效。

**模板**：`metrics/net_profit_attributable_excl_nonrecurring.yaml`（01-01 产物）是唯一模板，
字段顺序保持一致。

**并行隔离（重要）**：本计划与 01-04、01-05 在同一个 wave 并行运行，三者同时往 `metrics/` 写文件。
本计划的一切校验命令**必须限定在本批次自己的 7 个文件上**：`validate` 用显式路径列表；
不跑 `tests/test_conformance.py`（它 glob 整个目录）；不跑全仓 `scan` 与全目录 `stats`。
全目录级校验由 01-08 统一执行。批次内合规性由 `tests/test_metrics_cashflow_turnover.py` 逐份断言。

**词表只读**：`metrics/_flags.yaml` 不得修改。需要词表里没有的 flag 时按 CLAUDE.md 停止协议处理，
把需求写进 `docs/agent/phase-01/flag-requests-cashflow-turnover.md`
（EXPECTED / FOUND / IMPACT / OPTIONS），并在 SUMMARY 里点名。

**语法只读**：受限 DSL 的能力边界是字段引用、比较运算、and/or/not、`is_missing()`。
本批次有多个指标会诱惑作者去扩语法（平均数需要算术、天数基数需要常数运算、
现金循环周期需要引用其他指标）。**一律不许扩。** 遇到写不出来的约束，按
`design.md` §Risks 的规定记进 `docs/agent/phase-01/open-questions.md`，
本批次的定义改用能表达的形式或明确排除该指标。

**准则条号不许编**。核实不到具体条号就退回章名或 `全文`。
使用了退化定位的条目在 SUMMARY 中列出并说明原因。

**冻结目录只读**：`docs/agent/poc-01/` 下任何文件一个字节都不许改。
`operating_cash_flow_ratio` 在 POC-01 有一份 `version: 1` 同名定义——那份留在原地不动。

**每份定义的硬性下限**：`grain` 三键齐全；参与加减的每个 `source_fields` 有 `sign_convention`
与 `missing_representation`；`derivation` 显式二选一且 `note` 非空；`standard_basis` 每条四键齐全；
`flags` 引用词表且触发条件可解析；`undefined_conditions` ≥ 1 条且可解析；
`common_pitfalls` ≥ 3 条且每条恰有 `enforced_by` 或 `advisory_only` 之一。

**时点口径的统一立场**：与 01-05 保持一致，全部采用**期末时点**。
周转类指标的教科书写法用期初期末平均，但 `poc-01/field_inventory.md` 只为流动负债提供双值，
存货与应收账款均只有单值。因此本批次一律取期末时点，并在每份定义的 `derivation.note`
与陷阱中写明这一立场以及与平均口径不可直接比较。**不得**为了「更标准」而擅自引入不存在的期初字段。
这一立场的代价（周转天数会与常见数据库口径有系统性差异）在 SUMMARY 中如实记录，不粉饰。

**`advisory_only` 的使用纪律**：能写成规则的不许标 `advisory_only`。本批次目标是占比明显低于 50%。
</batch_rules>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 经营现金流量比率 与 净现比</name>
  <files>
    metrics/operating_cash_flow_ratio.yaml,
    metrics/ocf_to_net_profit_attributable.yaml,
    tests/test_metrics_cashflow_turnover.py
  </files>
  <read_first>
    - metrics/net_profit_attributable_excl_nonrecurring.yaml（模板）
    - metrics/_flags.yaml
    - docs/agent/poc-01/definitions/operating_cash_flow_ratio.yaml（**反面样本**：同一指标的 v1 定义）
    - docs/agent/poc-01/COMPARISON.md 的 Q2 段与 §5.2 的 S2 / S3 / S4 三行（别名过宽、分子为零或缺失未规定、半年/季度累计是否年化未规定）
    - docs/agent/poc-01/field_inventory.md 的 `cfs.*` 与 `bs.*` 段
    - openspec/specs/semantic-layer/metric-definition/spec.md Requirement 1、2、7
  </read_first>
  <behavior>
    - Test: 两份定义的 `validate_definition` 均返回空 Finding 列表
    - Test: `operating_cash_flow_ratio` 的 `period_semantics` 为 `混合`（分子时段、分母期末时点）
    - Test: `Registry.resolve("净现比")` 命中 `ocf_to_net_profit_attributable`
    - Test: 别名「现金流量比率」**不**被 `operating_cash_flow_ratio` 收录（POC-01 S2：该别名过宽，会把总负债口径的提问错误路由过来）
    - Test: 给一行流动负债期末余额为 0 的数据，`operating_cash_flow_ratio` 返回 `UNDEFINED_CONDITION_HIT`
    - Test: 给一行归母净利润为 0 的数据，`ocf_to_net_profit_attributable` 返回 `UNDEFINED_CONDITION_HIT`
    - Test: 给一行归母净利润为负、经营现金流为正的数据，`ocf_to_net_profit_attributable` 返回 `UNDEFINED_CONDITION_HIT`（分母为负时该比率的符号无经济含义）
  </behavior>
  <action>
    写 `metrics/operating_cash_flow_ratio.yaml`：分子
    `cfs.net_cash_flow_from_operating_activities`、分母 `bs.total_current_liabilities_period_end`。
    v2 相对 v1 必须补上的是 POC-01 三条单方缺陷：
    别名收窄——**不收录**「现金流量比率」这个过宽的别名（S2），只收「经营现金流量比率」
    与「经营活动现金流量比率」这类无歧义说法；
    分子为 0 或缺失的处理写进 `undefined_conditions`（S3），缺失用 `is_missing()`，为 0 不拒答
    （经营现金流恰为 0 是真实可能的取值）；
    报告期长度的约定写死在 `grain.period_type` 为 `annual`，并配一条陷阱说明
    半年报与季报的累计数不得直接与年度口径比较（S4），用 `flags.period_length_mismatch` 承载。
    分子的 `sign_convention` 必须声明——经营现金流量净额可以为负，方向必须明确。
    `period_semantics` 为 `混合`。
    `standard_basis` 含《企业会计准则第 31 号——现金流量表》（财政部）与
    《企业会计准则第 30 号——财务报表列报》（财政部），各带可核实定位单位与版本年份。

    写 `metrics/ocf_to_net_profit_attributable.yaml`：分子
    `cfs.net_cash_flow_from_operating_activities`、分母 `is.net_profit_attributable_to_parent`。
    这份的口径要点在**分母为负或为零时该比率无经济含义**：净现比的解读建立在
    「每一元净利润背后有多少经营现金」上，分母为负时比值的符号会误导。
    因此 `undefined_conditions` 必须覆盖分母 ≤ 0 与分母缺失两种情形——
    注意这与 01-04 的净利率立场相反（那里负分子是有意义的结果），
    差别在于净利率的负号有含义而净现比的负号没有。这个区别要写进陷阱并说明理由，
    否则复核者会认为两处不一致。
    分子分母的 `sign_convention` 均须声明。
    `common_pitfalls` 至少含：不得用含少数股东损益的净利润作分母（配 `enforced_by: source_fields.id`）、
    不得与用扣非归母净利润作分母的口径混比、以及一条关于该比率在重资产行业阈值不同的说明
    （可标 `advisory_only`）。

    建立 `tests/test_metrics_cashflow_turnover.py`，用内存构造的合成行数据驱动，
    不引入任何外部数据文件，文件顶部注明数据为合成样例。Task 2 与 Task 3 往同一文件追加。
  </action>
  <verify>
    <automated>.venv/Scripts/python -m pytest tests/test_metrics_cashflow_turnover.py -q</automated>
    <automated>.venv/Scripts/python -m semantic_layer validate metrics/operating_cash_flow_ratio.yaml metrics/ocf_to_net_profit_attributable.yaml --json; test $? -eq 0</automated>
    <automated>git status --porcelain metrics/_flags.yaml docs/agent/poc-01/ | wc -l | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/Scripts/python -m pytest tests/test_metrics_cashflow_turnover.py -q` 全绿，失败数为 0。
    - `.venv/Scripts/python -m semantic_layer validate metrics/operating_cash_flow_ratio.yaml metrics/ocf_to_net_profit_attributable.yaml --json`
      退出码 0，`findings` 为空数组。
    - `.venv/Scripts/python -m semantic_layer resolve "现金流量比率" --json` 退出码为 3，
      `.code` 为 `METRIC_NOT_DEFINED`——过宽别名未被收录（POC-01 S2 已修复）。
    - `operating_cash_flow_ratio.yaml` 的 `period_semantics` 为 `混合`，
      `grain.period_type` 为 `annual`，`flags` 含 `period_length_mismatch`。
    - `ocf_to_net_profit_attributable` 在分母为负的合成行上返回 `UNDEFINED_CONDITION_HIT`，
      且其陷阱中说明了为何此处与净利率的立场相反。
    - `git status --porcelain metrics/_flags.yaml docs/agent/poc-01/` 输出为空。
  </acceptance_criteria>
  <done>
    两份定义合规；POC-01 在经营现金流量比率上记录的三条单方缺陷（S2 / S3 / S4）在 v2 中各有承载体。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 存货周转天数、应收账款周转天数、应付账款周转天数</name>
  <files>
    metrics/inventory_turnover_days.yaml,
    metrics/accounts_receivable_turnover_days.yaml,
    metrics/accounts_payable_turnover_days.yaml,
    tests/test_metrics_cashflow_turnover.py
  </files>
  <read_first>
    - metrics/net_profit_attributable_excl_nonrecurring.yaml（模板）
    - metrics/_flags.yaml
    - docs/agent/poc-01/COMPARISON.md §5.1 的 C9 行全文（周转类整体缺位，字段粒度不统一——本任务是对它的直接回应）
    - docs/agent/poc-01/field_inventory.md 的 `bs.*` 与 `is.*` 段（`inventory` / `accounts_receivable` 均只有单值，无期初值）
    - openspec/changes/archive/2026-08-15-extend-metric-definition-schema/design.md §Risks 第一行（写不出来的记为 Open Question，不许扩语法）
    - openspec/specs/semantic-layer/metric-definition/spec.md Requirement 1、3、6、7
    - PROJECT_SPEC.md §5.1
  </read_first>
  <behavior>
    - Test: 三份定义的 `validate_definition` 均返回空 Finding 列表
    - Test: 三份定义的 `period_semantics` 均为 `混合`
    - Test: 三份定义的 `derivation.allow_from_components` 均为 `false`
    - Test: `Registry.resolve("存货周转天数")` 命中 `inventory_turnover_days`
    - Test: 给一行营业成本为 0 的数据，`inventory_turnover_days` 返回 `UNDEFINED_CONDITION_HIT`
    - Test: 给一行存货为 0 而营业成本为正的数据，`inventory_turnover_days` **不**拒答（存货为零是合法取值）
    - Test: 给一行营业收入缺失的数据，`accounts_receivable_turnover_days` 返回 `UNDEFINED_CONDITION_HIT`
  </behavior>
  <action>
    三份定义共用同一套结构，逐份填不同的分子分母，但有三个**必须逐份显式写出**的约定，
    因为它们各自都会静默改变结果：

    第一，**天数基数**。周转天数 = 基数 ÷ 周转率，基数取 360 还是 365 会带来约 1.4% 的系统性差异。
    受限 DSL 不含算术，天数基数无法写进 `formula` 的表达式求值里——但 `formula` 本身是字符串
    形式的公式描述，可以写出完整算式。基数的选择必须在 `derivation.note` 中显式声明取值
    （本批次统一取 365，理由是与会计年度实际天数一致，且不依赖任何行业惯例），
    并配一条 `enforced_by: derivation` 的陷阱说明采用 360 基数的口径与本定义结果不可直接比较。

    第二，**期末时点而非期初期末平均**。按批次规则统一取期末时点，
    `derivation.allow_from_components` 设为 `false`，`note` 写明不得用其他年度数据自行构造平均数。
    这一立场与常见财务数据库（多用平均）存在系统性差异，必须在陷阱中写明并说明理由是
    字段清单只提供单值（POC-01 C9），而不是认为平均口径不对。

    第三，**分子的口径归属**。存货周转天数分子用营业成本（不是营业收入）；
    应收账款周转天数分子用营业收入；应付账款周转天数分子用营业成本。
    每份都必须把「用错分子」写成有承载体的陷阱（`enforced_by: source_fields.id`），
    因为混用是这类指标最常见的错误，且错了以后数值仍然「看起来正常」。

    `metrics/inventory_turnover_days.yaml`：`bs.inventory` 与 `is.operating_cost`。
    `undefined_conditions` 覆盖营业成本 ≤ 0、营业成本缺失、存货缺失。存货为 0 不拒答。
    `standard_basis` 含《企业会计准则第 1 号——存货》（财政部）。

    `metrics/accounts_receivable_turnover_days.yaml`：`bs.accounts_receivable` 与
    `is.operating_revenue_current`。这份多一个口径分歧：应收账款取账面价值（已扣坏账准备）
    还是账面余额（未扣）。定义选账面价值（报表列示值就是账面价值），
    把账面余额写成显式排除并配陷阱。另有一条：营业收入不含税而应收账款含税，
    两者口径不严格配比——这是无法用现有字段修正的固有偏差，
    写成 `advisory_only` 陷阱（这是本批次少数几个恰当使用 `advisory_only` 的地方）。

    `metrics/accounts_payable_turnover_days.yaml`：分母字段 `bs.accounts_payable`
    在 `field_inventory.md` 中不存在，需新增，使用 `bs.` 前缀、`statement` 为「合并资产负债表」，
    在 01-03 受控集内，**无需修改 `scan_rules.yaml`**。分子用 `is.operating_cost`。
    陷阱须含：不得与应付票据合并计算（除非定义显式声明，本定义不含）、
    以及采购额与营业成本不等价的固有偏差（`advisory_only`）。

    往 `tests/test_metrics_cashflow_turnover.py` 追加本任务用例。
  </action>
  <verify>
    <automated>.venv/Scripts/python -m pytest tests/test_metrics_cashflow_turnover.py -q</automated>
    <automated>.venv/Scripts/python -m semantic_layer validate metrics/inventory_turnover_days.yaml metrics/accounts_receivable_turnover_days.yaml metrics/accounts_payable_turnover_days.yaml --json; test $? -eq 0</automated>
    <automated>git status --porcelain scan_rules.yaml metrics/_flags.yaml docs/agent/poc-01/ | wc -l | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/Scripts/python -m pytest tests/test_metrics_cashflow_turnover.py -q` 全绿，失败数为 0。
    - 对三个显式路径运行 `validate ... --json` 退出码 0，`findings` 为空数组。
    - 三份定义的 `derivation.note` 中均出现字面串 `365`，且均出现「期末」二字——
      天数基数与时点立场逐份显式，不靠继承批次规则。
    - 三份定义的 `derivation.allow_from_components` 均为 `false`，`period_semantics` 均为 `混合`。
    - `inventory_turnover_days` 在「存货为 0、营业成本为正」的合成行上**不**拒答；
      在「营业成本为 0」的合成行上返回 `UNDEFINED_CONDITION_HIT`。
    - `git status --porcelain scan_rules.yaml metrics/_flags.yaml docs/agent/poc-01/` 输出为空
      （新增的应付账款字段落在既有受控集内，未改扫描规则）。
  </acceptance_criteria>
  <done>
    POC-01 C9 指出的周转类缺位被填上；天数基数与时点口径这两个会静默改变结果的约定，
    在每份定义里都是显式声明而不是隐含约定。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 自由现金流、总资产周转率，与现金循环周期的显式排除</name>
  <files>
    metrics/free_cash_flow.yaml,
    metrics/total_asset_turnover.yaml,
    tests/test_metrics_cashflow_turnover.py,
    docs/agent/phase-01/open-questions.md
  </files>
  <read_first>
    - metrics/net_profit_attributable_excl_nonrecurring.yaml（模板）
    - metrics/_flags.yaml
    - openspec/changes/archive/2026-08-15-extend-metric-definition-schema/design.md §Risks 第一行与 §Open Questions（写不出来的记为 Open Question，不许扩语法）
    - PROJECT_SPEC.md §5.1 元规则段（`advisory_only` 的正当用法边界）
    - docs/agent/poc-01/field_inventory.md 的 `cfs.*` 段
    - EVAL_CASES.md §3 的 C2 类描述（拒答边界题「使用语义层里不存在的指标」——本任务产出的排除项就是它的靶子）
    - CLAUDE.md §停止协议（EXPECTED / FOUND / IMPACT / OPTIONS 格式）
  </read_first>
  <behavior>
    - Test: 两份定义的 `validate_definition` 均返回空 Finding 列表
    - Test: `Registry.resolve("自由现金流")` 命中 `free_cash_flow`
    - Test: `Registry.resolve("现金循环周期")` 返回 `Refusal`，`code` 为 `METRIC_NOT_DEFINED`
    - Test: `Registry.resolve("总资产周转率")` 命中 `total_asset_turnover`
    - Test: 给一行购建长期资产支付现金缺失的数据，`free_cash_flow` 返回 `UNDEFINED_CONDITION_HIT`
    - Test: `free_cash_flow` 在结果为负的合成行上不拒答（负自由现金流是有意义的结果）
    - Test: 本批次 7 份定义的 `advisory_ratio` 低于 0.5
  </behavior>
  <action>
    写 `metrics/free_cash_flow.yaml`：`cfs.net_cash_flow_from_operating_activities` 减
    购建固定资产、无形资产和其他长期资产支付的现金（新增字段，`cfs.` 前缀、
    `statement` 为「合并现金流量表」，在 01-03 受控集内）。
    这份的特殊之处：**自由现金流没有会计准则定义**，它是分析惯例而非列报项目。
    诚实的做法是把这一点写进定义本身而不是回避——`standard_basis` 只能引用
    《企业会计准则第 31 号——现金流量表》（财政部）来支撑**两个分项**的列报依据，
    并在 `derivation.note` 中显式声明：本指标的组合方式是本语义层的选择，无准则依据，
    其他常见变体（扣营运资金变动、扣股权激励费用、扣全部投资活动流出）与本定义结果不可比。
    这个诚实声明本身就是项目立论的展示物——`anthropics/financial-services` 的
    `FCF = Operating CF - CapEx` 恰恰没说这句（`RESEARCH.md` §2.1 已记录）。
    减项的 `sign_convention` 必须声明（现金流量表的支付项通常按正数列报，
    公式里做减法，符号约定错了结果直接翻倍）。
    `undefined_conditions` 覆盖任一分项缺失。结果为负不拒答。
    `common_pitfalls` 至少含：无准则定义故跨来源不可比（`enforced_by: derivation`）、
    不得与投资活动现金流量净额混用（`enforced_by: source_fields.id`）、
    以及一条关于该指标在扩张期企业解读方式的说明（`advisory_only`）。

    写 `metrics/total_asset_turnover.yaml`：`is.operating_revenue_current` 除以 `bs.total_assets`。
    与 01-04 的总资产收益率同构，同样是期末时点分母、`period_semantics` 为 `混合`。
    `undefined_conditions` 覆盖分母 ≤ 0 与分母缺失。
    陷阱须含：与采用平均总资产口径的结论不可比（`enforced_by: derivation`）、
    不得用营业总收入替代营业收入（`enforced_by: source_fields.id`）。

    建立 `docs/agent/phase-01/open-questions.md`，用 CLAUDE.md 停止协议的
    EXPECTED / FOUND / IMPACT / OPTIONS 四段格式记录本阶段发现的、**不许就地解决**的问题。
    首条必须是现金循环周期：
    EXPECTED —— 20 个指标的候选清单中原本包含现金循环周期；
    FOUND —— 它等于存货周转天数加应收账款周转天数减应付账款周转天数，
    需要「一个指标引用另外三个指标的结果」，而现行 schema 的 `source_fields` 与
    受限 DSL 都只支持**字段**引用，不支持**指标**引用；
    IMPACT —— 该指标无法在不扩展 schema 的前提下定义；扩 schema 属于权威文档变更，
    须走 OpenSpec，不属于本计划范围；同时它给 01-07 的 C2 类拒答题提供了一个真实靶子；
    OPTIONS —— (a) Phase 2 提 OpenSpec 变更引入复合指标能力；
    (b) 保持不定义，作为 C2 类拒答题的长期靶子；(c) 用字段直接展开（否决：会重复三份定义的
    全部口径约定，任何一份改动都要手工同步，正是语义层要消灭的问题）。
    第二条记录 `enforced_by` 引用 `undefined_conditions` 时只能用下标形式的问题
    （见 01-01 的说明：加稳定 id 会改动 §5.1 字段集）。
    其余在本批次执行中发现的、因语法边界而写不出来的约束，一并追加。

    往 `tests/test_metrics_cashflow_turnover.py` 追加本任务用例，并追加批次级断言：
    对本批次 7 份定义的**显式路径列表**调用 `collect_pitfall_stats`，断言 `advisory_ratio` 低于 0.5。
  </action>
  <verify>
    <automated>.venv/Scripts/python -m pytest tests/test_metrics_cashflow_turnover.py -q</automated>
    <automated>.venv/Scripts/python -m semantic_layer validate metrics/operating_cash_flow_ratio.yaml metrics/ocf_to_net_profit_attributable.yaml metrics/free_cash_flow.yaml metrics/inventory_turnover_days.yaml metrics/accounts_receivable_turnover_days.yaml metrics/accounts_payable_turnover_days.yaml metrics/total_asset_turnover.yaml --json; test $? -eq 0</automated>
    <automated>.venv/Scripts/python -m semantic_layer resolve "现金循环周期" --json; test $? -eq 3</automated>
    <automated>git status --porcelain scan_rules.yaml metrics/_flags.yaml docs/agent/poc-01/ | wc -l | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/Scripts/python -m pytest tests/test_metrics_cashflow_turnover.py -q` 全绿，失败数为 0。
    - 对本批次 7 个显式路径运行 `validate ... --json` 退出码 0，`findings` 为空数组。
    - 本批次 7 份定义的 `collect_pitfall_stats` 结果中 `advisory_ratio` 低于 0.5（由测试断言）。
    - `.venv/Scripts/python -m semantic_layer resolve "现金循环周期" --json` 退出码 3，
      `.code` 为 `METRIC_NOT_DEFINED`。
    - `docs/agent/phase-01/open-questions.md` 存在，且首条含 `EXPECTED`、`FOUND`、`IMPACT`、`OPTIONS`
      四个小节标题，内容为现金循环周期的复合指标能力缺口。
    - `free_cash_flow.yaml` 的 `derivation.note` 中显式声明本指标无会计准则定义、
      组合方式是本语义层的选择。
    - `free_cash_flow` 在结果为负的合成行上不拒答。
    - `git status --porcelain scan_rules.yaml metrics/_flags.yaml docs/agent/poc-01/` 输出为空。
    - SUMMARY 中列出本批次所有使用了 `全文` 或章名（而非具体条号）的 `standard_basis` 条目及原因，
      并如实记录期末时点立场与常见数据库平均口径的系统性差异。
  </acceptance_criteria>
  <done>
    本批次 7 份定义全部合规；`metrics/` 下 20 个指标齐备（1 + 6 + 6 + 7）；
    现金循环周期被显式记录为 Open Question 并成为 C2 类拒答题的真实靶子，而不是被静默丢弃。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 作者的领域知识 → 定义文件 | 字段名与准则条号由人手写，是非公开数据与虚构引用两类风险的共同入口 |
| 语法边界 → 作者的便利 | 本批次多个指标诱惑作者扩 DSL 语法，扩了就滑向通用表达式，D1 的两条理由同时失效 |
| 无准则定义的指标 → 权威外观 | 自由现金流没有准则依据，若照常填 `standard_basis` 会造出虚假权威 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-27 | Information Disclosure | 新增字段名（应付账款、购建长期资产支付现金） | critical | mitigate | 一律使用 `bs.` / `cfs.` 前缀 + 准则规定的报表名，落在 01-03 受控集内；验收断言 `scan_rules.yaml` git 状态为空 |
| T-01-28 | Elevation of Privilege | 为写出平均数或复合指标而扩 DSL 语法 | high | mitigate | 批次规则明令语法只读；现金循环周期改走 Open Questions 记录；验收断言 `src/` 不在本计划的改动文件清单内 |
| T-01-29 | Spoofing | 自由现金流被填上看似权威的准则依据 | high | mitigate | `derivation.note` 必须显式声明无准则定义、组合方式为本语义层选择；`standard_basis` 只支撑分项列报依据 |
| T-01-30 | Spoofing | 天数基数与时点口径隐含化 | high | mitigate | 三份周转定义的 `derivation.note` 逐份显式写出 365 与期末，验收断言字面串存在 |
| T-01-31 | Tampering | 冻结目录与词表被顺手改动 | high | mitigate | 每个 task 验收断言 `git status --porcelain metrics/_flags.yaml docs/agent/poc-01/` 为空 |
| T-01-32 | Repudiation | 写不出来的约束被静默丢弃 | medium | mitigate | `docs/agent/phase-01/open-questions.md` 用停止协议四段格式记录，交 01-08 汇总 |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | 本计划不新增依赖；如需新增则先回到 01-01 Task 0 的 package-legitimacy gate |
</threat_model>

<verification>
- `.venv/Scripts/python -m pytest tests/test_metrics_cashflow_turnover.py -q` 全绿
- 对本批次 7 个显式路径运行 `validate --json` 退出码 0
- 本批次 `advisory_ratio` 低于 0.5（测试内断言）
- `resolve "现金循环周期"` 退出码 3，`METRIC_NOT_DEFINED`
- `cd docs/agent/poc-01 && sha256sum -c SHA256SUMS` 5 行 OK
- `git status --porcelain scan_rules.yaml metrics/_flags.yaml docs/agent/poc-01/` 输出为空

（全目录 `validate` / 全仓 `scan` / 全目录 `stats` 由 01-08 统一执行，本计划不跑，避开并行竞态。）
</verification>

<success_criteria>
- 7 份现金流与周转定义全部 `version: 2` 且被机械判定合规（AC-01 贡献 7/20，全阶段累计 20/20）
- POC-01 C9 的周转类缺位被填上，且字段粒度不统一的问题被显式立场覆盖
- 无准则定义的指标（自由现金流）诚实声明其自定义性质，不伪造权威
- 写不出来的约束进了 Open Questions，语法边界未被扩张
</success_criteria>

<output>
Create `.planning/phases/01-semantic-layer/01-06-SUMMARY.md` when done
</output>
