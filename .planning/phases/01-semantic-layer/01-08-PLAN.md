---
phase: 01-semantic-layer
plan: 08
type: execute
wave: 5
depends_on: ["01-07"]
files_modified:
  - docs/agent/VERIFICATION.md
  - docs/agent/PROGRESS.md
  - docs/agent/phase-01/open-questions.md
  - docs/agent/phase-01/flag-vocabulary-review.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [SC-1, SC-2, SC-3, SC-4, SC-5, AC-01, AC-02, AC-04, AC-10]
user_setup: []

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Phase 1 的每条验收标准都有「验收标准 → 验证方法 → 实际命令 → 真实输出 → 证据位置 → PASS/FAIL/UNVERIFIED」的完整记录"
    - "advisory_only 占比已实测并与 50% 阈值比对，结论有真实命令输出为证"
    - "H1 停止条件（20 个指标里超过 5 个无法给出无歧义口径 → 停止）已被实际评估并给出判定"
    - "本阶段积累的 flag 新增请求与 Open Questions 已汇总，不遗留在各批次的散落文件里"
    - "没有真实执行证据的项目记 UNVERIFIED，不记 PASS"
  artifacts:
    - docs/agent/VERIFICATION.md
    - docs/agent/PROGRESS.md
    - docs/agent/phase-01/flag-vocabulary-review.md
    - .planning/STATE.md
  key_links:
    - "VERIFICATION.md 的每一行 → 一条可复现的实际命令与其逐字输出（PROJECT_SPEC §11 完成定义）"
    - "stats --fail-over 0.5 的实测结果 → PROJECT_SPEC §5.1 元规则「> 50% 视为元规则失效」的判定"
    - "flag-vocabulary-review.md → 各批次的 flag-requests-*.md（D2 要求的独立评审）"
---

<objective>
收口 Phase 1：把散落在 8 个计划里的产物汇总成一份可复核的验证记录，
并对三件只有到阶段末才能判的事作出判定——
`advisory_only` 元规则是否失效、H1 停止条件是否触发、flag 词表是否需要变更。

`PROJECT_SPEC.md` §11 的完成定义是硬要求：
「验收标准 → 验证方法 → 实际命令 → 真实输出 → 证据位置 → PASS / FAIL / UNVERIFIED」，
且「没有证据的项目一律记为 UNVERIFIED，不记为 PASS」。
本计划的产物就是这份记录，**不是一句「Phase 1 完成」**。

Purpose: 让 Phase 1 的结论可被独立复核；把该停的地方停住，而不是滑进 Phase 2。
Output: `VERIFICATION.md` 的 Phase 1 章节、词表评审记录、更新后的 STATE 与 ROADMAP。

**模式：VERIFY。** 跑真实命令、记录真实输出。**不修实现**——
发现问题时按停止协议报告 EXPECTED / FOUND / IMPACT / OPTIONS，不在本计划里顺手改代码。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@PROJECT_SPEC.md
@DECISIONS.md
@EVAL_CASES.md
@AGENTS.md
@CLAUDE.md
@docs/agent/VERIFICATION.md
@docs/agent/PROGRESS.md
@docs/agent/phase-01/open-questions.md
@.planning/phases/01-semantic-layer/01-01-SUMMARY.md
@.planning/phases/01-semantic-layer/01-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 全量门禁实测与三项阶段末判定</name>
  <files>
    docs/agent/phase-01/flag-vocabulary-review.md,
    docs/agent/phase-01/open-questions.md
  </files>
  <read_first>
    - PROJECT_SPEC.md §5.1 元规则段（「Phase 1 结束时统计 `advisory_only` 占比。> 50% 视为元规则失效，须重新设计而非接受现状」）
    - PROJECT_SPEC.md §10 停止条件（「H1 不成立：20 个指标里超过 5 个无法给出无歧义的机器可读口径 → 停止，重新定义问题」）
    - openspec/changes/archive/2026-08-15-extend-metric-definition-schema/design.md D2（新增 flag 走独立评审）与 §Risks（元规则失效的处置）
    - docs/agent/phase-01/ 下全部 flag-requests-*.md（若存在）
    - docs/agent/phase-01/open-questions.md（01-06 建立）
    - .planning/phases/01-semantic-layer/ 下 01-01 至 01-07 的全部 SUMMARY
    - .planning/phases/01-semantic-layer/01-02-SUMMARY.md 中人工对 flag 词表覆盖度的预判（与实际发生的请求对照）
  </read_first>
  <action>
    第一步，跑全量门禁并**逐字保存输出**，不摘要、不转述：
    全目录校验、全仓非公开数据扫描、全目录元规则统计、全量测试、
    两处冻结校验（POC-01 与 frozen-01）、以及 C2 类评测重跑。
    此刻三个批次已全部完成，全目录级命令不再有并行竞态，是它们第一次可以放心执行的时刻。

    第二步，作出**元规则判定**。取 `stats --json` 的 `advisory_ratio` 实测值与 50% 比对。
    §5.1 的规定是「> 50% 视为元规则失效，**须重新设计而非接受现状**」——
    因此若超阈值，正确动作不是记一笔 FAIL 继续走，而是在本计划产出中明确写出
    「元规则失效，Phase 2 前须重新设计」，并按停止协议列出 OPTIONS。
    同时用 `stats` 的 `per_metric` 分项指出占比最高的几份定义，
    让「重新设计」有具体对象而不是泛泛结论。

    第三步，作出 **H1 停止条件判定**。§10 的判据是「20 个指标里超过 5 个无法给出无歧义的
    机器可读口径」。判定方法要写清楚且可复核，不能凭印象：
    逐份检查 20 个定义，把「无法给出无歧义口径」操作化为三条可核实的迹象——
    该定义在批次执行中触发过停止协议（有 flag-request 或 open-question 记录）、
    该定义的 `derivation.note` 或陷阱中承认存在无法机械消解的口径分支、
    或该定义因语法边界而未能表达某条已知约束。
    逐份列表给出计数与依据。计数 > 5 → **停止，回到问题定义**，不得进入 Phase 2；
    计数 ≤ 5 → 记录实际计数与各自理由，并明确写出这不等于 H1 被证实
    （沿用 `STATE.md` 已有的措辞纪律：POC-01 的结论是「H1 未被证伪」而非「已被证实」）。

    第四步，写 `docs/agent/phase-01/flag-vocabulary-review.md`，把 01-04 / 01-05 / 01-06 各自
    可能产生的 `flag-requests-*.md` 汇总成一次评审（`design.md` D2 要求新增 flag 走独立评审，
    不由单个指标定义顺手引入）。内容含：本阶段实际提出的新增 flag 请求清单及其来源定义、
    与 01-02 检查点中人工对词表覆盖度的预判作对照（预判准不准本身是个有用信号）、
    以及对每条请求的处置建议（纳入 Phase 2 的词表变更 / 用现有 flag 覆盖 / 驳回）。
    **本计划不修改 `metrics/_flags.yaml`**——评审产出建议，词表变更走独立流程。
    若三个批次都没有产生 flag 请求，也要明写「零请求」并指出这说明 01-01 的 10 条词表预估准确。

    第五步，把本阶段新出现的 Open Questions 追加进 `docs/agent/phase-01/open-questions.md`，
    至少确认已含现金循环周期的复合指标能力缺口，以及 `enforced_by` 引用 `undefined_conditions`
    只能用下标形式的问题。
  </action>
  <verify>
    <automated>.venv/Scripts/python -m pytest -q</automated>
    <automated>.venv/Scripts/python -m semantic_layer validate --json; test $? -eq 0</automated>
    <automated>.venv/Scripts/python -m semantic_layer scan --json; test $? -eq 0</automated>
    <automated>.venv/Scripts/python -m semantic_layer stats --json --fail-over 0.5; test $? -eq 0</automated>
    <automated>cd docs/agent/poc-01 &amp;&amp; sha256sum -c SHA256SUMS</automated>
    <automated>cd eval/frozen-01 &amp;&amp; sha256sum -c SHA256SUMS</automated>
    <automated>.venv/Scripts/python -m eval.run --suite frozen-01 --category C2 --report eval/runs/</automated>
    <automated>ls metrics/*.yaml | grep -v '_flags' | wc -l | grep -qx 20</automated>
  </verify>
  <acceptance_criteria>
    - `ls metrics/*.yaml | grep -v '_flags' | wc -l` 输出 20——AC-01 的指标数量条件成立。
    - `.venv/Scripts/python -m semantic_layer validate --json` 退出码 0 且 `findings` 为空数组
      （20 份定义全部合规）。
    - `.venv/Scripts/python -m semantic_layer scan --json` 退出码 0 且 `findings` 为空数组（AC-10）。
    - `.venv/Scripts/python -m semantic_layer stats --json --fail-over 0.5` 退出码 0，
      且 `advisory_ratio` 的实测数值被逐字记录（不是「低于阈值」这样的转述）。
      **若退出码为 1**：不得继续走完本计划，按停止协议在 SUMMARY 中报告
      EXPECTED / FOUND / IMPACT / OPTIONS，并把元规则判定写为「失效，须重新设计」。
    - `cd docs/agent/poc-01 && sha256sum -c SHA256SUMS` 5 行 OK；
      `cd eval/frozen-01 && sha256sum -c SHA256SUMS` 21 行 OK；两者退出码均为 0。
    - `.venv/Scripts/python -m eval.run --suite frozen-01 --category C2 --report eval/runs/` 退出码 0，
      C2 4/4 PASS。
    - `docs/agent/phase-01/flag-vocabulary-review.md` 存在，含新增 flag 请求清单（可为空但须明写「零请求」）
      与对 01-02 预判的对照。
    - H1 停止条件判定含逐份列表与计数，且明确写出计数是否 > 5 及相应动作。
    - `git status --porcelain metrics/_flags.yaml eval/frozen-01/ docs/agent/poc-01/` 输出为空
      ——本计划是 VERIFY 模式，不改词表、不改冻结产物。
  </acceptance_criteria>
  <done>
    全量门禁的真实输出已取得并逐字保存；元规则判定、H1 停止条件判定、flag 词表评审三项
    阶段末判定各有明确结论与依据。
  </done>
</task>

<task type="auto">
  <name>Task 2: 写 VERIFICATION.md 的 Phase 1 章节</name>
  <files>
    docs/agent/VERIFICATION.md
  </files>
  <read_first>
    - PROJECT_SPEC.md §11 完成定义（六段格式）与 §9 验收标准 AC-01 / AC-02 / AC-04 / AC-10
    - .planning/ROADMAP.md 的 Phase 1 五条 Success Criteria
    - EVAL_CASES.md §7 Phase 1 门的四条
    - docs/agent/VERIFICATION.md 现有内容与格式
    - CLAUDE.md §完成的定义（不接受「应该可以了」；无证据一律记 UNVERIFIED）
    - 本计划 Task 1 保存的全部真实命令输出
    - .planning/phases/01-semantic-layer/ 下 01-01 至 01-07 的全部 SUMMARY
  </read_first>
  <action>
    在 `docs/agent/VERIFICATION.md` 追加 Phase 1 章节，逐条覆盖以下条目，
    每条都按 §11 的六段格式写完整：验收标准 → 验证方法 → 实际命令 → 真实输出 → 证据位置 →
    PASS / FAIL / UNVERIFIED。

    必须覆盖的条目：
    ROADMAP Phase 1 的五条 Success Criteria（20 个指标字段齐全、存在机械校验器、
    C2 拒答准确率 100%、问题集 ≥ 20 题已冻结、非公开数据扫描通过）；
    `PROJECT_SPEC.md` §9 的 AC-01 / AC-02 / AC-04 / AC-10；
    `EVAL_CASES.md` §7 Phase 1 门的四条；
    以及 §5.1 元规则的 `advisory_only` 占比监控结果。

    **真实输出必须逐字粘贴**，含命令的退出码。转述、摘要、"全部通过" 一律不接受——
    这份文件的读者是三个月后不记得细节的自己，或者一个不信任本项目结论的复核者。

    对本阶段**未被验证**的事项，明确记 `UNVERIFIED` 并说明原因，不留白也不粉饰。
    已知至少包括：C1 / C3 / C4 / C5 四类题在本阶段仅冻结未执行（各自原因见
    `eval/frozen-01/FREEZE.md`）；C3 的标准答案基于合成夹具，因此数值结论只证明计算逻辑、
    不证明数据抽取（D-013 的 PDF 抽取能力不在本阶段）；H1 的结论仍应读作「未被证伪」
    而非「已被证实」，理由沿用 `STATE.md` 已记录的两点（POC-01 压线通过、两位回答者同源模型）。

    在章节末尾写「本阶段暴露但未解决的问题」，指向
    `docs/agent/phase-01/open-questions.md` 与 `flag-vocabulary-review.md`，
    不在 VERIFICATION.md 里重复内容（避免两份文件各说一套）。
  </action>
  <verify>
    <automated>grep -c 'UNVERIFIED' docs/agent/VERIFICATION.md</automated>
    <automated>grep -c 'AC-01\|AC-02\|AC-04\|AC-10' docs/agent/VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/agent/VERIFICATION.md` 的 Phase 1 章节中，AC-01 / AC-02 / AC-04 / AC-10 四个编号各至少出现一次。
    - 该章节中每个验收条目都含「实际命令」与「真实输出」两段，且真实输出含命令退出码；
      抽查任意三条，其「实际命令」都能在当前仓库上原样重跑。
    - `grep -c 'UNVERIFIED' docs/agent/VERIFICATION.md` 输出 ≥ 1——
      本阶段确实存在未验证事项（至少 C1/C3/C4/C5 仅冻结未执行），若为 0 说明有事项被虚记为 PASS。
    - 章节中明确写出 H1 的结论读作「未被证伪」而非「已被证实」，并给出理由。
    - 章节中明确写出 C3 基于合成夹具、其结论不覆盖数据抽取。
    - 章节末尾指向 `docs/agent/phase-01/open-questions.md` 与 `flag-vocabulary-review.md`，
      且不重复这两份文件的内容。
  </acceptance_criteria>
  <done>
    Phase 1 的每条验收标准都有六段格式的完整记录，真实输出逐字可查，
    未验证事项明确记为 UNVERIFIED 而不是含糊带过。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 阶段结论人工复核与状态更新</name>
  <what-built>
    Phase 1 的全部产物：20 份 `version: 2` 口径定义、受限布尔 DSL 与 9 条 Requirement 的机械校验器、
    指标解析与机读拒答、非公开数据扫描器、元规则统计、冻结的 20 题评测集与运行器、
    以及 `VERIFICATION.md` 的 Phase 1 章节。

    三项阶段末判定已作出：`advisory_only` 元规则是否失效、H1 停止条件是否触发、
    flag 词表是否需要变更。三项的依据都是真实命令输出，不是估计。
  </what-built>
  <how-to-verify>
    1. 打开 `docs/agent/VERIFICATION.md` 的 Phase 1 章节，**随机挑三条**验收条目，
       把其中的「实际命令」原样复制到终端重跑，比对输出是否与记录一致。
       不一致 → 记录中有捏造或过期成分，直接打回。
    2. 打开 `metrics/` 下**任意两份**定义，只读 YAML 不看代码，回答两个问题：
       它的触发条件你读得懂吗？它的陷阱条目你能看出哪些有机械后果、哪些只是提示吗？
       读不懂 → D-003 的可复核性前提不成立，这比任何测试失败都严重。
    3. 打开 `eval/frozen-01/FREEZE.md`，确认其中「未曾看到任何模型输出」的确认语
       是你本人在 01-07 的检查点里写的原话。
    4. 跑一次 `cd eval/frozen-01 && sha256sum -c SHA256SUMS` 与
       `cd docs/agent/poc-01 && sha256sum -c SHA256SUMS`，两处都应全 OK。
    5. 看 `docs/agent/phase-01/flag-vocabulary-review.md` 里的实际 flag 请求，
       与你在 01-02 检查点作出的覆盖度预判对照——预判偏差大意味着对领域的把握不如自估。
    6. 判断是否进入 Phase 2。注意 ROADMAP 的依赖是「Phase 2 依赖 Phase 1」，
       但元规则失效或 H1 停止条件触发时，正确动作是停下重新设计，不是带着问题往前走。
  </how-to-verify>
  <read_first>
    - docs/agent/VERIFICATION.md 的 Phase 1 章节
    - docs/agent/phase-01/flag-vocabulary-review.md
    - docs/agent/phase-01/open-questions.md
    - eval/frozen-01/FREEZE.md
    - metrics/ 下任选两份定义
    - PROJECT_SPEC.md §10 停止条件
  </read_first>
  <acceptance_criteria>
    - 人工回复中给出第 1 步随机抽查的**三条验收条目编号**及重跑结果是否一致。
    - 人工回复中对第 2 步给出明确答复：抽查的两份定义，触发条件是否读得懂、
      陷阱的机械后果是否分得清。答「读不懂」时不得标记阶段完成。
    - 人工回复中明确给出下一步：进入 Phase 2 / 停下重新设计元规则 / 停下重新定义问题（H1）。
    - 确认通过后，由执行者更新 `.planning/STATE.md`（`current_phase`、`status`、
      `last_activity`、`progress`、累积上下文中的关键决策与待解决问题）
      与 `.planning/ROADMAP.md`（Phase 1 条目勾选、Plans 清单勾选），
      并在 `docs/agent/PROGRESS.md` 的 changelog 追加一条**记录已发生事实**的条目（D-008）——
      写实际做了什么，不写计划与工时。
  </acceptance_criteria>
  <resume-signal>回复 "approved: 抽查 &lt;三条编号&gt; 一致；定义可读；下一步 &lt;进入 Phase 2 / 停下重设计元规则 / 停下重定义问题&gt;" 或描述问题</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 执行记录 → 验证结论 | 本计划的产物是「证据」本身，记录失真等于全阶段结论失效 |
| 阶段末判定 → 是否进入 Phase 2 | 判定被软化会让一个不成立的语义层带着问题进入 L3 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-39 | Spoofing | `VERIFICATION.md` 的真实输出被转述或捏造 | critical | mitigate | 要求逐字粘贴含退出码；人工检查点第 1 步随机抽三条原样重跑比对 |
| T-01-40 | Spoofing | 未验证事项被记为 PASS | critical | mitigate | 验收断言 `UNVERIFIED` 至少出现一次；C1/C3/C4/C5 仅冻结未执行必须明记 |
| T-01-41 | Tampering | VERIFY 模式下顺手改实现掩盖失败 | high | mitigate | 本计划声明为 VERIFY 模式；验收断言 `metrics/_flags.yaml`、`eval/frozen-01/`、`docs/agent/poc-01/` 的 git 状态为空 |
| T-01-42 | Repudiation | 元规则失效或 H1 触发时被软化处理 | high | mitigate | 两项判定各有明确阈值与动作；`stats --fail-over 0.5` 退出码 1 时明令不得走完本计划，须走停止协议 |
| T-01-43 | Information Disclosure | 收尾时新增文档携带非公开内容 | critical | mitigate | Task 1 的全仓 `scan --json` 在本计划新增文档之后执行；人工检查点前完成 |
| T-01-44 | Tampering | 冻结产物在收尾阶段被改动 | high | mitigate | 两处 `sha256sum -c` 均在 verify 中；人工检查点第 4 步再复核一次 |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | 本计划不新增依赖；如需新增则先回到 01-01 Task 0 的 package-legitimacy gate |
</threat_model>

<verification>
- `.venv/Scripts/python -m pytest -q` 全绿
- `.venv/Scripts/python -m semantic_layer validate --json` 退出码 0，findings 为空
- `.venv/Scripts/python -m semantic_layer scan --json` 退出码 0，findings 为空
- `.venv/Scripts/python -m semantic_layer stats --json --fail-over 0.5` 退出码 0，实测占比逐字记录
- `ls metrics/*.yaml | grep -v '_flags' | wc -l` 输出 20
- `cd docs/agent/poc-01 && sha256sum -c SHA256SUMS` 5 行 OK
- `cd eval/frozen-01 && sha256sum -c SHA256SUMS` 21 行 OK
- `.venv/Scripts/python -m eval.run --suite frozen-01 --category C2` 退出码 0，C2 4/4 PASS
- `git status --porcelain metrics/_flags.yaml eval/frozen-01/ docs/agent/poc-01/` 输出为空
</verification>

<success_criteria>
- Phase 1 的每条验收标准都有六段格式的完整记录，真实输出逐字可查
- `advisory_only` 占比已实测并与 50% 阈值比对，元规则判定明确
- H1 停止条件已被实际评估，判定含逐份依据与计数
- flag 词表变更请求已汇总评审，未由单个定义顺手引入
- 未验证事项记 UNVERIFIED，不记 PASS
</success_criteria>

<output>
Create `.planning/phases/01-semantic-layer/01-08-SUMMARY.md` when done
</output>
