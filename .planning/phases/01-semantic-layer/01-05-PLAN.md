---
phase: 01-semantic-layer
plan: 05
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - metrics/current_ratio.yaml
  - metrics/quick_ratio.yaml
  - metrics/debt_to_asset_ratio.yaml
  - metrics/interest_bearing_debt_ratio.yaml
  - metrics/minority_interest_share.yaml
  - metrics/net_working_capital.yaml
  - tests/test_metrics_balance_sheet.py
autonomous: true
requirements: [SC-1, AC-01]
user_setup: []

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "6 个资产负债表与偿债指标各有一份 version: 2 定义，且全部被校验器判定合规"
    - "速动资产的口径分歧（扣什么）被显式声明，不留给读者理解"
    - "有息负债没有单一列报行项，其分项加总路径被 derivation 显式允许并限定范围"
    - "期末时点与期初期末平均的选择被显式声明，且跨口径不可比有承载体"
  artifacts:
    - metrics/current_ratio.yaml
    - metrics/quick_ratio.yaml
    - metrics/debt_to_asset_ratio.yaml
    - metrics/interest_bearing_debt_ratio.yaml
    - metrics/minority_interest_share.yaml
    - metrics/net_working_capital.yaml
    - tests/test_metrics_balance_sheet.py
  key_links:
    - "interest_bearing_debt_ratio 的 derivation.allow_from_components=true → spec Requirement 3 的「允许倒推」分支（与 tracer 的 false 分支互补，两条分支都要有真实实例）"
    - "6 份定义的 flags[].name → metrics/_flags.yaml（只引用，不新增）"
---

<objective>
产出 20 个指标中的 6 个：**资产负债表与偿债**批次。

这批的共同特征是**时点口径**与**分项加总**：分母取期末还是期初期末平均、
速动资产扣哪些项、有息负债在报表上根本没有对应行项。
其中 `interest_bearing_debt_ratio` 承担一个结构性任务——它是本阶段
`derivation.allow_from_components: true` 分支的真实实例。
tracer 与批次 A 的定义几乎全是 `false`，若 20 个指标里没有一个真实的 `true`，
spec Requirement 3 的「允许倒推」那一支就从未被检验过。

Purpose: 覆盖 AC-01 的 6/20，并让 spec R3 的两个分支都有实例支撑。
Output: 6 份 `version: 2` 定义 + 一个批次专属的行为测试文件。

**模式：IMPLEMENT。** 只写定义与测试。不改 `src/`、不改 `metrics/_flags.yaml`、不改权威文档。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@PROJECT_SPEC.md
@openspec/specs/semantic-layer/metric-definition/spec.md
@docs/agent/poc-01/field_inventory.md
@docs/agent/poc-01/COMPARISON.md
@metrics/_flags.yaml
@metrics/net_profit_attributable_excl_nonrecurring.yaml
@.planning/phases/01-semantic-layer/01-02-SUMMARY.md
</context>

<batch_rules>

以下规则对本批次全部 6 份定义生效。

**模板**：`metrics/net_profit_attributable_excl_nonrecurring.yaml`（01-01 产物）是唯一模板，
字段顺序保持一致。

**并行隔离（重要）**：本计划与 01-04、01-06 在同一个 wave 并行运行，三者同时往 `metrics/` 写文件。
本计划的一切校验命令**必须限定在本批次自己的 6 个文件上**：`validate` 用显式路径列表；
不跑 `tests/test_conformance.py`（它 glob 整个目录）；不跑全仓 `scan` 与全目录 `stats`。
全目录级校验由 01-08 统一执行。批次内合规性由 `tests/test_metrics_balance_sheet.py` 逐份断言。

**词表只读**：`metrics/_flags.yaml` 不得修改。需要词表里没有的 flag 时按 CLAUDE.md 停止协议处理，
把需求写进 `docs/agent/phase-01/flag-requests-balance-sheet.md`（EXPECTED / FOUND / IMPACT / OPTIONS），
该份定义改用现有 flag 或改用 `undefined_conditions` 承载，并在 SUMMARY 里点名。

**准则条号不许编**。`standard_basis[].article` 必须是执行者能实际核实的定位单位；
核实不到具体条号就退回章名或 `全文`，**不得写一个没核实过的条号**。
使用了 `全文` 或章名的条目在 SUMMARY 中列出并说明原因。

**冻结目录只读**：`docs/agent/poc-01/` 下任何文件一个字节都不许改。

**每份定义的硬性下限**：`grain` 三键齐全；参与加减的每个 `source_fields` 有 `sign_convention`
与 `missing_representation`；`derivation` 显式二选一且 `note` 非空；`standard_basis` 每条四键齐全；
`flags` 引用词表且触发条件可解析；`undefined_conditions` ≥ 1 条且可解析；
`common_pitfalls` ≥ 3 条且每条恰有 `enforced_by` 或 `advisory_only` 之一。

**时点口径的统一立场**：本批次全部采用**期末时点**。理由是 `poc-01/field_inventory.md` 里
只有流动负债提供期初期末双值，其余科目均只有单值（POC-01 的 C9 指出的字段粒度不统一）。
每份定义必须在 `derivation.note` 与陷阱中写明这一立场，并说明与采用平均口径的结论不可直接比较。
**不得**为了「更标准」而擅自引入不存在的期初字段。

**`advisory_only` 的使用纪律**：能写成规则的不许标 `advisory_only`。本批次目标是占比明显低于 50%。
</batch_rules>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 流动比率 与 速动比率</name>
  <files>
    metrics/current_ratio.yaml,
    metrics/quick_ratio.yaml,
    tests/test_metrics_balance_sheet.py
  </files>
  <read_first>
    - metrics/net_profit_attributable_excl_nonrecurring.yaml（模板）
    - metrics/_flags.yaml
    - docs/agent/poc-01/definitions/operating_cash_flow_ratio.yaml（**反面样本**：同样以流动负债为分母，看它缺什么）
    - docs/agent/poc-01/COMPARISON.md §5.1 的 C9 行与 §5.2 的 S5 行（期初期末双值、未显式禁用期初值）
    - docs/agent/poc-01/field_inventory.md 的 `bs.*` 段
    - PROJECT_SPEC.md §5.1
    - openspec/specs/semantic-layer/metric-definition/spec.md Requirement 1、3、6、7
  </read_first>
  <behavior>
    - Test: 两份定义的 `validate_definition` 均返回空 Finding 列表
    - Test: `Registry.resolve("流动比率")` 命中 `current_ratio`
    - Test: `Registry.resolve("速动比率")` 命中 `quick_ratio`
    - Test: 给一行流动负债期末余额为 0 的数据，两份定义的 `evaluate_refusal` 均返回 `UNDEFINED_CONDITION_HIT`
    - Test: 给一行存货缺失的数据，`quick_ratio` 的 `evaluate_refusal` 返回 `UNDEFINED_CONDITION_HIT`（缺失不得按 0 处理）
    - Test: 给一行存货取值恰为 0 的数据，`quick_ratio` **不**拒答（R6：缺失与零走不同分支）
    - Test: 两份定义的 `period_semantics` 均为 `时点`
  </behavior>
  <action>
    写 `metrics/current_ratio.yaml`：分子 `bs.total_current_assets`、
    分母 `bs.total_current_liabilities_period_end`。
    必须显式禁用 `bs.total_current_liabilities_period_begin`——这是 POC-01 的 S5 单方缺陷，
    v1 定义漏了它。禁用方式不是写在陷阱散文里，而是在 `derivation.note` 写明分母固定取期末余额，
    并配一条 `enforced_by: source_fields.id` 的陷阱。
    同样显式禁用 `bs.total_liabilities`（负债合计不是流动负债合计，这是 POC-01 两位回答者都主动
    排除的陷阱）。`derivation.allow_from_components` 设为 `false`。
    `undefined_conditions` 至少两条：分母为 0 或为负；分母缺失（对应报表未单独列示流动负债合计的情形，
    此时 `is_missing()` 是唯一合法写法）。`period_semantics` 为 `时点`。

    写 `metrics/quick_ratio.yaml`：分子为流动资产减存货、分母同上。
    核心口径分歧是**速动资产扣什么**：教科书有「扣存货」与「扣存货、预付款项、一年内到期的非流动资产、
    其他流动资产」两套算法，实务中两者差异可以很大。定义必须选定其一（选只扣存货，
    理由是它只依赖 `field_inventory.md` 已有的字段，不引入需要额外核实的科目），
    把另一套写成显式排除并配 `enforced_by` 的陷阱，说明两套口径的结果不可直接比较。
    存货的 `sign_convention` 与 `missing_representation` 必须声明——它是减项，
    这是 POC-01 C4 那类方向性风险的同型场景。
    `undefined_conditions` 除分母条件外，必须含一条存货缺失（`is_missing`）——
    存货缺失时按 0 处理会让速动比率等于流动比率，是一个安静且错误的答案。

    建立 `tests/test_metrics_balance_sheet.py`，用内存构造的合成行数据驱动，
    不引入任何外部数据文件，文件顶部注明数据为合成样例。Task 2 与 Task 3 往同一文件追加。
  </action>
  <verify>
    <automated>.venv/Scripts/python -m pytest tests/test_metrics_balance_sheet.py -q</automated>
    <automated>.venv/Scripts/python -m semantic_layer validate metrics/current_ratio.yaml metrics/quick_ratio.yaml --json; test $? -eq 0</automated>
    <automated>git status --porcelain metrics/_flags.yaml docs/agent/poc-01/ | wc -l | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/Scripts/python -m pytest tests/test_metrics_balance_sheet.py -q` 全绿，失败数为 0。
    - `.venv/Scripts/python -m semantic_layer validate metrics/current_ratio.yaml metrics/quick_ratio.yaml --json`
      退出码 0，`findings` 为空数组。
    - 两份定义的 `source_fields` 均不含 `bs.total_current_liabilities_period_begin`，
      也不含 `bs.total_liabilities`。
    - 两份定义的 `period_semantics` 均为 `时点`，`version` 均为整数 `2`。
    - `quick_ratio` 在「存货缺失」的合成行上返回 `UNDEFINED_CONDITION_HIT`，
      在「存货为 0」的合成行上不拒答——两条用例同时通过才算 R6 成立。
    - `git status --porcelain metrics/_flags.yaml docs/agent/poc-01/` 输出为空。
  </acceptance_criteria>
  <done>
    两份定义合规；期末时点立场、禁用项、以及「缺失不等于零」都有机械承载体。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 资产负债率 与 有息负债率</name>
  <files>
    metrics/debt_to_asset_ratio.yaml,
    metrics/interest_bearing_debt_ratio.yaml,
    tests/test_metrics_balance_sheet.py
  </files>
  <read_first>
    - metrics/net_profit_attributable_excl_nonrecurring.yaml（模板，注意它的 `derivation.allow_from_components` 是 `false`；本任务要写出 `true` 的实例）
    - metrics/_flags.yaml
    - openspec/specs/semantic-layer/metric-definition/spec.md Requirement 3 全文（含两个 Scenario：未声明倒推策略 → 不合规；禁止倒推时缺失即拒答）
    - docs/agent/poc-01/field_inventory.md 的 `bs.*` 段（注意只有 `short_term_borrowings` 一个有息科目，其余需新增）
    - PROJECT_SPEC.md §5.1 的 `derivation` 段
    - scan_rules.yaml 的 `semantic_layer_provenance`（新增字段前必须确认前缀与报表名在受控集内）
  </read_first>
  <behavior>
    - Test: 两份定义的 `validate_definition` 均返回空 Finding 列表
    - Test: `debt_to_asset_ratio` 的 `derivation.allow_from_components` 为 `false`
    - Test: `interest_bearing_debt_ratio` 的 `derivation.allow_from_components` 为 `true` 且 `note` 非空
    - Test: 给一行资产总计为 0 的数据，`debt_to_asset_ratio` 返回 `UNDEFINED_CONDITION_HIT`
    - Test: 给一行有息负债分项中任一项缺失的数据，`interest_bearing_debt_ratio` 返回 `UNDEFINED_CONDITION_HIT`（分项加总不得把缺失当 0）
    - Test: 给一行有息负债分项全部为 0 的数据，`interest_bearing_debt_ratio` 不拒答，结果为 0
  </behavior>
  <action>
    写 `metrics/debt_to_asset_ratio.yaml`：分子 `bs.total_liabilities`、分母 `bs.total_assets`，
    均为期末时点。`derivation.allow_from_components` 设为 `false` 并说明不得由
    流动负债与非流动负债相加倒推负债合计（报表已单独列示合计，倒推会在合计口径与分项口径
    不一致时静默产生差额）。`undefined_conditions` 含分母为 0 或为负、分母缺失。
    `common_pitfalls` 至少含：不得用母公司报表口径替代合并口径（配 `flags.parent_only_scope`
    作为 `enforced_by`）、不得与含永续债调整的口径混用（可标 `advisory_only`）。

    写 `metrics/interest_bearing_debt_ratio.yaml`：这一份是本阶段
    `derivation.allow_from_components: true` 的唯一真实实例，写法要经得起复核。
    有息负债在合并资产负债表上**没有单一列报行项**，只能由分项加总，
    因此倒推不是权宜之计而是唯一路径——`derivation.allow_from_components` 设为 `true`，
    `note` 必须**穷举**允许加总的分项范围并说明不得纳入什么。
    分项取合并资产负债表的准则规定列报项目：短期借款、交易性金融负债、一年内到期的非流动负债、
    长期借款、应付债券、租赁负债。除 `bs.short_term_borrowings` 已在 `field_inventory.md` 中外，
    其余需新增字段 id，全部使用 `bs.` 前缀、`statement` 为「合并资产负债表」，
    均在 01-03 的受控集内，**无需修改 `scan_rules.yaml`**。
    分母为 `bs.total_assets`。
    关键设计：分项缺失**不得按 0 处理**。加总类指标最容易出的错就是把「报表没列这一项」
    当成「这一项是 0」——两者在有息负债这个语境下含义完全不同（未列示可能是并入了其他项）。
    因此每个分项都要声明 `missing_representation`，且 `undefined_conditions` 里要有一条覆盖
    「任一分项缺失」的组合条件（用 `is_missing()` 以 `or` 串联，这在受限 DSL 的能力边界内）。
    `common_pitfalls` 至少含：分项范围随报表格式版本变化（配 `flags.basis_version_mismatch`）、
    不得把应付账款等经营性负债计入（配 `enforced_by: derivation`）、
    以及一条关于租赁负债自新租赁准则起才单独列示的说明。
  </action>
  <verify>
    <automated>.venv/Scripts/python -m pytest tests/test_metrics_balance_sheet.py -q</automated>
    <automated>.venv/Scripts/python -m semantic_layer validate metrics/debt_to_asset_ratio.yaml metrics/interest_bearing_debt_ratio.yaml --json; test $? -eq 0</automated>
    <automated>git status --porcelain scan_rules.yaml metrics/_flags.yaml docs/agent/poc-01/ | wc -l | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/Scripts/python -m pytest tests/test_metrics_balance_sheet.py -q` 全绿，失败数为 0。
    - `.venv/Scripts/python -m semantic_layer validate metrics/debt_to_asset_ratio.yaml metrics/interest_bearing_debt_ratio.yaml --json`
      退出码 0，`findings` 为空数组。
    - `interest_bearing_debt_ratio.yaml` 的 `derivation.allow_from_components` 为 `true`，
      `derivation.note` 非空且明确列出允许加总的分项；`source_fields` 条目数不少于 7
      （6 个有息分项 + 分母）。
    - `debt_to_asset_ratio.yaml` 的 `derivation.allow_from_components` 为 `false`。
    - `interest_bearing_debt_ratio` 在「任一分项缺失」的合成行上返回 `UNDEFINED_CONDITION_HIT`，
      在「分项全为 0」的合成行上不拒答——两条用例同时通过才算「缺失不等于零」成立。
    - `git status --porcelain scan_rules.yaml metrics/_flags.yaml docs/agent/poc-01/` 输出为空
      （新增字段全部落在既有受控集内，未改扫描规则）。
  </acceptance_criteria>
  <done>
    spec Requirement 3 的两个分支各有真实实例：`false` 分支由 tracer 与资产负债率承载，
    `true` 分支由有息负债率承载，且后者的允许范围被穷举而不是含糊授权。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 少数股东权益占比 与 营运资金</name>
  <files>
    metrics/minority_interest_share.yaml,
    metrics/net_working_capital.yaml,
    tests/test_metrics_balance_sheet.py
  </files>
  <read_first>
    - metrics/net_profit_attributable_excl_nonrecurring.yaml（模板；归母与少数股东的划分处理可参照）
    - metrics/_flags.yaml
    - docs/agent/poc-01/field_inventory.md 的 `bs.*` 段（`total_equity` / `equity_attributable_to_parent` / `minority_interests` 三者关系）
    - PROJECT_SPEC.md §5.1
    - openspec/specs/semantic-layer/metric-definition/spec.md Requirement 2（符号约定）与 Requirement 5（准则依据带版本）
  </read_first>
  <behavior>
    - Test: 两份定义的 `validate_definition` 均返回空 Finding 列表
    - Test: `Registry.resolve("少数股东权益占比")` 命中 `minority_interest_share`
    - Test: `Registry.resolve("营运资金")` 命中 `net_working_capital`
    - Test: `net_working_capital` 在流动资产小于流动负债时不拒答（负营运资金是有意义的结果）
    - Test: 给一行所有者权益合计为 0 的数据，`minority_interest_share` 返回 `UNDEFINED_CONDITION_HIT`
    - Test: 本批次 6 份定义的 `advisory_ratio` 低于 0.5
  </behavior>
  <action>
    写 `metrics/minority_interest_share.yaml`：分子 `bs.minority_interests`、
    分母 `bs.total_equity`。这份的口径要点是**分母取权益合计而非归母权益**，
    以及所有者权益合计可以为负（资不抵债公司），此时占比的经济含义失真——
    但这是解释问题不是取数问题，`undefined_conditions` 只覆盖分母为 0 与分母缺失，
    分母为负的情形由一条 `advisory_only` 陷阱承载（这是本批次少数几个**恰当**使用
    `advisory_only` 的地方：它确实没有机械后果）。
    `standard_basis` 必须含《企业会计准则第 33 号——合并财务报表》（财政部），
    带可核实的定位单位与版本年份——少数股东权益的界定出自该准则。
    `common_pitfalls` 至少含：不得把少数股东权益与少数股东损益混为一谈（前者时点、后者时段，
    配 `enforced_by: source_fields.id`）、合并范围变动会使该占比跳变（配 `flags.scope_change`）。

    写 `metrics/net_working_capital.yaml`：`bs.total_current_assets` 减
    `bs.total_current_liabilities_period_end`。这是本批次唯一的**绝对额**指标而非比率，
    因此没有零分母问题，但有两个必须处理的点：其一，两个字段的 `sign_convention` 必须声明
    （减法运算，POC-01 C4 的同型风险）；其二，**结果为负是有意义的取值，不是未定义**——
    必须在陷阱里写明，并确保 `undefined_conditions` 不把它列进去。
    很多实现会在这里犯「负值即异常」的错，把一个真实的经营信号变成拒答。
    `undefined_conditions` 覆盖任一被减项缺失。
    `flags` 声明 `unit_scale_mismatch`（绝对额指标对列报单位敏感，元与万元差两个数量级）。
    `common_pitfalls` 至少含：不得与流动比率互相换算后比较、列报单位必须随结果一并携带、
    以及期末时点立场的说明。

    往 `tests/test_metrics_balance_sheet.py` 追加本任务用例，并追加批次级断言：
    对本批次 6 份定义的**显式路径列表**调用 `collect_pitfall_stats`，断言 `advisory_ratio` 低于 0.5。
  </action>
  <verify>
    <automated>.venv/Scripts/python -m pytest tests/test_metrics_balance_sheet.py -q</automated>
    <automated>.venv/Scripts/python -m semantic_layer validate metrics/current_ratio.yaml metrics/quick_ratio.yaml metrics/debt_to_asset_ratio.yaml metrics/interest_bearing_debt_ratio.yaml metrics/minority_interest_share.yaml metrics/net_working_capital.yaml --json; test $? -eq 0</automated>
    <automated>git status --porcelain metrics/_flags.yaml docs/agent/poc-01/ | wc -l | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/Scripts/python -m pytest tests/test_metrics_balance_sheet.py -q` 全绿，失败数为 0。
    - 对本批次 6 个显式路径运行 `validate ... --json` 退出码 0，`findings` 为空数组。
    - 本批次 6 份定义的 `collect_pitfall_stats` 结果中 `advisory_ratio` 低于 0.5（由测试断言）。
    - `net_working_capital` 在「流动资产小于流动负债」的合成行上**不**拒答，返回负值结果。
    - `minority_interest_share.yaml` 的 `standard_basis` 中存在一条 `name` 含
      「合并财务报表」的条目，且其 `version` 字段非空。
    - `tests/test_metrics_balance_sheet.py` 中有一条用例对本批次 6 份定义逐份断言
      `source_fields[].statement` 属于 `scan_rules.yaml` 的 `allowed_statements`、
      字段 id 前缀属于 `allowed_field_prefixes`。
    - `git status --porcelain metrics/_flags.yaml docs/agent/poc-01/` 输出为空。
    - SUMMARY 中列出本批次所有使用了 `全文` 或章名（而非具体条号）的 `standard_basis` 条目及原因。
  </acceptance_criteria>
  <done>
    本批次 6 份定义全部合规；`advisory_only` 占比低于 50%；
    「负值是有意义的结果」与「负值是未定义」这两件事被明确区分开。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 作者的领域知识 → 定义文件 | 字段名与准则条号由人手写，是非公开数据与虚构引用两类风险的共同入口 |
| 新增字段 id → 扫描受控集 | 本批次新增 5 个有息负债分项字段，是本阶段新增字段最多的一处 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-22 | Information Disclosure | 有息负债分项字段名 | critical | mitigate | 新增字段一律 `bs.` 前缀 + `statement` 为「合并资产负债表」，落在 01-03 受控集内；批次测试逐份断言；`scan_rules.yaml` 不得被本计划修改（验收断言其 git 状态为空） |
| T-01-23 | Spoofing | `standard_basis[].article` 虚构条号 | high | mitigate | 批次规则明令不得编条号，核实不到即退回章名或 `全文`；SUMMARY 列出所有退化条目及原因 |
| T-01-24 | Tampering | `derivation.allow_from_components: true` 被当成万能授权 | high | mitigate | `note` 必须穷举允许加总的分项范围；`undefined_conditions` 覆盖任一分项缺失，杜绝把缺失当 0 |
| T-01-25 | Tampering | 冻结目录与词表被顺手改动 | high | mitigate | 每个 task 验收断言 `git status --porcelain metrics/_flags.yaml docs/agent/poc-01/` 为空 |
| T-01-26 | Spoofing | 负值结果被错误地当成未定义而拒答 | medium | mitigate | `net_working_capital` 与 `minority_interest_share` 各有一条专门用例断言负值场景不拒答 |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | 本计划不新增依赖；如需新增则先回到 01-01 Task 0 的 package-legitimacy gate |
</threat_model>

<verification>
- `.venv/Scripts/python -m pytest tests/test_metrics_balance_sheet.py -q` 全绿
- 对本批次 6 个显式路径运行 `validate --json` 退出码 0
- 本批次 `advisory_ratio` 低于 0.5（测试内断言）
- `cd docs/agent/poc-01 && sha256sum -c SHA256SUMS` 5 行 OK
- `git status --porcelain scan_rules.yaml metrics/_flags.yaml docs/agent/poc-01/` 输出为空

（全目录 `validate` / 全仓 `scan` / 全目录 `stats` 由 01-08 统一执行，本计划不跑，避开并行竞态。）
</verification>

<success_criteria>
- 6 份资产负债表与偿债定义全部 `version: 2` 且被机械判定合规（AC-01 贡献 6/20）
- spec Requirement 3 的 `true` 分支有了真实实例，且其允许范围被穷举
- 期末时点立场在全批次一致，并且跨口径不可比有承载体
</success_criteria>

<output>
Create `.planning/phases/01-semantic-layer/01-05-SUMMARY.md` when done
</output>
