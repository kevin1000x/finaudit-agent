---
phase: 01-semantic-layer
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - openspec/changes/lock-condition-dsl-and-flag-vocabulary/proposal.md
  - openspec/changes/lock-condition-dsl-and-flag-vocabulary/tasks.md
  - openspec/changes/lock-condition-dsl-and-flag-vocabulary/design.md
  - openspec/changes/lock-condition-dsl-and-flag-vocabulary/specs/semantic-layer/metric-definition/spec.md
  - openspec/specs/semantic-layer/metric-definition/spec.md
  - PROJECT_SPEC.md
autonomous: false
requirements: [SC-1, SC-2, AC-01]
user_setup: []

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "条件表达式的具体语法在权威文档里是规范性的，19 份定义的作者不需要靠读代码猜写法"
    - "全局 flag 词表的物理位置在 PROJECT_SPEC.md §5.1 里被点名，且其从属地位被写明（不是第三份权威文档）"
    - "对 PROJECT_SPEC.md 的改动走了 OpenSpec propose → apply → archive，没有直接编辑"
  artifacts:
    - openspec/changes/lock-condition-dsl-and-flag-vocabulary/proposal.md
    - openspec/changes/lock-condition-dsl-and-flag-vocabulary/design.md
    - openspec/specs/semantic-layer/metric-definition/spec.md
    - PROJECT_SPEC.md
  key_links:
    - "PROJECT_SPEC.md §5.1 的词表位置声明 → metrics/_flags.yaml（校验器读取的实际路径）"
    - "spec.md R4/R7 的语法条款 → src/semantic_layer/dsl.py 的解析器实现（契约与实现一一对应）"
---

<objective>
把 01-01 在写第一份 `version: 2` 定义时定下的两项决定，**升格进权威文档**：
条件表达式的具体语法，与全局 flag 词表的物理位置。

这两项本来就是 `design.md` §Open Questions 明确 defer 到 Phase 1 的，
且 `PROJECT_SPEC.md` §5.1 写着「词表位置在 Phase 1 有 5 个以上真实 flag 后确定」——
01-01 建立了 10 个 flag，前提已满足。

**为什么排在 19 份定义之前**：批量写定义时若语法未经权威确认，事后被否决就要重写 19 份文件的全部
条件字符串。这里多花一个 wave 的等待，换掉一次 19 倍返工的风险。

Purpose: 让 01-04/05/06 的作者对着规格写定义，而不是对着代码猜写法。
Output: 一个已 archive 的 OpenSpec 变更；更新后的 capability spec 与 `PROJECT_SPEC.md` §5.1。

**模式：ARCHITECT（规格变更），不写实现代码。** 本计划不改 `src/`、不改 `metrics/`、不改 `tests/`。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@PROJECT_SPEC.md
@DECISIONS.md
@AGENTS.md
@openspec/specs/semantic-layer/metric-definition/spec.md
@openspec/changes/archive/2026-08-15-extend-metric-definition-schema/design.md
@.planning/phases/01-semantic-layer/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 提出 OpenSpec 变更 lock-condition-dsl-and-flag-vocabulary</name>
  <files>
    openspec/changes/lock-condition-dsl-and-flag-vocabulary/proposal.md,
    openspec/changes/lock-condition-dsl-and-flag-vocabulary/tasks.md,
    openspec/changes/lock-condition-dsl-and-flag-vocabulary/design.md,
    openspec/changes/lock-condition-dsl-and-flag-vocabulary/specs/semantic-layer/metric-definition/spec.md
  </files>
  <read_first>
    - openspec/changes/archive/2026-08-15-extend-metric-definition-schema/proposal.md 与 design.md（同一 capability 的上一个变更，格式与论证深度的基准）
    - openspec/specs/semantic-layer/metric-definition/spec.md（现行 9 条 Requirement，本次只改 R4 与 R7）
    - PROJECT_SPEC.md §5.1 全文，特别是「全局 flag 词表」段落里「位置在 Phase 1 有 5 个以上真实 flag 后确定（候选：本文件内 / 独立文件）」这句
    - DECISIONS.md D-003（证据链可读性）与 D-009（三者不得为同一件事创建重复权威文档）
    - .planning/phases/01-semantic-layer/01-01-PLAN.md 的 &lt;resolved_design_questions&gt; 小节
    - src/semantic_layer/dsl.py 与 metrics/_flags.yaml（01-01 产物，本变更要把实现里已成立的事实写成规范）
  </read_first>
  <action>
    用 OpenSpec 的 propose 流程创建变更 id `lock-condition-dsl-and-flag-vocabulary`。

    `proposal.md` 的 Why 写清两条：其一，`design.md` §Open Questions 把这两项 defer 到 Phase 1，
    现在 01-01 已在真实定义上验证过，defer 的条件已满足；其二，19 份定义即将开写，规格不定死语法
    等于让 19 个文件对着实现猜写法，与 D-009「规格的唯一权威是 PROJECT_SPEC.md」相悖。

    `design.md` 记录被否决的方案与理由，至少包含：YAML 嵌套结构表示表达式（否决理由：§5.1 示例本身
    是单行字符串；且一条谓词摊成多行 YAML 会抬高复核成本，与 D-003 相悖）、
    以 Python 语法解析器做白名单校验（否决理由有硬证据：合并利润表前缀 `is` 是 Python 关键字，
    2026-08-15 于 Python 3.14 实测 `ast.parse` 对含该前缀的字段引用抛 SyntaxError；
    而该前缀同时出现在 §5.1 示例公式与 `poc-01/field_inventory.md`，改前缀就是改权威文档既有示例）、
    以及词表内联进 `PROJECT_SPEC.md`（否决理由：词表需被校验器机械消费，嵌在人读文档里等于让程序
    从散文里正则抠数据，正是本 capability 上一个变更要消灭的失效模式）。

    spec delta 只动两条 Requirement，其余七条不碰：
    MODIFIED Requirement「标记必须来自受控词表并具备可求值的触发条件」——补入受控词表的物理位置为
    `metrics/_flags.yaml`、词表条目必须含 `name` / `description` / `scope` / `affects_comparability` /
    `introduced_by`、`scope` 取值为 `definition` 或 `system`、`system` 域标记不得在定义文件内声明；
    并补入触发条件的规范语法：单行字符串，能力边界为字段引用（含至少一个点的点号路径）、
    六个比较运算符、逻辑 and/or/not、以及唯一谓词 `is_missing()`，不含算术、函数调用、循环与赋值。
    MODIFIED Requirement「未定义条件必须可机械求值且命中时拒答」——补入 `undefined_conditions[].expr`
    与标记触发条件共用同一套语法（沿用上一个变更 design.md D3 的共用机制决定），
    并补入求值语义：比较运算遇缺失操作数即判为不可求值并拒答，不得静默按假处理。

    每条 MODIFIED Requirement 至少补一条新 Scenario，覆盖新加的约束（例如：定义文件内声明了
    `system` 域标记 → 判定不合规；触发条件含算术运算 → 判定不合规）。

    `tasks.md` 列出 apply 阶段的动作清单，含 apply 前后各跑一次 `docs/agent/poc-01/SHA256SUMS` 校验
    （沿用上一个变更 Migration Plan 的做法，确认变更过程没碰冻结输入）。
  </action>
  <verify>
    <automated>openspec validate lock-condition-dsl-and-flag-vocabulary --strict</automated>
    <automated>cd docs/agent/poc-01 &amp;&amp; sha256sum -c SHA256SUMS</automated>
  </verify>
  <acceptance_criteria>
    - `openspec validate lock-condition-dsl-and-flag-vocabulary --strict` 退出码 0。
    - `openspec show lock-condition-dsl-and-flag-vocabulary --json --deltas-only` 的输出中，
      MODIFIED 的 Requirement 恰为 2 条，ADDED / REMOVED / RENAMED 均为 0 条。
    - spec delta 中出现字面串 `metrics/_flags.yaml`、`is_missing`、`affects_comparability`。
    - `design.md` 中记录了「Python 语法解析器」被否决，且给出的理由包含实测证据的日期与解释器版本。
    - `cd docs/agent/poc-01 && sha256sum -c SHA256SUMS` 5 行 OK，退出码 0。
    - `git status --porcelain PROJECT_SPEC.md` 输出为空——本任务只提案，不改权威文档。
  </acceptance_criteria>
  <done>
    变更提案已创建并通过严格校验，spec delta 只改 R4 与 R7 两条 Requirement，
    否决方案与理由（含 `is` 关键字冲突的实测证据）已记录在 design.md。
  </done>
  <reversibility rating="reversible">
    提案阶段无任何权威文档被改动，撤销 = 删除变更目录。
  </reversibility>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>是否批准 lock-condition-dsl-and-flag-vocabulary 并 apply 到权威文档</decision>
  <context>
    批准后：`openspec/specs/semantic-layer/metric-definition/spec.md` 的 R4 与 R7 被改写，
    `PROJECT_SPEC.md` §5.1 的「全局 flag 词表」段落被填实为 `metrics/_flags.yaml`，
    并补入条件表达式的规范语法。随后 01-04 / 01-05 / 01-06 的 19 份定义按此语法书写。

    此时否决的代价 = 重写 1 份定义 + 1 个解析器模块。
    apply 之后再否决的代价 = 重写 20 份定义的全部条件字符串，且冻结问题集可能已引用它们。
    这是本阶段最后一个便宜的反悔点。

    需要人复核的具体判断：`metrics/_flags.yaml` 的 10 条 flag 是否真的够覆盖 20 个指标
    （不够 → 后续批次会撞上停止协议）、以及条件表达式单行字符串对**你本人作为复核者**是否真的可读
    （这是 D-003 的判据，只有人能答）。
  </context>
  <options>
    <option id="approve">
      <name>批准并 apply</name>
      <pros>19 份定义有规格可依；§5.1 的待定项被填实；01-01 已在真实定义上验证过该语法</pros>
      <cons>语法自此对 20 份定义有约束力，事后修改成本陡增</cons>
    </option>
    <option id="revise-vocabulary">
      <name>先扩充 flag 词表再 apply</name>
      <pros>避免后续批次频繁撞上「需要新 flag」的停止协议</pros>
      <cons>在没有真实定义驱动的情况下扩词表，容易造出没人引用的 flag（词表膨胀本身是坏信号）</cons>
    </option>
    <option id="reject-syntax">
      <name>否决单行字符串语法，改用 YAML 嵌套结构</name>
      <pros>结构化表达无需自研解析器</pros>
      <cons>与 §5.1 既有示例不一致，须连带修改示例；复核可读性下降，与 D-003 相悖；01-01 的解析器作废</cons>
    </option>
  </options>
  <read_first>
    - openspec/changes/lock-condition-dsl-and-flag-vocabulary/proposal.md 与 design.md
    - openspec/changes/lock-condition-dsl-and-flag-vocabulary/specs/semantic-layer/metric-definition/spec.md
    - metrics/_flags.yaml（10 条 flag 全文）
    - metrics/net_profit_attributable_excl_nonrecurring.yaml（看真实条件字符串长什么样，判断可读性）
    - .planning/phases/01-semantic-layer/01-01-PLAN.md 的 20 个指标清单（判断 10 条 flag 是否够用）
  </read_first>
  <acceptance_criteria>
    - 人工回复中明确选定 `approve` / `revise-vocabulary` / `reject-syntax` 之一。
    - 选 `approve` 时，回复须包含一句对 flag 词表覆盖度的判断（够 / 预计还缺哪几类），
      该判断被抄进 SUMMARY，供 01-08 与实际发生的 flag 请求对照。
    - 选 `revise-vocabulary` 或 `reject-syntax` 时，须给出具体的修改内容，不接受「再想想」。
  </acceptance_criteria>
  <resume-signal>回复 "approve" / "revise-vocabulary: &lt;要加的 flag&gt;" / "reject-syntax: &lt;替代方案&gt;"</resume-signal>
</task>

<task type="auto">
  <name>Task 2: apply 变更到权威文档并 archive</name>
  <files>
    openspec/specs/semantic-layer/metric-definition/spec.md,
    PROJECT_SPEC.md,
    openspec/changes/lock-condition-dsl-and-flag-vocabulary/tasks.md
  </files>
  <read_first>
    - openspec/changes/lock-condition-dsl-and-flag-vocabulary/ 全部文件
    - PROJECT_SPEC.md §5.1 全文（apply 的落点）
    - openspec/changes/archive/2026-08-15-extend-metric-definition-schema/tasks.md（上一次 apply 的动作清单，照其节奏做）
    - AGENTS.md §文档优先级（确认 PROJECT_SPEC.md 上位于 openspec/，两者一一对应不重复权威）
  </read_first>
  <action>
    按 OpenSpec apply 流程把 spec delta 合并进 `openspec/specs/semantic-layer/metric-definition/spec.md`，
    随后编辑 `PROJECT_SPEC.md` §5.1。

    `PROJECT_SPEC.md` §5.1 的改动限定在两处，**不动其余任何段落**：
    其一，「全局 flag 词表」段落里「位置在 Phase 1 有 5 个以上真实 flag 后确定（候选：本文件内 / 独立文件）」
    这句被替换为确定结论：词表位于 `metrics/_flags.yaml`，条目字段为 `name` / `description` / `scope` /
    `affects_comparability` / `introduced_by`；并补一句从属声明——该文件是本节的可机读投影，
    只承载 flag 枚举，不定义字段语义、不作决策，冲突时以本文件为准（这是 D-009 不产生第三份权威文档的
    保证方式：靠范围，不靠位置）。
    其二，在字段集示例之后补一小段说明条件表达式的规范语法与能力边界，与 spec 的 R4 / R7 措辞一致。

    改完 `PROJECT_SPEC.md` 后核对 §9 的 AC-01 表述是否仍与新 spec 一致；若出现措辞漂移，
    在同一次变更内一并修正，不留到别处。

    apply 前后各跑一次冻结校验，确认整个过程没碰 POC-01 的任何文件。
    最后按 OpenSpec archive 流程把变更移入 `openspec/changes/archive/`，日期前缀用 apply 当天的 UTC 日期。
  </action>
  <verify>
    <automated>openspec validate --strict</automated>
    <automated>cd docs/agent/poc-01 &amp;&amp; sha256sum -c SHA256SUMS</automated>
    <automated>.venv/Scripts/python -m pytest -q</automated>
    <automated>grep -c 'metrics/_flags.yaml' PROJECT_SPEC.md</automated>
  </verify>
  <acceptance_criteria>
    - `openspec validate --strict` 退出码 0。
    - `grep -c 'metrics/_flags.yaml' PROJECT_SPEC.md` 输出 ≥ 1。
    - `grep -c '候选：本文件内' PROJECT_SPEC.md` 输出 0——待定措辞已被结论替换。
    - `openspec list` 的输出中不再含 `lock-condition-dsl-and-flag-vocabulary`
      （已移入 archive），且 `openspec/changes/archive/` 下存在以当天 UTC 日期为前缀的该变更目录。
    - `cd docs/agent/poc-01 && sha256sum -c SHA256SUMS` 5 行 OK，退出码 0；
      `git status --porcelain docs/agent/poc-01/` 输出为空。
    - `.venv/Scripts/python -m pytest -q` 仍全绿——规格变更未破坏 01-01 的实现。
    - `git diff --stat` 显示本任务改动的文件不含 `src/`、`metrics/`、`tests/` 下的任何路径。
  </acceptance_criteria>
  <done>
    条件表达式语法与 flag 词表位置已写入 `PROJECT_SPEC.md` §5.1 与 capability spec，
    变更已 archive，冻结输入零改动，01-01 的测试仍全绿。
  </done>
  <reversibility rating="costly">
    apply 后 19 份定义按此语法书写。回滚 = `git revert` 该 commit，但若已开始写批次定义则需连带回滚。
    checkpoint:decision 已置于本任务之前，正是为了让否决发生在 apply 之前。
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 变更提案 → 权威文档 | apply 会改写 `PROJECT_SPEC.md`，是本阶段唯一直接触碰最高优先级文档的动作 |
| 规格措辞 → 19 份定义 | 措辞含糊会被 19 份定义各自解读，产生 POC-01 已证实的漂移 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07 | Tampering | `PROJECT_SPEC.md` | high | mitigate | 改动必须走 OpenSpec propose → decision checkpoint → apply → archive；Task 1 的验收断言 `git status --porcelain PROJECT_SPEC.md` 在提案阶段为空 |
| T-01-08 | Tampering | 变更过程波及冻结输入 | high | mitigate | apply 前后各跑一次 `sha256sum -c SHA256SUMS`，并断言冻结目录 git 状态为空 |
| T-01-09 | Repudiation | 决策无记录 | medium | mitigate | 否决方案与理由（含 `is` 关键字冲突的实测证据）落在 `design.md`，随变更一并 archive |
| T-01-10 | Tampering | 规格与实现漂移 | medium | mitigate | apply 后重跑全量测试；spec 的 R4/R7 措辞与 `dsl.py` 的能力边界逐项对齐 |
</threat_model>

<verification>
- `openspec validate --strict` 退出码 0
- `PROJECT_SPEC.md` §5.1 的词表位置待定措辞已被结论替换
- 变更已 archive
- `sha256sum -c SHA256SUMS` 5 行 OK
- `.venv/Scripts/python -m pytest -q` 全绿
</verification>

<success_criteria>
- 条件表达式语法在权威文档中是规范性的，19 份定义的作者有据可依
- flag 词表位置已确定并写入 §5.1，且其从属地位被明写（D-009 不产生第三份权威文档）
- 对 `PROJECT_SPEC.md` 的改动全程走 OpenSpec，无直接编辑
</success_criteria>

<output>
Create `.planning/phases/01-semantic-layer/01-02-SUMMARY.md` when done
</output>
