---
phase: 01-semantic-layer
plan: 04
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - metrics/revenue_growth_yoy.yaml
  - metrics/gross_profit_margin.yaml
  - metrics/net_profit_margin_attributable.yaml
  - metrics/roe_weighted_average.yaml
  - metrics/return_on_total_assets.yaml
  - metrics/period_expense_ratio.yaml
  - tests/test_metrics_income_statement.py
autonomous: true
requirements: [SC-1, AC-01]
user_setup: []

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "6 个利润表与盈利能力指标各有一份 version: 2 定义，且全部被校验器判定合规"
    - "营业收入与营业总收入的口径分歧被显式挡住，不靠作者记得"
    - "加权平均净资产收益率不得由期末净资产自行倒推，缺披露即拒答"
    - "研发费用是否计入期间费用这一 2018 年格式变更带来的分歧被显式声明"
  artifacts:
    - metrics/revenue_growth_yoy.yaml
    - metrics/gross_profit_margin.yaml
    - metrics/net_profit_margin_attributable.yaml
    - metrics/roe_weighted_average.yaml
    - metrics/return_on_total_assets.yaml
    - metrics/period_expense_ratio.yaml
    - tests/test_metrics_income_statement.py
  key_links:
    - "6 份定义的 flags[].name → metrics/_flags.yaml（只引用，不新增；需新增即触发停止协议）"
    - "6 份定义 → tests/test_conformance.py 的 glob 参数化（无需改测试即被纳入校验）"
---

<objective>
产出 20 个指标中的 6 个：**利润表与盈利能力**批次。

这批的共同特征是「同一个中文名对应多种口径」——正是 `EVAL_CASES.md` C1 类要考的东西。
每份定义的价值不在于公式本身（公式都很短），而在于**把已知的取数分歧显式挡住**：
营业收入 vs 营业总收入、加权平均 vs 期末摊薄、研发费用是否计入期间费用、
上期比较数取重述后还是原始披露。

Purpose: 覆盖 AC-01 的 6/20，并为 C1 类评测题提供可判定的靶子。
Output: 6 份 `version: 2` 定义 + 一个批次专属的行为测试文件。

**模式：IMPLEMENT。** 只写定义与测试。不改 `src/`、不改 `metrics/_flags.yaml`、不改权威文档。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@PROJECT_SPEC.md
@openspec/specs/semantic-layer/metric-definition/spec.md
@docs/agent/poc-01/field_inventory.md
@docs/agent/poc-01/COMPARISON.md
@metrics/_flags.yaml
@metrics/net_profit_attributable_excl_nonrecurring.yaml
@.planning/phases/01-semantic-layer/01-02-SUMMARY.md
</context>

<batch_rules>

以下规则对本批次全部 6 份定义生效，逐份重复即冗余，在此声明一次。

**模板**：`metrics/net_profit_attributable_excl_nonrecurring.yaml`（01-01 产物）是唯一模板。
新定义与它同构，字段顺序保持一致，便于横向 diff。

**词表只读**：`metrics/_flags.yaml` 在本计划中**不得修改**。需要一个词表里没有的 flag 时，
按 CLAUDE.md 停止协议处理：把需求写进 `docs/agent/phase-01/flag-requests-income-statement.md`
（EXPECTED / FOUND / IMPACT / OPTIONS 四段格式），该份定义暂时改用现有 flag 或改用
`undefined_conditions` 承载，并在 SUMMARY 里点名。词表变更走独立评审（`design.md` D2），
不由单个指标定义顺手引入。

**准则条号不许编**。`standard_basis[].article` 必须是执行者能实际核实的定位单位。
核实不到具体条号时，退回到能核实的粒度：章名，或 `全文`（§5.1 的示例本身用过 `全文`）。
**任何情况下不得写一个没核实过的条号**——本项目的卖点就是引用可定位，编条号等于自毁立论。
凡使用 `全文` 或章名的条目，在 SUMMARY 里列出并说明为何未能定位到条。

**冻结目录只读**：`docs/agent/poc-01/` 下任何文件一个字节都不许改。
`revenue_growth_yoy` 在 POC-01 有一份 `version: 1` 同名定义——那份留在原地不动，
本批次写的是 `metrics/` 下的**新文件**，`version: 2`，两者不可跨版本比较（`design.md` D5）。

**并行隔离（重要）**：本计划与 01-05、01-06 在同一个 wave 并行运行，三者同时往 `metrics/` 写文件。
因此本计划的一切校验命令**必须限定在本批次自己的 6 个文件上**：
`validate` 用显式路径列表而非全目录；不跑 `tests/test_conformance.py`（它 glob 整个目录，
会读到别的批次尚未写完的半成品）；不跑全仓 `scan` 与全目录 `stats`。
全目录级的校验由 01-08 统一执行。批次内的合规性由 `tests/test_metrics_income_statement.py`
针对本批次 6 份定义逐份断言，覆盖面等价、无竞态。

**每份定义的硬性下限**：`grain` 三键齐全；参与加减的每个 `source_fields` 有 `sign_convention`
与 `missing_representation`；`derivation` 显式二选一且 `note` 非空；`standard_basis` 每条四键齐全；
`flags` 引用词表且触发条件可解析；`undefined_conditions` ≥ 1 条且可解析；
`common_pitfalls` ≥ 3 条且每条恰有 `enforced_by` 或 `advisory_only` 之一。

**`advisory_only` 的使用纪律**：能写成规则的不许标 `advisory_only`。§5.1 的监控指标是
「Phase 1 结束时 `advisory_only` 占比 > 50% 视为元规则失效」——本批次的目标是让占比
**明显低于** 50%，而不是踩线通过。每标一条 `advisory_only`，先自问一遍：
它真的没有机械后果，还是我懒得写触发条件。
</batch_rules>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 营业收入同比增长率 与 毛利率</name>
  <files>
    metrics/revenue_growth_yoy.yaml,
    metrics/gross_profit_margin.yaml,
    tests/test_metrics_income_statement.py
  </files>
  <read_first>
    - metrics/net_profit_attributable_excl_nonrecurring.yaml（模板）
    - metrics/_flags.yaml（可引用的 10 个 flag 及其 scope）
    - docs/agent/poc-01/definitions/revenue_growth_yoy.yaml（**反面样本**：同一指标的 v1 定义，看它缺什么）
    - docs/agent/poc-01/COMPARISON.md §5.1 的 C5 与 C6 两行（restated 判定路径缺失、上期字段描述自相矛盾）
    - docs/agent/poc-01/field_inventory.md 的 `is.*` 段（注意同时存在 `total_operating_revenue_*` 与 `operating_revenue_*` 两套）
    - PROJECT_SPEC.md §5.1 字段集与元规则
    - openspec/specs/semantic-layer/metric-definition/spec.md R1–R9
  </read_first>
  <behavior>
    - Test: 两份定义的 `validate_definition` 均返回空 Finding 列表
    - Test: `Registry.resolve("营收增长率")` 命中 `revenue_growth_yoy`
    - Test: `Registry.resolve("毛利率")` 命中 `gross_profit_margin`
    - Test: 给一行 `notes.restatement_flag` 为 true 的数据，`active_flags` 含 `restated`
    - Test: 给一行上期营业收入为 0 的数据，`evaluate_refusal` 返回 `UNDEFINED_CONDITION_HIT`
    - Test: 给一行缺失上期营业收入的数据，`evaluate_refusal` 返回 `UNDEFINED_CONDITION_HIT`（首次披露无上期比较数）
    - Test: 两份定义的 `advisory_only` 陷阱条数之和不超过陷阱总条数的一半
  </behavior>
  <action>
    写 `metrics/revenue_growth_yoy.yaml`：`grain` 用 `stock_code` / `fiscal_year` / `annual`；
    分子取本期与上期营业收入之差、分母取上期营业收入，两个字段均用
    `is.operating_revenue_current` 与 `is.operating_revenue_prior_as_presented`。
    **明确禁用** `is.operating_revenue_prior_as_originally_reported`：这是 POC-01 两位回答者都
    独立避开的陷阱，v1 定义把它写成散文，本版必须让它有承载体——在 `derivation.note` 里写明禁止取
    上年年报原始披露值作为比较基数，并配一条 `enforced_by: source_fields.id` 的陷阱。
    `flags` 声明 `restated`，触发条件引用 `notes.restatement_flag`；同时在
    `source_fields` 里显式声明 `notes.restatement_flag`（含 `missing_representation` 与
    `enum_values`），否则触发条件引用了未声明字段会被 R4 判不合规——POC-01 的 C5 说的正是这个缺口。
    `undefined_conditions` 至少两条：上期营业收入为 0 或为负；上期营业收入缺失（对应首次披露场景，
    这是 POC-01 Q4 的题面）。`period_semantics` 为 `时段`。
    `standard_basis` 至少含《企业会计准则第 14 号——收入》（财政部）与
    《企业会计准则第 30 号——财务报表列报》（财政部），各带可核实的定位单位与版本年份。

    写 `metrics/gross_profit_margin.yaml`：分子为营业收入减营业成本、分母为营业收入。
    这份的核心口径分歧是**营业收入 vs 营业总收入**：`is.operating_revenue_current` 与
    `is.total_operating_revenue_current` 是两个字段，含金融类业务的公司两者差异显著。
    定义必须选定其一（选营业收入，与营业成本配比）并把另一个写成显式禁用，
    配一条 `enforced_by: source_fields.id` 的陷阱。
    `derivation.allow_from_components` 设为 `false` 并说明不得由毛利额倒推。
    `undefined_conditions` 至少含分母为 0 或为负、以及营业成本缺失两条。
    `flags` 视需要声明 `unit_scale_mismatch`（分子分母列报单位不一致时不可直接相除）。

    `tests/test_metrics_income_statement.py` 建立本批次的行为测试文件，
    用内存构造的合成行数据驱动，**不引入任何外部数据文件**。文件顶部注明数据为合成样例。
    本任务先建文件并写上述用例，Task 2 与 Task 3 往同一文件追加。
  </action>
  <verify>
    <automated>.venv/Scripts/python -m pytest tests/test_metrics_income_statement.py -q</automated>
    <automated>.venv/Scripts/python -m semantic_layer validate metrics/revenue_growth_yoy.yaml metrics/gross_profit_margin.yaml --json; test $? -eq 0</automated>
    <automated>git status --porcelain metrics/_flags.yaml docs/agent/poc-01/ | wc -l | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/Scripts/python -m pytest tests/test_metrics_income_statement.py -q` 全绿，失败数为 0。
    - `.venv/Scripts/python -m semantic_layer validate metrics/revenue_growth_yoy.yaml metrics/gross_profit_margin.yaml --json`
      退出码 0，`findings` 为空数组。
    - `.venv/Scripts/python -m semantic_layer resolve "营收增长率" --json` 输出的 `.metric_id` 为
      `revenue_growth_yoy`、`.version` 为 2。
    - 两份定义的 `version` 均为整数 `2`；`grain` 均含 `entity`、`period`、`period_type` 三键。
    - `git status --porcelain metrics/_flags.yaml docs/agent/poc-01/` 输出为空（词表与冻结目录零改动）。
    - `revenue_growth_yoy.yaml` 中不出现字段 id `is.operating_revenue_prior_as_originally_reported`
      作为 `source_fields` 条目（它只能作为被禁用项出现在陷阱与 `derivation.note` 的文字里）。
    - `gross_profit_margin.yaml` 的 `source_fields` 中不含 `is.total_operating_revenue_current`。
    - 两份定义的每条 `common_pitfalls` 恰有 `enforced_by` 或 `advisory_only` 之一（由 R8 校验保证）。
  </acceptance_criteria>
  <done>
    两份定义合规、可被解析、别名可命中、已知取数分歧有机械承载体，词表与冻结目录未被触碰。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 归母净利率 与 加权平均净资产收益率</name>
  <files>
    metrics/net_profit_margin_attributable.yaml,
    metrics/roe_weighted_average.yaml,
    tests/test_metrics_income_statement.py
  </files>
  <read_first>
    - metrics/net_profit_attributable_excl_nonrecurring.yaml（模板；归母口径的处理方式可直接沿用）
    - metrics/_flags.yaml
    - docs/agent/poc-01/COMPARISON.md §5.1 的 C4 行（符号约定缺失导致方向性静默错误）
    - docs/agent/poc-01/field_inventory.md 的 `is.*` 与 `bs.*` 段
    - PROJECT_SPEC.md §5.1（特别是 `derivation` 的二选一要求）
    - openspec/specs/semantic-layer/metric-definition/spec.md Requirement 3（倒推策略）与 Requirement 7（未定义条件）
  </read_first>
  <behavior>
    - Test: 两份定义的 `validate_definition` 均返回空 Finding 列表
    - Test: `Registry.resolve("净利率")` 命中 `net_profit_margin_attributable`
    - Test: `Registry.resolve("加权平均ROE")` 命中 `roe_weighted_average`
    - Test: `roe_weighted_average` 的 `derivation.allow_from_components` 为 `false`
    - Test: 给一行缺失已披露加权平均净资产收益率的数据，`evaluate_refusal` 返回 `UNDEFINED_CONDITION_HIT`，而不是用期末净资产算一个出来
    - Test: 给一行归母净利润为负、营业收入为正的数据，`net_profit_margin_attributable` 不拒答（负净利率是有意义的结果，不是未定义）
  </behavior>
  <action>
    写 `metrics/net_profit_margin_attributable.yaml`：分子为
    `is.net_profit_attributable_to_parent`、分母为 `is.operating_revenue_current`。
    核心分歧是分子取归母净利润还是利润表底部的净利润（含少数股东损益）——
    与扣非归母净利润是同一类陷阱，处理方式沿用模板：把 `is.net_profit` 写成显式禁用项并配
    `enforced_by: source_fields.id` 的陷阱。分子必须声明 `sign_convention`（亏损年度分子为负是
    正常取值，不是异常，`undefined_conditions` 不得把它列进去——这一点在陷阱里写清）。
    `undefined_conditions` 含分母为 0 或为负、分母缺失两条。

    写 `metrics/roe_weighted_average.yaml`：这一份是本批次最能体现项目立论的一个。
    加权平均净资产收益率按《公开发行证券的公司信息披露编报规则第 9 号——净资产收益率和每股收益的
    计算及披露》（中国证监会）要求由公司在年报「主要会计数据和财务指标」中直接披露，
    其加权涉及报告期内净资产增减变动的月份权数，**年度粒度的报表数据不足以复算**。
    因此定义采取的立场是：取已披露值（字段前缀用 `kpi`，如 `kpi.roe_weighted_average_disclosed`），
    `derivation.allow_from_components` 设为 `false`，`note` 写明**禁止**用
    归母净利润除以期末或期初期末平均归属于母公司所有者权益来倒推——那算出来的是全面摊薄或简单平均
    口径，与加权平均不是同一个数。未披露即 `undefined_conditions` 命中，拒答。
    这正是 spec Requirement 3 的 Scenario「禁止倒推时缺失即拒答」的真实实例，
    也是 `EVAL_CASES.md` C1 类最好的靶子之一。
    `flags` 视需要声明 `restated` 与 `basis_version_mismatch`。
    `source_fields` 里为 `kpi.*` 字段声明 `statement` 为「主要会计数据和财务指标」——
    该章节名在 01-03 的公开报表受控集内。
    `common_pitfalls` 至少含：不得与全面摊薄口径混用、不得跨编报规则版本比较、
    以及一条关于该指标在净资产为负时经济含义的说明（这一条可标 `advisory_only`）。
  </action>
  <verify>
    <automated>.venv/Scripts/python -m pytest tests/test_metrics_income_statement.py -q</automated>
    <automated>.venv/Scripts/python -m semantic_layer validate metrics/net_profit_margin_attributable.yaml metrics/roe_weighted_average.yaml --json; test $? -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/Scripts/python -m pytest tests/test_metrics_income_statement.py -q` 全绿，失败数为 0。
    - `.venv/Scripts/python -m semantic_layer validate metrics/net_profit_margin_attributable.yaml metrics/roe_weighted_average.yaml --json`
      退出码 0，`findings` 为空数组。
    - `tests/test_metrics_income_statement.py` 中有一条用例对本批次已写完的定义逐份断言
      `source_fields[].statement` 属于 `scan_rules.yaml` 的 `allowed_statements`、
      字段 id 前缀属于 `allowed_field_prefixes`（`kpi.` 前缀与「主要会计数据和财务指标」章节名均在受控集内）。
      在批次内做这条断言而不是跑全仓 `scan`，是为了避开与并行批次的竞态。
    - `roe_weighted_average.yaml` 的 `derivation.allow_from_components` 为 `false`，
      且 `derivation.note` 非空。
    - `net_profit_margin_attributable.yaml` 的 `source_fields` 中不含 `is.net_profit`。
    - 对 `roe_weighted_average` 传入缺失已披露值的合成行，`resolve` 后 `evaluate_refusal` 返回的
      `code` 为 `UNDEFINED_CONDITION_HIT`。
    - `git status --porcelain metrics/_flags.yaml docs/agent/poc-01/` 输出为空。
  </acceptance_criteria>
  <done>
    两份定义合规；加权平均净资产收益率的「禁止倒推、缺披露即拒答」立场有机械承载体，
    不是写在陷阱散文里的建议。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 总资产收益率 与 期间费用率</name>
  <files>
    metrics/return_on_total_assets.yaml,
    metrics/period_expense_ratio.yaml,
    tests/test_metrics_income_statement.py
  </files>
  <read_first>
    - metrics/net_profit_attributable_excl_nonrecurring.yaml（模板）
    - metrics/_flags.yaml
    - docs/agent/poc-01/COMPARISON.md §5.1 的 C9 行（字段粒度不统一：流动负债有期初期末双值，其他只有单值）
    - docs/agent/poc-01/field_inventory.md 的 `bs.*` 段（注意 `bs.total_assets` 只有单值，没有期初值）
    - PROJECT_SPEC.md §5.1（`period_semantics` 的 时点 / 时段 / 混合 三取值）
    - openspec/specs/semantic-layer/metric-definition/spec.md Requirement 1（粒度）与 Requirement 6（取值域与缺失表示）
  </read_first>
  <behavior>
    - Test: 两份定义的 `validate_definition` 均返回空 Finding 列表
    - Test: `return_on_total_assets` 的 `period_semantics` 为 `混合`（分子时段、分母时点）
    - Test: `Registry.resolve("三费占比")` 命中 `period_expense_ratio`
    - Test: 给一行总资产为 0 的数据，`return_on_total_assets` 的 `evaluate_refusal` 返回 `UNDEFINED_CONDITION_HIT`
    - Test: 给一行研发费用缺失的数据，`period_expense_ratio` 的行为与定义声明的口径一致（不静默按 0 处理）
    - Test: 本批次 6 份定义合计的 `advisory_only` 占比低于 0.5
  </behavior>
  <action>
    写 `metrics/return_on_total_assets.yaml`：分子取 `is.net_profit`（此处**用含少数股东损益的净利润**
    与总资产口径配比，这是与归母类指标相反的选择，必须在陷阱里写明理由并配 `enforced_by`），
    分母取 `bs.total_assets`。核心口径分歧是分母取期末时点还是期初期末平均——
    POC-01 的 C9 已经指出字段粒度不统一（流动负债有期初期末双值，总资产只有单值）。
    本定义的立场：**取期末时点**，因为字段清单只提供单值；
    `derivation.allow_from_components` 设为 `false` 并在 `note` 里写明不得用其他年度的期末值
    自行构造平均数。`period_semantics` 为 `混合`。
    `undefined_conditions` 含总资产为 0 或为负、总资产缺失两条。
    `common_pitfalls` 必须含一条说明「本定义采用期末时点分母，与采用平均分母的口径不可直接比较」，
    并用 `enforced_by` 指向 `derivation` 或相应的陷阱承载体。

    写 `metrics/period_expense_ratio.yaml`：分子为销售费用、管理费用、财务费用之和，
    分母为营业收入。核心口径分歧是**研发费用是否计入**——
    财政部 2018 年修订一般企业财务报表格式后，研发费用从管理费用中单独列示，
    此前年度与此后年度的「期间费用」不是同一个集合。定义采取的立场必须显式：
    选定不含研发费用的三费口径，把研发费用写成显式排除项并配 `enforced_by` 的陷阱；
    `flags` 声明 `basis_version_mismatch`，触发条件引用 `standard_basis.version` 与
    `comparison_period.standard_basis.version` 的不等比较——这是 POC-01 C8「跨准则版本不可比没有
    对应承载体」的直接修复。
    分子的三个字段在 `poc-01/field_inventory.md` 里不存在，需要新增 `is.selling_expense` /
    `is.administrative_expense` / `is.financial_expense`，均属合并利润表的准则规定列报项目，
    在 01-03 的受控前缀与受控报表集内，无需改扫描规则。
    `derivation.allow_from_components` 设为 `true`（三费之和本身就是分项加总），
    `note` 写明允许加总的分项范围与不得纳入的项目。
    `undefined_conditions` 含分母为 0 或为负、以及三费中任一项缺失（缺失不得按 0 处理——
    这是 `EVAL_CASES.md` §5.2 举的多标签失败例子的同型陷阱）。

    往 `tests/test_metrics_income_statement.py` 追加本任务用例，并追加一条批次级断言：
    对**本批次这 6 份定义的显式路径列表**调用 `collect_pitfall_stats`，断言 `advisory_ratio` 低于 0.5。
    用显式路径而不是全目录，是为了避开与并行批次的竞态。
  </action>
  <verify>
    <automated>.venv/Scripts/python -m pytest tests/test_metrics_income_statement.py -q</automated>
    <automated>.venv/Scripts/python -m semantic_layer validate metrics/revenue_growth_yoy.yaml metrics/gross_profit_margin.yaml metrics/net_profit_margin_attributable.yaml metrics/roe_weighted_average.yaml metrics/return_on_total_assets.yaml metrics/period_expense_ratio.yaml --json; test $? -eq 0</automated>
    <automated>git status --porcelain metrics/_flags.yaml docs/agent/poc-01/ | wc -l | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/Scripts/python -m pytest tests/test_metrics_income_statement.py -q` 全绿，失败数为 0。
    - 对本批次 6 个显式路径运行 `.venv/Scripts/python -m semantic_layer validate ... --json` 退出码 0，`findings` 为空数组。
    - 本批次 6 份定义的 `collect_pitfall_stats` 结果中 `advisory_ratio` 低于 0.5（由测试断言）。
    - `metrics/` 下存在且仅新增本批次列出的 6 个文件名，`git status --porcelain metrics/` 中
      本批次之外的新增文件一律不属于本计划的改动。
    - `return_on_total_assets.yaml` 的 `period_semantics` 为 `混合`。
    - `period_expense_ratio.yaml` 的 `flags` 含 `basis_version_mismatch`，
      且 `derivation.allow_from_components` 为 `true`。
    - `git status --porcelain metrics/_flags.yaml docs/agent/poc-01/` 输出为空。
    - SUMMARY 中列出本批次所有使用了 `全文` 或章名（而非具体条号）的 `standard_basis` 条目及原因。
  </acceptance_criteria>
  <done>
    本批次 6 份定义全部合规，`metrics/` 下累计 7 份 `version: 2` 定义，
    `advisory_only` 占比低于 50%，词表与冻结目录零改动。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 作者的领域知识 → 定义文件 | 字段名与准则条号由人手写，是非公开数据与虚构引用两类风险的共同入口 |
| 定义文件 → 校验器与解析器 | 新增定义进入既有求值路径 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-17 | Information Disclosure | `metrics/*.yaml` 新增字段名 | critical | mitigate | 每个 task 的 verify 跑 `scan --json`；新增字段的 `statement` 与前缀必须落在 01-03 的公开受控集内 |
| T-01-18 | Spoofing | `standard_basis[].article` 虚构条号 | high | mitigate | 批次规则明令不得编条号，核实不到即退回章名或 `全文`；SUMMARY 必须列出所有退化条目及原因 |
| T-01-19 | Tampering | `docs/agent/poc-01/` 冻结定义被顺手「修好」 | high | mitigate | 每个 task 的验收断言 `git status --porcelain docs/agent/poc-01/` 为空；新定义写在 `metrics/` 下的新文件 |
| T-01-20 | Tampering | `metrics/_flags.yaml` 被顺手加 flag | medium | mitigate | 验收断言词表 git 状态为空；需新 flag 时走停止协议写 flag-request 文件，交 01-08 汇总评审 |
| T-01-21 | Spoofing | 陷阱被随手标 `advisory_only` 绕过元规则 | medium | mitigate | 批次级断言 `advisory_ratio` 低于 0.5；`stats --fail-over 0.5` 进 verify |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | 本计划不新增依赖；如需新增则先回到 01-01 Task 0 的 package-legitimacy gate |
</threat_model>

<verification>
- `.venv/Scripts/python -m pytest tests/test_metrics_income_statement.py -q` 全绿
- 对本批次 6 个显式路径运行 `validate --json` 退出码 0
- 本批次 6 份定义的 `advisory_ratio` 低于 0.5（测试内断言）
- `cd docs/agent/poc-01 && sha256sum -c SHA256SUMS` 5 行 OK
- `git status --porcelain metrics/_flags.yaml docs/agent/poc-01/` 输出为空

（全目录 `validate` / 全仓 `scan` / 全目录 `stats` 由 01-08 统一执行，本计划不跑，避开并行竞态。）
</verification>

<success_criteria>
- 6 份利润表与盈利能力定义全部 `version: 2` 且被机械判定合规（AC-01 贡献 6/20）
- 每份定义把至少一个已知取数分歧变成了有承载体的规则，而不是散文提示
- `advisory_only` 占比低于 50%
</success_criteria>

<output>
Create `.planning/phases/01-semantic-layer/01-04-SUMMARY.md` when done
</output>
