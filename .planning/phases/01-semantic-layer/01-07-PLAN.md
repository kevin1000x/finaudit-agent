---
phase: 01-semantic-layer
plan: 07
type: execute
wave: 4
depends_on: ["01-03", "01-04", "01-05", "01-06"]
files_modified:
  - eval/frozen-01/cases/*.yaml
  - eval/frozen-01/fixtures/synthetic-01.yaml
  - eval/frozen-01/FREEZE.md
  - eval/frozen-01/SHA256SUMS
  - eval/run.py
  - eval/testdata/sample-suite/
  - tests/test_eval_runner.py
autonomous: false
requirements: [SC-3, SC-4, AC-02, AC-04]
user_setup: []

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "存在 ≥ 20 题的评测问题集，覆盖 C1–C5 五类，题量分布与 EVAL_CASES.md §3 一致"
    - "问题集与标准答案在看到任何模型输出之前冻结，冻结时带 UTC 时间戳与 SHA-256（D-012 / AC-04）"
    - "评测运行器在跑之前先校验冻结哈希，校验不过即拒绝运行（fail-closed）"
    - "C2 类 4 题的拒答准确率为 100%，且每题的拒答理由正确（ROADMAP Phase 1 成功标准 3）"
    - "C3 题的标准答案来自仓库内的合成夹具，且该夹具被明确标注为合成，不冒充真实年报数据"
  artifacts:
    - eval/frozen-01/cases/
    - eval/frozen-01/fixtures/synthetic-01.yaml
    - eval/frozen-01/FREEZE.md
    - eval/frozen-01/SHA256SUMS
    - eval/run.py
    - tests/test_eval_runner.py
  key_links:
    - "C2 题的目标指标名 → 20 份定义中确实不存在的指标（现金循环周期来自 01-06 的 Open Questions，是真实缺口而非编造靶子）"
    - "eval/run.py 的冻结校验 → eval/frozen-01/SHA256SUMS（D-012 的物理保障）"
    - "C1 题的 expected.metric_id/metric_version → metrics/*.yaml 的实际 metric_id 与 version 2"
---

<objective>
产出并**冻结**评测问题集：≥ 20 题，覆盖 `EVAL_CASES.md` §3 的 C1–C5 五类，
每题有标准答案与判定规则，冻结时记录 UTC 时间戳与 SHA-256（AC-04 / D-012）。
同时交付评测运行器，并在 Phase 1 范围内执行 C2 类，验证拒答准确率为 100%。

> **本计划成文于 D-012 修订之前（2026-08-15 修订）。** `DECISIONS.md` 与 `EVAL_CASES.md`
> 上位于 `.planning/`，执行时以修订后的规则为准，本计划下列内容需按其调整：
> ① 冻结动作必须由脚本执行，**先跑 §2.1 前置门禁，校验不过拒绝冻结**，不手工打哈希；
> ② 作废按 §2.2 区分构造错误（移出分母）与迁就模型（禁止作废）；
> ③ C2 的 100% 门以**未作废** C2 题为分母，且存活题数 < 3 时该门不成立；
> ④ 冻结集只含题目/标准答案/判定规则/夹具，`eval/run.py` 不在冻结集内。
> 修订缘由：POC-01 有一份定义冻结后才被发现不是合法 YAML；而原「作废题仍留在分母」
> 叠加本计划的 C2 100% 门，会让 4 题里出 1 题构造错误就永久达不到门。

**D-012 是本项目最重要的自律**，原话：「做评测最大的失效模式是先看结果再定标准。」
Phase 1 是执行这条纪律成本最低的时刻——此刻项目里根本没有模型，
拒答判定由确定性的解析器完成。冻结在此发生，天然满足「看到任何模型输出之前」。

Purpose: 覆盖 SC-3 与 SC-4；给 Phase 2 的 H2 复核实验准备好不可事后修改的靶子。
Output: 冻结的问题集与夹具、`eval/run.py`、C2 类 4/4 通过的实际输出。

**模式：IMPLEMENT，冻结动作属于不可逆操作。** 冻结前有一个阻塞式决策检查点。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@PROJECT_SPEC.md
@DECISIONS.md
@EVAL_CASES.md
@docs/agent/poc-01/FREEZE.md
@docs/agent/phase-01/open-questions.md
@.planning/phases/01-semantic-layer/01-04-SUMMARY.md
@.planning/phases/01-semantic-layer/01-05-SUMMARY.md
@.planning/phases/01-semantic-layer/01-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 撰写 20 题问题集与合成夹具（尚未冻结）</name>
  <files>
    eval/frozen-01/cases/*.yaml,
    eval/frozen-01/fixtures/synthetic-01.yaml
  </files>
  <read_first>
    - EVAL_CASES.md 全文，重点是 §2 冻结规则、§3 问题集结构与五类题量、§3.1 单题格式、§3.2 拒答题判定、§5 失败归因分层
    - DECISIONS.md D-012（冻结）与 D-013（年报 PDF 为权威源、二手源只作对照）
    - docs/agent/poc-01/FREEZE.md（上一次冻结的格式与措辞，本次照其结构）
    - docs/agent/poc-01/questions.md（上一次的题面写法）
    - metrics/ 下全部 20 份定义的 `metric_id` 与 `aliases`（C1 题要能命中，C2 题要确保不命中）
    - docs/agent/phase-01/open-questions.md（现金循环周期为何不在语义层内——C2 题的真实靶子）
    - PROJECT_SPEC.md §9 AC-04
  </read_first>
  <action>
    在 `eval/frozen-01/cases/` 下按 `EVAL_CASES.md` §3.1 的单题格式写 20 题，每题一个 YAML 文件，
    文件名即题 id。题量分布严格按 §3 的表：C1 六题、C2 四题、C3 五题、C4 三题、C5 两题。
    每题必填 `id` / `category` / `question` / `expected` / `required_evidence` / `judging` /
    `attribution_hint`。`frozen_at` 字段在 Task 3 冻结时统一填入，本任务留空占位。

    **C1 口径歧义（6 题）**：题面必须用日常说法而不是 `metric_id`，考的是能否选对口径并声明版本。
    覆盖本阶段最容易混的六处：净利润（归母 vs 含少数股东 vs 扣非）、
    营业收入与营业总收入、净资产收益率（加权平均 vs 全面摊薄）、
    期间费用是否含研发费用、周转天数（期末 vs 平均口径）、
    速动资产扣什么。`expected` 填 `metric_id` 与 `metric_version: 2`，`judging.correctness` 用 `exact`。

    **C2 拒答边界（4 题）**：使用语义层里**确实不存在**的指标。四个靶子：
    现金循环周期（来自 01-06 的 Open Questions，是真实的复合指标能力缺口，不是编造）、
    EBITDA、息税前利润率、客户集中度。
    写题前必须逐一用 `python -m semantic_layer resolve <名称>` 确认返回 `METRIC_NOT_DEFINED`——
    **靶子必须验证过确实打不中**，否则 C2 类就成了摆设。
    `judging.refusal_expected` 设为 `true`，`expected` 中记录期望的拒答理由码
    （本阶段全部为 `METRIC_NOT_DEFINED`）。按 §3.2，强行给答案即使数字碰巧对也算失败。

    **C3 计算正确性（5 题）**：标准答案取自 `eval/frozen-01/fixtures/synthetic-01.yaml`。
    夹具是**合成数据**，文件首行与 `FREEZE.md` 中都必须明确标注这一点，
    绝不表述为来自任何真实公司或年报——D-010 与 D-013 之下，
    把合成数据说成真实数据是比数据本身更严重的问题。
    选合成而非真实年报的理由要写进 `FREEZE.md`：C3 考的是聚合粒度、缺失期、零分母、
    同名不同实体这四类**构造出来的陷阱**（§3 的陷阱设计栏原文），构造比寻找更可控；
    且 NFR-05 要求无网络时可完成评测。夹具的字段 id 与报表名必须沿用语义层的受控词汇，
    使其结构上真实。五题分别覆盖：缺失期、零分母、同名不同实体、跨口径版本比较、单位不一致。
    `judging.correctness` 用 `tolerance`，容差按 §3.1 的示例值。

    **C4 准则引用（3 题）**：考回答能否指回具体准则条号，涉及新旧准则交替期。
    覆盖：非经常性损益公告的 2008 与 2023 版差异、研发费用自 2018 年起单独列示、
    新收入准则实施年度的可比性。`expected` 记录期望被引用的准则名称与版本标识。
    **本阶段不执行这三题**（准则检索是 Phase 3 的 L2），只冻结。

    **C5 图谱必要性（2 题）**：按 §3 的陷阱设计，覆盖关联方循环交易识别与
    跨年度准则适用变更的传播路径。**本阶段不执行**，只冻结。
    题面须写成可被「纯向量 RAG 能否答出」检验的形式，为 D-007 的判定留出空间。

    每题的 `attribution_hint` 按 §3 的规定填写，并在文件中注明该字段判分时不可见，仅用于事后分析。
  </action>
  <acceptance_criteria>
    - `eval/frozen-01/cases/` 下的 YAML 文件数为 20。
    - 按 `category` 统计：C1 六题、C2 四题、C3 五题、C4 三题、C5 两题。
    - 每个题面文件含键 `id`、`category`、`question`、`expected`、`required_evidence`、`judging`、`attribution_hint`。
    - 四道 C2 题的目标指标名，逐一执行 `.venv/Scripts/python -m semantic_layer resolve <名称> --json`
      的退出码均为 3 且 `.code` 均为 `METRIC_NOT_DEFINED`——靶子已验证确实打不中。
    - 六道 C1 题的 `expected.metric_id` 均能在 `metrics/` 下找到同名定义，且 `expected.metric_version` 均为 2。
    - `eval/frozen-01/fixtures/synthetic-01.yaml` 首行明确标注数据为合成、非任何真实公司或年报。
    - 夹具中出现的每个字段 id 前缀均属于 `scan_rules.yaml` 的 `allowed_field_prefixes`。
    - 所有题面文件的 `frozen_at` 为空占位（冻结在 Task 3 执行）。
  </acceptance_criteria>
  <verify>
    <automated>ls eval/frozen-01/cases/*.yaml | wc -l | grep -qx 20</automated>
    <automated>.venv/Scripts/python -m semantic_layer resolve "现金循环周期" --json; test $? -eq 3</automated>
    <automated>.venv/Scripts/python -m semantic_layer resolve "EBITDA" --json; test $? -eq 3</automated>
    <automated>.venv/Scripts/python -m semantic_layer scan --json; test $? -eq 0</automated>
  </verify>
  <done>
    20 题按五类分布写完，C2 靶子已实测确实不在语义层内，合成夹具已标注且字段前缀合规，
    尚未冻结（`frozen_at` 为空）。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 评测运行器与 fail-closed 冻结校验</name>
  <files>
    eval/run.py,
    eval/testdata/sample-suite/,
    tests/test_eval_runner.py
  </files>
  <read_first>
    - EVAL_CASES.md §2 冻结规则、§3.2 拒答题判定、§5 失败归因分层、§6 评测指标、§7 Phase 1 门
    - DECISIONS.md D-012（冻结后允许重跑、禁止改标准答案）
    - src/semantic_layer/resolve.py（`Registry` / `Refusal` / `RefusalCode`，01-01 产物）
    - src/semantic_layer/definition.py 的 `normalize_row`（夹具行数据的归一化入口）
    - docs/agent/poc-01/SHA256SUMS（冻结清单的格式）
    - CLAUDE.md §常用命令（`python -m eval.run --suite frozen-01 --report reports/` 的既定形态）
  </read_first>
  <behavior>
    - Test: `verify_freeze` 在哈希一致时返回 True
    - Test: 篡改一个题面文件一个字节后 `verify_freeze` 返回 False
    - Test: `SHA256SUMS` 缺失时 `verify_freeze` 返回 False（不是抛未捕获异常，也不是当作通过）
    - Test: 冻结校验失败时 `main()` 以非零退出码结束且**不执行任何题目**（fail-closed）
    - Test: 对一道期望拒答且实际拒答且理由码正确的题，判定为 PASS
    - Test: 对一道期望拒答但实际给出了答案的题，判定为 FAIL 且归因含「口径层」
    - Test: 对一道期望拒答且实际拒答但理由码不符的题，判定为 FAIL 且归因含「口径层」
    - Test: 因**构造错误**作废的题移出分母，并在报告中单独列出（EVAL_CASES §2.2，2026-08-15 修订）
    - Test: 构造错误作废率 > 10% 时判整批构造失败，`main()` 非零退出（§2.2）
    - Test: C2 存活题数 < 3 时，即使拒答准确率为 100% 也不认为该门成立（§7 Phase 1 门第 2 条）
    - Test: `--category` 只跑指定类别，未跑的类别在报告中标为 `NOT_RUN` 而不是 PASS
  </behavior>
  <action>
    实现 `eval/run.py`，命令行形态与 `CLAUDE.md` 已登记的一致：
    `--suite`（套件目录名，如 `frozen-01`）、`--category`（可选，限定 C1–C5 之一）、
    `--report`（可选，输出目录）。

    `verify_freeze(suite_dir)` 是运行的前置门：读取套件目录下的 `SHA256SUMS`，
    逐个校验 `cases/` 与 `fixtures/` 下文件的 SHA-256。**任一不符、清单缺失、文件缺失**
    都返回 False，`main()` 随即非零退出且不执行任何题目。这条 fail-closed 是 D-012 的物理保障：
    改了题面就跑不了评测，而不是跑完了才发现题面被改过。

    判定逻辑严格按 `EVAL_CASES.md` §3.2：期望拒答的题，拒答且理由码正确记 PASS；
    拒答但理由错记 FAIL 并归因口径层；强行作答记 FAIL 并归因口径层，
    **即使数值碰巧正确也算失败**。归因按 §5 的四层（检索 / 口径 / 计算 / 表达）打标签，
    允许多标签，不允许「未知」；并按 §5.1，归因**不改变分数**——分母永远是全部题目，
    含 `VOIDED` 题，不因归因而裁剪。

    Phase 1 的执行范围要在代码里显式表达而不是靠人记：C1 与 C3 需要数值执行路径、
    C4 需要准则检索、C5 需要图谱，这些在 Phase 1 均不具备。因此未在本阶段执行的类别
    在报告中标为 `NOT_RUN`，**绝不标为 PASS**——把没跑的当通过是评测报告最常见的谎。
    报告同时输出 §6 的指标名，未具备条件的指标标 `N/A`。

    `eval/testdata/sample-suite/` 是**测试专用**的极小套件（2 题 + 自己的 `SHA256SUMS`），
    供 `tests/test_eval_runner.py` 驱动全部行为用例。测试**不得**依赖 `eval/frozen-01/`，
    也不得在测试中修改它——冻结套件只读。

    报告输出目录默认在 `eval/runs/`（`.gitignore` 已忽略），`--report` 可指向 `reports/`（同样已忽略）。
    评测结果不进版本控制，问题集进版本控制，这与 `.gitignore` 现有条目一致。
  </action>
  <verify>
    <automated>.venv/Scripts/python -m pytest tests/test_eval_runner.py -q</automated>
    <automated>.venv/Scripts/python -m eval.run --suite testdata/sample-suite --category C2</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/Scripts/python -m pytest tests/test_eval_runner.py -q` 全绿，失败数为 0。
    - 在 `eval/testdata/sample-suite/` 上篡改任一题面文件一个字节后运行 `eval.run`，
      退出码非 0，且 stdout 中不出现任何题目的判定结果（fail-closed 生效，未执行即退出）。
    - 报告中未执行的类别标记为 `NOT_RUN`，`grep -c 'NOT_RUN' <报告文件>` 输出 ≥ 1，
      且该报告中不存在把未执行类别记为 PASS 的行。
    - `tests/test_eval_runner.py` 中不出现路径 `eval/frozen-01`（测试与冻结套件隔离）。
    - `git status --porcelain eval/frozen-01/` 输出为空——本任务未触碰冻结套件。
  </acceptance_criteria>
  <done>
    评测运行器可用，冻结校验 fail-closed，拒答判定与失败归因按 EVAL_CASES 的规则实现，
    未执行的类别标 `NOT_RUN` 而非 PASS。
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>是否冻结 frozen-01 问题集</decision>
  <context>
    冻结是本阶段唯一**不可逆**的动作。按 D-012：
    冻结后允许重跑评测、改进实现、追加新批次问题（新批次单独编号）；
    冻结后**禁止**修改已冻结问题的标准答案或判定规则来迁就模型表现；
    若发现某题标准答案确实有误，**作废该题并公开记录，不修改后重用**，作废题仍留在分母里标 `VOIDED`。

    此刻冻结的成本是本项目生命周期里最低的：仓库内没有模型、没有 Agent，
    C2 判定由确定性解析器完成。「在看到任何模型输出之前」这个前提天然成立，
    往后每推进一个阶段，这个前提就更难干净地宣称一次。

    需要人复核的具体判断（只有人能答）：
    其一，20 题的题面是否覆盖了你真正关心的口径分歧，还是凑数量凑出来的；
    其二，C3 用合成夹具而非真实年报，这个取舍你是否接受——
    接受意味着 Phase 1 的数值结论只证明计算逻辑，不证明数据抽取；
    其三，C2 的四个靶子是否都是**真实的**语义层缺口，有没有哪个是为了好拒答而挑的软柿子。
  </context>
  <options>
    <option id="freeze">
      <name>冻结</name>
      <pros>在无模型输出的状态下冻结，D-012 的前提最干净；Phase 2 的 H2 复核实验有了不可事后修改的靶子</pros>
      <cons>不可逆。此后发现题目有问题只能作废并记录，不能修改后重用</cons>
    </option>
    <option id="revise-then-freeze">
      <name>先改题再冻结</name>
      <pros>此刻改题零成本，冻结后改题的成本是作废整题</pros>
      <cons>推迟冻结时间；须明确给出改哪几题、怎么改，不接受「再看看」</cons>
    </option>
    <option id="expand-c3-to-real-report">
      <name>C3 改用真实年报数据后再冻结</name>
      <pros>数值结论同时覆盖真实数据；与 D-013「年报 PDF 原文为权威源」更贴合</pros>
      <cons>Phase 1 无 PDF 抽取能力（D-013 已记录 `extract_financial_statements()` 撑不住），须人工转录，且转录错误无从机械发现；与 NFR-05「无网络可完成评测」冲突</cons>
    </option>
  </options>
  <read_first>
    - eval/frozen-01/cases/ 下全部 20 个题面文件
    - eval/frozen-01/fixtures/synthetic-01.yaml
    - EVAL_CASES.md §2 冻结规则全文
    - DECISIONS.md D-012 与 D-013
    - docs/agent/phase-01/open-questions.md（确认现金循环周期确为真实缺口）
  </read_first>
  <acceptance_criteria>
    - 人工回复中明确选定 `freeze` / `revise-then-freeze` / `expand-c3-to-real-report` 之一。
    - 选 `freeze` 时，回复须包含一句确认：截至此刻**未曾看到任何模型对本题集的输出**。
      这句确认逐字抄进 `FREEZE.md`，作为 D-012 前提成立的书面记录。
    - 选 `revise-then-freeze` 时，须逐题给出改哪几题、改成什么，不接受「再看看」。
  </acceptance_criteria>
  <resume-signal>回复 "freeze: 确认未见任何模型输出" / "revise-then-freeze: &lt;逐题修改&gt;" / "expand-c3-to-real-report"</resume-signal>
</task>

<task type="auto">
  <name>Task 3: 执行冻结并跑通 C2 类（拒答准确率 100%）</name>
  <precondition>上一个 checkpoint:decision 已由人工回复 `freeze`，且回复中含「未曾看到任何模型输出」的确认。未取得该确认不得执行本任务——冻结在前提不成立时执行，等于伪造 D-012 的合规记录。</precondition>
  <files>
    eval/frozen-01/cases/*.yaml,
    eval/frozen-01/FREEZE.md,
    eval/frozen-01/SHA256SUMS
  </files>
  <read_first>
    - docs/agent/poc-01/FREEZE.md（冻结记录的格式与措辞基准，本次照其结构）
    - docs/agent/poc-01/SHA256SUMS（清单格式：相对路径 + 哈希）
    - EVAL_CASES.md §2 与 §7 Phase 1 门
    - .gitattributes（`* text=auto eol=lf` —— 冻结哈希跨平台稳定的物理保障，不得改）
    - 上一个检查点的人工回复原文（其中的确认语要逐字抄进 FREEZE.md）
  </read_first>
  <action>
    先取当前 UTC 时间戳，把它写进全部 20 个题面文件的 `frozen_at` 字段——所有题同一时间戳，
    表示同一批次冻结。随后生成 `eval/frozen-01/SHA256SUMS`，
    覆盖 `cases/` 下 20 个文件与 `fixtures/synthetic-01.yaml`，
    清单格式与 `docs/agent/poc-01/SHA256SUMS` 一致（相对路径 + 哈希，相对于套件目录）。
    **顺序不能反**：先填 `frozen_at` 再算哈希，否则清单校验立刻失效。

    写 `eval/frozen-01/FREEZE.md`，结构照 `docs/agent/poc-01/FREEZE.md`，至少含：
    冻结的 UTC 时间戳；冻结时的 git commit；题量与五类分布；
    人工检查点回复中「未曾看到任何模型对本题集的输出」的确认语**逐字**抄录；
    C3 采用合成夹具的取舍说明与其效力边界（只证明计算逻辑，不证明数据抽取）；
    本阶段实际执行的类别（C2）与仅冻结未执行的类别（C1 / C3 / C4 / C5）及各自原因；
    以及 D-012 的作废规则重述——发现题目有误时作废并记录，不修改后重用，作废题留在分母里标 `VOIDED`。

    冻结完成后运行 C2 类，取真实输出。ROADMAP Phase 1 成功标准第 3 条要求
    **C2 拒答准确率 = 100%，一个都不能猜**。若不是 4/4：不许改题面（已冻结），
    不许改判定规则（已冻结），按 CLAUDE.md 停止协议报告 EXPECTED / FOUND / IMPACT / OPTIONS，
    修的是语义层实现而不是题目。这正是冻结机制存在的意义。

    把 C2 的实际命令与真实输出（逐字，不摘要）记进 SUMMARY，
    供 01-08 写 `VERIFICATION.md` 时按「验收标准 → 验证方法 → 实际命令 → 真实输出 → 证据位置 →
    PASS / FAIL / UNVERIFIED」的格式引用。
  </action>
  <verify>
    <automated>cd eval/frozen-01 &amp;&amp; sha256sum -c SHA256SUMS</automated>
    <automated>.venv/Scripts/python -m eval.run --suite frozen-01 --category C2 --report eval/runs/</automated>
    <automated>cd docs/agent/poc-01 &amp;&amp; sha256sum -c SHA256SUMS</automated>
  </verify>
  <acceptance_criteria>
    - `cd eval/frozen-01 && sha256sum -c SHA256SUMS` 输出 21 行 OK（20 题 + 1 夹具），退出码 0。
    - `.venv/Scripts/python -m eval.run --suite frozen-01 --category C2 --report eval/runs/` 退出码 0，
      报告中 C2 类 4 题全部 PASS，拒答准确率为 `100%` 或 `4/4`。
    - 全部 20 个题面文件的 `frozen_at` 为同一个 UTC 时间戳，且非空。
    - `eval/frozen-01/FREEZE.md` 中逐字包含人工检查点回复里的「未曾看到任何模型输出」确认语，
      并含冻结时的 git commit 短哈希。
    - `FREEZE.md` 中 C1 / C3 / C4 / C5 各自标注为仅冻结未执行，并各给出原因。
    - `cd docs/agent/poc-01 && sha256sum -c SHA256SUMS` 仍 5 行 OK，退出码 0。
    - SUMMARY 中记录了 C2 运行的实际命令与逐字真实输出，不是「4/4 通过」这样的转述。
  </acceptance_criteria>
  <done>
    问题集已冻结并带 UTC 时间戳与 SHA-256（AC-04 / D-012）；
    C2 类拒答准确率 100% 有真实输出为证（ROADMAP Phase 1 成功标准 3）；
    未执行的类别在冻结记录中明确标注，未被当作通过。
  </done>
  <reversibility rating="one-way">
    冻结后标准答案与判定规则不可修改，只能作废整题并公开记录（D-012，无反转触发条件）。
    前置的 checkpoint:decision 就是为这条不可逆性设的。
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 题面与标准答案 → 冻结清单 | 冻结后任何改动都是对实验可证伪性的破坏，这是 D-012 保护的核心资产 |
| 合成夹具 → 结论表述 | 合成数据若被表述为真实年报数据，比数据本身的问题严重得多 |
| 评测运行 → 报告 | 未执行的类别若被记为 PASS，报告即为谎 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-33 | Tampering | `eval/frozen-01/` 冻结后被改动 | critical | mitigate | `verify_freeze` 是运行前置门，哈希不符即非零退出且不执行任何题目；`.gitattributes` 的 LF 入库保证哈希跨平台稳定 |
| T-01-34 | Spoofing | 合成夹具被表述为真实年报数据 | critical | mitigate | 夹具首行与 `FREEZE.md` 双处标注为合成；验收断言首行标注存在 |
| T-01-35 | Spoofing | 未执行的类别被记为 PASS | high | mitigate | 运行器对未执行类别输出 `NOT_RUN`；验收断言报告中存在 `NOT_RUN` 且不存在把未执行类别记 PASS 的行 |
| T-01-36 | Repudiation | 冻结前提无书面记录 | high | mitigate | 人工确认语逐字抄进 `FREEZE.md`，并记录冻结时的 git commit |
| T-01-37 | Tampering | C2 不达标时回头改题面 | critical | mitigate | Task 3 明令不许改题面与判定规则，走停止协议；测试与冻结套件隔离（测试不得引用 `eval/frozen-01`） |
| T-01-38 | Spoofing | C2 靶子挑软柿子 | medium | mitigate | 四个靶子逐一实测 `resolve` 返回 `METRIC_NOT_DEFINED`；其中现金循环周期来自 01-06 的真实能力缺口记录 |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | 本计划不新增依赖；如需新增则先回到 01-01 Task 0 的 package-legitimacy gate |
</threat_model>

<verification>
- `cd eval/frozen-01 && sha256sum -c SHA256SUMS` 21 行 OK
- `.venv/Scripts/python -m eval.run --suite frozen-01 --category C2 --report eval/runs/` 退出码 0，C2 4/4 PASS
- `.venv/Scripts/python -m pytest tests/test_eval_runner.py -q` 全绿
- 篡改题面后 `eval.run` 非零退出且未执行任何题目
- `cd docs/agent/poc-01 && sha256sum -c SHA256SUMS` 5 行 OK
</verification>

<success_criteria>
- 问题集 ≥ 20 题、五类分布符合 EVAL_CASES §3、已冻结并带 UTC 时间戳与 SHA-256（AC-04 / SC-4）
- C2 类拒答准确率 100%，有逐字真实输出为证（SC-3 / AC-02）
- 冻结校验 fail-closed，改了题面就跑不了评测
- 未执行的类别标 `NOT_RUN`，合成夹具标注为合成，两处都不粉饰
</success_criteria>

<output>
Create `.planning/phases/01-semantic-layer/01-07-SUMMARY.md` when done
</output>
