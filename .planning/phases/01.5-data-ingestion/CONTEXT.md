# Phase 1.5 — 数据接入层 data-ingestion · CONTEXT

> **本文件的来源与效力**（先写清楚，否则它会被误当成一次 discuss-phase 的产物）：
>
> 本阶段**没有**跑 `/gsd-discuss-phase`。本文件是把**已经记录在权威文档里的决策与已实测的事实**
> 汇总到 GSD 要读的位置，**不新增任何决策**。
> 每一条都标了出处，可回溯。§4 列出的是**尚未裁决**的问题，
> **planner 不得替操作者裁决它们**，只能把它们排进计划或标为阻塞。
>
> 权威顺序（冲突时上位覆盖下位）：`PROJECT_SPEC.md` > `DECISIONS.md` > `EVAL_CASES.md` > `.planning/`
>
> 建立于 2026-08-16。

---

## 1. 阶段目标与成功标准

**Goal**：把年报 PDF 变成语义层能消费的结构化字段，并让指标真的算得出数值。

**为什么必须有这一阶段**：Phase 1 结束时 20 份口径定义全部合规，但**没有任何真实数据可喂**，
且 `formula` 只是字符串、受限 DSL 刻意不含算术，**系统算不出任何一个数**。
Phase 2 的 H2 复核实验要求复核者看着**真实数字与证据链**判断——没有真数据，那个实验做不了。

八条成功标准见 `.planning/ROADMAP.md` Phase 1.5 节，此处不复制（避免第二份权威）。
其中 **SC-7（阻断式勾稽校验）与 SC-8（验收看取到的值而非命中数）是 2026-08-16 新增的**，
理由分别在台账 N-13 与 A-4。

---

## 2. 已锁定的决策（planner 必须遵守，不得重新讨论）

| 决策 | 内容 | 出处 |
|---|---|---|
| **D-013** | 财务数值**以年报 PDF 原文为准**；AKShare 等二手源降级为**对照**，不作权威源。不一致时把「不一致」本身记入证据链，**不静默取其一** | `DECISIONS.md` |
| **D-014** | **禁止 AGPL 组件**。主库 **pdfplumber（MIT）**，备选 pypdfium2。`PyMuPDF` / `fitz` 禁入任何依赖（含间接） | `DECISIONS.md`；已由 `scripts/verify_deps.py` 机器强制 |
| **D-010** | 仅公开数据，不可协商。任何雇主/客户数据一律禁止，包括脱敏后的 | `AGENTS.md` / `DECISIONS.md` |
| **D-012** | `eval/frozen-01/` 与 `docs/agent/poc-01/` 是冻结产物，**一个字节都不许动** | `DECISIONS.md` |
| **台账 N-3** | **抽完就删 PDF**，默认不保留；落库的是「结构化行 + PDF 的 SHA-256 + 巨潮源 URL + 页码」。`--keep-pdf` 是显式开关 | `OPEN-ITEMS.md` |
| **台账 U-03** | 内核与壳的边界**不得在本阶段提前拍板**，等 Phase 2 的 `ARCHITECTURE.md` | `OPEN-ITEMS.md` / `DECISIONS.md` |

**环境**：Python 3.11+（`.venv` 由本机 3.14 建）。**默认 `python` 是 3.8.5，不可用于生产代码**
——它装着 pdfplumber，只允许用于探测。新增依赖必须过 `scripts/verify_deps.py`（含许可证禁列）。

---

## 3. 已实测的事实（不是假设，planner 可直接依赖）

全部于 2026-08-16 在**贵州茅台 2023 年报**（143 页，巨潮下载）上用 **pdfplumber 0.11.5 单库**跑出。
探测代码在 scratchpad，**未进仓库**。

### 3.1 判据已达成（台账 A-4）

| 项 | 结果 |
|---|---|
| 章节定位 | **8 张报表标题全部定位**，合并与母公司**干净分离** |
| 行项目命中 | **15/15**（行在有值 11 ｜ 行在格子空 4 ｜ 整行不存在 0） |
| 勾稽校验 | 资产总计 = 负债合计 + 所有者权益合计，**差额 0.00，通过** |
| y 容差敏感性 | 2.0–5.0 之间结果稳定 |

字段目标集是**用脚本从 `metrics/` 的 20 份定义取出的**，不是凭印象：
全部 30 个 `source_fields` = bs 15 / is 8 / cfs 2 / notes 4 / kpi 1，与 SC-2 的「30 个字段」对得上。

### 3.2 三条必须设计进去的失效模式（台账 A-6）

**都是实测撞出来的，第一版探测因此直接零命中。**

1. **表标题印在上一页的页脚位置**，数据从下一页开始
   ⇒ 任何「标题与锚点必须同页」的规则必然落空。
2. **合并表与母公司表的边界在页内**（p61 上半是合并表尾巴，下半是母公司表标题）
   ⇒ 按「页」定位必然混表。**这正是 D-013 记录的 cninfo
   `extract_financial_statements()`「最后一个匹配的表静默胜出」的成因。**
   **正解**：表标题当作**带 y 坐标的锚点**，区间 = [本标题, 下一标题)，跨页且可页内切分。
3. **行项目标签折行，且数值行夹在两个标签片段之间**：
   ```
   归属于母公司所有者权益
   215,668,571,607.43 | 197,480,041,239.46
   （或股东权益）合计
   ```
   缝合规则：`标签行 → 纯数值行 → 标签行` 三行属同一行项目。

**附注编号列**：数值正则要求两位小数即可排除附注号——但这是**单样本结论**，
遇到列出整数金额的报表会失效，**按列位置剔除更稳妥**。

### 3.3 A-2 的回归数据已就位

茅台 2023 合并资产负债表，六个有息负债分项**全部印在报表上**：

| 字段 | 值 |
|---|---|
| `bs.short_term_borrowings` / `long_term_borrowings` / `bonds_payable` / `trading_financial_liabilities` | **行在，格子空** |
| `bs.non_current_liabilities_due_within_one_year` | 57,054,879.48 |
| `bs.lease_liabilities` | 266,636,234.04 |

**整行不存在的字段：0 个。** 按「格子空 = 0」有息负债率 ≈ **0.00119**，
可算且接近零，而当前定义按 `is_missing` **拒答**。
⇒ SC-3 的回归用例数据齐了。**定义侧不动，修在抽取层。**

### 3.4 下载链路（2026-08-15 实跑）

```text
1. GET  cninfo.com.cn/new/data/szse_stock.json      取 orgId
2. POST cninfo.com.cn/new/hisAnnouncement/query     category=category_ndbg_szsh
3. GET  static.cninfo.com.cn/{adjunctUrl}           PDF，**不需要 cookies**
```
标题筛选必须排除摘要与英文版。一份茅台年报 143 页 = 3.5 MB。

---

### 3.5 `notes.business_combination_type` 可抽出 —— 且暴露了一处真实漏报（2026-08-21 实测）

**探针**：pdfplumber 0.11.5，茅台 2023 年报全 143 页逐页 `extract_text()`，exit 0。
探测代码在 scratchpad，**未进仓库**。

**能抽出。** p120–121「九、 合并范围的变更」是证监会披露模板的**固定六项**，
每项自带机器可读的适用性标记，`√` 与 `□` 都被文本路径干净抽出：

```text
1、非同一控制下企业合并      □适用 √不适用
2、同一控制下企业合并        □适用 √不适用
3、反向购买                  □适用 √不适用
4、处置子公司                □适用 √不适用
5、其他原因的合并范围变动    √适用 □不适用  → 正文叙述（定制营销公司清算注销）
6、其他                      □适用 √不适用
```

**必须设计进去的陷阱**：p77 另有「6. 同一控制下和非同一控制下企业合并的会计处理方法」
——那是**会计政策**，不是交易事实。只按关键词匹配会把政策段落误判成「发生了企业合并」，
与 cninfo「最后一个匹配的表静默胜出」同型。定位必须锚在「九、合并范围的变更」章节区间内。

**诚实限定**：单样本，且茅台本期 1–4 全为「不适用」，**没有一个真发生企业合并的正样本**。
已证明的是「字段位置与取值域可机械定位」，**未证明「发生合并时能抽对类型」**。
Phase 1.5 须补一家有真实企业合并的公司年做正样本回归。

**顺带查实一处漏报**（详见台账 A-8）：真实披露是**六个可同时成立的复选项**，
而 `notes.business_combination_type` 被当成单值标量（同一控制 / 非同一控制 / 无）。
茅台 2023 的实际情形是「无企业合并，但合并范围因清算注销而变动」——
按 `metrics/minority_interest_share.yaml:59` 的 trigger
`notes.business_combination_type != "无"`，`scope_change` **不会置位**，
而 `metrics/_flags.yaml:38` 对该 flag 的描述是「合并范围发生变动」。**真实年报上的假阴性。**

---

## 4. ~~尚未裁决的问题~~ → **已全部裁决（2026-08-21）**

原四问 + RESEARCH 新提的两问，共六个决策点，已由操作者逐条裁决并落成决策记录。
**planner 应当遵守它们，不得重新讨论。**

| 原编号 | 问题 | 裁决 | 记录 |
|---|---|---|---|
| §4.1 | 抽取器住在哪里 | `src/extractor/` 与语义层平级 + **单向依赖门禁**（不构成 U-03 边界决策） | D-015 |
| §4.2 | 映射表用什么形式 | 按命名空间拆五个文件；**标签 = 有序片段序列，逐字匹配**；AKShare 另立 | D-016 |
| §4.3 | 数值执行层怎么建 | **自建封闭算术解析器**，不引三方求值库，不动 `dsl.py` | D-017 |
| §4.4 | `notes.business_combination_type` 能否真抽出 | **能**（实测，见 §3.5）；但查实一处真实漏报，见台账 A-8 | — |
| RESEARCH | 勾稽校验粒度 | **批次级闸门** + 计算层在**读入边界**拒答，封闭理由码 | D-018 |
| RESEARCH | `page` 字段类型 | `page`（数值行所在页）+ `anchor_page`（表标题锚点所在页），**两个整数** | D-019 |

**仍然不得在本阶段拍板的**：U-03（内核与壳的边界）、U-01（证据链完整字段集）。
D-015 与 D-019 都明写了「本条不构成对它们的提前拍板」。

---

## 5. 本阶段明确不做

- 不做 OCR 兜底（先只处理文本型 PDF）
- 不做全市场批量（先单公司单年）
- 不引入向量检索（Phase 3）
- 不动 `metrics/` 的 20 份定义（A-2 修在抽取层，**定义侧不动**）
- 不碰 `eval/frozen-01/` 与 `docs/agent/poc-01/`（D-012）
- 不做界面（D-006：Web 在 Phase 2）

---

## 6. 外部项目的教训（有原文依据，`references/` 可查）

- **cninfo 完全没有勾稽校验**（读完 `pipeline.py`/`metrics.py`/`utils.py`/`downloader.py`/6 个测试）。
  根因是**抽取路径与计算路径不相交**：`pipeline.py:758` 硬编码 `'financial_statements': {}`，
  计算侧从不读它——**它抽了报表，却用 AKShare 的指标算结论**。
  ⇒ 本阶段必须在架构上保证这两条路径是**同一条**。
- **cninfo 的文本路径根本没关联页码**（`pdf_parser.py:94-98` 裸拼接）
  ⇒ **在结构上就不可能产出页级证据链**。页码必须在抽取第一步绑定，事后补不回来。
- **cninfo `pdf_parser.py:302` 无条件把第 0 行当表头**（`pd.DataFrame(table[1:], columns=table[0])`），
  续页表格会**静默吃掉一行真实数据**，而其文档宣称「支持跨页表格识别」。
  ⇒ 表头识别必须是**显式判定 + 留证**。
- **「命中数」这个指标本身能被标签前缀匹配骗过**——我自己中过一次（A-4）。⇒ SC-8。

---

## 7. 落地待办里必须并进本阶段设计的条目（T-4，2026-08-24）

**来源**：`docs/agent/LANDING-BACKLOG.md` §5（82 条去重登记册）。
本节是它的**子集**——只列影响抽取器 / 映射表 / 计算层的，
其余按目的地分批做，**不是本阶段的前置**。

> **为什么要有这一节**：planner 读的是 CONTEXT，不是登记册。
> 台账 N-34 量过一次账：hello-agents 第 12 章 2743 行早已读完，
> 而 `EVAL_CASES.md` 里的外部证据命中数当时是 **0**。
> **读了不并进计划，等于没读。**

### 7.1 抽取器与证据记录

| 编号 | 要求 | 出处 |
|---|---|---|
| `L-35` | 抽取记录的类别由**显式 `kind` 字段**判定，**禁止**「看起来像三大表某一行」这类形状启发式反推 | notes-part3 `C-A9` |
| `L-45` | **「被截断」必须报 `PARTIAL`，不得报 `SUCCESS`**——报成功则证据链里不会出现 `truncation_stats`，`AC-05` 的「字段齐全率 100%」会在一个**假成功**上判过 | framework-tools `FT-A3` |
| `L-55` | 留痕的**采集点放在包装层**，不放在每个抽取函数内部。理由是实测：hello-agents 把截断放在调用点，结果 **6 个调用点只覆盖了 2 个** ⇒ **凡是要求「每次都做」的事，就不能放在调用点** | framework-tools `FT-A4` |
| `L-2` | 证据指针必须携带**取数口径三元组**（抽样方式 / 排序键 / 截断位置）。年报抽取里「取前 N 行 / 按某列排序 / 页内截断」与出处的病例逐项同构 | notes-part3 `C-A1` |

### 7.2 映射表（D-016 的补充节）

| 编号 | 要求 | 出处 |
|---|---|---|
| `L-36` | 映射表加载时**零命中的规则即加载失败**。单一权威表里，一条匹配不到任何字段的规则只可能是写错了 | notes-part3 `C-N5` |
| `L-37` | **派生集合不得与权威表并列存储**，只能在读入时按需物化。**「并列存储」包括写进文档**——文档也是一份派生数据，且是唯一没有机制校验的那份 | ch06 `X6-7` ≡ `S6-3` |

### 7.3 计算层与闸门

| 编号 | 要求 | 出处 |
|---|---|---|
| `L-34` | 口径类开关（口径版本 / 抽样策略 / 单位换算）**不设默认值**，缺失即在**读入边界** fail-closed，而不是取一个「合理默认」 | notes-part3 `C-A6` |
| `L-38` | 口径版本号必须**随抽取记录一起冻结**；复核时按记录里的版本求值，**不是按当前配置**。否则同一条证据在两个时间点复核会得到不同结果，`D-012` 的冻结就失去意义 | notes-part3 `C-N4` |
| `L-13` | **重试不得跨闸门批次**。`D-018` 的闸门是批次级，一批里出现证据写入失败时必须闩住这一批，否则重试成功的部分与失败的部分会混进同一次回答 | notes-part3 `C-N3` |
| `L-12` | 证据对齐失败时**不得部分复用旧指针**——未匹配、冲突映射、长度变了的数组，一律用新值 | notes-part3 `C-A17` |

### 7.4 拒答与降级

| 编号 | 要求 | 状态 |
|---|---|---|
| `L-47` | `RefusalCode` 新增 **`UNAVAILABLE`**（口径服务或数据源不可达），抽取层的下载/取数失败**返回 `Refusal` 而不是抛异常**，且该拒答进证据链、被 `AC-05` 计入 | **决策已落（`D-022` 判据 1–3），实现是本阶段的活**。已核：枚举当前仍是 7 支 |
| `L-9` | `Refusal` 必须携带**责任方标识**（哪个数据源 / 哪条口径规则拒的），不能只说「证据不足」 | OPEN |

### 7.5 评测侧已就位，本阶段要满足

这三条**已经落地**（`EVAL_CASES` §3.3），列在这里是因为**抽取器的设计必须能满足它们**：

- **`J-6`** 证据 id 的**字面存在性**必须可机械检查 ⇒ 抽取记录里的 `field_id` / `page` /
  表格锚点必须是**可被逐字比对的稳定标识**，不能是运行时拼出来的临时串。
- **`J-7`** 引用截断 / 抽样 / 重排来源时必须给出取数口径三元组 ⇒ 与 `L-2` 同一件事的两侧。
- **`J-5`** 闸门类断言必须配负控制 ⇒ 勾稽闸门（`D-018`）与列绑定（`D-016` 的 `column_header`）
  的验收，**必须各配一条「去掉它就会失败」的用例**，且**红的原因要正确**。

### 7.6 明确**不并进**本阶段的（`BLOCKED`，理由见登记册 §3.4）

登记册 §5.F 的 8 条被 `U-01`（证据链字段集未定）与 `U-03`（内核与壳的边界）挡着。
**把它们当待办催办，会导致在数据不足时提前拍板证据链字段集——那正是 `U-01` 存在的理由。**
本阶段产出的实测数据是解开 `U-01` 的输入，不是它的消费者。
