---
phase: 01.5-data-ingestion
plan: 02
type: execute
wave: 2
depends_on: ["01.5-01"]
files_modified:
  - src/extractor/locate.py
  - scripts/probe_fields.py
  - docs/agent/phase-01.5/PROBE-14.md
  - docs/agent/phase-01.5/PROBE-COMBINATION.md
  - docs/agent/OPEN-ITEMS.md
  - tests/fixtures/maotai_2023_probe_rows.json
  - tests/fixtures/combination_positive_probe_rows.json
autonomous: false
requirements: [SC-1, SC-2, SC-8, AC-10]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - 14 个此前零实测的字段（is 8 / cfs 2 / notes 3 / kpi 1）逐个有一条「取到的值 + 页码 + 与年报原文核对结论」的记录
    - 探测结论记的是取到的值，不是命中数——报告里不存在任何只有 X/Y 命中数而没有值的字段行（SC-8）
    - 合并利润表、合并现金流量表的标题锚点被定位，且与各自的母公司表分离
    - kpi.roe_weighted_average_disclosed 的位置被确认在「主要会计数据和财务指标」章节，不在三张报表内
    - notes.reporting_period_months 的取值来源被确认（它根本不在三张报表里，此前无人验证过它能不能抽）
    - 存在一家有真实企业合并的公司年作为正样本，「九、合并范围的变更」六个子项中至少一项为「适用」且属于 1–4 项之一
    - 会计政策段落（茅台 2023 p77「同一控制下和非同一控制下企业合并的会计处理方法」）不被误判为交易事实
    - 探测脚本在仓库内，任何人可用一条命令重跑，不依赖 scratchpad（A-10）
    - statement: SC-2 的「≥24/30」门槛在逐字段核对结果出来后由操作者显式确认或按 EVAL_CASES §2.2 调整，不由执行者自行放宽
      verification: backstop
  artifacts:
    - scripts/probe_fields.py
    - docs/agent/phase-01.5/PROBE-14.md
    - docs/agent/phase-01.5/PROBE-COMBINATION.md
    - tests/fixtures/maotai_2023_probe_rows.json
    - tests/fixtures/combination_positive_probe_rows.json
  key_links:
    - PROBE-14.md 的逐字段结论 → 01.5-03 五份映射表的 label_fragments（探测出来的原文标签就是映射表要逐字写进去的东西）
    - PROBE-COMBINATION.md 的正样本 → D-020 判据 3 的回归用例输入
    - locate.find_statement_anchors → 利润表 / 现金流量表 / 附注 / 主要会计数据章节的锚点覆盖
---

<objective>
把 14 个零实测字段探一遍，并补一家有真实企业合并的公司年做正样本。

**为什么这件事必须排在横向扩展之前**：SC-2 要求 30 个字段里 ≥ 24 个能抽出，
而实测覆盖只有 16/30（`bs` 15 + `notes.business_combination_type`）。
`is` 8 / `cfs` 2 / `notes` 3 / `kpi` 1 共 **14 个字段零实测**，而 ≥24 的门槛
**只允许失手 6 个**。这 14 个还不同质：`notes.reporting_period_months` 根本不在三张报表里，
`kpi.roe_weighted_average_disclosed` 在「主要会计数据和财务指标」章节。
**它们不在 01.5-01 已经跑通的那条定位链路上。** 在可行性是零证据的情况下先写映射表与抽取器，
是在拿 SC-2 的验收赌运气。

**本计划不写抽取器。** 它只做三件事：探、记、判。探测脚本进仓库（A-10 的教训是
探测语料反复从磁盘消失），记的是**取到的值**而不是命中（SC-8），
判的是 SC-2 的门槛还立不立得住——那一判由操作者做，不由执行者做。

Purpose: 让 SC-2 的 ≥24 有真实证据支撑，而不是建在半个样本上；
同时补上 D-020 判据 3 缺的那个正样本。
Output: `scripts/probe_fields.py`、两份逐字段列值的探测报告、两份可回放的抽取产物固件、
一个关于 SC-2 门槛的显式裁决。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01.5-data-ingestion/CONTEXT.md
@.planning/phases/01.5-data-ingestion/01.5-01-PLAN.md
@CLAUDE.md
@rules/failure-modes.md
@rules/commands.md
@DECISIONS.md
@EVAL_CASES.md
@docs/agent/OPEN-ITEMS.md
@src/extractor/locate.py
@src/extractor/download.py
@scan_rules.yaml
</context>

## Artifacts this phase produces（本计划部分）

**新增脚本**

- `scripts/probe_fields.py` —— CLI：`--stock` `--year` `--namespace {is,cfs,notes,kpi}` `--out`

**新增函数（`src/extractor/locate.py` 扩展）**

- `locate.find_section_anchors(pdf, titles)` —— 章节锚点定位的通用形态，供附注与「主要会计数据和财务指标」复用
- `locate.CHECKBOX_APPLICABLE` / `locate.CHECKBOX_NOT_APPLICABLE` —— 「√适用」「□不适用」两个标记常量
- `locate.read_applicability(pdf, anchor)` —— 读披露模板的适用性复选项

**新增文档**

- `docs/agent/phase-01.5/PROBE-14.md`
- `docs/agent/phase-01.5/PROBE-COMBINATION.md`

**新增固件**

- `tests/fixtures/maotai_2023_probe_rows.json`
- `tests/fixtures/combination_positive_probe_rows.json`

**修改的既有文档**

- `docs/agent/OPEN-ITEMS.md` —— A-10 与 A-6 的判据状态更新

<tasks>

<task type="auto">
  <name>Task 1：探测 is 的 8 个字段与 cfs 的 2 个字段</name>
  <files>src/extractor/locate.py, scripts/probe_fields.py, docs/agent/phase-01.5/PROBE-14.md, tests/fixtures/maotai_2023_probe_rows.json</files>
  <read_first>
  - `src/extractor/locate.py`（01.5-01 建的锚点定位与行重组，本任务直接复用，不重写）
  - `src/extractor/download.py`（取 PDF 的入口）
  - `.planning/phases/01.5-data-ingestion/CONTEXT.md` §3.1 / §3.2（8 张报表标题全部定位；三条失效模式；y 容差 2.0–5.0 稳定）
  - `metrics/gross_profit_margin.yaml`、`metrics/period_expense_ratio.yaml`、`metrics/revenue_growth_yoy.yaml`、`metrics/net_profit_margin_attributable.yaml`、`metrics/return_on_total_assets.yaml`、`metrics/accounts_payable_turnover_days.yaml`、`metrics/free_cash_flow.yaml`、`metrics/operating_cash_flow_ratio.yaml`（这 10 个字段的 `line_item` 原文，探测要拿它去对）
  - `.planning/ROADMAP.md` Phase 1.5 的 SC-2 计数口径（「能抽出」= 取到的值经逐个人工核对正确，不是标签命中）与 SC-8
  - `docs/agent/OPEN-ITEMS.md` A-4（`bs.total_equity` 匹配到章节小标题而数值为空的真实反例——同型错误在利润表上要主动找）
  - `scan_rules.yaml`（`forbidden_tracked_file_types` 含 `.pdf`：固件只能是 JSON）
  </read_first>
  <action>
  写 `scripts/probe_fields.py`，参数是 `--stock` `--year` `--namespace` `--out`。它调 `src/extractor/download.py` 取 PDF、调 `src/extractor/locate.py` 定位与重组，然后**把某个命名空间下的候选行连同 x/y 坐标、页码、各列取值一并 dump 成 JSON**。它不做映射、不做判定——判定是人做的，脚本只负责把原文摆出来。这一点很重要：脚本一旦开始替人判「这一行是不是那个字段」，SC-8 就失守了。

  用它探这 10 个字段，逐个记录：`is.operating_revenue_current`、`is.operating_revenue_prior_as_presented`、`is.operating_cost`、`is.selling_expense`、`is.administrative_expense`、`is.financial_expense`、`is.net_profit`、`is.net_profit_attributable_to_parent`、`cfs.net_cash_flow_from_operating_activities`、`cfs.cash_paid_for_fixed_intangible_and_other_long_term_assets`。

  有三个已知会出事的点，探测时要主动去撞而不是绕开：

  第一，`is.operating_revenue_current` 与 `is.operating_revenue_prior_as_presented` 是**同一行的两列**——本期与上期。这正是 A-9 说的「勾稽闸门在结构上抓不到整列取错」的地方，探测报告里必须把两列的表头文字原样记下来（茅台的利润表列头通常是「本期金额」「上期金额」，而资产负债表是「期末余额」「期初余额」——**两张表的列头文字不一样**，这一条是 `01.5-03` 写映射表时的直接输入）。

  第二，`is.net_profit` 与 `is.net_profit_attributable_to_parent` 是利润表底部两行相邻、标签高度相似的行；对照臂实测里两个模型在 Q-C1-001 上**错得逐字相同**，都取了含少数股东损益的净利润。整行逐字相等的匹配规则能不能分开这两行，本任务要给出证据，不是断言。

  第三，`cfs.cash_paid_for_fixed_intangible_and_other_long_term_assets` 的行项目标签很长（「购建固定资产、无形资产和其他长期资产支付的现金」），大概率折行，要确认三行缝合规则在现金流量表上同样成立。

  写 `docs/agent/phase-01.5/PROBE-14.md` 的前两节。每个字段一行，列出：字段 id ｜ 年报页码 ｜ 匹配到的行标签**原文**（含折行时的片段序列）｜ 列表头原文 ｜ **取到的值** ｜ 人工核对结论（正确 / 取错行 / 取错列 / 取不到）。**不要写「10/10 命中」这种汇总**——ROADMAP 的 SC-8 就是因为命中数被标签前缀匹配骗过一次才加上的。

  把 dump 出来的候选行存成 `tests/fixtures/maotai_2023_probe_rows.json`，供后续回归用例回放。PDF 本身不进仓库。

  探完把 `docs/agent/OPEN-ITEMS.md` 的 A-10 补一行状态：探测脚本已在仓库内，重跑是一条命令。
  </action>
  <verify>
    <automated>.venv/Scripts/python scripts/probe_fields.py --stock 600519 --year 2023 --namespace is --out /tmp/probe_is.json && .venv/Scripts/python scripts/probe_fields.py --stock 600519 --year 2023 --namespace cfs --out /tmp/probe_cfs.json && .venv/Scripts/python -m semantic_layer scan</automated>
  </verify>
  <acceptance_criteria>
  - `scripts/probe_fields.py` 存在，`.venv/Scripts/python scripts/probe_fields.py --help` 退出码 0 且输出含 `--namespace`
  - `docs/agent/phase-01.5/PROBE-14.md` 中，以下 10 个字符串各自逐字出现至少一次：`is.operating_revenue_current`、`is.operating_revenue_prior_as_presented`、`is.operating_cost`、`is.selling_expense`、`is.administrative_expense`、`is.financial_expense`、`is.net_profit`、`is.net_profit_attributable_to_parent`、`cfs.net_cash_flow_from_operating_activities`、`cfs.cash_paid_for_fixed_intangible_and_other_long_term_assets`
  - 上述每个字段所在行都含一个形如 `\d{1,3}(,\d{3})*\.\d{2}` 的数值或显式写着「取不到」，不允许留空
  - `PROBE-14.md` 中记录了利润表的列表头原文，且该文字与资产负债表的「期末余额」不同（若相同，需在报告里显式说明为何相同）
  - `tests/fixtures/maotai_2023_probe_rows.json` 存在且是合法 JSON（`.venv/Scripts/python -c "import json,pathlib;json.loads(pathlib.Path('tests/fixtures/maotai_2023_probe_rows.json').read_text(encoding='utf-8'))"` 退出码 0）
  - `.venv/Scripts/python -m semantic_layer scan` 退出码 0 —— 它的 `forbidden_tracked_file_types` 已含 PDF 扩展名且 fail-closed，这既是 AC-10 也是「下载产物没进版本控制」的机械判据
  </acceptance_criteria>
  <done>利润表 8 个字段与现金流量表 2 个字段各有一条「取到的值 + 页码 + 核对结论」的记录；净利润与归母净利润这对易混行有明确证据说明能不能分开。</done>
</task>

<task type="auto">
  <name>Task 2：探测 notes 的 3 个字段、kpi 的 1 个字段与合并范围变更六复选项</name>
  <files>src/extractor/locate.py, scripts/probe_fields.py, docs/agent/phase-01.5/PROBE-14.md, tests/fixtures/maotai_2023_probe_rows.json</files>
  <read_first>
  - `.planning/phases/01.5-data-ingestion/CONTEXT.md` §3.5（p120–121「九、 合并范围的变更」六个固定子项，`√` 与 `□` 都被文本路径干净抽出；以及 p77 会计政策段落这个必须避开的陷阱）
  - `DECISIONS.md` D-020 全文（`notes.consolidation_scope_change` 新字段的定义就是「六个子项中任一为适用」；三处可独立复核的原文行号）
  - `docs/agent/OPEN-ITEMS.md` A-8（真实假阴性的完整推导）
  - `metrics/net_profit_attributable_excl_nonrecurring.yaml`（`notes.nonrecurring_pl_net_attributable_to_parent` 的 `line_item` 原文）、`metrics/roe_weighted_average.yaml`（`kpi.roe_weighted_average_disclosed` 与「取披露值、禁止倒推」这条口径立场）
  - `src/semantic_layer/dsl.py` 第 60–70 行（`kpi` 命名空间的注释逐字说明它承载的是「主要会计数据和财务指标」章节的**已披露值**，不是报表行项目）
  - `src/extractor/locate.py`
  </read_first>
  <action>
  这四个字段与 Task 1 的十个**结构上不同**，所以单列一个任务：它们都不在三张报表里。

  `notes.nonrecurring_pl_net_attributable_to_parent` 在附注的「非经常性损益项目及金额」表里；
  `notes.restatement_flag` 与 `notes.reporting_period_months` 在附注或报表附注说明里，**此前从来没有人验证过它们能不能抽**——尤其 `notes.reporting_period_months`，它可能根本不是一个被印在纸上的数字，而要从报告期起止日推出来。如果是后者，探测报告里就要如实写「不是一个可抽取的印刷字段」，并把它列为 SC-2 计数里的一个失手位，**不要为了凑数把它算成能抽**。

  `kpi.roe_weighted_average_disclosed` 在「主要会计数据和财务指标」章节。这个章节的形状是表格但不是三张报表之一，`find_statement_anchors` 的八条标题里没有它。给 `locate.py` 加一个通用的 `find_section_anchors(pdf, titles)`，把「主要会计数据和财务指标」与附注章节标题都当锚点处理——**复用同一套「标题当带 y 坐标的锚点、区间 = [本标题, 下一标题)」的模型**，不要为这两处另发明一套定位方法。

  合并范围变更那六个子项另建一条路径：`read_applicability(pdf, anchor)` 定位「九、 合并范围的变更」章节区间，在区间内逐项读「√适用 / □不适用」标记，返回六个布尔值。**定位必须锚在这个章节区间内**——CONTEXT §3.5 已经点名 p77 另有一段「同一控制下和非同一控制下企业合并的会计处理方法」，那是**会计政策**不是交易事实，只按关键词匹配会把政策段落误判成「发生了企业合并」，与 cninfo「最后一个匹配的表静默胜出」同型。探测报告里要显式记一行：p77 那一段在本次探测中**没有**被算进来，以及是靠什么排除的。

  `notes.consolidation_scope_change`（D-020 新增的字段）的取值 = 六项中任一为「适用」。本任务只探测并记录六项的真实取值，**不改 `metrics/` 的任何 YAML**——定义侧的改动在 `01.5-05-PLAN.md`。

  把这四个字段与六复选项的结果补进 `PROBE-14.md` 的后两节，格式与 Task 1 一致：逐字段列出取到的值，不写命中数。同时把探测出的候选行追加进 `tests/fixtures/maotai_2023_probe_rows.json`。
  </action>
  <verify>
    <automated>.venv/Scripts/python scripts/probe_fields.py --stock 600519 --year 2023 --namespace notes --out /tmp/probe_notes.json && .venv/Scripts/python scripts/probe_fields.py --stock 600519 --year 2023 --namespace kpi --out /tmp/probe_kpi.json && .venv/Scripts/python -m semantic_layer scan</automated>
  </verify>
  <acceptance_criteria>
  - `docs/agent/phase-01.5/PROBE-14.md` 中以下 4 个字符串各自逐字出现至少一次：`notes.nonrecurring_pl_net_attributable_to_parent`、`notes.restatement_flag`、`notes.reporting_period_months`、`kpi.roe_weighted_average_disclosed`
  - 上述每个字段所在行含具体取值或显式写着「取不到」及其原因，不允许留空
  - `PROBE-14.md` 含一节记录「九、 合并范围的变更」六个子项的逐项适用性取值，六项齐全
  - `PROBE-14.md` 含一行显式说明 p77 的会计政策段落未被计入，并写明排除依据
  - `grep -v '^#' src/extractor/locate.py | grep -c 'find_section_anchors'` 输出 ≥ 1
  - `PROBE-14.md` 末尾含一张汇总表，逐字段给出「人工核对正确」的判定，且该表的行数为 14
  - `.venv/Scripts/python -m semantic_layer scan` 退出码 0
  </acceptance_criteria>
  <done>14 个零实测字段全部有了「取到的值」级别的证据；合并范围变更六项的真实取值在册；会计政策段落这个已知陷阱被显式排除并留了证。</done>
</task>

<task type="auto">
  <name>Task 3：补一家有真实企业合并的公司年做正样本</name>
  <files>scripts/probe_fields.py, docs/agent/phase-01.5/PROBE-COMBINATION.md, tests/fixtures/combination_positive_probe_rows.json</files>
  <read_first>
  - `.planning/phases/01.5-data-ingestion/CONTEXT.md` §3.5 的「诚实限定」一段（单样本，茅台本期 1–4 全为「不适用」，**没有一个真发生企业合并的正样本**；已证明的是「字段位置与取值域可机械定位」，**未证明「发生合并时能抽对类型」**）
  - `DECISIONS.md` D-020 判据 3 与「反转触发条件」（若六个子项无法可靠判读则本条退回，改为在证据链里显式记录「该 flag 不可判定」，**不得**用一个求不出值的 trigger 冒充规则）
  - `AGENTS.md` 的数据边界（只允许公开数据；候选公司必须是 A 股上市公司，年报从巨潮取）
  - `rules/failure-modes.md` F-1（不要用「代码里没有」断言「实际没有」——找不到正样本时如实写「我在这几家里没找到」，不写「不存在」）
  - `src/extractor/download.py`、`src/extractor/locate.py`
  </read_first>
  <action>
  茅台 2023 的六项里 1–4 全是「不适用」，所以「发生企业合并时能不能抽对类型」这件事目前**零证据**。D-020 判据 3 要的回归用例、以及 `notes.business_combination_type` 的取值域是否真的够用，都卡在这里。

  选一家 A 股上市公司的某一年年报，条件是「九、 合并范围的变更」的第 1 项（非同一控制下企业合并）或第 2 项（同一控制下企业合并）为「√适用」。选公司时优先考虑当年有公开披露过重大收购的主板公司——这一步允许试几家，**试过哪几家、各自六项的取值是什么，全部记进报告**，不要只记成功的那一家。

  用同一条 `scripts/probe_fields.py --namespace notes` 路径探它，验证三件事：六个复选项在**另一家公司的排版下**依然能被文本路径干净抽出；第 1 项或第 2 项为「适用」时，`read_applicability` 返回的布尔值正确；该公司年报里同样存在会计政策段落而它没有被误算进来。

  写 `docs/agent/phase-01.5/PROBE-COMBINATION.md`：公司代码与年度、年报页码、六项的逐项取值原文、`notes.business_combination_type` 应取的值、`notes.consolidation_scope_change` 应取的值，以及一句诚实的覆盖结论——现在是两家公司两个样本，仍然不是「跨排版稳健」的证据。

  把候选行存成 `tests/fixtures/combination_positive_probe_rows.json`，它是 `01.5-05-PLAN.md` 里 D-020 判据 3 那条回归用例的输入。

  **如果试了若干家都找不到第 1/2 项为「适用」的样本**：如实写下试过哪几家、各自的取值，并在报告里把 D-020 的反转触发条件标为「待评估」，交给 Task 4 的检查点一并裁决。不要为了让判据看起来达成而降低样本标准。
  </action>
  <verify>
    <automated>.venv/Scripts/python -c "import json,pathlib;d=json.loads(pathlib.Path('tests/fixtures/combination_positive_probe_rows.json').read_text(encoding='utf-8'));assert d, 'fixture empty'" && .venv/Scripts/python -m semantic_layer scan</automated>
  </verify>
  <acceptance_criteria>
  - `docs/agent/phase-01.5/PROBE-COMBINATION.md` 存在，含公司代码、年度、年报页码三项具体值
  - 该文件含「九、 合并范围的变更」六个子项的逐项取值，且明确标出哪一项为「适用」；若一项都没找到，则含一张列出所有试过的公司代码与其六项取值的表
  - 该文件含一行说明该公司年报中的会计政策段落未被计入
  - `tests/fixtures/combination_positive_probe_rows.json` 是合法 JSON 且非空
  - `.venv/Scripts/python -m semantic_layer scan` 退出码 0 —— 它的 `forbidden_tracked_file_types` 已含 PDF 扩展名且 fail-closed，这既是 AC-10 也是「下载产物没进版本控制」的机械判据
  </acceptance_criteria>
  <done>「发生企业合并时能不能抽对类型」这件事有了茅台之外的第二个样本，或者有了一份诚实记录说明试过哪些、为什么没找到。</done>
</task>

<task type="auto">
  <name>Task 4：按 D-026 的规则给出 SC-2 结论（**不再停下来等裁决**）</name>
  <decision-record>
  原为 `checkpoint:decision`。操作者已于 2026-08-25 裁决：**规则先定，结果后判**，落为 **D-026**。
  本任务按该规则自行推进，只在**一种情况**下才停。

  **D-026 的规则**：按失手**原因**分两类——
  1. 「该字段根本不是报表上印出来的值」⇒ 走作废通道：移出分母、单独计数、**公开记录**
  2. 「印在那里但抽不出来」⇒ 真实失手，计入分母，**不得作废**

  **两条限定必须随结论一起写进产物**：
  - 第 1 类走的是 `EVAL_CASES` §2.2 的作废通道，而 **§2.2 本来是管评测题目的**，
    用到 SC-2 的字段集上是一次**类比迁移**——记录里必须写明是迁移，
    不得写成「§2.2 本来就覆盖」
  - **10% 上限一并继承**：30 个字段 ⇒ 最多作废 **3 个**。超过即判 SC-2 的字段集构造失败

  **唯一仍需停下来的情况**：扣除合规作废后**仍 < 24**。那是 SC-2 不成立，
  按 `PROJECT_SPEC.md` §10 的停止条件处理，是范围决策。
  </decision-record>
  <files>docs/agent/phase-01.5/PROBE-14.md, docs/agent/OPEN-ITEMS.md</files>
  <read_first>
  - `DECISIONS.md` 的 **D-026**（本任务的规则来源）与 **D-020**（反转触发条件，见下）
  - `EVAL_CASES.md` §2.2（作废通道的两类划分与**可判定性分界线**：
    一旦需要引用系统输出来主张某项该废，它就落入第 2 类）
  - `.planning/ROADMAP.md` 的 SC-2 全文（含 2026-08-21 写死的计数口径）
  - 本计划前三个 task 产出的 `PROBE-14.md` 逐字段核对结论
  </read_first>
  <behavior>
  - `PROBE-14.md` 的汇总表对**每个失手字段**标注属第 1 类还是第 2 类
  - 第 1 类的理由**不引用抽取器输出**（§2.2 的可判定性分界线）
  - 作废数 > 3 时，产物里明写「超过 10% 上限，SC-2 字段集需整体重审」而不是继续算通过数
  - 扣除合规作废后 ≥ 24 ⇒ 记 SC-2 达成；< 24 ⇒ 记 SC-2 不成立并**停下来**
  </behavior>
  <acceptance_criteria>
  - `docs/agent/phase-01.5/PROBE-14.md` 含一张汇总表，每个零实测字段一行，
    含「取到的值」「人工核对结论」「失手类别（1/2）」三列
  - 该文件含字符串「类比迁移」（限定必须在场，不得省略）
  - 该文件给出作废数与作废率，且作废率 > 10% 时含字符串「整体重审」
  - `python scripts/check_xrefs.py` 退出码 0（新写的 D-0xx / SC-x 引用不得悬空）
  </acceptance_criteria>
  <action>
  读 Task 1–3 产出的逐字段核对结果，按 D-026 的两类规则给每个失手字段定性，写进
  `PROBE-14.md` 的汇总表。定性理由必须能在**不看抽取器输出**的前提下成立——
  例如「报告期月数是报告的元数据，不是报表里的行项目」是合格理由，
  「抽取器对这个字段返回了空值」不是（那是第 2 类）。
  作废清单同时写进 `docs/agent/OPEN-ITEMS.md` 的 N 区，供 `01.5-06` 汇入 `VERIFICATION.md`。

  **附带一问仍要回答**：Task 3 若没找到企业合并正样本，D-020 的反转触发条件
  （六个子项无法可靠判读则本条退回，改为在证据链里显式记录「该 flag 不可判定」）是否触发
  ——在 `PROBE-COMBINATION.md` 里给出明确结论，**不要留空**。
  </action>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 公网 → 探测脚本 | 复用 01.5-01 的下载器，边界与缓解与那份计划相同 |
| 探测产物 → 版本控制 | 探测会 dump 大量原文行，可能把不该进仓库的东西带进来 |
| 第二家公司的年报 → 仓库 | 新引入一个数据源实例，D-010 的公开数据边界要再确认一次 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.5-08 | Information Disclosure | `scripts/probe_fields.py` 的 dump 产物 | high | mitigate | 固件只存数值行与坐标，不存整页原文段落；每个 task 的 verify 跑 `semantic_layer scan`（AC-10）；PDF 由 `.gitignore` 与 `forbidden_tracked_file_types` 双重拦住 |
| T-01.5-09 | Tampering | 正样本公司的选取 | medium | mitigate | 只从巨潮取 A 股公开年报（D-010）；报告里记公司代码与年报页码，任何人可独立复核 |
| T-01.5-10 | Repudiation | 探测结论的可复现性 | high | mitigate | 探测脚本进仓库而不是留在 scratchpad（A-10 的直接判据）；报告记完整命令与页码；产物固件进版本控制 |
| T-01.5-11 | Denial of Service | 探测多家公司时的重复下载 | low | accept | 单次探测量级是几份 PDF，且 01.5-01 已有大小与超时上限；不做并发抓取 |
</threat_model>

<verification>
1. `.venv/Scripts/python scripts/probe_fields.py --help` 退出码 0
2. `PROBE-14.md` 的汇总表有 14 行，每行都有取到的值或显式的「取不到 + 原因」
3. `PROBE-COMBINATION.md` 存在且含公司代码、年度、页码
4. `.venv/Scripts/python -m semantic_layer scan` 退出码 0
5. `.venv/Scripts/python -m semantic_layer scan` 退出码 0（AC-10：被跟踪文件里没有二进制数据产物）
6. 六道门按 F-8 串联实跑：`.venv/Scripts/python -m pytest -q && .venv/Scripts/python -m semantic_layer scan && .venv/Scripts/python -m semantic_layer validate && .venv/Scripts/python scripts/check_xrefs.py && .venv/Scripts/python scripts/check_reading_ledger.py && .venv/Scripts/python scripts/check_gates.py`
</verification>

<success_criteria>
- 14 个字段逐个有「取到的值」级别的记录，一个都没有只写命中数
- 探测脚本在仓库内，重跑是一条命令（A-10 判据达成）
- 企业合并正样本已就位，或有一份诚实记录说明试过哪些、为什么没有
- SC-2 的门槛由操作者显式裁决，不由执行者自行放宽或宣布达成
</success_criteria>

<output>
Create `.planning/phases/01.5-data-ingestion/01.5-02-SUMMARY.md` when done
</output>
