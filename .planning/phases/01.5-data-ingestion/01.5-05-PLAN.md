---
phase: 01.5-data-ingestion
plan: 05
type: execute
wave: 4
depends_on: ["01.5-03"]
files_modified:
  - src/extractor/notes.py
  - data/mappings/pdf/notes.yaml
  - metrics/_flags.yaml
  - metrics/minority_interest_share.yaml
  - tests/test_consolidation_scope.py
  - tests/fixtures/maotai_2023_notes_scope.json
autonomous: true
requirements: [SC-2, SC-8, AC-01, AC-10]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - notes.consolidation_scope_change 存在于 minority_interest_share 的 source_fields，且 scope_change 的 trigger 改挂它（D-020 判据 1）
    - metrics/_flags.yaml 里 scope_change 的 description 不再把「合并范围变动」与「同一控制下合并追溯调整比较期」混写（D-020 判据 2）
    - 存在一条以茅台 2023 为输入的回归用例，锁住「无企业合并但合并范围变动」时 scope_change 会置位（D-020 判据 3）
    - notes.business_combination_type 保留原义（只表企业合并类型），未被本计划改写
    - 六个复选子项的读取锚在「九、 合并范围的变更」章节区间内，p77 的会计政策段落不被计入
    - 复选项的判定由显式的「√适用 / □不适用」标记读出，不靠关键词出现与否反推（L-35 的同型约束）
    - eval/frozen-01/ 与 docs/agent/poc-01/ 的哈希在本计划改动后仍全部 OK（D-012）
    - metrics/ 的改动严格限于 D-020 批准的两处，其余 19 份定义 diff 为空（CONTEXT §5 的非目标只被本条放宽）
    - statement: 若六个子项在正样本公司的排版下无法可靠判读，则触发 D-020 的反转条件，改为在证据链里显式记录「该 flag 不可判定」，不得用一个求不出值的 trigger 冒充规则
      verification: backstop
  artifacts:
    - src/extractor/notes.py
    - tests/test_consolidation_scope.py
    - tests/fixtures/maotai_2023_notes_scope.json
  key_links:
    - notes.read_applicability 的六个布尔值 → notes.consolidation_scope_change 的取值（任一为「适用」即 True）
    - notes.consolidation_scope_change → minority_interest_share 的 scope_change trigger（这条链路的断裂就是 A-8 查实的那处真实假阴性）
    - RecordKind.NOTE_CHECKBOX → pipeline 的分派点（01.5-03 已建，本计划实现被分派到的那一侧）
---

<objective>
关掉 A-8 查实的那处**真实年报上的假阴性**。

三处原文可独立复核：`metrics/_flags.yaml` 说 `scope_change` 是「合并范围发生变动」；
`metrics/minority_interest_share.yaml` 的 trigger 是
`notes.business_combination_type != "无"`；茅台 2023 p120–121 企业合并三项**全部「不适用」**，
而「5、其他原因的合并范围变动」**是「适用」**（定制营销公司清算注销）。
于是字段取「无」→ trigger 为假 → **flag 不置位，而合并范围确实变了**。

D-020 已裁决处置方式（新增字段 `notes.consolidation_scope_change`，trigger 改挂它），
并明写**执行放 Phase 1.5**：新字段的取值判定依赖映射表的 `notes` 侧，先定枚举再定映射
等于凭印象造取值域——那正是 F-2 的形状。现在映射表已经在 `01.5-03` 落地，条件具备。

**这是 CONTEXT §5「不动 `metrics/` 的 20 份定义」这条非目标唯一被放宽的地方，放宽范围仅限本条。**
本计划对 `metrics/` 的改动必须严格限于 D-020 点名的两处，其余 19 份定义的 diff 必须为空。

Purpose: 让 `revenue_growth_yoy` 真正需要的那条覆盖第一次成立；
同时验证「元规则写出来的规则会不会正确触发」——这是 `STATE.md` 列的已知证据缺口之一。
Output: `notes.consolidation_scope_change` 的抽取实现、两处定义侧改动、一条真实回归用例。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01.5-data-ingestion/CONTEXT.md
@.planning/phases/01.5-data-ingestion/01.5-02-PLAN.md
@.planning/phases/01.5-data-ingestion/01.5-03-PLAN.md
@DECISIONS.md
@PROJECT_SPEC.md
@docs/agent/OPEN-ITEMS.md
@rules/failure-modes.md
@rules/commands.md
@metrics/_flags.yaml
@metrics/minority_interest_share.yaml
@src/semantic_layer/vocabulary.py
@src/semantic_layer/dsl.py
@src/semantic_layer/validate.py
</context>

## Artifacts this phase produces（本计划部分）

**新增模块与函数**

- `src/extractor/notes.py`
- `notes.ScopeChangeItem`（枚举值：`NON_COMMON_CONTROL_COMBINATION` `COMMON_CONTROL_COMBINATION`
  `REVERSE_ACQUISITION` `SUBSIDIARY_DISPOSAL` `OTHER_SCOPE_CHANGE` `OTHER`）
- `notes.ScopeChangeReading`（字段：`items` `anchor_page` `page` `raw_lines` `undecidable`）
- `notes.extract_checkbox_field(pdf, anchor, mapping)` —— `RecordKind.NOTE_CHECKBOX` 的处理入口
- `notes.read_scope_change(pdf)` —— 读「九、 合并范围的变更」六个子项
- `notes.derive_consolidation_scope_change(reading)` —— 六项任一为「适用」即 True

**新增字段（`metrics/` 侧，D-020 批准范围内）**

- `notes.consolidation_scope_change`（布尔）—— 进入 `minority_interest_share` 的 `source_fields`

**修改的既有符号**

- `metrics/minority_interest_share.yaml`：`flags[].scope_change.trigger` 改挂新字段
- `metrics/_flags.yaml`：`scope_change` 的 `description` 收窄

**新增固件与测试**

- `tests/fixtures/maotai_2023_notes_scope.json`
- `tests/test_consolidation_scope.py`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1：六个复选子项的读取实现（锚在章节区间内，不靠关键词）</name>
  <files>src/extractor/notes.py, data/mappings/pdf/notes.yaml, tests/fixtures/maotai_2023_notes_scope.json, tests/test_consolidation_scope.py</files>
  <read_first>
  - `.planning/phases/01.5-data-ingestion/CONTEXT.md` §3.5 全文（p120–121 六个固定子项的原文与 `√` / `□` 标记；p77 会计政策段落这个必须避开的陷阱；以及那句诚实限定：已证明的是字段位置与取值域可机械定位，**未证明「发生合并时能抽对类型」**）
  - `docs/agent/phase-01.5/PROBE-14.md` 与 `docs/agent/phase-01.5/PROBE-COMBINATION.md`（01.5-02 探到的六项真实取值，以及正样本公司的对照）
  - `src/extractor/locate.py` 的 `find_section_anchors` 与 `read_applicability`（01.5-02 建的，本任务在它之上做字段级封装，不重写定位）
  - `src/extractor/mapping.py` 与 `data/mappings/pdf/notes.yaml`（01.5-03 已给 `notes.consolidation_scope_change` 留了位置，`kind` 是 `NOTE_CHECKBOX`）
  - `src/extractor/pipeline.py`（01.5-03 已建的 `kind` 分派点，本任务实现被分派到的那一侧；**不要改 `pipeline.py`**）
  - `DECISIONS.md` D-020 的「反转触发条件」原文
  </read_first>
  <behavior>
  - `read_scope_change(pdf)` 在茅台 2023 上返回六项，其中 1–4 与 6 为「不适用」、第 5 项「其他原因的合并范围变动」为「适用」
  - 返回值里 `anchor_page` 是「九、 合并范围的变更」标题所在页，`page` 是复选项所在页，两者都是整数
  - p77「同一控制下和非同一控制下企业合并的会计处理方法」那一段不出现在 `raw_lines` 里
  - 把「九、 合并范围的变更」这个锚点从输入里去掉后，函数返回 `undecidable=True`，而不是退回全文关键词搜索
  - `derive_consolidation_scope_change(reading)` 在茅台 2023 的读数上返回 True
  - 六项中任何一项既不是「√适用」也不是「□不适用」（标记读不出来）时，`undecidable` 为 True
  - `extract_checkbox_field` 产出的 `ExtractionRecord` 的 `kind is RecordKind.NOTE_CHECKBOX`
  </behavior>
  <action>
  `src/extractor/notes.py` 新建。`read_scope_change(pdf)` 先用 `find_section_anchors` 定位
  「九、 合并范围的变更」，**只在该章节区间内**逐项读六个子项的「√适用 / □不适用」标记。
  区间之外的任何文本都不参与判定——CONTEXT §3.5 已经点名 p77 另有一段
  「同一控制下和非同一控制下企业合并的会计处理方法」，那是**会计政策**不是交易事实，
  只按关键词匹配会把政策段落误判成「发生了企业合并」，与 cninfo
  「最后一个匹配的表静默胜出」同型。要有一条测试断言那一段没被计入。

  判定必须读**标记**，不读关键词的出现与否。这是 `L-35` 在附注侧的同型约束：
  类别由显式标记判定，不靠「这一段看起来像在讲企业合并」这类形状反推。
  `√` 与 `□` 两个字符在 CONTEXT §3.5 的实测里都被 pdfplumber 的文本路径干净抽出，
  所以这条路是通的，不需要另找办法。

  **找不到锚点时返回 `undecidable=True`，不要退回全文搜索兜底。** 退回兜底就是把
  「读不出来」偷偷变成「读出来了一个可能是错的值」，而 D-020 的反转触发条件明写：
  若六个子项无法可靠判读，则本条退回、改为在证据链里显式记录「该 flag 不可判定」，
  **不得**用一个求不出值的 trigger 冒充规则。`undecidable` 就是那个「显式记录」的载体。

  `derive_consolidation_scope_change(reading)` = 六项中任一为「适用」。
  注意它与 `notes.business_combination_type` 的分工：后者只表**企业合并类型**
  （对应六项里的 1、2、3），D-020 明写它**保留原义、不被本条改写**。
  茅台 2023 的实际情形正是「无企业合并（1–4 全不适用），但合并范围因清算注销而变动（第 5 项适用）」
  ——两个字段在同一份年报上取值不同，这本身就是它们必须是两个字段的证明。

  把六项读数存成 `tests/fixtures/maotai_2023_notes_scope.json`，
  `tests/test_consolidation_scope.py` 的用例跑在固件上，不需要每次真下 PDF。
  </action>
  <verify>
    <automated>.venv/Scripts/python -m pytest tests/test_consolidation_scope.py -x -q && .venv/Scripts/python -m semantic_layer scan</automated>
  </verify>
  <acceptance_criteria>
  - `grep -v '^#' src/extractor/notes.py | grep -c 'undecidable'` 输出 ≥ 2
  - `tests/test_consolidation_scope.py` 含一条测试断言茅台 2023 的六项读数中，第 5 项为「适用」而第 1–4 项为「不适用」（逐项断言，不写聚合断言）
  - `tests/test_consolidation_scope.py` 含一条测试断言字符串「同一控制下和非同一控制下企业合并的会计处理方法」不出现在 `raw_lines` 中
  - `tests/test_consolidation_scope.py` 含一条测试断言：去掉章节锚点后返回 `undecidable is True`，且返回值里没有任何一项被判为「适用」
  - `tests/test_consolidation_scope.py` 含一条测试断言 `derive_consolidation_scope_change` 在茅台读数上返回 True，而同一份读数上 `notes.business_combination_type` 应取「无」
  - `tests/fixtures/maotai_2023_notes_scope.json` 是合法 JSON 且含六项
  - `git diff --stat src/extractor/pipeline.py` 输出为空（分派点由 01.5-03 建好，本计划不动它）
  - `.venv/Scripts/python -m semantic_layer scan` 退出码 0
  </acceptance_criteria>
  <done>六个复选子项能在章节区间内被机械读出，读不出来时系统说「不可判定」而不是编一个值；会计政策段落这个已知陷阱有测试守着。</done>
</task>

<task type="auto">
  <name>Task 2：定义侧的两处改动（D-020 批准范围内，一个字都不多改）</name>
  <files>metrics/_flags.yaml, metrics/minority_interest_share.yaml</files>
  <read_first>
  - `DECISIONS.md` D-020 全文，尤其是「决策」四条与「已核实不碰冻结产物」那一段（`minority_interest_share` 与 `scope_change` 在 `eval/frozen-01/` 全目录**零命中**；且夹具是**超集**，仓库中**没有**「夹具字段集 == `source_fields` 集」的相等断言）
  - `metrics/minority_interest_share.yaml` 全文（现有 `source_fields` 三条、`flags[].scope_change.trigger` 的原文、以及那条关于枚举值必须加引号的注释——不加引号时「无」会被词法器当作裸标识符）
  - `metrics/_flags.yaml` 的 `scope_change` 条目（现文案把「合并范围发生变动」与「同一控制下合并追溯调整比较期」混在一句里，而**处置子公司不产生追溯调整**，两件事必须分开表述）
  - `PROJECT_SPEC.md` §5.1（`source_fields` 的必填键：`id` / `statement` / `line_item`，参与加减的字段还要 `sign_convention`；`missing_representation` 须与「取值为零」可区分）
  - `src/semantic_layer/validate.py`（9 条 Requirement 的机械校验器——改完必须过它）
  - `src/semantic_layer/dsl.py`（trigger 是受限布尔 DSL，`notes.consolidation_scope_change == true` 这种写法要能过词法器）
  - `docs/agent/OPEN-ITEMS.md` A-8
  </read_first>
  <action>
  两处改动，一处都不能多。

  **第一处**：`metrics/minority_interest_share.yaml` 的 `source_fields` 增加一条
  `notes.consolidation_scope_change`，`statement` 是「财务报表附注」，
  `line_item` 写「九、 合并范围的变更 —— 六个子项中任一为适用」，
  `missing_representation` 与既有三条保持一致的写法。它是布尔字段、不参与加减，
  所以按 §5.1 不需要 `sign_convention`。然后把 `flags` 里 `scope_change` 的 trigger
  从 `notes.business_combination_type != "无"` 改成挂新字段。
  **`notes.business_combination_type` 那条 `source_fields` 保留**——D-020 明写它保留原义，
  不被本条改写；它仍然是 `common_pitfalls` 里那条「合并范围变动会使该占比跳变」的一部分承载。

  同时检查 `common_pitfalls` 里 `enforced_by: flags.scope_change` 那一条：
  trigger 改挂之后它承载的东西变了（从「发生企业合并」变成「合并范围发生变动」），
  陷阱正文若与新语义不符要一并调整——但只调那一条，别顺手改别的。

  **第二处**：`metrics/_flags.yaml` 里 `scope_change` 的 `description`。
  现文案是「合并范围发生变动（同一控制下合并追溯调整比较期）」，把两件事混在一句里，
  而**处置子公司不产生追溯调整**。按 D-020 收窄为只表述「合并范围发生变动」这一件事，
  追溯调整那半句要么去掉、要么明确写成「其中同一控制下合并会追溯调整比较期」这样的从属关系。
  `vocabulary_version` 是否要 +1 由执行者判断——它是词表的版本号，
  改 description 不改取值域，倾向于不动；若动了，要在提交信息里说明理由。

  **改完立刻验冻结。** D-020 已核实过 `eval/frozen-01/` 对这两个名字零命中，但那是决策当时的核实，
  改完还要自己再验一次——`PROJECT_SPEC.md` §11 不接受「应该可以了」。

  **其余 19 份定义的 diff 必须为空。** CONTEXT §5 的非目标只被 D-020 这一条放宽，
  放宽范围仅限本条。跑一次 `git diff --stat metrics/` 自己看清楚改了哪几个文件。
  </action>
  <verify>
    <automated>.venv/Scripts/python -m semantic_layer validate && .venv/Scripts/python -m semantic_layer stats && .venv/Scripts/python -m pytest -q && cd eval/frozen-01 && sha256sum -c SHA256SUMS && cd ../../docs/agent/poc-01 && sha256sum -c SHA256SUMS</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c 'notes.consolidation_scope_change' metrics/minority_interest_share.yaml` 输出 2（一处在 `source_fields`，一处在 trigger）
  - `grep -c 'notes.business_combination_type' metrics/minority_interest_share.yaml` 输出 ≥ 1（保留原义，未被删除）
  - `git diff --name-only metrics/` 的输出恰好是两行：`metrics/_flags.yaml` 与 `metrics/minority_interest_share.yaml`
  - `grep -c '同一控制下合并追溯调整比较期' metrics/_flags.yaml` 输出 0，或该字符串出现在一个明确表述从属关系的句子里（由执行者在提交信息中说明选了哪种）
  - `.venv/Scripts/python -m semantic_layer validate` 退出码 0（9 条 Requirement 全过）
  - `.venv/Scripts/python -m semantic_layer resolve minority_interest_share --json` 退出码 0
  - `cd eval/frozen-01 && sha256sum -c SHA256SUMS` 全部 OK
  - `cd docs/agent/poc-01 && sha256sum -c SHA256SUMS` 全部 OK
  - `.venv/Scripts/python -m pytest -q` 退出码 0（现有 400+ 测试无一因本改动变红）
  </acceptance_criteria>
  <done>`scope_change` 从今天起挂在一个真的能表达「合并范围变动」的字段上；`_flags.yaml` 的描述不再把两件不同的事写成一句；20 份定义里只有该动的那一份动了。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3：D-020 判据 3 的真实回归 —— 无企业合并但合并范围变动时 flag 会置位</name>
  <files>tests/test_consolidation_scope.py, tests/fixtures/maotai_2023_notes_scope.json</files>
  <read_first>
  - `DECISIONS.md` D-020 判据 3 原文（「一条以茅台 2023 为输入的回归用例，锁住『无企业合并但合并范围变动』时 `scope_change` **会**置位」）
  - `src/semantic_layer/resolve.py` 的 `active_flags()`（trigger 求值的入口；求值不了会返回 `Refusal(UNEVALUABLE_CONDITION)` 而不是跳过）
  - `EVAL_CASES.md` §3.3 的 `J-5`（闸门类断言必须配一条**去掉它就会失败**的负控制，且**红的原因要正确**）
  - `rules/commands.md` 的「假闸门的验证程序：造回归 → 看红 → 回退」三步，尤其第 2 步的后半句
  - `docs/agent/phase-01.5/PROBE-COMBINATION.md`（正样本公司的六项取值）
  - `metrics/minority_interest_share.yaml`（Task 2 改完之后的版本）
  </read_first>
  <behavior>
  - 用茅台 2023 的真实读数构造行数据（`notes.business_combination_type` 取「无」、`notes.consolidation_scope_change` 取 True）调 `active_flags`，返回的集合含 `scope_change`
  - 同一份行数据在**改动前**的 trigger（挂 `notes.business_combination_type`）下**不含** `scope_change` —— 这条就是 A-8 那个假阴性的复现
  - 正样本公司（`PROBE-COMBINATION.md` 里那家）的读数上，`notes.business_combination_type` 取非「无」的值，且 `scope_change` 同样置位
  - `notes.consolidation_scope_change` 缺失时 `active_flags` 返回 `Refusal`，不静默当假
  </behavior>
  <action>
  三条断言，缺一条这个判据就只成立一半。

  **正向**：用茅台 2023 的真实读数构造一行数据，断言 `active_flags` 返回的集合里有 `scope_change`。
  这是 D-020 判据 3 的字面要求。

  **假阴性复现（J-5 的负控制）**：同一份行数据，用**改动前**的 trigger 表达式
  （`notes.business_combination_type != "无"`）求值一次，断言结果**不含** `scope_change`。
  这条断言证明的是「这次改动确实修掉了一个真实存在的漏报」，而不是「新 trigger 恰好也能过」。
  按 `J-5` 的原话，闸门类断言必须配一条「去掉它就会失败」的用例，**且红的原因要正确**——
  这里「去掉它」的具体形态就是把 trigger 换回旧表达式，红的原因必须是
  `scope_change` 不在集合里，而不是求值抛错。要在断言里把这两种失败分开。

  实现上不要去改 `metrics/` 里的 YAML 来做这条负控制（那会污染工作区并可能被误提交），
  直接在测试里构造一个只改了 trigger 字符串的 `MetricDefinition` 副本，或直接调
  `dsl.parse_condition` + `dsl.evaluate` 对两个表达式各求一次值。

  **缺失时拒答**：`notes.consolidation_scope_change` 缺失的行数据上，`active_flags` 应返回
  `Refusal`（`UNEVALUABLE_CONDITION`），不静默当假。fail-closed 是 `resolve.py` 已确立的立场
  ——漏掉一个 `affects_comparability` 为真的标记，等于让一次本该被阻断的跨期比较悄悄通过。

  如果 `PROBE-COMBINATION.md` 里有正样本公司，再加一条用它的读数驱动的断言：
  `notes.business_combination_type` 取非「无」时 `scope_change` 同样置位。
  若 01.5-02 的 Task 4 裁决为「没找到正样本」，这条改为一条显式记录（测试模块 docstring 里写明
  为什么没有这条断言，并指向 D-020 的反转触发条件），**不要留一条空测试假装覆盖了**。
  </action>
  <verify>
    <automated>.venv/Scripts/python -m pytest tests/test_consolidation_scope.py -x -q && .venv/Scripts/python -m pytest -q && .venv/Scripts/python scripts/check_gates.py</automated>
  </verify>
  <acceptance_criteria>
  - `tests/test_consolidation_scope.py` 含一条测试断言 `"scope_change" in active_flags(defn, row)` 为真（茅台 2023 真实读数）
  - `tests/test_consolidation_scope.py` 含一条测试用旧 trigger 表达式求值并断言结果**不含** `scope_change`，且该测试显式区分「求值为假」与「求值抛错」两种情形（后者应当让测试失败并给出不同的失败信息）
  - `tests/test_consolidation_scope.py` 含一条测试断言字段缺失时返回的是 `Refusal` 且 `.code is RefusalCode.UNEVALUABLE_CONDITION`
  - `git diff --name-only metrics/` 在跑完全部测试之后仍只有 Task 2 改的那两个文件（负控制未污染工作区）
  - `.venv/Scripts/python scripts/check_gates.py` 退出码 0（新增测试全部有可失败点，无恒真断言）
  - `.venv/Scripts/python -m pytest -q` 退出码 0
  </acceptance_criteria>
  <done>A-8 那个真实假阴性有了一条会红的回归，而且同一个测试文件里同时留着「修之前它确实是漏报的」这条证据——将来有人把 trigger 改回去，测试会告诉他为什么不行。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 附注章节文本 → 复选项判定 | 不可信文本参与一个会影响可比性标记的判定 |
| `metrics/*.yaml` → 9 条 Requirement 校验器 | 定义文件是配置输入；改错一处会让整份定义不合规 |
| 本计划的 diff → 冻结产物 | `metrics/` 改动有触及冻结哈希的理论风险 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.5-24 | Spoofing | 会计政策段落被当成交易事实 | high | mitigate | 判定锚在「九、 合并范围的变更」章节区间内；测试断言 p77 那段不出现在 `raw_lines` 中；找不到锚点时 `undecidable=True` 而不是退回全文搜索 |
| T-01.5-25 | Tampering | 定义侧改动越出 D-020 批准范围 | high | mitigate | `git diff --name-only metrics/` 必须恰好两行；`semantic_layer validate` 退出码 0；`pytest -q` 全绿 |
| T-01.5-26 | Repudiation | 冻结产物被间接改动 | high | mitigate | 两处 `sha256sum -c SHA256SUMS` 在同一条 `&&` 链里跑（D-012） |
| T-01.5-27 | Tampering | 负控制测试污染工作区 | medium | mitigate | 负控制在内存里构造定义副本，不改磁盘上的 YAML；跑完测试后再验一次 `git diff --name-only metrics/` |
| T-01.5-28 | Repudiation | 「不可判定」被静默当成「未变动」 | high | mitigate | `undecidable` 是返回值的一部分且进证据链；D-020 的反转触发条件写明不得用求不出值的 trigger 冒充规则 |
</threat_model>

<verification>
1. `git diff --name-only metrics/` 恰好两行
2. `.venv/Scripts/python -m semantic_layer validate` 退出码 0
3. 两处冻结校验 `sha256sum -c SHA256SUMS` 全部 OK
4. 六道门按 F-8 串联实跑 exit 0
5. `git diff --stat src/extractor/pipeline.py` 为空
</verification>

<success_criteria>
- D-020 三条判据全部达成，或第 3 条因无正样本而按反转触发条件显式记录
- `scope_change` 在茅台 2023 上会置位，而在旧 trigger 下不会——两条断言同时在册
- `metrics/` 的改动严格限于批准的两处
- 冻结产物一个字节没动
</success_criteria>

<output>
Create `.planning/phases/01.5-data-ingestion/01.5-05-SUMMARY.md` when done
</output>
