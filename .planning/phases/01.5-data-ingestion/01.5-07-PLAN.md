---
phase: 01.5-data-ingestion
plan: 07
type: execute
wave: 5
depends_on: ["01.5-05"]
files_modified:
  - src/extractor/notes.py
  - src/extractor/pipeline.py
  - tests/test_report_metadata.py
  - tests/fixtures/maotai_2023_restatement.json
autonomous: true
requirements: [SC-2, SC-5, SC-8, AC-01]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: medium

must_haves:
  truths:
    - COLUMN_HEADER_PRESENCE 的判定锚在「近三年主要会计数据和财务指标」章节区间内，p78 与 p107 的「调整后」不被计入
    - notes.restatement_flag 在茅台 2023 上取到 True，且证据里带着列头原文与页码
    - REPORT_METADATA **不从版面取值**，它由报告类型派生；实现里不得出现任何「在页面上找这一行」的代码路径
    - notes.reporting_period_months 在年度报告上取 12，且该值标注为 derived 而非 extracted
    - 判不了时返回 None 并给出理由，不返回一个看起来合理的默认值（与 01.5-05 的 undecidable 同一套语义）
    - pipeline 的 _KIND_HANDLED_HERE 分派点接上这两支之后，六个 kind 全部有实现或有显式拒绝，不存在静默跳过
    - eval/frozen-01/ 与 docs/agent/poc-01/ 的哈希不变（D-012）
    - metrics/ 的 diff 为空 —— 本计划不碰任何定义
  artifacts:
    - src/extractor/notes.py（扩两支）
    - tests/test_report_metadata.py
---

<objective>
把 `COLUMN_HEADER_PRESENCE` 与 `REPORT_METADATA` 两支 `kind` 实现掉。

**它们此前不属于任何一份 PLAN**（`N-43`：那句「三支由 `01.5-05` 实现」是一句
比 PLAN 晚两天写的代码注释编出来的）。操作者 2026-08-28 裁决：
**新写一份 PLAN 并进 wave 5**，不推到 Phase 2。

Purpose: 解掉 6 个指标的 `restated` 可比性标记判不了这个缺口 ——
它是 `COMPUTED-VALUES §2` 记的那条「数值算得出来、可比性标记判不了」的直接成因。
Output: 两支 `kind` 的实现 + 真实回归；`flags_status` 该不该阻断可以拿真实取值来定。
</objective>

<context>
@.planning/phases/01.5-data-ingestion/CONTEXT.md
@docs/agent/phase-01.5/PROBE-14.md
@docs/agent/OPEN-ITEMS.md
@DECISIONS.md
@src/extractor/notes.py
@src/extractor/locate.py
@data/mappings/pdf/notes.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1：COLUMN_HEADER_PRESENCE —— 判「调整后 / 调整前」这两个列头在不在</name>
  <files>src/extractor/notes.py, tests/test_report_metadata.py, tests/fixtures/maotai_2023_restatement.json</files>
  <read_first>
  - `data/mappings/pdf/notes.yaml` 里 `notes.restatement_flag` 那条（`column_header: 调整后`，`derived_from` 写清了值的来源）
  - `docs/agent/phase-01.5/PROBE-14.md` §3（p5 三处列头的 y 坐标与那句互证文字）
  - `src/extractor/notes.py` 的 `scope_change_from_lines`（本任务照它的分层：纯函数内核吃行文本，`read_*` 吃 pdf）
  </read_first>
  <behavior>
  - 在茅台 2023 上返回 `True`，证据里带列头原文与页码
  - **p78 与 p107 的「调整后」不被计入** —— 实测全篇「调整后」命中 3 页 5 行，只有 p5 那 3 行在本章节区间内
  - 找不到章节锚点时返回「判不了」而不是 `False`
  - 章节在、但区间内一个「调整后」都没有时返回 `False`（这是**读出来了没有**，与判不了不同）
  </behavior>
  <action>
  `notes.read_restatement_flag(pdf)` 与纯函数内核 `restatement_from_lines(texts, ...)`。
  区间用 `find_section_anchors(("近三年主要会计数据和财务指标", "境内外会计准则下会计数据差异"))`
  —— 实测下一章正是「八、境内外会计准则下会计数据差异」（p6 y=77.7）。

  **判定读列头文字的字面在场，不做语义推断。** 与 `01.5-05` 的复选项同一条约束（`L-35`）。

  ⚠️ **这一支的风险与复选项不同**：复选项有 `√` / `□` 两个互斥标记，
  读不到标记就是读不到；而列头只有「在」与「不在」，**「不在」和「没读到」长得一样**。
  所以 `False` 只在**章节锚点定位成功**的前提下才允许返回 —— 锚点没定到就是判不了。

  ⚠️ **单样本，跨排版未验证**（`notes.yaml` 已写明）：「有 调整后/调整前 列 ⟺ 发生追溯重述」
  这条规则在茅台 2023 上成立且与 p5 y=487.2 的文字互证，但**反例形态一次都没观察到**。
  按 `F-1`，实现与测试里都不许把它写成「已验证」。
  </action>
  <acceptance_criteria>
  - `tests/test_report_metadata.py` 含一条断言：p78 / p107 的「调整后」不进判定（用真实页码构造）
  - 含一条断言：锚点缺失 ⇒ 返回判不了，**不是 `False`**
  - 含一条断言：章节在但区间内无列头 ⇒ `False`（与判不了是不同的返回）
  - 茅台 2023 上取到 `True`，证据含列头原文与页码
  </acceptance_criteria>
  <done>`restated` 的取值第一次能从真实年报上判出来，且已知的两处同名干扰被区间挡在外面。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2：REPORT_METADATA —— 由报告类型派生，不从版面取值</name>
  <files>src/extractor/notes.py, src/extractor/pipeline.py, tests/test_report_metadata.py</files>
  <read_first>
  - `data/mappings/pdf/notes.yaml` 里 `notes.reporting_period_months` 那条（`derived_from`：由报告类型决定，年度报告 ⇒ 12；纸上不印这一行）
  - `DECISIONS.md` D-026 第 1 类与 `EVAL_CASES.md` §2.2 的作废通道
  - `rules/failure-modes.md` F-2（给 flag 编造求不出值的 trigger）
  </read_first>
  <behavior>
  - `derive_reporting_period_months("annual")` 返回 12
  - 报告类型不是已穷举的那几种时**抛异常**，不返回 12
  - 实现里**没有任何**在页面上搜索这个值的代码路径
  - 产出的记录标注为派生而非抽取（`RetrievalOutcome` / `basis_confirmation` 如实反映）
  </behavior>
  <action>
  这一支最容易做错的方式是**给它编一条版面规则**。
  `PROBE-14 §3` 已实测：整份 143 页逐行搜「月份数」/「报告期涵盖」/「报告期月」/「涵盖的月」
  四个串**全部零命中**。它不在纸上。

  ⇒ 实现是一张**穷举的报告类型 → 月份数**表，加一条「不认识就抛」。
  **不许有 fallback 到 12。** 一个「大概是年报所以 12」的默认，
  与 `units.py` 那条「单位大概是元」是同一个形状：错了也没有任何东西会报错。

  同时把 `pipeline._KIND_HANDLED_HERE` 的分派点接上这两支 ——
  接上之后六个 `kind` 要么有实现、要么有显式拒绝，**不存在静默跳过**。
  </action>
  <acceptance_criteria>
  - `grep -c "月份数\|报告期涵盖" src/extractor/notes.py` 输出 0（除注释外不得有版面搜索）
  - 含一条负向断言：未知报告类型抛异常而不是返回 12
  - `pipeline` 对六个 `kind` 无静默跳过分支
  </acceptance_criteria>
  <done>报告元数据由它真正的来源派生，且「不认识的报告类型」会炸而不是猜一个 12。</done>
</task>

</tasks>

<verification>
1. `.venv/Scripts/python -m pytest -q` 退出码 0
2. `git diff --stat metrics/` 为空
3. 两处冻结校验全部 OK
4. 六道门按 `F-8` 串联实跑 exit 0
</verification>
