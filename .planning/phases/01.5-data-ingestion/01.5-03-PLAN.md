---
phase: 01.5-data-ingestion
plan: 03
type: execute
wave: 3
depends_on: ["01.5-01", "01.5-02"]
files_modified:
  - data/mappings/pdf/bs.yaml
  - data/mappings/pdf/is.yaml
  - data/mappings/pdf/cfs.yaml
  - data/mappings/pdf/notes.yaml
  - data/mappings/pdf/kpi.yaml
  - src/extractor/mapping.py
  - src/extractor/locate.py
  - src/extractor/record.py
  - src/extractor/pipeline.py
  - tests/test_mapping.py
  - tests/test_column_binding.py
  - tests/test_missing_semantics.py
  - tests/fixtures/maotai_2023_bs_rows.json
autonomous: true
requirements: [SC-2, SC-3, SC-8, AC-01, AC-10]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 五份 PDF 映射文件存在，各带整数 mapping_version（D-016 判据 1）
    - 五份文件的 field_id 并集恰好等于 metrics/ 全部 source_fields[].id 的集合，不多不少（D-016 判据 2）
    - 上面那条断言里的 30 个 id 是在测试运行时从 metrics/ 读出来的，仓库里不存在第二份写死的字段清单（L-37）
    - 映射表加载器对缺必需键 fail-closed，有负向用例（D-016 判据 3）
    - 映射表里任何一条在整份 PDF 上零命中的规则都会让这一批加载判为失败，不会被当成「这个字段缺失」（L-36）
    - 每条映射条目绑定 column_header，取列不靠列序号（D-016 补充节）
    - 续页无表头时列绑定继承首页且 header_inherited 留证，不会像 cninfo 那样无条件把第 0 行当表头
    - 「行在但格子空」判为 EMPTY_CELL 且值为 0；「整行不存在」判为 ROW_ABSENT 且值缺失，二者各有真实茅台数据驱动的回归用例（SC-3 / A-2）
    - 有息负债率在茅台 2023 上算得出约 0.00119 的真实数值，而不是因 is_missing 拒答（A-2）
    - 标签匹配是整行逐字相等，不存在任何子串或正则容错路径（SC-2「不允许模糊匹配」）
    - statement: 映射表条目的标签描述与 metrics/*.yaml 的 line_item 不一致时会被发现，而不是静默漂移
      verification: backstop
  artifacts:
    - data/mappings/pdf/is.yaml
    - data/mappings/pdf/cfs.yaml
    - data/mappings/pdf/notes.yaml
    - data/mappings/pdf/kpi.yaml
    - tests/test_column_binding.py
    - tests/test_missing_semantics.py
  key_links:
    - metrics/*.yaml 的 source_fields[].id → 五份映射文件的 field_id（方向单向：映射表引用定义，不反过来）
    - PROBE-14.md 记录的行标签原文 → label_fragments（探测抽出来的原文就是这里要逐字写进去的东西）
    - ColumnBinding.header_text → ExtractionRecord.column_header（勾稽闸门抓不到整列取错，只有它抓得到）
    - CellState → formula 求值的缺失语义（EMPTY_CELL 按 0 参与，ROW_ABSENT 拒答）
---

<objective>
把 tracer 里只走通了三条的映射表补成五份完整的，并把两件 tracer 阶段刻意推迟的事做实：
**列到底取的是哪一列**，以及**「格子空」与「行不存在」的区分**。

这两件事是本阶段最容易被自证机制骗过去的地方。A-9 已经推导清楚：勾稽恒等式
「资产总计 = 负债合计 + 所有者权益合计」**在期末列与期初列上都成立**，三个数全取期初列时
D-018 的闸门照样放行——闸门证明的是「同一列内部自洽」，不是「取的是对的那一列」。
A-2 则是反面：茅台四个有息负债分项**行在报表上但格子是空的**，按 `is_missing` 拒答，
而正确答案是 0，有息负债率可算且约等于 0.00119。

Purpose: 让 SC-2 与 SC-3 从设计变成有真实数据驱动的回归；
让「不允许模糊匹配」这条要求有机械形态而不是纪律。
Output: 五份完整映射文件、一条防映射表与定义漂移的相等断言、两组真实回归用例。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01.5-data-ingestion/CONTEXT.md
@.planning/phases/01.5-data-ingestion/01.5-01-PLAN.md
@.planning/phases/01.5-data-ingestion/01.5-02-PLAN.md
@DECISIONS.md
@docs/agent/OPEN-ITEMS.md
@rules/failure-modes.md
@rules/commands.md
@src/extractor/mapping.py
@src/extractor/locate.py
@src/extractor/record.py
@src/semantic_layer/vocabulary.py
@src/semantic_layer/dsl.py
@scan_rules.yaml
</context>

## Artifacts this phase produces（本计划部分）

**新增数据文件**

- `data/mappings/pdf/is.yaml`、`cfs.yaml`、`notes.yaml`、`kpi.yaml`（各带 `mapping_version: 1`）

**新增函数**

- `mapping.load_all_pdf_mappings()` —— 一次加载五个命名空间并做跨文件校验
- `mapping.declared_field_ids()` —— **运行时**从 `metrics/*.yaml` 物化 `source_fields[].id` 集合，不读任何写死的清单
- `mapping.assert_mapping_covers_definitions()` —— D-016 判据 2 的相等断言
- `mapping.MappingCoverageError` —— 覆盖不等或零命中时抛出的异常类型
- `locate.resolve_column(row, binding)` —— 按表头绑定取格子，返回 `(value, CellState)`

**新增测试文件**

- `tests/test_column_binding.py`
- `tests/test_missing_semantics.py`

**扩展的既有符号**

- `data/mappings/pdf/bs.yaml`：由 3 条扩到 15 条
- `record.ExtractionRecord`：`cell_state` 在本计划开始承载真实取值（tracer 阶段三条记录全是 `VALUE_PRESENT`）

<tasks>

<task type="auto" tdd="true">
  <name>Task 1：补齐五份映射文件，并让「映射表与定义漂移」变成一条会红的测试</name>
  <files>data/mappings/pdf/bs.yaml, data/mappings/pdf/is.yaml, data/mappings/pdf/cfs.yaml, data/mappings/pdf/notes.yaml, data/mappings/pdf/kpi.yaml, src/extractor/mapping.py, tests/test_mapping.py</files>
  <read_first>
  - `DECISIONS.md` D-016 全文含补充节（三条判据；「标签是有序文本片段序列」；「列由表头文字显式绑定，不得靠列序号」；以及「为什么按命名空间拆而不是单一全局文件」那段关于 `notes` 与报表行是两种结构的论证）
  - `docs/agent/phase-01.5/PROBE-14.md`（01.5-02 探测出来的行标签**原文**——本任务要逐字抄进映射表的就是它，不要凭 `line_item` 的人读描述反推）
  - `src/extractor/mapping.py`（01.5-01 建的加载器，本任务扩展它，不重写）
  - `src/semantic_layer/vocabulary.py`（fail-closed 加载器的既有形状）与 `src/semantic_layer/definition.py`（`iter_definition_paths` / `load_definition` 是运行时读 `metrics/` 的现成入口）
  - `src/semantic_layer/dsl.py` 第 66 行 `ROOT_NAMESPACES` 与 `scan_rules.yaml` 的 `allowed_field_prefixes`（`tests/test_conformance.py` 已有断言校验两处不漂移，本任务的第三处要并进同一套心智模型）
  - `.planning/phases/01.5-data-ingestion/01.5-RESEARCH.md` 的 Q2 一节（cninfo 用子串匹配对齐 AKShare 列名产生三处「看起来合理、实际取错」的映射，`references/cninfo.md:204-213` 有原文）
  </read_first>
  <behavior>
  - `load_all_pdf_mappings()` 返回五个 `PdfMappingTable`，每个的 `mapping_version` 是整数
  - `declared_field_ids()` 返回的集合大小为 30，且它是遍历 `metrics/*.yaml` 得来的——把 `metrics/` 下任一定义文件的一个 `source_fields[].id` 改名后，该集合随之变化
  - `assert_mapping_covers_definitions()` 在五份文件与定义完全对齐时通过；从任一映射文件删掉一条 → 抛 `MappingCoverageError` 并指出缺哪个 id；往任一映射文件加一条 `metrics/` 里不存在的 id → 同样抛错并指出多了哪个
  - 加载器对缺 `column_header` / `label_variants` / `kind` / `statement` 任一必需键都抛异常
  - `label_fragments` 的匹配是整行逐字相等：给一条「资产总计（合计）」的行文本，`bs.total_assets` 不匹配
  - 某条规则在整份 PDF 上零命中时，`extract_batch` 判整批失败并报出是哪条规则，而不是把该字段记成缺失
  </behavior>
  <action>
  按 `PROBE-14.md` 与 01.5-01 的 tracer 结果，把五份文件写完整：`bs.yaml` 从 3 条扩到 15 条，
  `is.yaml` 8 条，`cfs.yaml` 2 条，`notes.yaml` 4 条，`kpi.yaml` 1 条，合计 30 条。
  每条的 `label_variants` 里的片段**逐字抄自 `PROBE-14.md` 记录的年报原文**，不要从
  `metrics/*.yaml` 的 `line_item` 反推——`line_item` 是给人看的文档字符串（`definition.py`
  加载它时不做任何解析），拿它当匹配规则用就是在制造一份没人校验的派生数据。

  `is.yaml` 的 `column_header` 与 `bs.yaml` **不同**：资产负债表是「期末余额」/「期初余额」，
  利润表是「本期金额」/「上期金额」（具体文字以 `PROBE-14.md` 记录的原文为准）。
  `is.operating_revenue_current` 与 `is.operating_revenue_prior_as_presented` 是同一行的两列，
  它们的区别**只在 `column_header`**——这两条是列绑定这件事最直接的证明位。

  `notes.yaml` 的四条与报表行不同构：`notes.business_combination_type` 与
  `notes.consolidation_scope_change`（D-020 新增，本计划先在映射表里给它位置，
  定义侧的改动在 `01.5-05-PLAN.md`）走的是「章节锚点 + 适用性复选项」，`kind` 是 `NOTE_CHECKBOX`；
  另两条走表格。D-016 明写这正是「为什么按命名空间拆而不是单一全局文件」的理由——
  塞进一份文件就得引入判别式联合。

  **顺带把 `NOTE_CHECKBOX` 的分派挂好，但不实现它。** `pipeline.extract_batch` 按映射条目的
  `kind` 分派：`STATEMENT_LINE` 走 01.5-01 已有的行重组路径，`NOTE_CHECKBOX` 分派到
  `extractor.notes.extract_checkbox_field`（该模块由 `01.5-05-PLAN.md` 实现）。
  本任务只建分派点与一条「未实现的 kind 抛 `NotImplementedError`」的占位，
  **不要**在 `pipeline.py` 里写复选项的读取逻辑——按 `L-35`，类别判定只能靠显式 `kind`，
  而按 `L-55`，各类别的处理逻辑不该堆在包装层里。

  **D-016 判据 2 那条相等断言，写法本身就是一条约束。**
  `declared_field_ids()` 必须**在运行时遍历 `metrics/*.yaml`** 把 30 个 `source_fields[].id`
  物化出来，绝不允许在代码里、在测试里、或在任何一份文档里再写一份 30 个 id 的清单去对照。
  理由是 `L-37`：派生集合不得与权威表并列存储，而「并列存储」**包括写进文档**
  ——文档也是一份派生数据，且是唯一没有机制校验的那份。
  （`PROBE-14.md` 里列出的 14 个 id 不算并列存储：它是一次观测的记录，不是权威清单，
  也不被任何代码读取。这个区别要在 `mapping.py` 的 docstring 里写一句，免得下一个人误删它。）

  **零命中即加载失败（`L-36`）在本任务落全。** `assert_mapping_covers_definitions()` 管的是
  「映射表与定义对不对得上」；零命中管的是「映射表与真实 PDF 对不对得上」。后者放在
  `pipeline.extract_batch` 结束时判：某条规则在整份 PDF 上一次都没匹配到 → 整批失败并报出
  是哪条规则。**不能记成「这个字段缺失」**——单一权威表里一条匹配不到任何东西的规则
  只可能是写错了，把它当数据缺失会让一个写错的规则一直活着。

  匹配路径里**不允许出现任何子串包含或正则容错**。SC-2 的原话是「不允许模糊匹配」，
  cninfo 与本项目自己的探测脚本都已实证子串匹配会产出静默错误
  （把「流动负债合计」的列错误映射到 `bs.total_liabilities`，把「净利润」错误映射到归母口径字段）。
  唯一允许的容错是**片段之间的纯数值行**，那是 A-6 实测的折行形态，不是模糊。
  </action>
  <verify>
    <automated>.venv/Scripts/python -m pytest tests/test_mapping.py -x -q && .venv/Scripts/python -m semantic_layer validate && .venv/Scripts/python -m semantic_layer scan</automated>
  </verify>
  <acceptance_criteria>
  - 五份文件都存在：`ls data/mappings/pdf/bs.yaml data/mappings/pdf/is.yaml data/mappings/pdf/cfs.yaml data/mappings/pdf/notes.yaml data/mappings/pdf/kpi.yaml` 退出码 0
  - `grep -h -v '^#' data/mappings/pdf/*.yaml | grep -c 'mapping_version:'` 输出 5
  - `grep -h -v '^#' data/mappings/pdf/*.yaml | grep -c 'field_id:'` 输出 30
  - `.venv/Scripts/python -c "from extractor.mapping import declared_field_ids; print(len(declared_field_ids()))"` 输出 30
  - `.venv/Scripts/python -c "from extractor.mapping import assert_mapping_covers_definitions as a; a(); print('ok')"` 输出 `ok` 且退出码 0
  - `tests/test_mapping.py` 含一条测试：把某个 `field_id` 从映射文件的内存副本里删掉后，用 `pytest.raises(MappingCoverageError)` 断言相等断言会红，且异常消息里逐字含被删的那个 id
  - `tests/test_mapping.py` 含一条测试：加入一个 `metrics/` 里不存在的 id 后同样抛 `MappingCoverageError`
  - `tests/test_mapping.py` 含一条测试：某条规则零命中时 `extract_batch` 判整批失败，且失败信息里含那条规则的 `field_id`
  - `tests/test_mapping.py` 含一条测试断言「资产总计（合计）」不匹配 `bs.total_assets`（整行逐字相等，不是子串包含）
  - `grep -rn -v '^#' src/extractor/ tests/test_mapping.py | grep -c 'declared_field_ids'` 输出 ≥ 2（一处定义、一处被断言使用），且 `src/extractor/mapping.py` 中不存在任何字面写死的 30 元素字段 id 列表
  - `.venv/Scripts/python -m semantic_layer validate` 退出码 0（`metrics/` 未被本任务改动）
  </acceptance_criteria>
  <done>五份映射表齐了，且「映射表与定义漂移」这件事从今天起有一条会红的测试；30 个 id 在仓库里只有一份权威来源。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2：列由表头文字绑定 —— 期末列与期初列的回归</name>
  <files>src/extractor/locate.py, src/extractor/record.py, src/extractor/pipeline.py, tests/test_column_binding.py, tests/fixtures/maotai_2023_bs_rows.json</files>
  <read_first>
  - `docs/agent/OPEN-ITEMS.md` A-9 全文（含「决定性推论」那段：恒等式在期末列与期初列上都成立，三个数全取期初列时闸门照样放行；以及「这是第四个自证机制说通过了但它证明的不是它声称证明的事的实例，前三个都是外部项目，这一个在我们自己的设计里」）
  - `DECISIONS.md` D-016 的 2026-08-21 补充节全文，特别是补充判据的原话：「存在一条回归用例……且**单独跑勾稽闸门不足以让该用例通过**——即证明这条用例查的是列绑定，不是恒等式」
  - `.planning/phases/01.5-data-ingestion/CONTEXT.md` §6 第三条（cninfo `pdf_parser.py:302` 无条件把第 0 行当表头，续页表格会静默吃掉一行真实数据，而其文档宣称支持跨页表格识别 ⇒ 表头识别必须是**显式判定 + 留证**）
  - `docs/agent/OPEN-ITEMS.md` A-6 第 3 条（`单位:元 币种:人民币` 与列名只印在表的首页，p59–p60 是裸续页；p61 一页之内从合并表切到母公司表）
  - `src/extractor/locate.py` 的 `bind_columns` 与 `ColumnBinding`（01.5-01 建的）
  - `docs/agent/phase-01.5/PROBE-14.md`（各张表的列表头原文）
  </read_first>
  <behavior>
  - 对茅台 2023 合并资产负债表，`bs.total_assets` / `bs.total_liabilities` / `bs.total_equity` 三个字段取到的都是**期末余额**列的值，且三个值与年报原文逐字一致
  - 构造一个「把三个字段全部改成期初余额列的值」的批次：勾稽闸门**仍然通过**（因为期初列自己也平），而列绑定校验**不通过**
  - 续页（无表头）上的行，其记录的 `header_inherited` 为 True，首页上的行为 False
  - 表头文字与映射条目的 `column_header` 不相等时不取该格子，返回 `CellState` 而不是猜一个列序号
  - 利润表上 `is.operating_revenue_current` 与 `is.operating_revenue_prior_as_presented` 取到两个不同的值，且各自对应自己的表头文字
  </behavior>
  <action>
  `resolve_column(row, binding)` 按表头绑定的 x 区间取格子，**不接受列序号作为入参**——D-016 补充节的原话是「列由表头文字显式绑定，不得靠列序号」，理由与「标签必须是片段序列」同源：不显式绑定，取列就只能靠位置约定，而位置约定在换一家公司的排版时**静默失效**。

  表头继承要留证：续页没有表头时继承首页的 `ColumnBinding` 并把 `header_inherited` 置 True，
  这个布尔值进 `ExtractionRecord`。这是对 cninfo 那条教训的直接回应——它无条件把第 0 行当表头，
  续页因此静默吃掉一行真实数据。**显式判定加留证**的意思是：继承是允许的，但继承这件事本身必须在证据里看得见。

  `tests/test_column_binding.py` 是本任务的核心产物，它必须做到 D-016 补充判据要求的那件事：
  **证明这条用例查的是列绑定，不是恒等式。** 具体做法是同一份固件跑两条断言——

  第一条：正常抽取 → 三个字段取到期末列的值 → 与年报原文逐字相等。
  第二条：构造一个「三个字段全取期初列」的批次 → 断言 `reconcile_batch` 的 `passed` **仍为 True**
  （期初列自己也平，这正是 A-9 的推论），同时断言列绑定校验 `passed` 为 False。
  第二条断言里那个 `reconcile.passed is True` 不是笔误，它就是这条用例存在的理由——
  它逐字证明了「单独跑勾稽闸门不足以让这条用例通过」。

  在测试模块的 docstring 里写清楚这两条断言的分工，别让后来的人以为第二条写反了。

  把期初列的值补进 `tests/fixtures/maotai_2023_bs_rows.json`（tracer 阶段只存了期末列），
  否则构造不出「期初列自己也平」的那个批次。
  </action>
  <verify>
    <automated>.venv/Scripts/python -m pytest tests/test_column_binding.py -x -q && .venv/Scripts/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
  - `tests/test_column_binding.py` 含一条测试断言三个 `bs.*` 字段取到的是「期末余额」列，且值与固件里记录的年报原文值相等
  - `tests/test_column_binding.py` 含一条测试同时断言两件事：全取期初列的批次上 `reconcile_batch(...).passed is True`，且列绑定校验为 False
  - `tests/test_column_binding.py` 的模块 docstring 里逐字含「单独跑勾稽闸门不足以让该用例通过」
  - `tests/test_column_binding.py` 含一条测试断言续页行的 `header_inherited is True`、首页行为 False
  - `tests/test_column_binding.py` 含一条测试断言 `is.operating_revenue_current` 与 `is.operating_revenue_prior_as_presented` 取到的值不相等
  - `grep -v '^#' src/extractor/locate.py | grep -c 'header_inherited'` 输出 ≥ 2
  - `.venv/Scripts/python -m pytest -q` 退出码 0
  - `.venv/Scripts/python scripts/check_gates.py` 退出码 0（新增测试全部有可失败点，R1/R3 通过）
  </acceptance_criteria>
  <done>「取的是对的那一列」这件事有了一条会红的测试，而且这条测试自己证明了它查的不是恒等式——A-9 从一个推导变成了一条机械保障。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3：「行在格子空 = 0」与「整行不存在 = 缺失」的真实回归</name>
  <files>src/extractor/record.py, src/extractor/pipeline.py, tests/test_missing_semantics.py, tests/fixtures/maotai_2023_bs_rows.json</files>
  <read_first>
  - `docs/agent/OPEN-ITEMS.md` A-2 全文（实证：茅台 2023 年报 p58/p59，短期借款 / 长期借款 / 应付债券**行项目印在报表上但值的格子是空的**；当前定义按 `is_missing` 拒答，而正确答案是 0；判据是「抽取层区分两者，定义侧不改」）
  - `.planning/phases/01.5-data-ingestion/CONTEXT.md` §3.3（六个有息负债分项的逐项真实取值表：四个「行在格子空」、`non_current_liabilities_due_within_one_year` 是 57,054,879.48、`lease_liabilities` 是 266,636,234.04、**整行不存在的字段 0 个**；按「格子空 = 0」有息负债率 ≈ 0.00119）
  - `metrics/interest_bearing_debt_ratio.yaml`（六个分项的 `source_fields`、`formula`、以及它现在的 `undefined_conditions` —— 定义侧**不动**，这是 CONTEXT §5 的非目标，D-020 的放宽只限那一条）
  - `src/semantic_layer/dsl.py` 第 1–12 行与 `MISSING` 哨兵（「缺失与取值为零走不同分支」是 R6 的机械实现，抽取层要延续同一套语义而不是另起一套）
  - `src/extractor/record.py` 的 `CellState`（01.5-01 定义的三个成员）
  - `src/extractor/formula.py` 的求值缺失语义（01.5-01 已定：`ROW_ABSENT` 拒答、`EMPTY_CELL` 按 0）
  </read_first>
  <behavior>
  - 茅台 2023 上，`bs.short_term_borrowings` / `bs.long_term_borrowings` / `bs.bonds_payable` / `bs.trading_financial_liabilities` 四个字段的 `cell_state` 是 `EMPTY_CELL`，`value` 是 `Decimal("0")`，`status` 是 `SUCCESS`
  - `bs.non_current_liabilities_due_within_one_year` 的值是 `Decimal("57054879.48")`，`bs.lease_liabilities` 是 `Decimal("266636234.04")`，两者 `cell_state` 为 `VALUE_PRESENT`
  - 构造一个「整行不存在」的固件（把某一行从行集合里整条删掉）：该字段 `cell_state` 是 `ROW_ABSENT`，`value` 是缺失而不是 0
  - `interest_bearing_debt_ratio` 在真实茅台批次上算出一个约 0.00119 的数值，不返回拒答
  - 同一个指标在「整行不存在」的构造批次上返回拒答，不返回 0
  </behavior>
  <action>
  这一条是 SC-3，也是 A-2 的判据。要点在于**定义侧一个字都不改**——`metrics/interest_bearing_debt_ratio.yaml`
  的 `undefined_conditions` 保持原样，修的是抽取层：格子空时抽取层给出的是一个**有值的 0**，
  而不是一个缺失哨兵，于是 `is_missing(...)` 自然为假，定义里那条拒答条件不触发。
  这就是 A-2 说的「抽取层区分两者，定义侧不改」。

  实现落在 `resolve_column` 的返回上：表头绑定的 x 区间内找不到任何字符 → `EMPTY_CELL`，值取 `Decimal("0")`；
  整行在重组结果里不存在（映射规则匹配不到任何行）→ `ROW_ABSENT`，值缺失。
  注意与 `L-36` 的分工：`ROW_ABSENT` 是**单份 PDF 上这一行确实没印**，而 `L-36` 的零命中是
  **这条规则在整份 PDF 上一次都没匹配上**。茅台 2023 的实测事实是「整行不存在的字段 0 个」，
  所以这两者在本样本上都不该出现；`ROW_ABSENT` 的回归用例只能靠**构造**固件，这一点要在测试里写明。

  `tests/test_missing_semantics.py` 用真实数据驱动六个有息负债分项，逐个断言 `cell_state` 与 `value`
  ——**逐个断言，不写一条「四个都是 EMPTY_CELL」的聚合断言**。SC-8 的要求是验收看取到的值，
  聚合断言恰好是命中数的另一种写法。

  然后跑一次 `interest_bearing_debt_ratio`：真实批次上算出约 0.00119，构造的 `ROW_ABSENT` 批次上拒答。
  一正一反两条，缺一条这个语义就只被验了一半。
  </action>
  <verify>
    <automated>.venv/Scripts/python -m pytest tests/test_missing_semantics.py -x -q && .venv/Scripts/python -m pytest -q && .venv/Scripts/python -m semantic_layer validate</automated>
  </verify>
  <acceptance_criteria>
  - `tests/test_missing_semantics.py` 中六个字符串各自逐字出现：`bs.short_term_borrowings`、`bs.long_term_borrowings`、`bs.bonds_payable`、`bs.trading_financial_liabilities`、`bs.non_current_liabilities_due_within_one_year`、`bs.lease_liabilities`
  - 该文件含逐个字段的独立断言，其中 `57054879.48` 与 `266636234.04` 两个数字各自逐字出现
  - 该文件含一条测试断言四个空格子字段的 `status is ExtractionStatus.SUCCESS`（空格子是一次成功的抽取，结论是 0，不是失败）
  - 该文件含一条用构造固件驱动的测试，断言 `cell_state is CellState.ROW_ABSENT` 且该字段的值不等于 `Decimal("0")`
  - 该文件含一条测试断言 `interest_bearing_debt_ratio` 在真实批次上返回的数值落在 `0.0011` 与 `0.0013` 之间
  - 该文件含一条测试断言同一指标在 `ROW_ABSENT` 构造批次上返回拒答对象而不是数值
  - `git diff --stat metrics/` 输出为空（定义侧不动，CONTEXT §5 的非目标）
  - `.venv/Scripts/python -m semantic_layer validate` 退出码 0
  - `.venv/Scripts/python -m pytest -q` 退出码 0
  </acceptance_criteria>
  <done>「格子空」和「行不存在」在抽取层被分开了，两者各有真实样本或构造样本驱动的回归；有息负债率不再对一家实际无有息负债的公司拒答。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `data/mappings/pdf/*.yaml` → 加载器 | 映射表是配置输入；写错一条会让抽取器静默取错行或取错列 |
| PDF 行文本 → 标签匹配 | 不可信文本参与匹配判定，模糊匹配会产出静默错误 |
| 表头文字 → 列归属 | 表头识别错误会让整列取错，而勾稽闸门抓不到 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.5-12 | Tampering | 标签匹配路径 | high | mitigate | 整行逐字相等，匹配路径里不存在子串包含与正则容错；负向用例断言「资产总计（合计）」不匹配 `bs.total_assets` |
| T-01.5-13 | Tampering | 列归属（整列取错） | high | mitigate | `column_header` 显式绑定 + `header_inherited` 留证；`tests/test_column_binding.py` 的双断言用例自证它查的不是恒等式（D-016 补充判据） |
| T-01.5-14 | Spoofing | 映射表与定义漂移 | high | mitigate | `assert_mapping_covers_definitions()` 相等断言 + 两条负向用例（少一条、多一条各红一次）；30 个 id 运行时从 `metrics/` 物化，仓库无第二份清单（L-37） |
| T-01.5-15 | Repudiation | 写错的映射规则长期存活 | high | mitigate | 零命中即整批失败并报出是哪条规则（L-36），不记成「字段缺失」 |
| T-01.5-16 | Tampering | 「格子空」被当成缺失 | medium | mitigate | `CellState` 三态显式区分；六个有息负债分项逐个断言，不写聚合断言 |
| T-01.5-17 | Information Disclosure | 固件里混入年报原文段落 | low | accept | 固件只存数值行与坐标；`semantic_layer scan` 在每个 task 的 verify 里兜底 |
</threat_model>

<verification>
1. `.venv/Scripts/python -c "from extractor.mapping import assert_mapping_covers_definitions as a; a(); print('ok')"` 输出 `ok`
2. `grep -h -v '^#' data/mappings/pdf/*.yaml | grep -c 'field_id:'` 输出 30
3. `git diff --stat metrics/` 为空
4. 六道门按 F-8 串联实跑 exit 0：`.venv/Scripts/python -m pytest -q && .venv/Scripts/python -m semantic_layer scan && .venv/Scripts/python -m semantic_layer validate && .venv/Scripts/python scripts/check_xrefs.py && .venv/Scripts/python scripts/check_reading_ledger.py && .venv/Scripts/python scripts/check_gates.py`
5. 两处冻结校验全部 OK
</verification>

<success_criteria>
- 五份映射表齐全，并集恰好等于 30 个 `source_fields[].id`
- 映射表与定义漂移、映射规则零命中、列取错、格子空被当缺失——四件事各有一条会红的测试
- 有息负债率在茅台 2023 上算得出约 0.00119 的真实数值
- `metrics/` 未被本计划改动一个字节
</success_criteria>

<output>
Create `.planning/phases/01.5-data-ingestion/01.5-03-SUMMARY.md` when done
</output>
