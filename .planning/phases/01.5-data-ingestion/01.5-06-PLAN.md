---
phase: 01.5-data-ingestion
plan: 06
type: execute
wave: 5
depends_on: ["01.5-04", "01.5-05"]
files_modified:
  - tests/test_reconciliation.py
  - tests/test_column_binding.py
  - tests/test_evidence_ids.py
  - src/extractor/record.py
  - src/extractor/pipeline.py
  - docs/agent/VERIFICATION.md
  - docs/agent/LANDING-BACKLOG.md
  - docs/agent/OPEN-ITEMS.md
  - docs/agent/PROGRESS.md
  - DECISIONS.md
  - references/README.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [SC-1, SC-2, SC-3, SC-4, SC-5, SC-6, SC-7, SC-8, AC-01, AC-02, AC-05, AC-10]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - 勾稽闸门有一条负控制用例，去掉闸门后它会红，且红的原因正是勾稽不通过而不是解析失败（J-5）
    - 列绑定有一条负控制用例，去掉列绑定后它会红，且单独跑勾稽闸门不足以让它通过（J-5 + D-016 补充判据）
    - 两条负控制都按 rules/commands.md 的三步程序实跑过「造回归 → 看红 → 回退」，记录写进提交信息（L-5）
    - 抽取记录里的每个证据标识（field_id / page / anchor_page / batch_id）都能被逐字比对，有一条机械检查（J-6）
    - 每条证据指针携带的取数口径三元组齐全，缺任一项即判失败（J-7 / L-2）
    - AC-05 的「齐全」按写死的口径判：字段有真值，空字符串 / null / 恒定占位值 / 恒零计数一律不计入齐全
    - LANDING-BACKLOG 里本阶段落地的 12 条 L-nn 状态已更新，且每个编号在 references/ 里有回标（check_gates 的 R5 会强制）
    - 外部参考仓库的 commit 短哈希已钉进 references/，使「结论引自哪个版本」可复核（A-10 扩充判据）
    - VERIFICATION.md 的 Phase 1.5 一节按「验收标准 → 验证方法 → 实际命令 → 真实输出 → 证据位置 → PASS/FAIL/UNVERIFIED」逐条填写，没有证据的记 UNVERIFIED 不记 PASS
    - SC-8 的验收由操作者逐字段看取到的值完成，不由执行者代答
    - 六道门 + 供应链门禁 + 两处冻结校验在同一条 && 链上全绿
  artifacts:
    - tests/test_evidence_ids.py
    - docs/agent/VERIFICATION.md（Phase 1.5 一节）
  key_links:
    - GATES 注册表 / gates.yml / rules/commands.md 三者一致（check_gates 的 R4 强制）
    - LANDING-BACKLOG 的状态词 → references/ 的 L-nn 回标（check_gates 的 R5 强制）
    - ExtractionRecord 的标识字段 → tests/test_evidence_ids.py 的逐字比对
---

<objective>
收口：把本阶段声称做到的每一件事都变成一条可复跑的证据，把做过的事回标到台账，
把没做到的事如实记成 `UNVERIFIED`。

本阶段有两处特别容易在收口时出事，所以单列出来：

1. **闸门的负控制。** `EVAL_CASES` 的 `J-5` 与 `rules/commands.md` 的三步程序都要求
   闸门类断言配一条「去掉它就会失败」的用例，**且红的原因要正确**。
   2026-08-24 建第六道门时第一次实跑这个程序就跑砸了——造回归的脚本把受害文件切成语法错误，
   门禁崩在 traceback 上，**它红了，但红的原因不是它声称在查的那件事**。
   本阶段有两道闸门（勾稽、列绑定），两条负控制都要真跑一遍这个程序。

2. **做了没回标。** 台账 N-35 记的就是这个：做了但台账没回标，`未落地` 标注会继续骗下一个读它的人。
   `check_gates.py` 的 R5 已经把它机械化了——`LANDING-BACKLOG` 里状态为「已落地 / 部分落地 / 已关闭」
   的每个 `L-nn`，编号必须在 `references/` 里出现过。本阶段落地 12 条，不回标就是一道红门。

Purpose: PROJECT_SPEC §11 的完成定义；把本阶段的产出接回台账与规格，不留悬空。
Output: 两条负控制、一份 J-6/J-7 的机械检查、`VERIFICATION.md` 的 Phase 1.5 一节、
以及一次由操作者亲自做的 SC-8 验收。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01.5-data-ingestion/CONTEXT.md
@PROJECT_SPEC.md
@DECISIONS.md
@EVAL_CASES.md
@CLAUDE.md
@rules/failure-modes.md
@rules/commands.md
@docs/agent/VERIFICATION.md
@docs/agent/LANDING-BACKLOG.md
@docs/agent/OPEN-ITEMS.md
@docs/agent/ARCHITECTURE.md
@scripts/check_gates.py
@.github/workflows/gates.yml
</context>

## Artifacts this phase produces（本计划部分）

**新增测试**

- `tests/test_evidence_ids.py` —— J-6 的字面存在性检查与 J-7 的三元组齐全性检查

**新增函数**

- `record.evidence_identifiers(record)` —— 返回该记录全部证据标识的字符串集合，供逐字比对
- `pipeline.completeness_report(batch)` —— 按 AC-05 写死的口径统计字段齐全率（有真值才算齐全）

**修改的既有文档**

- `docs/agent/VERIFICATION.md` —— 新增 `## Phase 1.5` 一节
- `docs/agent/LANDING-BACKLOG.md` —— 12 条 `L-nn` 状态更新
- `docs/agent/OPEN-ITEMS.md` —— A-2 / A-6 / A-9 / A-10 的判据状态更新
- `docs/agent/PROGRESS.md` —— changelog 追加（append-only）
- `references/README.md` —— 12 条 `L-nn` 回标 + 外部仓 commit 短哈希
- `DECISIONS.md` —— 补记 01.5-01 Task 2 检查点裁决出来的契约形状（若裁决为扩展 D-019 或新增 RefusalCode）
- `.planning/STATE.md`、`.planning/ROADMAP.md` —— 阶段状态与 Plans 勾选

**明确不新增门禁**：J-6 落成一条 pytest，不落成第七道门。理由是 `check_gates.py` 的
R2/R4/R5 对每道登记门禁都有连带要求（具名负控制 + 进 `gates.yml` + 同步 `rules/commands.md`），
为一条能用测试表达的检查再加一道门是净增流程开销，触及 D-009 的 30% 红线。
这条判断要写进 `tests/test_evidence_ids.py` 的模块 docstring，免得下次有人当成漏做。

<tasks>

<!-- 2026-08-25 第二轮并入（CONTEXT.md §7.7）：
     - L-70：「preview 进上下文 + 全文落盘 + stats 记裁剪量」三件套。
       与 L-45（截断报 PARTIAL）是同一件事的两半——L-45 说状态怎么报，
       本条说被裁掉的东西去哪。收口时核：凡报了 PARTIAL 的记录，
       其 truncation_stats 非空，且全文可由记录里的指针找回。
       （实证：hello-agents 的 truncator 形状完全正确，全文确实写盘了，
       但指针在唯二两个调用点被丢弃，全包零处读回。）
-->


<task type="auto" tdd="true">
  <name>Task 1：两道闸门的负控制 —— 造回归、看红、确认红的原因正确、回退</name>
  <files>tests/test_reconciliation.py, tests/test_column_binding.py</files>
  <read_first>
  - `rules/commands.md` 的「假闸门的验证程序：造回归 → 看红 → 回退」全节，尤其是第 2 步后半句与那段关于 2026-08-24 第一次实跑就跑砸的记录（造回归的脚本把受害文件切成语法错误，门禁崩在 traceback 上）
  - `EVAL_CASES.md` §3.3 的 `J-5` 全文（含那句「本项目已有同型实证……**所以负控制不只要会红，还要红的原因正确**」，以及「实施侧的对应物是 `scripts/check_gates.py` 的 R2 与 `rules/commands.md` 的三步程序，**两者都不能替代本条**：那两者管的是仓库自己的门禁，本条管的是**被评测系统**的闸门」）
  - `DECISIONS.md` D-016 补充判据（「**单独跑勾稽闸门不足以让该用例通过**——即证明这条用例查的是列绑定，不是恒等式」）与 D-018 判据 1
  - `docs/agent/OPEN-ITEMS.md` A-9 全文
  - `scripts/check_gates.py` 的 R1 / R3（每个 `test_*` 必须有可失败点；恒真断言不算断言；`NO-ASSERT-BY-DESIGN` 的例外要求理由**全仓唯一**）
  - `tests/test_reconciliation.py` 与 `tests/test_column_binding.py`（01.5-01 与 01.5-03 建的现有用例）
  </read_first>
  <behavior>
  - 把 `reconcile_batch` 的返回硬改成恒 `passed=True` 之后，勾稽负控制用例失败，且失败信息里出现的是「勾稽未通过却被放行」这一类断言失败，不是 `AttributeError` / `ImportError` / 解析错误
  - 把列绑定改成「按列序号取第一个数值列」之后，列绑定负控制用例失败，而勾稽用例**仍然通过**
  - 回退之后两条用例都恢复绿
  - 两条负控制用例本身有 `assert`，`check_gates.py` 的 R1/R3 通过
  </behavior>
  <action>
  这一条不是写代码，是**实跑一个程序并留下记录**。

  **勾稽闸门的负控制。** 造回归的方式是把 `reconcile.reconcile_batch` 的判定改成恒真
  （只改判定那一行，其余一字不动——`rules/commands.md` 明写改法要「只替换前缀、其余一字不动」，
  并在动手前先确认改完的文件语法仍然合法）。然后跑 `tests/test_reconciliation.py`，
  确认它红，**并读失败输出确认红的原因是那条构造失败用例没被拦住**，
  而不是导入失败、语法错误或别的什么。确认后回退，复跑，确认变绿。

  **列绑定的负控制。** 造回归的方式是把 `locate.resolve_column` 改成按列序号取第一个数值列
  （即回到 D-016 补充节明令禁止的那种位置约定）。跑 `tests/test_column_binding.py`，
  确认它红；**同时跑 `tests/test_reconciliation.py`，确认它仍然绿**。
  后半句是这条负控制的全部意义所在——它逐字证明了 D-016 补充判据要求的那件事：
  单独跑勾稽闸门不足以让列绑定用例通过。回退，复跑，确认两者都绿。

  回退用 `trap ... EXIT` 写，别指望自己记得还原（`rules/commands.md` 给了写法）。

  **四段真实输出（两次红、两次回退后的绿）连同命令一起写进提交信息**，
  这是 `L-5` 要求的「跑的记录写进提交信息」。不要只写「已按程序验证」。

  如果某一条负控制**没能红**，那说明那道闸门是假的，此时的正确动作是修闸门而不是修用例，
  并把这件事记进 `docs/agent/OPEN-ITEMS.md`——这正是这个程序存在的理由。
  </action>
  <verify>
    <automated>.venv/Scripts/python -m pytest tests/test_reconciliation.py tests/test_column_binding.py -x -q && .venv/Scripts/python scripts/check_gates.py</automated>
  </verify>
  <acceptance_criteria>
  - `tests/test_reconciliation.py` 中存在一条测试，其名称或 docstring 逐字含「负控制」，且它断言构造的失败批次会被闸门拦住
  - `tests/test_column_binding.py` 中存在一条测试，其名称或 docstring 逐字含「负控制」，且模块 docstring 里逐字含「单独跑勾稽闸门不足以让该用例通过」
  - 提交信息中含四段带命令与退出码的真实输出：勾稽造回归后的红、回退后的绿、列绑定造回归后的红（含同时跑勾稽仍绿的输出）、回退后的绿
  - `.venv/Scripts/python scripts/check_gates.py` 退出码 0
  - `.venv/Scripts/python -m pytest -q` 退出码 0
  - 工作区干净：`git status --porcelain src/extractor/` 无输出（造回归已全部回退）
  </acceptance_criteria>
  <done>两道闸门各自被证明「真的会拦住它声称要拦的东西」，而且证明过程本身留了可复核的记录；列绑定那条同时证明了勾稽闸门替代不了它。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2：证据标识的字面存在性与取数口径三元组的齐全性（J-6 / J-7 / AC-05 口径）</name>
  <files>tests/test_evidence_ids.py, src/extractor/record.py, src/extractor/pipeline.py</files>
  <read_first>
  - `EVAL_CASES.md` §3.3 的 `J-6` 全文（「回答里出现的每一个证据标识（`field_id` / `page` / 表格锚点 / 准则条号）必须**逐字出现在**本次提供给系统的上下文或抽取记录里；对不上的记 `FAIL`，归因为证据层，**不得因为最终数字正确而放过**」，以及「这是唯一一条不需要 LLM 判分就能跑的证据链检查」）
  - `EVAL_CASES.md` §3.3 的 `J-7` 全文（缺三元组任一项记 `FAIL`，归因为证据层）
  - `PROJECT_SPEC.md` §9 的 `AC-05` 与其**计数口径**（「齐全」= 字段有真值，不是键存在；空字符串、`null`、恒定占位值、恒零计数**一律不计入齐全**；反面实证是 hello-agents 八键齐全率 100% 而其中三个是假的——`llm_provider` 恒为 `"unknown"`、`total_tokens` 恒 0、工具哈希不含参数）
  - `PROJECT_SPEC.md` §9 的「AC-05 的已知盲区」一段（AC-05 只能抓证据缺失，抓不到证据不相关；无自动判据）
  - `docs/agent/ARCHITECTURE.md` §8.5.2（三元组的定义与 harness 的实证）
  - `src/extractor/record.py`（`ExtractionRecord` 与 `SelectionProvenance` 的现有形状）
  - `scripts/check_gates.py` 的 R1（每个 `test_*` 必须有可失败点）
  </read_first>
  <behavior>
  - `evidence_identifiers(record)` 返回的每个字符串都能在该记录的序列化输出里逐字找到
  - 记录里的 `field_id` 逐字出现在对应的映射文件中；`page` 与 `anchor_page` 是整数；`batch_id` 在同一批次的所有记录上相同
  - 把某条记录的 `field_id` 改成一个映射表里没有的串之后，检查会红并指出是哪一条
  - `completeness_report(batch)` 对一个所有键都存在但 `unit` 是空字符串的批次给出的齐全率**小于** 100%
  - 三元组任一项为空的记录被 `completeness_report` 判为不齐全
  - 一个 `truncation_stats` 非空而 `status` 是 `SUCCESS` 的记录被判为不合法（L-45）
  </behavior>
  <action>
  `record.evidence_identifiers(record)` 返回该记录的全部证据标识（`field_id`、`page`、`anchor_page`、
  `batch_id`、`column_header`、`pdf_sha256`）的字符串集合。`tests/test_evidence_ids.py` 拿它做两件事：

  **J-6 的字面存在性。** 逐条断言 `field_id` 逐字出现在对应命名空间的映射文件里、
  `page` 与 `anchor_page` 是整数、`batch_id` 全批一致。再加一条负向用例：
  把某条记录的 `field_id` 换成一个映射表里没有的串，断言检查会红并指出是哪一条。
  这条检查的价值在于它**根本不需要 LLM**——`J-6` 明写它与 `J-1` 天然互补：
  `J-1` 限制什么时候能用 LLM 判分，本条给出一个不需要 LLM 的判据。
  依据是 hello-agents `ch9:1526` 的示例答案引用了一个**从未进入上下文的笔记 id**。

  **J-7 / L-2 的三元组齐全性。** 逐条断言 `selection` 的 `sampling` / `order_key` / `truncation`
  三项都非空。缺任一项记失败，归因为证据层——`J-7` 的原话是「它不是证据少，是证据的含义被改了」。

  `pipeline.completeness_report(batch)` 按 `AC-05` **写死的口径**统计：
  「齐全」= 字段有真值，**不是键存在**。空字符串、`null`、恒定占位值（如 `"unknown"`）、
  恒零计数一律不计入齐全。这个口径是 2026-08-22 写死的，反面实证是 hello-agents 的
  八键齐全率也是 100% 而其中三个是假的。要有一条测试拿一个「键全在但 `unit` 是空串」的批次
  去跑，断言齐全率小于 100%——**没有这条测试，`completeness_report` 自己就会变成那个假 100%**。

  再补一条 `L-45` 的交叉断言：`truncation_stats` 非空而 `status is SUCCESS` 的组合判为不合法。
  报成功的话证据链里不会出现 `truncation_stats`，`AC-05` 就会在一个假成功上判过。

  在模块 docstring 里写清楚**两件本检查抓不到的事**，不许把「这个测试绿了」读成「证据链是对的」：
  一是 `AC-05` 的已知盲区——它只能抓「证据缺失」，抓不到「证据不相关」
  （`PROJECT_SPEC.md` §9 已记，实证是 hello-agents 的 `arun_stream` 每步调两次 LLM，
  屏幕上的推理与实际执行的工具调用无因果关系，而字段齐全率仍是 100%）；
  二是本项目不为此新增第七道门的理由（见本计划开头「明确不新增门禁」一节）。
  </action>
  <verify>
    <automated>.venv/Scripts/python -m pytest tests/test_evidence_ids.py -x -q && .venv/Scripts/python -m pytest -q && .venv/Scripts/python scripts/check_gates.py</automated>
  </verify>
  <acceptance_criteria>
  - `tests/test_evidence_ids.py` 含一条测试断言每条记录的 `field_id` 逐字出现在对应映射文件的文本中
  - `tests/test_evidence_ids.py` 含一条负向测试：把 `field_id` 换成映射表里没有的串后检查失败，且失败信息里含那个串
  - `tests/test_evidence_ids.py` 含一条测试断言 `selection` 的三项均非空，以及一条断言任一项为空时判为不齐全
  - `tests/test_evidence_ids.py` 含一条测试：一个所有键都存在但 `unit` 为空串的批次，`completeness_report` 给出的齐全率 < 1.0
  - `tests/test_evidence_ids.py` 含一条测试断言 `truncation_stats` 非空且 `status is ExtractionStatus.SUCCESS` 的组合被判为不合法
  - `tests/test_evidence_ids.py` 的模块 docstring 里逐字含「抓不到」，并写明 AC-05 的已知盲区与不新增门禁的理由
  - `.venv/Scripts/python scripts/check_gates.py` 退出码 0
  - `.venv/Scripts/python -m pytest -q` 退出码 0
  </acceptance_criteria>
  <done>证据标识是不是系统自己编出来的、取数口径三元组齐不齐、字段齐全率是不是被空值刷出来的——三件事各有一条会红的测试。</done>
</task>

<task type="auto">
  <name>Task 3：台账回标、决策补记、VERIFICATION.md、六道门全量实跑</name>
  <files>docs/agent/LANDING-BACKLOG.md, references/README.md, docs/agent/OPEN-ITEMS.md, docs/agent/VERIFICATION.md, docs/agent/PROGRESS.md, DECISIONS.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
  - `scripts/check_gates.py` 的 R5 实现（`check_landed_items_are_back_annotated`，第 575 行起）与其 docstring 的「R5 只查有没有回标，不查回标写得对不对」一段——**它挡的是遗漏，而遗漏正是实际发生过的那件事**
  - `docs/agent/LANDING-BACKLOG.md` §5 的表格行格式（`| L-nn | ... |`）与状态词（「已落地」/「部分落地」/「已关闭」）
  - `docs/agent/OPEN-ITEMS.md` 的 A-2 / A-6 / A-9 / A-10 四条的**判据**原文（判据是待办不是已办，逐条对照本阶段的实际产出）与 N-35
  - `docs/agent/VERIFICATION.md` 的 Phase 1 一节（14 行验收矩阵 + 三项阶段末判定 + 附录 A 九段逐字命令输出的既有格式，Phase 1.5 照此写）
  - `PROJECT_SPEC.md` §11（完成定义的六段式）与 §10（停止条件）
  - `rules/failure-modes.md` F-4（记进台账 ≠ 做了）与 F-8（验证与提交一律 `&&` 串联）
  - `.planning/phases/01.5-data-ingestion/01.5-01-PLAN.md` 的 Task 2 检查点结论（决定了 `DECISIONS.md` 要不要补记）
  - `.planning/phases/01.5-data-ingestion/01.5-02-PLAN.md` 的 Task 4 检查点结论（SC-2 门槛的裁决）
  </read_first>
  <action>
  **回标 12 条。** 本阶段落地了 `L-2` / `L-9` / `L-12` / `L-13` / `L-34` / `L-35` / `L-36` /
  `L-37` / `L-38` / `L-45` / `L-47` / `L-55`。把 `LANDING-BACKLOG` §5 里这 12 行的状态改成
  「已落地」或「部分落地」并注明落在哪个产物、哪一节；**同时**在 `references/README.md` 里
  给每个编号写一条回标（写在与其出处对应的那份 reference 的覆盖表里）。
  R5 会强制这件事：状态一改成已落地，编号没在 `references/` 里出现过就是一道红门。
  写「部分落地」的要注明还差什么——`L-2` 尤其要注意，它原本就挂着
  「**仍须确认是否构成对 D-019 的扩展**」，01.5-01 的 Task 2 检查点已经把它确认掉了，
  回标时要把这句待确认删掉并写上裁决结果。

  **A-10 的扩充判据**：在 `references/README.md` 里把外部参考仓库的 commit 短哈希钉死
  （deepseek-harness 是 `47f9438`，其余两个仓照此补），使「结论引自哪个版本」可复核。
  语料一丢，既有引用的行号全部无法回指——这已经发生过一次。

  **台账 A 区四条**逐条对照判据更新：A-2（区分格子空与整行不存在 + 真实回归用例）、
  A-6（三条失效模式各有一个茅台真实数据驱动的回归用例）、A-9（列绑定回归用例，
  且证明单独跑勾稽闸门不足以让它通过）、A-10（下载链路已是仓库内脚本 + commit 钉死）。
  **判据是待办不是已办**——逐条看本阶段的实际产出对不对得上，对不上的如实写「未达成」，
  不要因为「做了差不多的事」就勾掉。

  **决策补记。** 若 01.5-01 的 Task 2 检查点裁决为「把 `selection` 三元组作为 D-019 的扩展」，
  在 `DECISIONS.md` 的 D-019 下补一节记录该扩展及其判据；若裁决为「新增
  `RefusalCode.RECONCILIATION_FAILED`」，补一条新决策记录 AC-02 取值域的这次变更。
  若两者都选了「不扩展」的选项，也要写一句记录（「问题因裁决为 X 而关闭」和「问题没被提出」是两回事）。
  01.5-02 Task 4 关于 SC-2 门槛的裁决同理：若选了作废通道或调低门槛，
  要在 `ROADMAP.md` 的 SC-2 与 `DECISIONS.md` 里留下记录与理由。

  **`VERIFICATION.md` 新增 `## Phase 1.5` 一节**，照 Phase 1 的格式：
  一张按 SC-1…SC-8 逐条的验收矩阵（验收标准 → 验证方法 → 实际命令 → 真实输出 → 证据位置 →
  PASS / FAIL / UNVERIFIED），加一个附录逐字贴命令输出与退出码。
  **没有真实执行证据的一律记 `UNVERIFIED`，不记 PASS。** SC-8 那一行先留空，
  由 Task 4 的人工检查点填。

  同时把本阶段的**已知证据缺口**如实列出来，至少包括：单样本或双样本的覆盖限度、
  `AC-05` 抓不到「证据不相关」这个盲区、以及 01.5-02 Task 4 裁决里被作废或失手的字段。
  Phase 1 的收口就是靠这一节才没有粉饰，照此办理。

  **`PROGRESS.md` 追加 changelog**（append-only，记已发生的事实不是计划）。
  **`STATE.md` 与 `ROADMAP.md`** 更新阶段状态、勾选六份 Plan、更新规模表（测试数、门禁数、
  指标定义数、`data/mappings/` 的条目数）。

  **最后跑全量门禁**，按 F-8 一律 `&&` 串联，**禁止 `;`，禁止在门禁命令后接管道**：
  六道门 + 供应链门禁 + 两处冻结校验一条链跑完，把逐字输出贴进 `VERIFICATION.md` 的附录。
  要看尾部输出就先重定向到文件再单独 `tail`，把退出码留给 `&&`。
  </action>
  <verify>
    <automated>.venv/Scripts/python -m pytest -q && .venv/Scripts/python -m semantic_layer scan && .venv/Scripts/python -m semantic_layer validate && .venv/Scripts/python scripts/check_xrefs.py && .venv/Scripts/python scripts/check_reading_ledger.py && .venv/Scripts/python scripts/check_gates.py && .venv/Scripts/python scripts/verify_deps.py && cd eval/frozen-01 && sha256sum -c SHA256SUMS && cd ../../docs/agent/poc-01 && sha256sum -c SHA256SUMS</automated>
  </verify>
  <acceptance_criteria>
  - `.venv/Scripts/python scripts/check_gates.py` 退出码 0（R5 通过即证明 12 条回标齐了）
  - 12 个编号各自在 `references/` 下逐字出现至少一次：`grep -rhoE '\bL-(2|9|12|13|34|35|36|37|38|45|47|55)\b' references/ | sort -u | wc -l` 输出 12
  - `grep -c '47f9438' references/README.md` 输出 ≥ 1
  - `.venv/Scripts/python scripts/backlog_status.py` 退出码 0；把它在本任务开始前与完成后的两次输出都贴进 SUMMARY，「已落地」类计数的增量 ≥ 12（用脚本读，不用裸 grep——`~~OPEN~~ → 已落地` 会被裸 grep 数成 OPEN）
  - `docs/agent/VERIFICATION.md` 含 `## Phase 1.5` 一节，且该节含 SC-1 到 SC-8 八行，每行有 PASS / FAIL / UNVERIFIED 之一
  - `docs/agent/VERIFICATION.md` 的 Phase 1.5 附录含至少 9 段带退出码的逐字命令输出
  - `.venv/Scripts/python scripts/check_xrefs.py` 退出码 0（新增的台账交叉引用不悬空）
  - `.venv/Scripts/python scripts/check_reading_ledger.py` 退出码 0（`references/` 的回标没把覆盖表改坏）
  - 全量门禁按 F-8 用 `&&` 串联跑完，末端 exit 0，输出贴进 `VERIFICATION.md`
  - `cd eval/frozen-01 && sha256sum -c SHA256SUMS` 与 `cd docs/agent/poc-01 && sha256sum -c SHA256SUMS` 全部 OK
  </acceptance_criteria>
  <done>本阶段落地的 12 条待办在台账与 references 两侧都有记录，四条 A 区台账逐条对照判据更新过；VERIFICATION.md 的 Phase 1.5 一节可以让任何人照着重跑一遍。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4：SC-8 的人工验收 —— 逐字段看取到的值</name>
  <what-built>
  抽取器、映射表、勾稽闸门、算术解析器与 AKShare 对照通路全部落地；
  `docs/agent/phase-01.5/PROBE-14.md`、`TRACER-EVIDENCE.md`、`COMPUTED-VALUES.md` 三份报告
  逐字段列出了取到的值；`VERIFICATION.md` 的 Phase 1.5 一节除 SC-8 那一行外已填完。
  </what-built>
  <how-to-verify>
  **SC-8 的原话是「抽取器的验收看『取到的值』，不看『命中数』」，而这件事执行者代答无效**
  ——它加进 ROADMAP 的起因就是 A-4 里 `bs.total_equity` 匹配到章节小标题
  「所有者权益（或股东权益）：」而数值为空，报表上算命中、实际取错了行。
  一个只会数命中的验收者会放过它。

  请做三件事：

  1. **抽查 8–10 个字段**，打开 `docs/agent/phase-01.5/PROBE-14.md` 与
     `TRACER-EVIDENCE.md`，按里面记的年报页码翻到贵州茅台 2023 年报对应页
     （用 `.venv/Scripts/python -m extractor fetch --stock 600519 --year 2023 --keep-pdf`
     把 PDF 取回来，PDF 落在 `data/raw/`，不进版本控制），
     **逐个核对取到的值与纸面上的数字是否逐字一致**。
     重点抽这几个：`bs.total_equity`（A-4 的反例）、
     `is.net_profit` 与 `is.net_profit_attributable_to_parent`（对照臂两个模型在这里错得逐字相同）、
     `is.operating_revenue_current` 与 `is.operating_revenue_prior_as_presented`（两列之分）、
     以及四个「行在格子空」的有息负债分项。

  2. **看 `COMPUTED-VALUES.md` 里至少 2 个指标的手算复核**，自己拿计算器算一遍，
     确认算出来的数与报告里写的一致。

  3. **判 SC-2**：按 01.5-02 Task 4 的裁决口径数一遍「人工核对正确」的字段数，
     确认它与报告里写的数字一致，并确认 SC-2 那一行该记 PASS 还是 FAIL 还是 UNVERIFIED。

  核对完把结论填进 `docs/agent/VERIFICATION.md` 的 Phase 1.5 一节 SC-8 那一行，
  连同抽查了哪几个字段、有没有发现不一致。
  **发现不一致是好结果，不是坏结果**——那正是这道检查点存在的原因。
  </how-to-verify>
  <resume-signal>回复抽查了哪几个字段、各自是否一致、以及 SC-2 与 SC-8 两行各记 PASS / FAIL / UNVERIFIED；发现问题则描述问题</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 造回归的改动 → 工作区 | 负控制程序会临时改源码，忘记回退会把一个坏掉的闸门提交上去 |
| 台账状态词 → 门禁 R5 | 状态一改成「已落地」就触发 R5 的强制回标要求 |
| `VERIFICATION.md` 的结论 → 下游阶段 | Phase 2 会把本阶段的结论当既成事实用 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.5-29 | Tampering | Task 1 的造回归改动未回退 | high | mitigate | 回退写成 `trap ... EXIT`；acceptance 要求 `git status --porcelain src/extractor/` 无输出；全量 `pytest -q` 在回退后复跑 |
| T-01.5-30 | Repudiation | 假闸门被当成真闸门收口 | high | mitigate | 两条负控制按三步程序实跑，**且确认红的原因正确**；四段真实输出进提交信息（L-5） |
| T-01.5-31 | Spoofing | 「做了但没回标」让台账继续骗人 | high | mitigate | `check_gates.py` 的 R5 机械强制；acceptance 直接数 `references/` 里 12 个编号的出现 |
| T-01.5-32 | Repudiation | 无证据的结论被记成 PASS | high | mitigate | `PROJECT_SPEC.md` §11 的六段式；SC-8 由 `blocking` 人工检查点填，执行者代答无效；已知证据缺口单列一节 |
| T-01.5-33 | Information Disclosure | 人工验收时 `--keep-pdf` 取回的 PDF 被误提交 | medium | mitigate | PDF 落 `data/raw/`（`.gitignore` + `forbidden_tracked_file_types` 双拦）；收口的 `semantic_layer scan` 兜底 |
| T-01.5-34 | Tampering | `gates.yml` 与 `rules/commands.md` 分叉 | medium | mitigate | 本计划不新增门禁（理由已写明），故 R4 的分叉面为零；若最终仍新增，须同步三处并跑 R4 |
</threat_model>

<verification>
1. 六道门 + 供应链门禁 + 两处冻结校验在同一条 `&&` 链上跑完，末端 exit 0
2. `references/` 下 12 个 `L-nn` 编号齐全（R5 通过）
3. `docs/agent/VERIFICATION.md` 的 Phase 1.5 一节 SC-1…SC-8 八行齐全，每行有明确判定
4. `git status --porcelain src/extractor/` 无输出
5. SC-8 那一行由操作者填写，不由执行者代答
</verification>

<success_criteria>
- 两道闸门各有一条负控制，且都实跑过「造回归 → 看红（原因正确）→ 回退」
- J-6 / J-7 / AC-05 口径各有一条会红的测试
- 12 条 `L-nn` 在台账与 `references/` 两侧都已回标，R5 绿
- `VERIFICATION.md` 的 Phase 1.5 一节可被任何人照着重跑，已知证据缺口如实列出
- 没有任何一条结论在缺乏真实执行证据的情况下被记成 PASS
</success_criteria>

<output>
Create `.planning/phases/01.5-data-ingestion/01.5-06-SUMMARY.md` when done
</output>
