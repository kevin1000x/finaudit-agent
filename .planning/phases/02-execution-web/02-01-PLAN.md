---
phase: 02-execution-web
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agent/__init__.py
  - src/agent/intent.py
  - src/agent/gate.py
  - src/agent/answer.py
  - tests/test_intent.py
  - tests/test_gate.py
  - tests/test_answer.py
autonomous: true
requirements: [AC-05, AC-09, SC-1, SC-5, SC-6]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 4
  confidence: medium

must_haves:
  truths:
    - LLM 只出现在意图解析这一段；口径解析之后的每一步都是确定性的，且有一条测试锁住「答案路径不 import llm_client」
    - pre_execute 是**前置**钩子：任何执行路径都必须先过它，不存在绕过它的配置或参数
    - 被拒答的请求同样留下完整日志记录，与成功的请求同一套出处字段
    - AC-09 的每个危险样本都有**负控制**：移除该校验后那个样本确实被放行
    - 证据链字段齐全率在 frozen-01 上是 100%（AC-05），不齐全即非零退出
    - eval/frozen-01/ 与 docs/agent/poc-01/ 的哈希不变（D-012）
  artifacts:
    - src/agent/（意图解析 + 闸门 + 答案组装）
    - frozen-01 系统臂的一次全量运行产物
---

<objective>
把已有的零件串成一条问答路径，并在同一次改动里把闸门建成**前置**的。

**Phase 2 不是从零开始**（盘点见 ROADMAP）：口径解析、20 份定义、DSL 语法树、
取数、计算、证据链齐全率判据都已存在，`eval/` 的评测骨架也已跑过。
缺的是**把它们串起来的那条路径**，以及路径上的闸门。

Purpose: 让 `SC-1` / `SC-5` / `SC-6` 三条同时成立 —— 它们必须一起做，
因为「闸门不可绕过」是**结构性质**，事后加的闸门天然有旁路。
Output: `src/agent/` 三个模块 + frozen-01 系统臂的一次全量运行。
</objective>

<context>
@docs/agent/ARCHITECTURE.md
@PROJECT_SPEC.md
@DECISIONS.md
@src/semantic_layer/dsl.py
@src/extractor/pipeline.py
@eval/run.py
</context>

<tasks>

<task type="auto" tdd="true">
**T1 —— 意图解析：自然语言 → `(metric_id, 主体, 期间)`，到此为止。**

🔴 **这是 LLM 在本系统里的唯一位置。** 越过这条线，证据链就不再可复核（`D-001` / `D-003`）。

- 输出是一个**结构化的三元组**，不是自由文本
- 解析不出来 ⇒ **拒答并说明缺哪一项**，不猜（`F-2`）
- 别名走 `metrics/*.yaml` 的 `aliases`，**先查别名表再问模型** ——
  能确定性解决的不要交给模型

⚠️ **一条测试锁住 LLM 的边界**：`src/agent/answer.py` 与 `src/agent/gate.py`
**不得 import 任何 LLM 客户端**，走 AST 断言（`RV-9`：`in` 子串会被注释满足）。
</task>

<task type="auto" tdd="true">
**T2 —— `pre_execute`：单调前置钩子。**

检查对象是 `semantic_layer.dsl` 的语法树（那七个节点），**不是字符串**。

`SC-6` 的两条必须同时成立：
1. **不存在任何配置或插件路径能绕过它** —— 用一条测试证明：
   执行入口只有一个，且它无条件先调 `pre_execute`
2. **被拒答的请求同样留下完整日志** —— 与成功请求同一套出处字段。
   ⚠️ 拒答少记字段是最容易犯的：**拒答恰恰是最需要能复核的那一类**

`AC-09`：每个已知危险样本都要有**负控制** —— 移除该校验后那个样本确实被放行。
⚠️ 这一条是 2026-08-27 加强过的措辞，**不是「跑过就算」**（`N-45` 的教训：
绿的原因也可能是错的）。
</task>

<task type="auto" tdd="true">
**T3 —— 答案组装：把计算结果与证据链拼成一个可复核的答案对象。**

证据链字段齐全率必须是 **100%**（`AC-05`），机械判据已存在
（`extractor.pipeline.evidence_completeness`），**复用它，不要另写一套**。

⚠️ `AC-05` 的措辞 2026-08-27 修订过：它**不含**「证据与答案同源」——
那一条由 `02-03` 的人工复核承担。**不要在这里声称自己证明了同源。**
</task>

<task type="checkpoint">
**T4 —— 在 frozen-01 上跑系统臂全量，把报告里空着的槽填上。**

`eval/run.py` 的报告 schema 已经预留了七个指标。本计划要填的是
`口径正确率` / `拒答准确率` / `答案正确率` / `证据链完整率` 四个；
`复核一致率` / `引用可定位率` / `归因覆盖率` 留给 `02-02` 与 `02-03`。

🔴 **红线**：`eval/frozen-01/` 的标准答案与判定规则**一个字节都不许改**（`D-012`）。
跑出来难看就是难看 —— **迁就模型表现去改题，`FREEZE.md` 明写计为失败**。

⚠️ **本任务不自行判定 H2。** H2 的判定门是 `AC-06` 的复核实验（`02-03`），
不是这里的四个数。
</task>

</tasks>

<verification>
- 七道门 `&&` 串联全 exit 0（含最后一次改动之后从头跑）
- frozen-01 冻结校验 21 行 OK、poc-01 5 行 OK
- `AC-09` 每个样本的负控制**逐个**验过，不是抽验
- AST 断言：答案路径与闸门路径不 import LLM 客户端
- ⚠️ 报告里出现的每个数，都能指回产生它的那次运行（`prompt_sha256` 已有先例）
</verification>
