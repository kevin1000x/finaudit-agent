# 本项目的高代价陷阱

> 这些是**预防性**的告诫。已经真实犯过的错在 `failure-modes.md`。


1. **把非公开数据带进来。** 这是唯一不可挽回的错误（D-010）。任何来自实习、雇主内部系统、客户的字段名、表结构、数值——包括脱敏与聚合后的——都不允许出现。写代码前先问：这个字段名是从哪来的？
2. **从 `data secret` 复制代码或语料。** 机制思想可以借，资产不能搬（D-004）。两个项目的可信度是互相独立的，交叉污染会同时毁掉两边。
3. **先看模型输出再定评测标准。** 评测集一旦冻结就不许为了好看而改（D-012）。发现题目本身错了 → 作废并公开记录，不修改后重用。
4. **为了做图而做图。** 建图前必须先写出 3 个「纯向量 RAG 答不了」的具体问题并证明其答不了（D-007）。
5. **把 TUI 做上瘾。** 它是交付形式不是卖点（D-006），Phase 2 只做到能展示证据链为止。
6. **在语义层没做完时跑去搭 Agent 编排。** 语义层是唯一别人抄不走的一层（D-002）。
7. **流程超过项目。** 三套方法论叠加容易产生 Ceremony Engineering。任一阶段流程开销 > 产出时间的 30% → 删掉贡献最小的那套（D-009）。Harness 要定期做减法。

---

## 从外部项目源码里实测出来的陷阱（2026-08-22 新增）

**这几条不是告诫，是别人已经踩塌的坑，而且我们的语料形态会精准命中它们。**
出处见 `references/hello-agents-framework-tools.md`。

8. **用 `text.split()` 给中文算相关性——结果恒为 0，且静默。**
   hello-agents 的 `context/builder.py:164-169` 用
   `set(user_query.lower().split()) & set(content.lower().split())` 算重叠度。
   空格分词对中文**不产生任何 token 交集**。
   本会话实测复刻该逻辑：中文 query「贵州茅台2023年的销售毛利率是多少」+ 含答案的中文年报摘录
   → `relevance = 0.0000`；同义英文 → `0.7143`。
   默认 `min_relevance=0.3` 下**每一段中文证据被静默丢弃，不报错不警告**。
   ⇒ **Phase 3 做准则 RAG 时，任何相关性/召回打分必须先在中文样本上验一次「非零」**，
   把「打分恒为 0」做成一条测试，而不是等召回率难看了再回头查。

   **对策栏补充（2026-08-26，落地待办 L-74）——光验「非零」不够，还要有非文本的兜底信号。**
   出处 `references/hello-agents-ch09-context.md` C9-A5（`ch9:1572`）：
   书稿在最佳实践里给了**按类型的先验权重**（blocker > action > conclusion），
   即排序不全靠文本相似度。这一条本身该借。
   ⇒ **本项目的证据检索排序必须至少有一个非文本信号**，候选：
   **字段类型**（三大表本表 > 附注 > MD&A 散文）、**报表科目层级**（合计行 > 明细行）、
   **年度距离**（问的是 2023 年，2023 年的行优先）。
   理由不是「多一个信号更准」，而是：**文本相似度在中文上已经被证明会整体塌到 0**，
   一个纯文本的排序器在那种状态下**返回的是任意顺序，而不是空结果**——
   空结果会被发现，任意顺序不会。

9. **判据看一个量、执行看另一个量。**
   `truncator.py:97` 用**字节数**判断要不要截断，`:141-150` 的执行只按**行数**切
   ⇒ 实测 60000 字节的**单行**文本返回 `truncated: True` 而 `preview` 一字未减，
   `kept_bytes == original_bytes`。`max_bytes` 形同虚设。
   ⇒ **年报 PDF 抽出的表格文本正是「超长、少换行」**，这条会直接命中。
   凡是「阈值」与「执行」用了不同的量纲，必须有一条测试同时断言两者。

   **增补（2026-08-26，落地待办 L-82 / L-83）——同一个病的另外两个形状，
   都出自 `references/hello-agents-ch09-context.md`：**

   **(a) 同一系统里三套计数口径**（C9-B11）：`ch9:572` 是手写中英估算、
   `ch9:1423` 是 `len // 4`、`builder.py:345-353` 是 tiktoken。
   **预算守护建立在一个自己都不自洽的度量上。**
   ⇒ **预算 / 阈值类度量全项目单一口径**，且需一条测试断言各调用点用的是**同一个函数**
   （不是「结果接近」，是同一个可 import 的符号）。

   **(b) 过滤用一个分、排序用另一个分**（C9-B12，`ch9:363-364`）：
   过滤用**裸相关性**，排序用**含新近性权重的复合分**。
   净效果是**权重设计在过滤那一步就已经失效**——
   新近性再高的条目，只要裸相关性没过阈值就根本进不了排序。
   ⇒ **凡是「先筛后排」的两步，筛与排必须用同一个分**；
   要用两个分就必须写清「为什么第二个分不需要参与筛」，写不出来就是缺陷。
   本项目的命中面：证据召回（先按相关性筛掉一批，再按页码/年度重排）。

10. **截断算法缺前置检查，把短输入放大。**
    `truncator.py:145-147` 的 `head_tail` 策略不检查「输入是否已经短于目标」，
    实测 10 行输入 → 输出 21 行，**放大 2.1x**。

11. **不可逆压缩 + 一份给它发合格证的测试。**
    `context/history.py:144` 一次整体重新赋值，被压缩的原文**不写任何地方**
    （`grep -cE 'path|save|dump|archive|open\('` 在 `history.py` 为 **0**，
    在同包 `truncator.py` 为 **11**）。
    默认摘要（`core/agent.py:365-383`）返回的是「此会话包含 N 轮对话 / 用户消息 X 条」
    ——**一个字内容都没有，全是计数**。净效果是把 N 条真实对话换成一句
    「这里曾经有 N 条对话」：**这不是有损压缩，是带回执的删除**。
    而 `test_context_engineering.py:117-138` 逐条断言「前 3 轮已消失、只剩一条摘要」并判定通过
    ——**这不是漏测，是被测试确认过的规格**。
    ⇒ 本项目任何「压缩上下文」的动作，**原文落盘位置必须是返回值的一部分**（见 `ARCHITECTURE.md` §8.5），
    且必须有一条测试断言**原文能被找回**，而不是断言「摘要生成了」。

---

## 2026-08-26 新增（登记册 `LANDING-BACKLOG.md` §5 落地）

**为什么接在第 11 条之后而不是并进上面那批**：上面 8–11 条全出自 `framework-tools`
一份产物；下面这七条来自四份不同产物，且其中两条**是从 `failure-modes.md` 改道过来的**
（理由见第 14、18 条的开头）。分批标注是为了让「哪一批读出来的」可追。

12. **「X 代替了 Y」类断言，必须去代码里核对 Y 是否真的消失了。**（`L-28`）
    出处 `references/hello-agents-ch06-frameworks.md` X6-9：
    `ch6:499` 称 AgentScope「以消息驱动**代替**状态机」，
    而同仓的 `run_game`（`main_cn.py:311-365`）**就是一个手写的顺序状态机**，
    `MsgHub` 只换掉了「谁能听见谁」。**书自己也没做到它宣称的替代。**
    ⇒ 本项目的 pipeline 必须有显式阶段（读入 → 校验 → 解析 → 计算 → 出证据）。
    这句话如果被当真，会导致阶段边界被「消息路由的灵活性」掩盖掉。
    **这条约束的是读外部材料的方式，不是机制**：
    任何形如「A 让 B 变得不必要」的说法，先 grep B 还在不在。
    与 `failure-modes.md` F-1 的关系：F-1 管的是**我们自己**下「X 没有 Y」的断言，
    本条管的是**别人**下的同型断言被我们照单全收。

13. **文档不能替代闸门：陈旧口径必须在读入边界被拒，不是在文档里被预告。**（`L-29`）
    出处 `references/deepseek-harness-notes-part3.md` C-N2（源 `S-45:17,32`）：
    harness 的启动器**不校验产物新鲜度**，陈旧包可以静默提供旧浏览器代码，
    配套做法是**把这件事写进 onboarding 与 CLI 参考**。
    对开发者工具，「文档 + 显式两条命令」是合理取舍；
    **对证据链不成立——读者不读我们的 README，他读的是那条答案。**
    ⇒ 凡是打算用「已在文档里说明」结案的静默行为，回到 `D-018`：
    **fail-closed 放在读入边界**。文档只能解释闸门，不能代替闸门。

14. **`?? {}` / `or []` 兜底会把「未声明」读成「已声明为空」。**（`L-21`）
    > **目的地更正**：登记册原写落 `rules/failure-modes.md`。
    > 但那份文件开头明写「**这里只放已经真实犯过的错，不放假想风险**」，
    > 而这一条是**外部项目犯的错**，本仓无实例。⇒ 改落本文件（预防性陷阱）。
    > 这不是降级处理：`failure-modes.md` 的准入条件本身就是它的价值所在。

    出处 `references/deepseek-harness-notes-part3.md` C-A7（源 `S-21:13`）：
    harness 的 `?? {}` 把「这个配置没写」变成了「配了一个空对象」，
    于是**一个可选特性因此默认开启**。
    ⇒ **配置读入处禁止 `or {}` / `or []` / `dict.get(k, {})` 这类空容器兜底。**
    缺失必须与「显式配成空」在类型上可分（`None` vs `{}`，或 sentinel）。
    与 `failure-modes.md` F-2 同族：F-2 是「凑一个求不出值的 trigger」，
    这一条是「凑一个求得出值但含义错了的默认」。
    本项目最先命中的地方：口径开关（口径版本 / 抽样策略 / 单位换算）的读入——
    那一处的正解已写死在 Phase 1.5 计划里（口径开关不设默认值，缺失即 fail-closed）。

15. **降级分支本身写得没错，错的是它的触发条件已被上游排除。**（`L-73`）
    出处 `references/hello-agents-framework-tools.md` FT-B5：
    `token_counter.py:7`/`:22` 声称「tiktoken 不可用时降级到字符估算」，
    而 `:10` 是**顶层无保护** `import tiktoken`，`requirements.txt:10` 是硬依赖
    ⇒ **tiktoken 不可用时整个包 import 就失败了，那个降级分支不可达。**
    ⇒ 识别方法（一句话，可当场执行）：写完降级分支后问
    **「让它触发的那个世界里，程序还活着吗？」** 答不上来就是假降级。
    本项目的命中面：PDF 抽取的三条文本路径（pdfplumber / PyMuPDF / OCR）
    ——`D-014` 已把 PyMuPDF 禁入，**任何写着「PyMuPDF 不可用时降级」的分支都是死码**。
    （产物原文把归属留给操作者裁决：犯过 → `failure-modes.md`，没犯过 → 本文件。
    登记册当时已裁为本文件，此处沿用。）

16. **不借「定期清理归档过时 action」——「过时」恰恰是复核当时那次回答所需的。**（`L-78`）
    出处 `references/hello-agents-ch09-context.md` C9-B5（`ch9:1565-1568`）：
    书稿的最佳实践之一是「对于过时的 action，及时删除或更新」。
    ⇒ **对可审计系统方向相反**：一条后来被推翻的结论，正是复核「当初为什么这么答」的材料。
    删掉它，证据链就只剩下**修正之后的版本**，而复核要复核的是**修正之前那次回答**。
    ⇒ 证据、抽取记录与笔记**只追加、只标失效，不删除**
    （落盘语义见 `ARCHITECTURE.md` §8.5.7：append-only + tombstone）。
    注意与 `D-009`「Harness 要定期做减法」不冲突：
    **做减法的对象是流程与文档，不是证据。**

17. **不借「命令白名单 + `shell=True`」当安全边界。**（`L-81`）
    出处 `references/hello-agents-ch09-context.md` C9-B8
    （`ch9:1649-1673` / `terminal_tool.py:62-79`、`:191-200`）：
    白名单**只查 `parts[0]`**，`;` / `&&` / `$()` 全放行；
    白名单本身含 `bash` / `sh` / `python` / `node`；
    `awk` / `sed` / `find -exec` 都是执行原语——**书自己的示例就在用 `find -exec`**（`ch9:1941`）。
    ⇒ **本项目不给 Agent 通用命令执行能力**，文件访问一律走白名单化的**具名函数**，
    参数是数据不是命令行。
    `D-017`（不引三方求值库、自建封闭算术解析器）已经在**算术**这一处落地了同一原则，
    本条是把它一般化到**进程与文件系统**。
    ⚠️ 这里只落「不给通用执行能力」这条陷阱；
    是否要为它单开一条决策**须操作者裁决**，本文件不代拍。

18. **不让测试给缺陷发合格证。**（`L-68`）
    > **目的地更正**：登记册原写落 `rules/failure-modes.md`，同第 14 条的理由改落本文件
    > ——**外部项目犯的错不算我们犯过的**。本仓目前没有「测试锁定了错误行为」的实例；
    > 一旦出现，那时再从本条升格进 `failure-modes.md`。

    出处 `references/hello-agents-framework-core.md` FC-B6（该文 §7.7）：
    `test_compress`（`test_context_engineering.py:117-138`）逐条断言
    「前 3 轮已消失、只剩一条摘要」并判定通过；
    `test_simple_summary_generation`（`test_smart_summary.py:47-70`）
    **逐字断言了那四个计数字符串**（`"用户消息：2 条"` / `"总消息数：4 条"`）
    ——即把一个**零内容的摘要**判定为正确输出。
    ⇒ 于是第 11 条判定为「带回执的删除」的那个行为，在这里是**被测试锁定的规格**。
    **漏测可以补测；被断言过的行为要改，得先有人意识到那条断言本身是错的。**
    可执行的一半：任何断言**输出内容形状**的测试，必须同时断言**内容里有什么**
    （不是「生成了摘要」，是「摘要里能找回哪句原文」）。
    与第六道门 `check_gates.py` 的关系：R1/R3 只挡「没有可失败点」与「恒真断言」，
    **挡不住「断言写得很具体但断的是错的东西」**——那正是本条。

---

## 2026-08-27 新增（登记册剩余 `OPEN` 的第二批）

19. **覆盖 / 完整性检查的基准集合，必须独立于被检查物。**（`L-23`）
    > **目的地更正**：登记册原写落 `rules/failure-modes.md`，与第 14、18 条同一理由改落本文件
    > ——**这条规则本身是从外部项目读出来的预防性告诫**，不是我们犯错之后写下的事故记录。
    > ⚠️ **但下面第三段给出的 R5 实例是本仓的，且三条准入条件都填得满**。
    > 是否据此在 `failure-modes.md` 另立一条事故，**留给操作者裁决**，本轮不代拍——
    > 规则与事故是两种东西，规则落这里，事故该不该记是另一个判断。

    出处 `references/deepseek-harness-notes-part3.md` C-A16
    （源 `HN-B13:27`；`HN-` 是本行加的来源前缀，指 harness `.agents/notes/implemented/bug-fix`
    的第 13 篇——**原编号裸写会被 `check_xrefs.py` 解析成本仓台账 B 区的条目**，
    写这一条时当场报了一次红，见登记册 §3.2）：
    harness 第一次尝试**从「已经重绑过的样式表」反推「应该抬升的表面集合」**，
    于是**一个还没有人重绑过的表面——恰恰是这个检查存在的目的——会把自己定义成「未抬升」**。
    实测后果：todo 面板未重绑，派生出来的检查**一直是绿的**。

    ⇒ **判据（一句话，可当场执行）**：问「**这个检查的『应该有什么』是从哪来的？**」
    答案若是「从已经处理过的那些里数出来的」，它**在构造上不可能发现遗漏**，
    只能发现损坏。**能发现遗漏的基准，必须来自被检查物之外**：
    独立的注册表、规格文档、上游目录列表，或一个人写下来的清单。

    **本仓已经有一个真实实例，就在本轮**：第六道门的 **R5** 查「登记册里已落地的 `L-nn`
    有没有在 `references/` 里回标」——它的基准集合（「哪些算已落地」）**取自登记册自身**。
    于是 2026-08-27 查出的那三条「内容已在 `pitfalls.md`、登记册却仍标 `OPEN`」
    （`L-74` / `L-82` / `L-83`）**R5 一条都发现不了**：它们不在 R5 的基准集合里，
    因为基准集合正是从被检查物派生的。**这不是 R5 写错了，是它的形状决定了它只能查一个方向。**
    ⚠️ 目前没有为此加门禁——反方向的基准（「权威文档里已经写了哪些条目」）
    需要一份独立于登记册的清单，而那份清单不存在。**如实记在这里，不假装有覆盖。**

    本项目最先命中的地方：`AC-05`（证据链字段齐全率 100%）。
    「齐全」的分母若从**已经产出的字段**里数，永远是 100%；
    它必须来自 `PROJECT_SPEC.md` §5.1 的字段规格——那份规格独立于任何一次运行的产物。

20. **裸 `except:` 会把 `KeyboardInterrupt` 一起吃掉。**（`L-66`）
    > 登记册的「目的地」列写的是「实现约定」——**那不是一份文件**，落不了笔。
    > 产物给的落点是 `rules/commands.md`（或本文件），按「这是一条预防性陷阱」落本文件。

    出处 `references/hello-agents-framework-core.md` FC-A5：
    该仓 `run_as_subagent`（`agent.py:946-948`）**先把 `KeyboardInterrupt` re-raise、
    `finally` 的还原照常跑**——这一处是对的，且是全仓少数几处正确的异常分层。
    反例在同仓 §4.2：三个裸 `except:`（`react_agent.py:1055`、
    `file_tools.py:189` / `:196`）会把 `KeyboardInterrupt` 与 `SystemExit` 一并吞掉。

    ⇒ **本项目的实现约定**：
    - **禁止裸 `except:`**；捕获一律写 `except Exception`，**不得覆盖 `BaseException`**；
    - 中断信号必须能穿透到顶层，**清理与还原逻辑放 `finally`**（不是放在 `except` 里）。

    现实理由不是洁癖：**长跑的 PDF 抽取与批量评测必须能被 Ctrl-C 干净中断**，
    否则一次跑错要么等它跑完，要么留下半截产物——而半截产物正好命中第 19 条
    与 `L-22`（陈旧产物导致的假通过）。
