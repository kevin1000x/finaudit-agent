# Handoff: finaudit-agent - 2026-08-10 00:42（末次更新 2026-08-19）

## Task

把「可审计的财务分析 Agent」从骨架推进到可执行的语义层。

- **原始任务（已完成）**：执行 POC-01，验证 H1「财务指标口径可否被写成无歧义的机器可读定义」。→ 判定 **PASS**（压线）。
- **Phase 1（已完成并收口）**：20 个指标的机器可读口径定义 + 机械校验器 + 冻结评测集。
  8/8 计划执行完毕，五条成功标准全部 `VERIFIED`，`VERIFICATION.md` 已落地。
- **当前任务：Phase 1.5 — 数据接入层 data-ingestion**（PDF 抽取器 + 数值执行层）。
  `CONTEXT.md` 与 `01.5-RESEARCH.md` 已就位，**卡在 6 个决策点待操作者裁决**（见 §Status 的决策点表）。
  裁决后即可拉 `gsd-planner` 出 PLAN。
- **A-3 已解除**：操作者裁决换 pdfplumber，落为 D-014，并已由 `verify_deps.py` 的许可证禁列机器化。

## Scope

**In scope**：本仓库 `07-finaudit-agent/` 内的一切；Phase 1.5（数据接入层）；Phase 0（cninfo 补实证结论，可并行）。

**Out of scope**：
- `data secret` 项目 = GitHub 上的 `kevin1000x/privacy-preserving-agent-poc`（本地路径 `C:\Users\Kevin\Documents\data secret`）——**不要读它的语料、不要复制代码、不要引用其 Iteration 结论作为本项目证据**（D-004）。
  理由是**资产隔离**，不是「它还没做完」：本项目不以它的任何进度或结论为前置条件。**不存在「等 data secret 完成再做 X」这种排期**（D-004 无时序依赖条款）
- 通用 Data Agent / 多数据源 / NL2SQL 引擎（D-001）
- 训练与微调（D-011）
- 知识图谱——除非先证明「非图不可」（D-007）
- **口径不披露的二手源不得作权威源**（D-013）。AKShare / 雪球只可作对照；财务数值以年报 PDF 原文为准
- 估值建模（`/dcf` `/lbo` 那类）—— `PROJECT_SPEC.md` §8 Non-goals

## Files

**开场读这四个**：
1. `AGENTS.md`（跨 Agent 地图 + 硬规则）
2. `CLAUDE.md`（索引，细则在 `rules/`）→ 其中 `rules/failure-modes.md` **必读**，现有 **8 类**已真实犯过的错
3. `docs/agent/OPEN-ITEMS.md`（未决事项台账，**A 区是阻塞项**）
4. `docs/agent/PROGRESS.md`（跨会话 changelog，append-only）

⚠️ **台账编号命名空间已于 2026-08-18 变更**：原「本轮讨论新增」D 区**改名为 N 区**。
`D-0xx` 现在唯一属于 `DECISIONS.md`。历史 commit message 与本文件早期段落里的旧编号**刻意不改**。

权威文档（冲突时上位覆盖下位）：
- `PROJECT_SPEC.md` - 假设 H1–H3、L0–L4、AC-01…AC-10、停止条件。**§5.1 是口径字段集**
- `DECISIONS.md` - D-001…**D-014**、未解决 U-01…U-03
- `EVAL_CASES.md` - 五类问题集、复核实验、失败归因四层、阶段门

行为契约：`openspec/specs/semantic-layer/metric-definition/spec.md` - 9 条 Requirement / 21 个 Scenario

**冻结产物（一个字节都不许动）**：
- `docs/agent/poc-01/` - 校验 `cd docs/agent/poc-01 && sha256sum -c SHA256SUMS`（5 行 OK）
- `eval/frozen-01/` - 20 题 + 夹具 + FREEZE.md + SHA256SUMS（**21 行 OK**）

Phase 1 产物：
- `src/semantic_layer/` - dsl / vocabulary / definition / validate / report / resolve / stats / scan / __main__
- `metrics/` - **20/20 份 `version: 2` 定义**；`metrics/_flags.yaml` 10 条受控 flag
- `tests/` - **358 tests passed**
- `.planning/phases/01-semantic-layer/` - 8 份 PLAN，01-02/03/06/08 有 SUMMARY

**Phase 1.5 产物（本次会话新增）**：
- `.planning/phases/01.5-data-ingestion/CONTEXT.md` - **不含新决策**，只汇总已有决策 + 已实测事实；§4 是待裁决问题
- `.planning/phases/01.5-data-ingestion/01.5-RESEARCH.md` - 453 行，四问的选项与代价（**未替操作者裁决**）

**门禁（本次会话由 2 道增至 4 道）**：
- `scripts/verify_deps.py` - 供应链 + **许可证禁列**（AGPL 系拒绝，含传递依赖扫描）
- `scan_rules.yaml` + `semantic_layer scan` - AC-10 非公开数据扫描，**rules_version 2**
- `scripts/check_xrefs.py` + `tests/test_xrefs.py` - **交叉引用门禁**（本次新增）
- `semantic_layer validate` - 20 份定义的 9 条 Requirement 校验

**验证记录**：`docs/agent/VERIFICATION.md` - Phase 1 章节含 14 行验收矩阵 + 三项判定 + 附录 §A.1–§A.9 逐字命令输出

本机同级仓库（**都在仓库外**）：
- `../cninfo-financial-analyzer` / `../cninfo-analyzer-web` / `../financial-services`
- scratchpad 内：`deepseek-harness`（clone）、`ha_md/`（hello-agents 转好的 markdown）、`maotai_2023.pdf`

## Discoveries

（2026-08-09 至 08-15 的发现保留，摘要如下）

- **通用赛道已饱和**：`datagallery-lab/datafoundry` 7 周 648★ → 只能走垂直（D-001）。
- **空隙在于**：公开实现的语义层都是「用户自配」，**没有把口径绑定到准则依据**。
- **POC-01 判定 PASS 但压线**：完全一致题数恰为阈值下限 4；两位回答者是**同源模型**、错误相关。
- **最有价值的发现不是那个 PASS**：`common_pitfalls` 是给人读的散文，不是可执行规则。
- **cninfo 的财务字段极窄**（`["stock_code","year","roa","ocf"]`）；**审计意见不是缺数据，是缺抽取器**。
- **`ast` 解不了 `is.` 前缀** —— 自研 DSL 存在的硬理由，已固化成回归测试。
- **PDF 取数的真正难点是坐标不是库**；**空格子 ≠ 缺失**（茅台实证）。
- **「代码里没有」不等于「实际没有」**，一天内犯了三次；**给 flag 编造求不出值的 trigger**，也犯了三次。

（2026-08-16 至 08-19 新增）

- **🔴 三个外部项目凑齐了同一个失效模式的三个实例**，这是本次会话最有价值的发现：

  | 项目 | 实例 |
  |---|---|
  | cninfo | `PROJECT_SUMMARY.md:328`「已通过对中文财报的人工编码进行验证」——**全仓零支撑物** |
  | harness | postmortem 0002：快照套件通过，**因为期望值被 refresh 成了那个错误** |
  | hello-agents | 正文写 4.2/5.0、通过率 85%，同仓提交的产物是 **3.32/5.0、40%** |

  **自证机制说通过了，但它证明的不是它声称证明的事。** `VERIFICATION.md` 要求「真实输出逐字粘贴含退出码」正是针对这个，现在有三份外部佐证。

- **对照臂证明了本项目要解决的问题真实存在**：两个模型（`deepseek-v4-pro` / `v4-flash`）在 Q-C1-001 上**错得逐字相同**——都取 `is.net_profit`（含少数股东损益）作分子，答 **0.124** 而正解 0.112，**且都不拒答**。Q-C2-001 两次都把三个分量复合出一个语义层中不存在的指标并给出 76.5457，`citations` 为空数组。
  **限定**：同厂商同代两个模型，推不到「所有通用 Agent」。
- **口径定义预测到了模型会怎么错**：flash 在 Q-C1-005 用了 **360 天基数**，而 `inventory_turnover_days` 的 `derivation.note` 逐字预写了这个陷阱。
- **闸门结论由推断升级为源码级定论**：读完 `vendor/cordis/src/events.ts` 与 `cordis-api/`，**waterfall 不可能充当不可绕过的闸门**——`Plugin.Base` 恰好五个字段无 priority/order/guard；`EventOptions` 恰好两个字段；`inject` 依赖一变就卸载重跑 ⇒ 位置会漂移。更狠的两条：**`ctx.isolate()` 能在子树里把服务整个换掉，父作用域完全无感**；**`Context.filter` 每次派发都查，闸门忘写 `global` 会静默地根本收不到事件**。
  → `resolve_scope() -> ScopeSpec | Refusal` 这个形状是对的，理由比原先强：**连被守卫的对象本身都能被换掉**。
- **harness 自己给了正解范例**：`PreToolDecision` 是三分支联合 `allow | deny{reason} | ask{reason?}`，**没有任何分支能携带 args ⇒ 改写参数在类型上不可表达，而非运行时被拒**。
- **harness 的拒绝事件形状不能抄**：`core/tools/src/index.ts:1496` 拒绝时只填 `error: { message }` 无 `info`，落盘处 `...error?.info ? {...} : {}` ⇒ **durable 事件里连 `error` 字段都不出现**。若照抄，AC-05 只能靠正则匹配句子来验。
- **harness 的 `.agents/notes/` 制度本身就是一套决策证据链**（674 篇正式笔记）。五条可直接搬：路径即元数据且 class 被 gate 强制封闭；`Status:` 三选一且与目录交叉校验；**「Alternatives are recorded, never invented」**（重建不出的老笔记必须写死标记注释，gate 只对旧文件放行）；归档 = 冻结 + 哈希 + append-only manifest 且**归档件不得作为当前行为的依据**；**「git history 不算证据留存」**。
- **cninfo 给了 Phase 1.5 两条硬性施工要求**：`pdf_parser.py:302` 无条件把第 0 行当表头，续页表格**静默吃掉一行真实数据**（而文档宣称「支持跨页表格识别」）；`pdf_parser.py:94-98` 文本路径**根本没关联页码** ⇒ **在结构上就不可能产出页级证据链**，页码必须在抽取第一步绑定。
- **cninfo 完全没有勾稽校验**，根因是**抽取路径与计算路径不相交**（`pipeline.py:758` 硬编码 `'financial_statements': {}`）——它抽了报表，却用 AKShare 的指标算结论。
- **hello-agents 的 LLM-as-judge 判分者与被判者是同一个模型**，且**全书 16 章「校准」零命中**，position-bias / self-preference 同样零命中。与 POC-01「同源模型」是同一个问题，**而他们连提都没提**。
- **hello-agents 的解析失败被静默归入 Tie** —— 失败被计成中性结果。
- **hello-agents 数据集完全外部化（好）但零冻结**：`冻结/哈希/污染` 三词全书零命中 → **D-012 在这点上我们领先，不是落后**。
- **BFCL 的 `irrelevance` 类别确认存在**，C2 拒答边界有**类别**先例但**无判分**先例；**τ-bench 全书零命中**（前几轮提过它，属无据）。
- **Ch14 的引用链是装饰性的**：`source_urls = [r["url"] for r in search_results]` 返回全部检索结果，与其声称的「从摘要抽取引用」直接矛盾 —— D-003 最锋利的反面案例。
- **`\b` 在中文紧邻时不成立**：Python 的 `\w` 含 CJK，「台账N-13」里没有词边界。这让第一版交叉引用门禁**静默漏检 49 处**。
- **`formula` 从未被任何现有代码解析为表达式**：`dsl.py` 的词法器对 `+ - * /` 没有任何分支（`dsl.py:236` 直接报错）；`validate.py:59-61` 唯一碰它的地方是一次**子串包含检查**。25 条规则码里只有 R2 的两条与 `formula` 有关，且只做子串匹配。
- **`source_disagreement` 词表里有但没有任何代码置位它**；`resolve.active_flags()` 结构上只处理 `definition` 域，需新建与 `check_comparable()` 平行的函数。
- **一个数字要更正**：此前说「21 个规则码」是错的。实际 grep 出 **26 个**不同的规则码字面量，researcher 报 25——差 1 未逐条核，但 21 确定错。

## Work Done

（2026-08-09 骨架 / 2026-08-10 POC-01 与决策 / 2026-08-15 Phase 1 wave 1–4 的条目保留，摘要如下）

- [x] `d940a24` 首次版本化并推送私有仓；`d791aaf` 冻结校验做成可执行命令
- [x] `85016e8` D-004 无时序依赖条款；`b4c4622` **D-008 取消时间预算上限**
- [x] `a3e00bb` 克隆 cninfo 并实查资产；`99b0345` **新增 D-013**；`b512d68` 调研 `anthropics/financial-services`
- [x] `9114040` OpenSpec apply（§5.1 字段集扩展，红测先行）；`44d728c` archive
- [x] `1d1043d` Phase 1 的 8 份 PLAN / 5 个 wave；`7a1e7e7` **Phase 1 tracer 落地**
- [x] wave 2/3：`8ed26b7` OQ-03 关闭、`c05ec97` OQ-04 落地、`ccd88fb` kpi 命名空间、`2110c63` `9f4dc9e` `8b002fd` **19 份定义落地，20/20 补齐**
- [x] `77f71bc` **frozen-01 冻结** @ `2026-08-15T14:47:51Z`；`1f4b0fc` 建 `references/`
- [x] `0591afa` 补读三个项目原文；`ac78231` **ROADMAP 插入 Phase 1.5**；`dfb8324` 三份深读报告归档

（2026-08-16 至 08-19，44 → **58 提交**）

- [x] `345bed5` **A-3 裁决落地为 D-014**：换 pdfplumber，禁 AGPL。含为什么不选 (b)/(c) 与反转触发条件。ROADMAP / references 里的 9/12 标注为「fitz 实现产生、换库后未实测」的待复现基线
- [x] `a619aaa` **接入 LLM 对照臂 + 许可证禁列门禁（A-5 关闭）**。`eval/baseline.py` + `eval/llm_client.py`，零新增依赖（stdlib urllib），密钥走仓库外 `--key-file`。`verify_deps.py` 查三处许可证声明 + 扫全量已装分发覆盖传递依赖。**禁的是 AGPL 不是 copyleft**，GPL/LGPL 放行
- [x] `58adfb5` 并入两份续读成果；**更正上一轮关于 harness 的两条错误断言**（fail-closed 在 persistence 的 load 边界不在 projection；`sourceEventSeqs` 只挂 3 个 surface 事件）
  ⚠️ **这个提交是带着一个红测试提交的**——`pytest | tail` 把非零退出码吞掉了。已在 `db52052` 修复
- [x] `551daa5` **对照臂首次全量跑通**（`deepseek-v4-pro`，机械判定 15/20）；修 AC-10 扫描把 npm 的 `包名@版本` 当邮箱的真实误报，`rules_version` 1 → 2
- [x] `f9eee72` `e82db17` **wave 5 收口 + H1 裁决**：全量门禁全绿；三项阶段末判定（元规则 19.0% 未失效 / H1 严格读法计 5 不触发 / flag 零新增请求）；`VERIFICATION.md` Phase 1 章节含九段逐字输出；操作者裁决「严格」，**Phase 1 收口**
- [x] `6262aa1` **cninfo 读完**（代码与文档）；harness 补两轮（postmortem/tools/subagent + 持久化契约）
- [x] `495d143` `3b25855` **A-4 判据实测达成**：pdfplumber 单库在茅台 2023 上 8 张表标题全定位、**15/15 命中**、勾稽差额 0.00。查清三条真实失效模式。**自我修正一处**：中间版本报 15/15，其中 `bs.total_equity` 匹配到章节小标题，看起来命中其实取错行
- [x] `6e3898c` 补读 `PROJECT_SPEC.md` 原文（此前全是二手转引），产出 Phase 1.5 接入契约；**发现 A-7 两条 NFR 与已定决策冲突**
- [x] `f4016b7` **hello-agents Ch12 读完**（2743 行 + 五份评测产物）——三个外部项目凑齐同一失效模式的三个实例
- [x] `7a7da5c` **闸门结论升级为源码级定论**（Cordis 读完）；ROADMAP 补 Phase 1.5 的 SC-7（阻断式勾稽校验）与 SC-8（验收看取到的值不看命中数）
- [x] `a8cc782` **建 Phase 1.5 CONTEXT.md**，GSD 规划链路打通（`phase_found: true`，`phase_dir` = `01.5-data-ingestion`）
  ⚠️ **这个提交是在扫描已经红了的情况下提交的**——`scan; git commit` 的分号不短路
- [x] `db52052` 修 AC-10 红灯（引自书中的 SSH clone 命令与邮箱同形）
- [x] `b733f5e` **新增 F-8**：跑了门禁但没让它挡住后面的动作（同日两次）。硬规则：验证与提交一律 `&&` 串联
- [x] `1a373ee` **`.agents/notes` 普查落地**：分母数清楚为 674 篇正式笔记（此前说「200+」是低估）；发现**我们自己的交叉引用是裸编号，机器查不了**
- [x] `2537a8d` **Phase 1.5 RESEARCH 完成**（453 行，researcher 自行提交，只含该文件）
- [x] `163cccc` **交叉引用门禁（N-30）**：台账 D 区改名 N 区（51 处 / 7 文件），消灭 `D-00x` 与 `D-x` 的全面碰撞；`scripts/check_xrefs.py` 七个命名空间 / 85 定义点 / **522 处引用**；`tests/test_xrefs.py` 8 条含端到端负向用例
  - **写门禁过程中自己犯了两个错，都被测试抓住**：① **假绿**——门禁一度报「通过」但引用正则仍是旧的，**根本没在检查 `N-` 引用**；② `\b` 在中文紧邻时不成立，改用 lookaround 后被检查引用从 473 涨到 522，**那 49 处此前静默漏检**

## Status

**Phase 1 已收口。Phase 1.5 的 CONTEXT 与 RESEARCH 就位，卡在 6 个决策点，未出 PLAN。**

| | |
|---|---|
| 指标定义 | **20 / 20** 合规；`advisory_only` 19.0%，未分类 0 |
| 测试 | **358 passed** |
| 提交 | **58 个**，工作树干净，已推送 `origin/main` @ `163cccc` |
| 门禁 | **4 道**：pytest / `semantic_layer scan` / `semantic_layer validate` / `check_xrefs` —— 全部 exit 0 |
| 评测集 | frozen-01 冻结，21 文件哈希；**C2 拒答 4/4，门成立** |
| 对照臂 | 两个模型臂跑完，报告在 `eval/runs/baseline/`（gitignored） |
| 决策 | D-001…**D-014** |
| ROADMAP | Phase 1 ✅ 全部完成；Phase 1.5 有 CONTEXT + RESEARCH，**Plans 待出** |

**验证命令与真实输出**（2026-08-19 实跑，`&&` 串联）：
```
pytest -q                                        358 passed
semantic_layer scan                              exit 0（规则版本 2）
semantic_layer validate                          exit 0
scripts/check_xrefs.py                           exit 0（85 定义点 / 522 引用 / 0 悬空）
cd eval/frozen-01 && sha256sum -c SHA256SUMS     21 行 OK
cd docs/agent/poc-01 && sha256sum -c SHA256SUMS  5 行 OK
```

### ✅ Phase 1.5 的 6 个决策点已全部裁决（2026-08-21）

| 原编号 | 裁决 | 记录 |
|---|---|---|
| D1 抽取器位置 | `src/extractor/` 与语义层平级 + **单向依赖由测试强制**（明写不构成 U-03 边界决策） | D-015 |
| D2 映射表 | 按命名空间拆五份；**标签 = 有序片段序列逐字匹配**；AKShare 另立；**外加 `column_header`** | D-016 |
| D3 数值执行层 | **自建封闭算术解析器**，不引 `simpleeval`（它建在 Python `ast` 上，会撞 `dsl.py` 当初不用 `ast` 的同一条理由） | D-017 |
| D4 附注字段 | **不是裁决，是实测**：能抽出（p120–121 披露模板固定六项，每项自带 `√适用/□不适用`）；**但无正样本** | CONTEXT §3.5 |
| D5 勾稽粒度 | **批次级闸门** + 计算层**读入边界**拒答，封闭理由码 | D-018 |
| D6 `page` 类型 | `page` + `anchor_page` 两个整数（**区间不需要**：数字不可能跨页） | D-019 |

**同轮一并裁决的三条**：A-7 → D-021（NFR-02 加静态托管豁免并写死边界；NFR-04 改写）；
A-8 → D-020（新增 `notes.consolidation_scope_change`，`scope_change` 改挂它，
**放宽了「不动 metrics/ 的 20 份定义」这条非目标，仅限本条**）；
A-9 → D-016 补充节（列由表头文字显式绑定，不得靠列序号）。

**台账 A 区现已无 🟡 需操作者裁决的条目。** 剩 A-2 / A-6 / A-10，都不需要操作者。

### 🔴 A-9 是本轮最值得记住的一条

勾稽恒等式「资产总计 = 负债合计 + 所有者权益合计」**在期末列与期初列上都成立**
⇒ 三个数全取期初列，D-018 的批次级闸门**照样放行**。
**勾稽证明的是「同一列内部自洽」，不是「取的是对的那一列」。**
这是「自证机制说通过了，但它证明的不是它声称证明的事」的**第四例**，
前三例都是外部项目，**这一例在我们自己刚做的设计里**。

### 计划层的三条改进（已落地）

1. **SC-2 计数口径写死**为「取到的值经逐个人工核对正确」，不是命中数。
2. **实测覆盖只有 16/30**（bs 15 + `notes.business_combination_type`），
   `is` 8 / `cfs` 2 / `notes` 3 / `kpi` 1 共 **14 个字段零实测**，而 ≥24 意味着最多失手 6 个。
   且它们不同质（`notes.reporting_period_months` 根本不在三张报表里）。
   ⇒ **Phase 1.5 第一个 wave 应是把这 14 个探一遍。**
3. **A-10**：探测语料已不在磁盘（茅台 PDF、探测脚本、harness clone 的 `docs/` 与 `.git` 索引全失）。
   判据：下载链路做成仓库内脚本 + 外部参考仓库在 `references/` 里钉死 commit。

### 三个外部项目的沉淀情况（2026-08-22 更新，已全部提交入库）

| 项目 | 状态 | 产出（行数） | 仍未读 |
|---|---|---|---|
| **cninfo**（两仓） | ✅ 代码与文档已读完 | `cninfo.md` 314 + deep-read 220/479/438 = **1451 行** | 无重要缺口 |
| **deepseek-harness** | ⚠️ 契约文档已收口，笔记覆盖 **91/674** | 主文件 238 + part2…part6 + deep-read 269 + **docs-read 1191** + notes-survey **979** + notes-part2 **3370** ≈ **10 700 行** | 笔记 **583/674 未读**（feature 169 / archived 142 / bug-fix 75 / simplification 46 为最大空白）；`docs/` 110 份英文 md 覆盖 23 份，**87 份未覆盖**（`subsystems/` 全目录、`cookbook/`、`cordis-api/`、`postmortem/` 等） |
| **hello-agents** | ✅ 四个空洞已补齐 | 主文件 127 + deep-read 268 + part2 779 + part3 848 + **part4 1496** = **3518 行** | Ch1/2/5/6 剩余、Ch10.1/10.2；Ch11 按 D-011 跳过 |

**覆盖度第一次有账可查**：harness 笔记 674 篇 = 全文读完 91 + 只读标题 142 + 完全未碰 441，
逐目录三档相加均等于该目录总数（本会话用脚本重算过）。
此前 handoff 写的「约 22 篇」是估数。

**版本已钉死**：harness 的结论引自 commit `47f9438`。语料丢过一次，换 commit 后行号会全部漂移。

**下一轮续读优先级**（agent 的建议，我认同）：harness 的 `implemented/simplification`(46)
与 `bug-fix`(75) 优先于 `feature`(169) —— 前者记录「删掉了什么、为什么删得起」，
与已读完的 `rejected/` 互补；后者藏着真实踩过的坑。`survey` §5.2 已列 34 个具体篇名作起点。

**「边读边写」这条指令连续两轮被验证有效**，应固化进以后所有深读 prompt：

| | 上一轮中断 | 这一轮中断 |
|---|---|---|
| 留存 | **0 行** | **886 行**（后续轮次又增至 1500+） |

同样是撞上 API session limit，上一轮零留存；加了「读完一章立刻落盘」之后留下了实质产出。

## Next Steps

**开场顺序**：`rules/failure-modes.md`（**8 类**真实犯过的错）→ `docs/agent/OPEN-ITEMS.md`（台账，注意 D 区已改名 N 区）→ `.planning/STATE.md`。

1. [ ] 🟡 **裁决 6 个 Phase 1.5 决策点**（见 §Status 的 D1–D6 表）。这是拉 planner 之前的唯一前置。
   建议顺序：先 **D3**（数值执行层，影响面最大）→ **D2**（映射表，与 D3 耦合）→ **D5**（勾稽粒度，影响任务拆分）→ D1 / D4 / D6
2. [ ] **`/gsd-plan-phase 1.5`** 拉 `gsd-planner`。CONTEXT 与 RESEARCH 已就位，
   init 已验证 `phase_found: true`、`phase_dir` = `.planning/phases/01.5-data-ingestion`
3. [ ] 🟡 **裁决 A-7**（两条 NFR 与已定决策冲突）
4. [ ] **补做 N-20**：随机读两份 `metrics/*.yaml`，只读 YAML 不看代码，书面回答
   「触发条件读得懂吗 / 陷阱里哪些有机械后果」。**Phase 2 前必须做，agent 代答无效**
5. [ ] **补完外部项目阅读**（见 §Status 的「仍未读」栏）。优先级：
   harness `.agents/notes/` 中 `rejected/` 之外的部分（**被否决方案最有价值**）> hello-agents Ch3 与四个空洞 > harness `docs/user/`
6. [ ] **Phase 1.5 执行时的既定约束**（已实测，不要重新研究）：
   - 表标题印在**上一页页脚位置**，数据从下一页开始
   - 合并表与母公司表的边界**在页内** ⇒ 标题当作带 y 坐标的锚点，区间 = [本标题, 下一标题)
   - 标签折行 ⇒ `标签行 → 纯数值行 → 标签行` 三行缝合
   - **验收看取到的值，不看命中数**（SC-8）
7. [ ] **A-2 的回归数据已就位**：茅台六个有息负债分项全部印在报表上，四个格子空两个有值，
   按「格子空 = 0」有息负债率 ≈ 0.00119。**定义侧不动，修在抽取层**
8. [ ] （可并行）**Phase 0** —— 审计报告正文就在年报 PDF 里、紧邻合并资产负债表，与 Phase 1.5 共用同一条下载定位链路。
   **但 Fog 实现要重写**（cninfo 的 `common_vocab` 缺失导致静默降级、`\n` 作分句符）
9. [ ] **Phase 2 架构预备**（结论已源码级证实，可直接写进 `ARCHITECTURE.md`）：
   闸门放进**调用签名**（`resolve_scope() -> ScopeSpec | Refusal`），不用 waterfall 也不用「可选工具」；
   证据日志的 fail-closed 放在**读入边界**；`Refuse` 必须是**封闭枚举**不是自由文本
10. [ ] （可选，20 分钟）**POC-01 补强**：冻结输入交一位真人财务背景回答者复跑

**红线**：`eval/frozen-01/` 与 `docs/agent/poc-01/` 的标准答案与判定规则**不可修改**。
发现题目有误只能按 `EVAL_CASES.md` §2.2 作废并公开记录——构造错误移出分母、迁就模型禁止作废、作废率 > 10% 整批重建。

## Code

**常用命令**（验证与提交**一律 `&&` 串联**，按 F-8）：
```bash
cd "C:/Users/Kevin/Desktop/excel/学校/求职/秋招/07-finaudit-agent"
export PYTHONIOENCODING=utf-8

# 四道门，任一红即不提交
.venv/Scripts/python -m pytest -q > /tmp/p.txt 2>&1 \
  && .venv/Scripts/python -m semantic_layer scan > /tmp/s.txt 2>&1 \
  && .venv/Scripts/python -m semantic_layer validate > /dev/null 2>&1 \
  && .venv/Scripts/python scripts/check_xrefs.py > /tmp/x.txt 2>&1 \
  && echo "四道门全 exit 0"
```

```bash
# 冻结校验（声明任何结论前必跑）
cd eval/frozen-01 && sha256sum -c SHA256SUMS
```

```bash
# 其他
.venv/Scripts/python -m semantic_layer resolve "毛利率"
.venv/Scripts/python -m semantic_layer stats --fail-over 0.5
.venv/Scripts/python scripts/verify_deps.py
.venv/Scripts/python -m eval.run --suite frozen-01 --category C2 --report eval/runs/
```

**环境**：`.venv` 由**本机 Python 3.14** 建。默认 `python` 是 3.8.5，**不可用于生产代码**——
它装着 akshare / pdfplumber / camelot，**只允许用于探测**。

**API 密钥**：`C:\Users\Kevin\Documents\Api-key\`（`DeepSeek.txt` / `opencode_go.txt`）。**密钥不得进仓库**。
- DeepSeek 官方：`https://api.deepseek.com`，模型 `deepseek-v4-pro` / `deepseek-v4-flash`
  （**两个都是推理模型**，`max_tokens` 给不够会返回空正文——空正文必须记 ERROR，不得记成模型答错）
- opencode go：`https://opencode.ai/zen/go`。qwen/minimax 走 `/v1/messages`（`x-api-key`），
  glm/kimi/deepseek 走 `/v1/chat/completions`（Bearer）。
  ⚠️ **不带常规 UA 时 Cloudflare 返回 403 + `error code 1010`，看着像密钥无效，其实不是**

**Phase 1.5 的可复现抽取起点**（pdfplumber 单库，探测代码在 scratchpad **未进仓库**）：
```text
1. GET  cninfo.com.cn/new/data/szse_stock.json      取 orgId
2. POST cninfo.com.cn/new/hisAnnouncement/query     category=category_ndbg_szsh
3. GET  static.cninfo.com.cn/{adjunctUrl}           PDF，不需要 cookies
4. 表标题 = 带 y 坐标的锚点；表区间 = [本标题, 下一标题)，跨页且可页内切分
5. page.extract_words() → 按 top 聚簇重组行（y 容差 2.0–5.0 均稳定）
6. 折行缝合：标签行 → 纯数值行 → 标签行 ⇒ 合为一行
7. 数值正则 ^-?[\d,]+\.\d{2}$（两位小数）⇒ 附注编号列天然不匹配
```
实测：茅台 2023（143 页）8 张表标题全定位、**15/15 命中**、勾稽差额 **0.00**。

**`formula` 的真实写法**（RESEARCH 逐字取自仓库，D3 决策的直接输入）：
```text
return_on_total_assets:  is.net_profit / bs.total_assets
current_ratio:           bs.total_current_assets / bs.total_current_liabilities_period_end
ar_turnover_days:        365 * bs.accounts_receivable / is.operating_revenue_current
interest_bearing_ratio:  (bs.short_term_borrowings + bs.trading_financial_liabilities
                          + bs.non_current_liabilities_due_within_one_year
                          + bs.long_term_borrowings + bs.bonds_payable
                          + bs.lease_liabilities) / bs.total_assets
```
语法边界：字段引用（点号路径）、`+ - * /`、括号、数值字面量。
**无比较/逻辑运算符、无 `is_missing()`** —— 与 `dsl.py` 的条件语法**完全不相交**，两者互补而非重叠。

⚠️ **Kill criterion 仍然有效**：不做「再改改定义」的循环。冻结的 3 份 v1 定义**一个字节都不许动**，
包括那份语法非法的 `revenue_growth_yoy.yaml` —— 校验器已把它报成 `R0.YAML_UNPARSEABLE`，
并有回归测试锁住「恰好一份文件语法非法」这个事实。
