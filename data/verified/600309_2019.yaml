# 万华化学 600309 · 2019 年年度报告 —— **人工逐字段核对过的清单**
#
# 用法与限定同 `600519_2023.yaml` 的抬头，不重复。
#
# 🔴 **这份清单里的 28 个字段不是同一个签署等级，见 `signed_off` 与 `pending_signature`。**
#
# §D.18 核了 25 个（bs / is / cfs），已由操作者签署。
# 末尾三个 `notes.*` **不在 §D.18 的核对表里** —— 它们是 2026-09-06 补核的（§D.19）。
#
# 补核的起因不是「顺手多核几个」：`notes.restatement_flag` 缺失时，
# `debt_to_asset_ratio` 的可比性标记求不了值 ⇒ 整题拒答 `UNEVALUABLE_CONDITION`。
# 也就是说少这一个字段，**万华一道题都答不了**。
#
# ⚠️ 这三个是**执行者核对、尚待操作者签署**（§C.13 的分工：legwork 由执行者做，
# 签署由操作者做）。它们已经进了导出，因为拦住它们的代价是整家公司不可用；
# 但「已核对」与「已签署」是两件事，这里不合并表述。

meta:
  stock_code: "600309"
  fiscal_year: 2019
  short_name: 万华化学
  full_name: 万华化学集团股份有限公司
  source_pdf: data/raw/600309_2019.pdf
  source_pdf_sha256: 87411c60fd8471d7b9cdfe4a0f127c660622634e34905d21ad5e1c539a682d12
  signed_off: 2026-09-02 操作者签署：接受 §D.18 的 25 / 25（**只覆盖前 25 个字段**）
  pending_signature: 末尾三个 `notes.*` 由 §D.19 补核（2026-09-06），执行者已核、操作者未签
  verification_sections:
    - D.18
    - D.19
  # 第二重独立约束：七组**版面自带**的算术恒等式，25 个里有 15 个被至少一条约束住
  # （§D.18「七组版面自带的算术互证」）。剩下 10 个只有「逐字读到」这一层证据。
  cross_checked_against: 年报版面自带的七组算术恒等式（§D.18，15 / 25 被约束）

fields:
  bs.accounts_receivable: "4433077609.63"
  bs.inventory: "8586883294.06"
  bs.total_current_assets: "23483577397.65"
  bs.total_assets: "96865322655.29"
  bs.short_term_borrowings: "20034036150.27"
  # 「行在、格子空」；同一张表的 `交易性金融资产` 有值（30,367,333.35）——
  # 判 EMPTY_CELL 而不是去够那个数，**「资产 / 负债」只差一个字**。§D.18 辨析 3
  bs.trading_financial_liabilities: "0.00"
  bs.long_term_borrowings: "5962595245.57"
  bs.bonds_payable: "0.00"
  bs.accounts_payable: "8024420861.29"
  bs.non_current_liabilities_due_within_one_year: "3324127445.00"
  bs.lease_liabilities: "0.00"
  bs.total_current_liabilities_period_end: "44799566175.61"
  # p75 上半页是**母公司**表的续页，那里印着 `负债合计 45,431,055,319.24` ——
  # 同样的标签、不同的数。取对靠的是报表锚点 + 章节区间。§D.18 辨析 1（最强干扰项）
  bs.total_liabilities: "52934063683.66"
  bs.minority_interests: "1567164415.90"
  bs.total_equity: "43931258971.63"
  # ⚠️ `N-48`：本条与上一行 `一、营业总收入` **值完全相同**（免检字段，取错了也看不出来）
  is.operating_revenue_current: "68050668650.78"
  is.operating_revenue_prior_as_presented: "72837108238.47"
  is.operating_cost: "48997610501.85"
  is.selling_expense: "2782908073.79"
  is.administrative_expense: "1433850380.23"
  is.financial_expense: "1079748165.38"
  # ⚠️ `N-48`：本条与下一行 `1.持续经营净利润` **值完全相同**（同样免检）
  is.net_profit: "10593318819.77"
  is.net_profit_attributable_to_parent: "10129985097.55"
  cfs.net_cash_flow_from_operating_activities: "25932941200.65"
  cfs.cash_paid_for_fixed_intangible_and_other_long_term_assets: "17814814580.17"

  # ---- 以下三个由 §D.19 补核（2026-09-06），**待操作者签署** ----
  # p169「1. 非同一控制下企业合并 √适用」、p171「2. 同一控制下企业合并 √适用」、
  # p173「3. 反向购买 □适用 √不适用」⇒ 恰好两个，第三个正确地没进来。
  # 这是**真辨析测试**：一个「有就全返回」或「有就返回第一个」的实现都会在这里露馅。
  notes.business_combination_type: [COMMON_CONTROL_COMBINATION, NON_COMMON_CONTROL_COMBINATION]
  # p173「5. 其他原因的合并范围变动 √适用」，表内：万华化学(四川)有限公司 · 新设。
  # ⇒ 这一条有**自己的**版面依据，不是从上一条推出来的。
  notes.consolidation_scope_change: true
  # p6 三处表头印着 `调整后` / `调整前`，且两列**数值不同**
  # （2018 年营业收入 72,837,108,238.47 调整后 vs 60,621,193,436.91 调整前）。
  # ⚠️ 比茅台那次（§D.17 #8，只有列头存在性）强一档：这里重述**真的发生了**，
  # 不是只印了一个表头。且 `is.operating_revenue_prior_as_presented` 取的正是
  # 调整后那一列（72,837,108,238.47，与 §D.18 #17 逐字相同）—— 两处互证。
  notes.restatement_flag: true
