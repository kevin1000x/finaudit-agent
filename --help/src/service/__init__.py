"""对外服务：把答案路径包成一个**无状态请求–响应**端点（`02-04` T1/T2）。

## 这个包存在的前提是一次决策修订

`D-021` 2026-09-06 第三次修订（豁免三）才允许出现服务端运行时。
边界不是软的，判据是两句话：

1. **有没有一个进程在没人请求的时候运行？** 有 ⇒ 越界。
2. **把存储全部清空，有没有东西永久丢失？** 有 ⇒ 越界。

⇒ 本包里**没有**：计划任务、后台作业、作为记录系统的数据库、账号体系。
唯一的「状态」是把只读的口径注册表缓存一份（`lru_cache`）——
它可由磁盘重新推导，清掉不丢任何东西，属于豁免三明确允许的**可丢弃缓存**。

## 它刻意**不**依赖任何 Web 框架

对外的契约是**纯函数**：`answer_endpoint(payload) -> (状态码, 响应体)`。
HTTP 那一层由部署方给（cninfo 那边是 FastAPI + Cloudflare Pages Function 转发），
本包只提供一个 stdlib 的本地运行器供开发与测试用。

这样做的理由有两条，都不是洁癖：

- **少一个依赖就少一次许可证风险。** `D-014` 的前提（服务上线 ⇒ AGPL §13 触发 ⇒
  与 `D-005` 主仓私有不能同时成立）已经成真，服务端依赖闭包里进一个 AGPL 组件，
  `D-005` 当场失效。
- 部署形态还没定（`02-04` T4）。**把 HTTP 层焊死在这里，等于替那个决策先做了选择。**

## 不收用户的任何财务数据

端点只接受**一个问题字符串**，没有文件上传、没有 CSV、没有粘贴表格入口。
这是 `D-010`，不可协商 —— 产品的价值是「用公开数据也能讲清口径」，
一旦开始接用户的数，这个项目的合规前提就没了。
"""
